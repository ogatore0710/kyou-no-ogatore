# REPORT-C2-2026-08-04-build22-yellow-return

【appdev→alan5】build22(黄色回帰「案B」Z-1〜Z-9)の実装完了報告です。両OSビルド/テスト・qa.js全項目確認済み。**alan5検収後のビルドGOをお待ちしています。**

## 0. サマリ

- Z-1〜Z-9の9件すべて反映(詳細は1〜9節)。発注書 `TASK-C2-2026-08-04-build22-yellow-return.md` を正本として実装。
- 主ボタンを藍→黄「案B」へ復帰(濃文字`#26261F`+金茶枠`#8A6D00`)。`btnPrimaryBg`トークンは削除。**藍系トークン残存ゼロ・grep実施済み(11節)**。
- 実装途中、Z-6(ダーク枠線5:1底上げ)の対象漏れを1件発見しその場で追加修正しました(6-4節)。
- Z-6は「全border系トークン5:1」のうち、**構造系(カード/入力欄/ラインボタン枠)は完全対応、カテゴリ配色系(相談室チップ・検索カテゴリタブ等の色付き枠)は今回スコープ外としました**。理由と実測値は6-5節で報告し、alan5判断を仰ぎます。
- iOS `xcodebuild build` BUILD SUCCEEDED。Android `JAVA_HOME=~/android-toolchain/jdk/Contents/Home ANDROID_HOME=~/android-toolchain/sdk ./gradlew compileDebugKotlin testDebugUnitTest --rerun-tasks` BUILD SUCCESSFUL。`node scripts/qa.js` 461項目全PASS。
- 実描画はiOSシミュレータで撮影(12節)。**Android実機/エミュレータでの実描画は今回未実施**(環境準備の都合・コードは完全1:1移植のため見た目は同一想定)。iOSでも一部画面(ホーム下部のつづけた日数/相談室カード、マイ記録タブの図鑑バナー)はタップ操作を伴う画面遷移のため、この環境のCLIから到達できず**未撮影**です(12節に詳細)。

---

## 1. Z-1: 主ボタン黄色復帰(案B)

`btnPrimaryBg`トークンを削除し、地は`colors.yellow`直参照へ。新トークン`kyonoBtnPrimaryText`(`#26261F`)・`kyonoBtnPrimaryBorder`(`#8A6D00`)を追加し、両テーマ共通で適用(ダークも同一値。build21以前のダーク主ボタンは元々黄色だったため実質「藍化前に復帰」)。`btnPrimaryShadow`も両テーマ`#E8BE1E`に戻した。

対象: `KyonoPrimaryButton`本体、ツアーモック内の「きょうやった！」プレビュー、Home「きょうの分は完了」表示(Z-7で構造自体は変更)。

## 2. Z-2: セグメント/セカンダリ/ラインボタン/catbtnを案B仕様へ(ライトのみ)

| 部位 | ライト新値 | ダーク |
|---|---|---|
| セグメント選択中(`KyonoSegmentedControl`) | 白地+ink文字+枠`#6B6857` 2pt(新設) | 既存(card地・ink文字・枠なし)維持 |
| セカンダリ(`KyonoGhostButton`) | 地`#DFF5F2`+文字`#0F5A50`+枠`#177065` 2.5pt | 既存(tealSoft/tealStrong/tealInk)維持 |
| ラインボタン(`KyonoLineButton`) | 文字/枠とも`#4A473D` | 既存(sub2文字・枠は後述Z-6で底上げ)維持 |
| カテゴリタブ(catbtn・検索/相談室) | build21の藍化を全面ロールバック、pre-D2の`yellowInk`/`yellow`地に完全復帰 | 変更なし(元々黄地) |

## 3. Z-3: 初回チャット淡色カテゴリ選択肢の強化(IMG_8768)

`ObgColor`に`text`フィールドを追加し、5カテゴリの濃縮色を新設(枠2.5pt化とセット)。淡い塗り自体(`bg`)は変更していない。ダークは無変更(build21から不変・`text`はダークink`#F2EDE1`をそのまま流用)。

同一パレットを共有する「かたさチェック」Q1-Q4の段階色カード(`QuizOptionCard`)にも同基準で連動: `labelColor`パラメータを新設し`c?.text`を渡すよう配線(Android側も同様に文字色を連動)。**棚卸し結果: 他に同型の淡色チップ/選択肢は見当たらず**(相談室モーダル内チップ・時間帯チップは別トークン系統`colors.line`ベースで、Z-6で別途対応)。

| カテゴリ | 淡い塗り(不変) | 新・文字/枠濃縮色 | 文字vs塗り | 枠vs新ページ背景`#F7EEDC` |
|---|---|---|---|---|
| 肩こり・首(mint) | `#EAF8F1` | `#177065` | 5.42:1 | 5.14:1 |
| 腰(yellow) | `#FFF3CB` | `#7A5E00` | 5.52:1 | 5.31:1 |
| 前屈できない(orange) | `#FBE3C6` | `#995400` | 4.67:1 | 5.03:1 |
| 眠り(rose) | `#F2D7CD` | `#863213` | 6.20:1 | 7.35:1 |
| とくにない(blue) | `#D9ECF7` | `#006199` | 5.46:1 | 5.75:1 |

全カテゴリで文字vs塗り≥4.5:1・枠vsページ背景≥3:1を達成(orangeの4.67:1が最も薄いマージン)。

## 4. Z-4: ホーム動画カードの縦間隔拡大

「きょうの1本」3枚のカード間、および連続再生ボタン前の間隔を、旧spacing 0(密着)から**+7pt/dp**に統一(VStack/Column spacingで一括制御。カード内部の密度=サムネ/文字パディングは無変更)。「1.5倍」の指示に対し実際の旧値が0だったため厳密な倍率ではなく、指示の副目安「+6〜8pt/dp」の範囲内・中央値で採用。おおきめ文字(1.30倍)でも実描画で窮屈さは見られない(12-4節)。

## 5. Z-5: ライト背景深化+玉突き調整

`bg: #FFFAF3 → #F7EEDC`。玉突きで`line`(トラック等の面色)がbg比1.04:1まで沈んでいたため、`line: #F2EADB → #EBDFC8`へ半段調整し、旧背景比の差(1.15:1)に近い1.14:1を確保。ダーク・Web版は無変更。

## 6. Z-6: ダークモード磨き込み(A案)

### 6-1. 背景/カード分離

| トークン | 旧 | 新 | 備考 |
|---|---|---|---|
| `bg` | `#211E19` | `#1C1915` | 目標`#1E1B16`近辺→実測ベースでさらに一段沈め |
| `card` | `#2C2822` | `#3A342C` | 目標`#312C24`近辺→実測ベースでさらに一段浮かせ |
| bg↔card 分離度 | 1.13:1 | **1.42:1** | 明確に拡大(+26%)。両者とも同一色相(暖色ブラウン、色相角約30〜37°)を保持し、藍化・黒化なし |

### 6-2. 新設トークン`borderStrong`(枠線専用)

`line`トークンは「サムネ枠・進捗バートラック・タブ区切り線・カテゴリチップ未選択地」等、**枠だけでなく面塗りとしても広範に流用されている**ため、`line`自体の値を上げると面塗り用途(薄いグレーの背景的な使われ方)が破綻する。そのため**構造系の枠線専用に新トークン`borderStrong`を追加**し、実際に`.stroke()`/`.border()`で枠線として使われている箇所だけを付け替えた(iOS 44箇所・Android 32箇所、両OS grep突合済み)。

| トークン | ライト | ダーク | 用途 |
|---|---|---|---|
| `borderStrong`(新設) | `#EBDFC8`(=`line`と同値、無変更) | `#B4AB9A`(新規選定) | KyonoCard/GradientCard枠・入力欄枠・チャット吹き出し枠・ラインボタン枠・とどくメーター枠 等 |

| 組み合わせ | 実測 |
|---|---|
| `borderStrong` vs 新bg`#1C1915` | 7.70:1 |
| `borderStrong` vs 新card`#3A342C` | 5.41:1(より厳しい方の基準で5:1超を確保) |
| (旧比較)ラインボタン枠`#4A443A` vs 旧bg`#211E19`(既知の未解決課題) | 1.72:1 → **根治** |
| (旧比較)`line` vs 旧card(生の構造枠として使われていた実態) | 1.26:1 → **根治** |

### 6-3. ラインボタン枠の恒久修正

`KyonoLineButton`のダーク枠ハードコード値`#4A443A`(build21report 8節で報告した既知の1.52〜1.72:1未達)を`colors.borderStrong`参照に置き換え。今後`borderStrong`を更新すれば自動追随する。

### 6-4. 実装中に発見した対象漏れ(その場で追加修正)

Android側、Material3の`TextField`/`OutlinedTextField`は`.border()`ではなく`focusedIndicatorColor`/`unfocusedBorderColor`等の専用パラメータで枠線を指定する。当初の`.border(...colors.line)`のみを対象にした一括置換ではこの記法を拾えず、**入力欄6箇所(BragScreen/GuideScreen/MainActivity(メモ欄)/SearchScreen/SoudanSheet/SettingsScreen)が取りこぼされていた**ことに気づき、追加でgrepし直して`colors.borderStrong`へ統一した(iOS側は元々`.overlay().stroke()`で統一されており漏れなし)。

### 6-5. 今回スコープ外にした箇所(alan5判断待ち)

発注書は「枠線系トークンは背景に対しすべて5:1以上」と包括的に書かれていましたが、以下の**色付き・意味づけのある枠**(相談室モーダルの入り口チップ・検索カテゴリタブ・セカンダリボタンのteal枠等)は今回**据え置き**にしました。

| 部位 | 現状の実測 |
|---|---|
| セカンダリボタン(ghost)ダーク枠`tealStrong` vs bg | 3.44:1 |
| とどくメーター選択中ダーク枠`tealStrong` vs bg | 3.44:1 |
| オンボ/かたさチェックのobgDarkチップ枠(5色) vs bg | 1.74〜2.59:1 |
| 検索カテゴリチップのダーク枠(4色) vs bg | 1.82〜2.25:1 |

**据え置きにした理由**: これらは全てZ-1〜Z-3で確定した「案B」の意味づけ色(teal=セカンダリ系統、カテゴリ5色/4色=識別色)であり、5:1達成のために明度を上げると、実測上は元の色相のまま大幅に明るいトーン(例: mint枠`#2E5A48`→`#50AF88`相当)まで持っていく必要があり、**個別カテゴリの識別色としての「濃さ」自体が変わってしまう**懸念があります。Z-6発注文の「藍化・黒化はしない」は主に背景/枠の中立トーンを指した制約と解釈し、この一群は今回のパスでは触らずに実測のみ報告する形にしました。あわせて修正すべきか、次便でご判断ください。

## 7. Z-7: ホーム「続けた日数」再設計(案1・数字が主役)

- 見出し `続けた日数（通算）` → **`つづけた日数`**(「通算」の語は本文からも全廃)
- `KyonoStreakText`を再設計: 中央にBanananumフォントの大きな「N日」ヒーロー数字(ピンク系`pinkInk`)。連続記録の付帯情報(「いま○日連続」「きょうやると新しい章のスタート」)は数字下の小さい1行へ格下げ(情報は落とさず表示形式のみ変更)。「通算 1 日」の行自体は廃止。
- 完了時: 数字の下に小さく1行「きょうの分は完了！おつかれさま」(`lineLimit(1)`/`maxLines=1`で折り返しなしを保証)。未完了時: 従来どおり黄ボタン「きょうやった！」(Z-1仕様)を数字の下に配置。ロジック(mark-done処理・ハプティクス・カード演出等)は完全に不変、条件分岐の位置だけ変更。
- メモ入力欄+「メモをのこす」ボタンを1行に合体。全幅の旧ボタンは廃止し、黄色い円形ミニボタン「のこす」(未保存時)/「✓」(保存後)を入力欄の右に配置。placeholderの色を`colors.subFaint`(ほぼ見えない既定薄灰)から`colors.sub`へ濃色化(「枠の中の字が見えない」指摘の解消)。
- 記録カードボタンの文言を「記録カードをつくる」へ変更(セカンダリ=ghostスタイルは元々のまま)。下の説明行「カード画像を保存かシェアでのこしてね」を削除(モーダル内の案内で足りるとの判断どおり)。

## 8. Z-8: ホーム相談室カードのチップ削除

「タップでそのまま聞けるよ」の行とおすすめチップ4つ(肩こり・首こり/腰痛/前屈できない/時間がない・続かない)を削除。カードは見出し+一言+「相談する」ボタンだけの構成に。相談室モーダル内のチップ行(入力の入口として機能)は不変・未変更。

**参照棚卸し**: iOS側は呼び出し元が無くなった`HomeSoudanChip`構造体と`picks`計算プロパティを削除(他画面からの参照なし・確認済み)。Android側は共有コンポーネント`KyonoChip`を使用していたため、その定義自体は`SoudanSheet.kt`内の他8箇所で引き続き使用中のため残置し、呼び出し箇所と`picks`計算のみ削除。

## 9. Z-9: 図鑑ボタン+プレビュー4枚を1つの枠に統合

マイ記録タブの「お楽しみ機能」カード内、「カード図鑑（N/106）」ボタン(`KyonoPrimaryButton`)とミニサムネ4枚(別要素・別々に`onOpenDex`を持っていた)を、新設`DexBannerCard`(iOS)/`DexBannerCard`(Android)として1つの角丸枠に統合。配色はじまんカード等と同じ「案B」ghost配色(ライト: ミント地`#DFF5F2`+濃緑枠`#177065` 2.5pt+濃緑文字`#0F5A50`。ダーク: `tealSoft`/`tealStrong`/`tealInk`)に整合させた。枠全体が単一のタップ領域(`onOpenDex`)になり、内側の旧ボタンは行表示(見出し+件数のみ)に格下げして「二重ボタン感」を解消。じまんカード/せんぱいの声/ひとことにっきの3ボタンは変更なし。

---

## 10. 実測コントラスト表(全変更箇所・まとめ)

| 部位 | 組み合わせ | 実測 |
|---|---|---|
| Z-1 主ボタン文字 | `#26261F` vs 黄`#FFD93B` | 11.05:1 |
| Z-1 主ボタン枠 vs 黄面(非文字3:1) | `#8A6D00` vs `#FFD93B` | 3.57:1 |
| Z-1 主ボタン枠 vs ページ背景(非文字3:1) | `#8A6D00` vs `#F7EEDC` | 4.27:1 |
| Z-2 セグメント選択枠 vs 背景(非文字3:1) | `#6B6857` vs `#F7EEDC` | 4.87:1 |
| Z-2 ghost文字 vs 自面 | `#0F5A50` vs `#DFF5F2` | 7.11:1 |
| Z-2 ghost枠 vs 背景(非文字3:1) | `#177065` vs `#F7EEDC` | 5.14:1 |
| Z-2 ラインボタン文字/枠 vs 背景 | `#4A473D` vs `#F7EEDC` | 8.06:1 |
| Z-3 mint文字 vs 自面 | `#177065` vs `#EAF8F1` | 5.42:1 |
| Z-3 mint枠 vs ページ背景(非文字3:1) | `#177065` vs `#F7EEDC` | 5.14:1 |
| Z-3 yellow文字 vs 自面 | `#7A5E00` vs `#FFF3CB` | 5.52:1 |
| Z-3 yellow枠 vs ページ背景(非文字3:1) | `#7A5E00` vs `#F7EEDC` | 5.31:1 |
| Z-3 orange文字 vs 自面 | `#995400` vs `#FBE3C6` | 4.67:1 |
| Z-3 orange枠 vs ページ背景(非文字3:1) | `#995400` vs `#F7EEDC` | 5.03:1 |
| Z-3 rose文字 vs 自面 | `#863213` vs `#F2D7CD` | 6.20:1 |
| Z-3 rose枠 vs ページ背景(非文字3:1) | `#863213` vs `#F7EEDC` | 7.35:1 |
| Z-3 blue文字 vs 自面 | `#006199` vs `#D9ECF7` | 5.46:1 |
| Z-3 blue枠 vs ページ背景(非文字3:1) | `#006199` vs `#F7EEDC` | 5.75:1 |
| Z-5 line vs 新背景(非文字3:1目安) | `#EBDFC8` vs `#F7EEDC` | 1.14:1(旧1.15:1相当を維持) |
| Z-6 bg↔card 分離度 | `#1C1915` vs `#3A342C` | 1.42:1(旧1.13:1) |
| Z-6 borderStrong vs 新bg(非文字3:1) | `#B4AB9A` vs `#1C1915` | 7.70:1 |
| Z-6 borderStrong vs card(非文字3:1・より厳しい方) | `#B4AB9A` vs `#3A342C` | 5.41:1 |
| Z-6 ラインボタン枠(旧・既知課題) vs 旧bg | `#4A443A` vs `#211E19` | 1.72:1(根治済み) |
| Z-6 line(構造枠として使われていた実態) vs 旧card | `#3D382F` vs `#2C2822` | 1.26:1(根治済み) |

文字上の非WCAG項目(枠線)は「非文字用途は3:1以上が目安」という既存タスクの基準に沿って判定。全項目クリア(Z-3 orangeの4.67:1が最薄マージン)。

## 11. 藍トークン残存ゼロ・grep監査

```
grep -rn "0x073A71|0x2278C0|0x19578B|0x052A52|btnPrimaryBg" ios-native/.../*.swift
grep -rn "0xFF073A71|0xFF2278C0|0xFF19578B|0xFF052A52|btnPrimaryBg" android-native/.../*.kt
```
→ 両OSとも**0件**。build21の藍系トークン(値・識別子とも)は完全に払拭済み。

## 12. 実描画確認

iOSシミュレータ(iPhone 17・iOS 26.5)へ実機ビルドをインストールし、`kyono-store.json`を直接書き換えて状態を切り替えながら撮影しました(この環境にタップ/スワイプ操作を送る手段が無いため、画面遷移を伴わない範囲での確認です)。

### 12-1. ホーム(ライト・ふつう・未完了)
Z-4(カード間隔+7pt)・Z-5(背景`#F7EEDC`への深化)を確認。

### 12-2. ホーム(ダーク・ふつう)
Z-6の背景/カード分離拡大(1.42:1)・カード枠線の可視化を確認。旧描画(build21以前)では枠がほぼ見えなかった箇所が、今回はっきり縁取りされています。

### 12-3. 初回チャット・もじの大きさ選択(ライト)
Z-3の濃縮色チップ(mint/yellow)を確認。淡い塗りに沈んでいた旧描画と異なり、枠線と文字がはっきり視認できます。

### 12-4. ホーム(ライト・おおきめ文字)
Z-4のカード間隔が、文字拡大時(1.30倍)でも窮屈に見えないことを確認。

### 12-5. 未撮影の画面(要・次便でのフォロー)
以下はタップ操作(画面遷移・スクロール・「きょうやった！」ボタン押下等)を伴うため、この環境から到達できず**未撮影**です。実装はコードレビュー+ビルド成功で検証済みですが、実際の見た目はalan5のご確認をお願いします。
- Z-7: 「つづけた日数」完了後の数字ヒーロー+労いの1行(状態を`kyono-store.json`のstreak2直接書き換えで模擬しましたが、対象要素がスクロール要求範囲にあり画面に収まりませんでした)
- Z-8: 相談室カードのチップ削除後の見た目(ホーム下部、要スクロール)
- Z-9: マイ記録タブの図鑑バナー統合(タブ切り替えが必要)
- Z-3の「初回チャット・悩み選択」5色チップ全種(もじの大きさ設問の先、タップで進行するため2色までしか到達できず)
- Android全般(実機/エミュレータでの実描画は今回未実施。コードは1:1移植でiOSと同一構造)

---

## 13. ビルド/テスト結果

- `node scripts/qa.js`(=`npm test`): 461項目 **全PASS**
- iOS: `xcodebuild build -project KyouNoOgatore.xcodeproj -scheme KyouNoOgatore -destination 'platform=iOS Simulator,name=iPhone 17'` → **BUILD SUCCEEDED**
- Android: `JAVA_HOME=~/android-toolchain/jdk/Contents/Home ANDROID_HOME=~/android-toolchain/sdk ./gradlew compileDebugKotlin testDebugUnitTest --rerun-tasks` → **BUILD SUCCESSFUL**(27 tasks executed)

---

## 14. お願い

- 6-5節(Z-6の色付き枠を今回スコープ外にした判断)、12-5節(未撮影画面)について、次便で判断・確認をいただけますと幸いです。
- 上記以外は検収基準どおり実装・確認完了しています。**ビルドGOをお待ちしています**(バージョンバンプ・archive・upload待機中)。
