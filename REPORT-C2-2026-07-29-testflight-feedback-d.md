# TestFlight実機フィードバック 第3弾(D) — 完了報告

指示どおり **D1 → D2 → D5 → D6 → D3** の順で対応しました。途中、D5について1件
差し戻し(安全文言を畳みすぎていた)をいただき、その場で修正しています。

---

## D1 🔴🔴 サムネイルが全画面で出ない → 原因を実測で特定・修正完了

**指示どおり「読んで直す」ことはせず、まず観測しました。**

### やったこと(観測)

`KyonoAsyncImage.swift`の`.task(id:)`・`loadImage()`の各段に一時的なログを仕込み、
既存のUIテスト(`SearchViewUITests`)で「動画を探す」を実際に開かせ、シミュレータの
統合ログ(`log show`)で実際に何が起きているかを見ました。

1回目: **`.task`発火のログが1件も出ませんでした。**(検索結果24行、全行)
NSLogの取得経路自体は生きていること(制御用のログは出た)を先に確認済みなので、
「ログが出ない」=「`.task`が本当に一度も走っていない」という結論になります。

### わかったこと(結論)

`KyonoAsyncImage.body`が
```swift
Group {
    if let uiImage { Image(uiImage: uiImage)... }
}
.task(id: url) { ... }
```
という形で、**`uiImage`が`nil`の間は`Group`の中身が完全に空になり、その空の`Group`に
付けた`.task`が一度も発火しない**(SwiftUIが中身の無いGroupをレンダーツリーにmountしない)
ことが原因でした。`.task(id:)`が一度も走っていないか、というalan5の④の疑いがそのまま
当たりでした。

body を`Color.clear.overlay { if let uiImage {...} }`という「常に実体のあるビュー」を
アンカーにする形に変えたところ、同じ実測(ログ)で**検索結果24行すべての`.task`発火・
画像取得(200 OK)を確認**しました。この1箇所の修正で、`KyonoAsyncImage`を使う3箇所
(検索結果・プレイリストサムネイル・使い方ツアー1枚目「きょうの1本」)すべてが直ります。

### シミュレータでの再現

再現しました(検証もシミュレータで行っています)。

### Android確認

Androidの同等コンポーネント(`KyonoAsyncImage.kt`)は`produceState`をComposable本体の
先頭で無条件に呼んでおり、iOSのように「描画結果が空だと発火しない」という構造的な弱点が
そもそもありません。Androidの検索結果を実機(エミュレータ)で確認したところサムネイルは
最初から正常に表示されており、**Androidに同じ穴はありませんでした。**

### 相談室について(重要・保留にしました)

D1の指示書は3画面とも「同じ欠陥」として扱っていましたが、コードを見ると**相談室だけ
性質が違いました**。相談室の動画紹介は現状`KyonoGhostButton("▶ 動画を見る")`という
文字だけのボタンで、**サムネイル画像を描く実装がそもそも存在しません**(iOS/Android
とも)。Web版の`sdVideoHTML()`(index.html:3039-3046)はサムネイル+バッジ+タイトル+
説明文つきの本格的な動画カードを出しており、これは「バグ」ではなく「Web版にある機能が
未移植」という一段階前の話です。今回は指示どおり検索・ツアーの実バグだけを直し、
**相談室への新規UI実装には手を付けていません。** 詳細と再開時の指針をHANDOFF.mdに
記録しました。ここは判断をお願いします。

---

## D2 🔴 共有メニューに「写真に保存」が出ない → 原因確定どおり修正・実際の保存まで確認

`INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription`をアプリ本体のDebug/Release両方に
追加しました。文言:「つくった記録カードを写真フォルダに保存するときに使います。」

`ShareImage.swift`の`UIActivityItemSource`はご指示どおり消さずに残しています。

### 実際に保存されるかまで確認

シミュレータで実機に近い形で検証しました。「マイ記録→じまんカード」でカードを作成→
「保存・シェアする」→共有シートに**「Save Image」が出現**→タップ→
**シミュレータの写真ライブラリのDB(`Photos.sqlite`)のアセット数が6→7に増え、
`IMG_0007.PNG`が検証と同時刻で追加されたことを確認しました。** 押しただけでなく、
実際に写真フォルダへ入るところまで確認済みです。

### Androidの権限漏れ確認

Androidの共有は`Intent.ACTION_SEND`+`FileProvider`で、iOSの`Info.plist`権限文言のような
「送信側アプリが宣言する保存許可」という仕組み自体がありません(保存できるかどうかは
受け取り側アプリ=Googleフォト等の権限の話であり、送信側の権限宣言に依存しません)。
実際にエミュレータで「保存・シェアする」を押し共有シートを確認しましたが、Nearby Share/
Gmail/Print/Drive/Maps等が並び、これはインストール済みアプリの構成によるものでした。
**Androidに同種の権限漏れはありませんでした**(そもそも同じ種類の問題が起きない設計)。

---

## D5 🔴 相談室でキーボードを出すと会話が隠れる → 修正・途中差し戻し反映済み

`@FocusState`で入力欄の編集中かどうかを見て、キーボード表示中は下記を畳むようにしました。

- 注意書き(当初は2行とも畳んでいましたが、**alan5差し戻しにより「強い痛み・しびれが
  あるときは医療機関へ」だけは畳まず残す**形に修正済みです。畳むのは前の2文
  「※回答はオガトレ監修のパターン集から選んでいます」「※目安をつかむ相談室です」だけ)
- カテゴリタブ+チップ行
- 「とじる」(下部のLineButton。ヘッダー右上の✕はそのまま残ります)

`reduceMotion`を見て演出の有無を切り替え、`.animation(value: inputFocused)`で
畳む/戻すの両方をアニメーションさせています。シミュレータでUIテストを1回書いて実測し、
キーボード表示中は「オガトレ相談室(タイトル)・✕・会話・入力欄・送信」だけが残る
(注意書き・カテゴリ・チップ・下部とじるは消える)ことを確認しました。検証後、テストは
削除しています。

### Android確認

Androidも実機(エミュレータ)でキーボードを出して確認しましたが、**同じようには壊れて
いませんでした。** Android側は`imePadding()`ベースでレイアウト全体が比例して縮むため、
キーボード表示中でも会話が2行ぶん・プランチップ・入力欄・送信ボタンが同時に見える状態を
維持していました(iOSのように高さが完全に0になることはない)。指示どおり「同じなら
同じ方針で直す」の対偶で、**壊れていないためAndroidには手を入れていません。**

---

## D6 🟠 使い方ツアーで「つぎへ」が上下に動く → 修正(iOS・Android両方)

### 直した内容

- 内容(タイトル・モックアップ・説明文・ドット)とボタン列(もどる・つぎへ・とばす)を
  別の領域に分離。ボタン列は画面下端に固定し、内容側だけが独立してスクロールします
  (index.html:524 `#obLog{flex:1;max-height:52vh;overflow-y:auto}`/528 `#obChips`の
  1:1移植)。ステップが変わるたびに内容側のスクロール位置を先頭へ戻す処理も追加しました
  (`log.scrollTop=0`相当・入れないと前のステップのスクロール位置が持ち越されて見える)。
- ボタン文言から絵文字を外し「つぎへ ➡️（N/9）」→「つぎへ」に。ドット進捗が既に上に
  あるため、文字側の「(N/9)」は重複と判断して落としました(alan5の見立てのとおり、
  ドットのほうを残す判断にしています。理由: ドットは全ステップ数と現在地を一目で示せて
  おり、文字の数字表記は同じ情報の重複であるうえ、ボタン文言を短くするほど折り返しにも
  強くなるため)。

シミュレータのUIテストで9ステップ最後まで実際に送り、**「つぎへ」の画面上のY座標が
説明文の長さに関わらず一切変わらないこと**(ドット未表示のstep0〜7で完全に同一の座標)を
実測で確認しました。もどる/とばすの有無でボタン列自体の高さが変わる場面(1ステップ目・
最終ステップ)でだけ数十pt動きますが、これはボタンの集合が変わることによる自然な変化で、
Web版の`#obChips`も同様です(内容の長さでは一切動きません)。

### Android確認

**同じ穴がありました。** `TourScreen`も単一の`Column(...verticalScroll...)`で内容と
ボタンが同居しており、iOSと同型の「内容の長さでボタンが動く」構造でした。同じ方針
(内容用Column+weight(1f)+verticalScroll／ボタン用Columnを外側に分離)で修正し、
絵文字除去・(N/9)除去も同様に適用しました。エミュレータで4ステップぶん実機確認し、
説明文が4行に伸びた状態でも「つぎへ」の位置が画面下端で変わらないことを確認済みです。

---

## D3 🟠 記録カードのボタンが不揃い → 原因判明・Web版と1:1に統一

### 原因

Web版の実際のマークアップ(index.html:1197-1199)を確認したところ、**「保存・シェアする」
と「とじる」は横並びではなく縦積み、それぞれ幅100%**でした
(`<button class="btn btn-primary">📤 保存・シェアする</button>` → 
`<button class="btn btn-line">とじる</button>`の順)。ネイティブ(iOS)は3画面
(BragView/HomeView/MyRecordView)ともHStackで横並びにしていたため、幅を分け合った
「保存・シェアする」(フォントサイズ20)だけが2行に折り返し、1行で収まる「とじる」と
高さ・上端が揃っていませんでした。

### やったこと

3画面ともHStack→VStackに変更し、Web版と同じ順序(保存・シェアするが上・とじるが下)・
同じボタン種別(「とじる」は`KyonoGhostButton`(teal色)ではなくWeb版`.btn-line`に対応する
`KyonoLineButton`(枠線・グレー)へ変更)にしました。シミュレータの実測で、両ボタンとも
幅322pt・折り返しなしの1行になることを確認しています。

**絵文字について**: Web版の該当ボタンには📤が付いていますが、本人の新ガイドライン
(ボタン・タブ・見出しにはOS絵文字を使わない・アイコンはデザイン生成のものを使う)を
受けて外しました。1:1移植を徹底するほどWeb版由来の絵文字がネイティブへ流れ込む、と
alan5からの指摘があり、今回この1件が実例だったため対応しています。ボタン文言は
「保存・シェアする」のみです。

### カード下の大きな余白

`KyonoCardModalOverlay`が原因でした。`.background()`(角丸のカード背景)を`ScrollView`
自体に付けていたため、`ScrollView`が(親の`Color.black...ignoresSafeArea()`で画面いっぱいに
なる影響を受けて)画面ほぼ全高まで広がり、実際の内容(画像+ボタン)より下に大きな空白が
できていました。`.background()`を内容(`content()`)側に付け替え、カードの見た目の大きさが
内容の実寸に戻るようにしました(この1箇所の修正で、この`KyonoCardModalOverlay`を使う
全てのカードモーダルに効きます)。

### Android確認

Androidの「保存・シェアする」「とじる」は元から横並びで折り返しておらず(D2検証時の
実機スクリーンショットで確認済み)、モーダル自体も`Dialog`ベースで内容の実寸に自動で
収まる作りのため、**同じ不具合はありませんでした。** 変更していません。

---

## 全体の回帰確認

- iOS: `SafetyCore`(8+fixtures111/111)・`RecordCore`(41)・`CardCore`(16+golden55/55)・
  `WidgetCore`(3) すべてpass
- iOS: `SearchViewUITests`(C2再発防止テスト)再実行・pass
- iOS: `xcodebuild`(Debug・Simulator)ビルド成功
- Android: `compileDebugKotlin`・`testDebugUnitTest --rerun-tasks` すべてgreen
- `npm test` 443 all green・Web版配信ファイルは無変更
- D1〜D6検証用に使った一時UIテスト(D1〜D3・D5・D6ぶん、計5本)はすべて検証後に削除・
  pbxprojからも参照除去済み(常設のUIテストは`SearchViewUITests`のみ)

## 検収基準チェック

- [x] D1: 「どこで落ちていたか」を実測(ログ)で確定 → `.task`未発火が原因、修正済み。
      Android未該当を確認。相談室の動画カードは「未実装」と判明し、HANDOFF.mdに保留記録
- [x] D2: 共有シートに「写真に保存」が出る(シミュレータ確認)。押して実際に写真フォルダに
      入るところまで確認(DBのアセット数増加で確認)。Android権限漏れなしを確認
- [x] D5: キーボード表示中も直前のやりとりが見える。送信後は返事の頭が見える位置まで動く
      (B5挙動を壊していない)。Android確認(壊れていないため未変更)
- [x] D6: ボタンが内容の長さで動かない(実測で座標不変を確認)。Android確認・同型の穴を発見し修正
- [x] D3: 2つのボタンの高さ・上端が揃う。カード下の大きな余白が消える。Android確認(未該当)

以上でappdev側の手順は完了です。E1(硬さチェック6タイプぶんのキャラ画像対応)は
「急がなくていい」とのご指示のため、この報告のあとに着手します。

## 絵文字ガイドラインについて

**「ボタン・タブ・見出しにはOS絵文字を使わない、アイコンはデザイン生成のものを使う、
文章中の絵文字はそのまま」**という新ガイドラインを受け取りました。今回D3で見つけた
📤(Web版由来)は外し、D6の絵文字除去(つぎへボタン)は結果的にこのルールと一致していました。
**iOS側213箇所の全面掃除については、正式な指示書が出るまで着手しません**(新しく足さない
だけを徹底します)。今後の移植作業でボタン・タブ・見出しの絵文字に出会った場合も、
持ち込まずに落とす方針で進めます。
