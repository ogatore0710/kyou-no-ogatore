# 🚨 最優先タスク（C2/appdev向け）— オンボ1問目の選択肢が押せない（背後のホームにタップを奪われる）

## これは何

**アプリを初めて起動した人が、オンボの1問目から先に進めません。** 実機で3回再現しています。
初回体験の入口が塞がっている状態なので、他のすべてより先に見てください。

## 再現手順（Android実機・emulator-5556・00:28時点のビルド）

```
adb shell pm clear jp.ogatore.kyouno
adb shell am start -n jp.ogatore.kyouno/.MainActivity
（9秒待つ＝オンボ挨拶4吹き出し＋1問目が出るまで）
adb shell input tap 540 2242     # ← 「大きめ（いまのまま）」の中心座標
```

**結果: 選択肢が選ばれず、オガトレ相談室が開く。**（3回とも同じ）

## 何が起きているか

uiautomatorで測った座標:
- オンボ1問目「大きめ（いまのまま）」= `bounds=[90,2206][990,2278]` → 中心 **(540,2242)**
- 背後のホーム画面の相談室FAB = `bounds=[356,2215][724,2271]` → 中心 **(540,2243)**

**1ピクセル差で完全に重なっています。** そして手前にあるはずのオンボ側ではなく、
**背後のホーム画面のボタンがタップを受け取っています。**

スクショでも、オンボシートの下端の外側に背後のホームの「💬 相談する」ボタンが透けて見えており、
そこに選択肢が重なっているのが確認できます。

問題は2つ重なっているように見えます:
1. **オンボのシートが背後のタップを遮断していない**（スクリムがタップを通している）
2. **1問目の選択肢がシートの下端からはみ出した位置**（画面最下部 y=2206-2278）に描画されている

## いつ入った回帰か

`TASK-C2-2026-07-27-screen-transitions.md`でオンボを**シート表示に変更**した際の回帰と思われます。
それ以前は通常の全画面コンテンツだったので、この重なりは起きません。

## Web版の仕様（正本 index.html:1419-1434 `updateFabs()`）

Web版はそもそも**オンボ表示中はFABを全部隠します**:

```js
const hide = currentSection==="quiz" || currentSection==="reach" || sdSheetOpen()
          || modalOpen("obuModal") || modalOpen("cardModal") || modalOpen("welcome") || ...;
document.body.classList.toggle("fabs-hide", hide);   // ← welcome=オンボ表示中は全FAB非表示
```

## ⚠️ さらに: FABの表示制御が丸ごと移植されていません

上を調べる過程で分かりましたが、ネイティブのFAB表示条件は
`MainActivity.kt:319` の **`if (currentTab != null)` だけ**です。
Web版`updateFabs()`にある非表示条件が**すべて落ちています**:

| Web版の条件 | 意味 | Web版がそう決めた理由（コード内コメント） |
|---|---|---|
| `modalOpen("welcome")` | オンボ中は全FAB非表示 | — |
| `currentSection==="quiz"`/`"reach"` | かたさチェック・とどくメーター中は非表示 | — |
| `sdSheetOpen()`/`obuModal`/`cardModal`/`dexModal` | 各モーダル表示中は非表示 | — |
| 相談室FAB: `currentSection==="home"` | ホームでは出さない | 「ホームタブには相談室カードが常設されているため、FABまで縦積みで重複表示すると**誤タップの元になる**」（2026-07-19 Fableレビュー対応） |
| 相談室FAB: `currentSection==="guide"` | 使い方タブでは出さない | 「FABがFAQグループ見出しの▾を**物理的に覆う実測があり**」（2026-07-20監査④） |
| 相談室FAB: `currentSection==="result"` | 結果画面では出さない | 「『相談室で聞いてみる』リンクとの二重導線になるため」（2026-07-20監査⑤） |
| 相談室FAB: `!sdKb()` | 知識ベース未読込なら出さない | — |
| 通信FAB: `currentSection==="guide"` | 使い方タブでは出さない | 「本文・FAQ見出しへの被り対策」 |
| 通信FAB: 1日目チュートリアル当日 | 全面非表示 | 「一本道の邪魔をしないよう。**2026-07-21実走で練習宣言の吹き出しに被るのを確認して追加**」 |

**この表の後半5行は、Web版で実際に誤タップ・被りが起きたのを実測して積み上げた対策です**
（2026-07-19・07-20・07-21の3回）。それが全部落ちているので、ネイティブでは同じ問題が再発します。

実機で確認済み:
- **ホーム**: 相談室カードのすぐ横にFABが出ている（Web版は非表示）
- **ガイド**: FABが出ている（Web版は非表示。しかも「▾を物理的に覆う」と実測記録あり）

## やること

### 1.【緊急】オンボの選択肢が確実に押せるようにする
- オンボのシートが**背後のタップを完全に遮断**すること（スクリムでブロック）
- 選択肢がシートの内側に収まって表示されること（下端からはみ出さない）
- **かたさチェック・相談室シート・カードモーダル等、他のシート/モーダルも同じ問題がないか併せて確認**すること

### 2. `updateFabs()`を移植する
上の表の条件をすべて実装してください。判定に使う「いまどの画面か」は既存の`Screen` sealed class /
enumから取れます。`fdActive()`も既存のものを呼ぶだけにして、判定ロジックを再実装しないこと。

## 検収基準

- [ ] **`pm clear`→起動→1問目の選択肢の中心をタップして、正しく次の設問へ進む**（両OS）
- [ ] オンボ表示中はFABが1つも出ない
- [ ] かたさチェック中・とどくメーター中・各モーダル表示中もFABが出ない
- [ ] ホーム・使い方・結果画面で相談室FABが出ない
- [ ] 使い方タブで通信FABが出ない／1日目チュートリアル当日は通信FABが出ない
- [ ] 他のシート/モーダルでも背後にタップが抜けないこと
- [ ] **オンボを最初から最後まで通しで完走できることを実機で確認**（4問＋締めボタン）
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test` 443緑・Web版配信ファイル無変更

## やらないこと

- オンボの設問内容・判定ロジック・ルート決定は変更しない
- シート化そのものを取りやめない（演出は残したまま、タップの遮断だけ直す）
- Web版（PWA）側の配信ファイルは一切変更しない

## 優先度

**最優先。** C（きょうやった！の中央寄せ）・じまんカード・chips一式より先にこれを見てください。
初回起動の一番最初で詰まる問題です。

## 報告

完了時、ドア配達で報告してください。**`pm clear`からオンボを完走した実機の記録**
（各設問のスクショか、通しで進めたログ）を必ず添えてください。
