# 報告: UXアップデート案 13案(Fableアイデア出し)

発注元: alan5(TASK-C2-2026-07-30-ux-ideation-fable.md、本人指示・GO済み)
実行: appdev → Agentツール経由でFableサブエージェント1体に委譲(コード探索+アイデア出し+自己選別を1体で実施)
実装: なし(本タスクは発注書の指示どおりアイデア出しのみ。書き込み操作は一切行っていない)

前提: 全案「迷いを減らす／摩擦を減らす／既にあるものを見えるようにする」のいずれかの型。コード根拠は実際に読んで確認した行番号を添え、実機でしか分からないものは「未確認」と明記(全艦共通の書式に準拠)。

appdev側で案1・案3の核心的な事実根拠(showDoneNudgeがfalseに戻る経路の有無、recordDaylogの呼び出し元)をgrepで再検証済み。Fableの主張どおり、両方とも実装済みコード上の事実として確認できた。

---

## 案1: 記録したあとも「押してね」と言い続けるひとことバブルの矛盾を直す

- **対象**: iOS `ios-native/KyouNoOgatore/KyouNoOgatore/HomeView.swift:116, 285-289` / Android `android-native/.../MainActivity.kt:815, 879, 935`
- **いま何に引っかかるか**: 動画から戻ると吹き出しが「おかえりなさい／おわったら下の「きょうやった！」を押してね✅」に変わるが、ネイティブの`showDoneNudge`は**trueにセットされるだけで、記録後もfalseに戻る経路がコード上存在しない**(appdev再確認: 両OSともgrepで`showDoneNudge = false`の代入が0件)。一方Web版は`pendingVideoReturnActive()`(index.html:2130-2135)が描画のたびに「きょう未記録か」を含めて導出するため、記録した瞬間に通常の「きょうのひとこと」へ自然に戻る。ネイティブでは記録後、無効化されたボタン(「きょうの分は完了！」)を指して「押してね」と言い続ける矛盾状態が画面を離れるまで残る。実機表示は未確認だが、状態遷移はコードから確定。
- **提案**: Web版と同じく「状態から導出」に直す(sticky状態の**引き算**)。表示条件に`!did`を含めるだけで、労い直後の画面から矛盾した指示文が消える。
- **実装規模**: S
- **憲法チェック**: 新要素ゼロ・表示条件の是正のみ。数字/演出の追加なし。○

## 案2: markDoneが捨てている「労いの3つの一言」を表示する(おやすみ券・第N章・あした節目予告)

- **対象**: iOS `HomeView.swift:416`(戻り値を破棄) / Android `MainActivity.kt:1254`(同) / ロジックは移植済み: `RecordCore/Sources/RecordCore/RecordLogic.swift:108-158`
- **いま何に引っかかるか**: Web版のmarkDoneは労いメッセージの前に、状況に応じた一言(note)を添える(app-record.js:72-151): ①「おやすみ券を◯枚つかったので連続はつながっています」②「第N章のスタート！通算はぜんぶ残ってます 戻ってくる人がいちばん強い✨」③節目前日のみ「あしたで N日目🎉 おたのしみに！」。ネイティブは`MarkDoneOutcome`(usedFreezeCount / newChapter / chapters を**既に返している**・`@discardableResult`)を**両OSとも捨てており**、休み明けの人も券で繋がった人も同じランダムcheerしか受け取らない。「おたのしみに」文言はネイティブに0件(grep確認)。
- **提案**: **既にあるもの(RecordCoreの戻り値+Web承認済み文言)を見えるようにする**。cheerTextの上に1行差すだけで、本人の言う「記録した直後の労い・明日も頑張ろうと思えるわくわく」と「やらなかった日の人が責められない」の両方に直撃する。完了演出の順番再設計(本日完了分)とは独立の欠落。
- **実装規模**: M(両OS・文言はWeb版から1:1)
- **憲法チェック**: 全文言がWeb版で稼働中の既存コピー。連続日数を煽らず「券で守られた事実」「戻ってきた人への祝福」のみ。○

## 案3: 「▶この日の動画」リンクが永遠に出ない — recordDaylogの未配線を直す

- **対象**: iOS `RecordCore/Sources/RecordCore/RecordLogic.swift:237-242`・`MyRecordView.swift:387-397` / Android `record/RecordLogic.kt:223`・`MainActivity.kt:2020-2026`
- **いま何に引っかかるか**: マイ記録のカレンダー日タップ詳細は「▶ この日の動画をYouTubeでチェックする」を表示する実装を両OS持っているが、書き込み側の`recordDaylog()`は**両OSともテスト以外に呼び出し元が1件も無い**(appdev再確認: iOSは`RecordLogicTests.swift`のみ、Androidは定義のみでUI側からの呼び出し0件)。Web版はmarkDone時に「実際にタップした動画ID」をdaylogへ保存している(app-record.js:117-127、タップ捕捉はindex.html:3952-3956)。つまりネイティブ利用者は何日使っても常に「この日は「やった！」の印だけ残っています」になり、表示コードが死んでいる。
- **提案**: **既にあるものを見えるようにする**。ホーム「きょうの1本」タップ時に動画IDを控え(iOSは`openTodayVideo`が既にタップを捕捉している)、markDone時に`recordDaylog`を呼ぶだけ。30日目の人が「あの日効いたやつ、どれだっけ」を辿れるようになる。
- **実装規模**: M(両OS)
- **憲法チェック**: 新UIゼロ・既存表示への配線のみ。○

## 案4: 設定画面の「もどる」がどこから来てもホームへ飛ぶ

- **対象**: iOS `KyouNoOgatoreApp.swift:411` / Android `MainActivity.kt:366`
- **いま何に引っかかるか**: 設定への入口はマイ記録(`MyRecordView.swift:549`)・記録カードモーダルの「記録のひかえを取る」(`HomeView.swift:697-700`)・使い方タブの「機種変更したい」(`GuideView.swift:229`)の3つだが、戻り先は両OSとも`.home`固定。マイ記録→設定→もどる、でホームに放り出される。図鑑・声・じまん・にっきは「入口は常にマイ記録なのでマイ記録へ戻す」原則で修正済み(A2・`KyouNoOgatoreApp.swift:380-387`のコメント)なのに、設定だけ原則から取り残されている。
- **提案**: `.obu(returnTo:)`と同じ「来た場所へ戻す」(**順番替え**)。デジタル弱者レンズで「戻ったら知らない画面」は迷子の典型。
- **実装規模**: S
- **憲法チェック**: 導線の整合のみ。機能追加なし。○

## 案5: 設定画面に残る`.primary`/`.secondary`をテーマ色へ統一(アプリ内ダークで読めなくなる恐れ)

- **対象**: iOS `SettingsView.swift:155, 215, 246-247, 262-263, 279, 290-292, 297, 339, 345-346, 365, 382-383, 386`
- **いま何に引っかかるか**: `KyonoTheme`は`.preferredColorScheme`を設定しない(HANDOFF.md C1節に明記)ため、「じどう」テーマで19時以降アプリ内だけダークになっても、システムがライトのままなら`.primary`はほぼ黒のまま。設定画面は§4修正でハードコード16進色はテーマ変数化されたが、`.primary`/`.secondary`が約12箇所残っており、「やるタイミング」「毎日のおしらせ」「時/分」ラベル等が**暗い背景に黒文字**になるリスクがある。実機表示は**未確認**(コード構造からの推定)。Android版は同コメント(SettingsView.swift:44-47)によればテーマ変数対応済み。
- **提案**: `colors.ink`/`colors.sub`へ置換して他画面と同じ解決系に統一(**見えるようにする**の最も文字どおりの形)。老眼・低視力レンズの必須確認項目。
- **実装規模**: S
- **憲法チェック**: 色の是正のみ。○

## 案6: 使い方タブが存在しない「あさ/よる」切替を教えている(G3と同型)

- **対象**: iOS `GuideView.swift:262-267`+モック`GuideView.swift:547-563` / Android `GuideScreen.kt:318-325` / 現状の実態: `HomeView.swift:896-899`
- **いま何に引っかかるか**: ガイド「2日目からの毎日」は「気分をかえたい日は「あさ」「よる」へどうぞ」と書き、あなた用/あさ/よるのセグメントの**静止モックまで見せている**が、ネイティブの「きょうの1本」はセグメント切替UI未実装(自動選出のみ・「第2段へ送る」とコメントに明記)。真面目に読む人ほど存在しないボタンを探す——これはG3(ボタン名不一致)で本人GOが出たのと同じ壊れ方。
- **提案**: セグメントが第2段で入るまで、ガイド側の当該文+モックを現状(自動でおすすめが替わる)に合わせて**引き算**する。※逆にセグメント本実装を選ぶ手もあるが、それは既知の第2段スコープなのでここでは文言是正のみを提案。
- **実装規模**: S
- **憲法チェック**: 約束と実物を一致させる引き算。○

## 案7: 相談室の会話が、閉じるたびに全部消える(Web版はセッション内保持)

- **対象**: iOS `KyouNoOgatoreApp.swift:127-130, 304-321`・`SoudanSheetView.swift:192` / Android `SoudanSheet.kt`(同型と推定・実機未確認)
- **いま何に引っかかるか**: Web版はシートを`hidden`にするだけでDOMが残り、閉じて開き直しても会話が続く(index.html:3472-3480)。ネイティブは「相談室シートは開閉のたびに再生成される」(KyouNoOgatoreApp.swift:129のコメントが自認)ため、`messages`は毎回空の`@State`から始まり、挨拶フラグ(`sdGreeted`)だけがルートに退避されている。プラン提案チップまで進めた会話を、うっかり✕やスワイプで閉じると全損する。誤操作の多い層ほど被害が大きい。
- **提案**: 会話状態(`messages`/`chipsMode`等)を`sdGreeted`と同じくルート階層へ持ち上げ、Web版と同じ「セッション内は続きから」にする(**摩擦の引き算**・新機能ではなくWebパリティ)。
- **実装規模**: M(両OS)
- **憲法チェック**: Web版既存挙動への一致。演出追加なし。○

## 案8: ボタン・見出しの残存絵文字の一掃(相談室FABの💬含む)

- **対象**: iOS `KyonoTabBar.swift:206-229`(FABが`Text(emoji)`描画・💬は`KyouNoOgatoreApp.swift:243`)、`GuideView.swift:228-231`(📅📱🩹🔔の4ボタン)・`165-174`(🌱📖チップ)、`MyRecordView.swift:536`(💬せんぱいの声)・`391, 405`(▶/🖼のタップ文字リンク)、`BragView.swift:120, 149`(🎬見出し・✨ボタン)、`OnboardingViews.swift:1019`(📖)、`SoudanSheetView.swift:933`(📏)、`SearchView.swift:390`(📋タップ文字)。Android側に対応箇所あり(未網羅)
- **いま何に引っかかるか**: 本人ガイドライン(2026-07-29)「ボタン・見出しにOS絵文字を使わない」の本編34種は完了済みだが、上記の**ボタン用途の絵文字が両OSに残存**している(grepで確認)。特に相談室FABは画面右下に常駐する最も目立つボタンで、タブバーのCanvasアイコン群と質感が揃っていない。`KyonoIcon.soudanBubble`は既に存在し「相談する」ボタンで使用中。
- **提案**: 既存アイコンへの差し替え+対応アイコンの無いものは絵文字削除(icon-system本編と同じ判断基準の**続き**)。FABは`soudanBubble`を流用。
- **実装規模**: S〜M(箇所数は多いが1件ずつは機械的)
- **憲法チェック**: 物差し5(トンマナ統一)の残作業そのもの。○

## 案9: 「毎日のおしらせ」トグルと時刻設定が離れていて、時刻の見出しが「カレンダーの」を名乗っている

- **対象**: iOS `SettingsView.swift:231-269(時刻ピッカー), 287-318(トグル)` / Android `SettingsScreen.kt`対応箇所
- **いま何に引っかかるか**: 通知時刻は「**カレンダーの**おしらせ時間」という見出しの下にあり、その下にAppleカレンダー/Googleカレンダーボタン+説明文2行を挟んでから「毎日のおしらせ」トグルが「**上の時間に**お知らせします」と参照する。通知だけ使いたい人は「カレンダー」見出しの下の時刻が自分の通知時刻だと気づきにくく、カレンダー連携と通知の2機能が1つの時刻を共有していることが構造から読み取れない。実機での迷い方は未確認だがラベルと参照距離はコードから確定。
- **提案**: **順番替え**: 見出しを「おしらせの時間」に改め、時刻ピッカー直後に「毎日のおしらせ」トグルを置き、カレンダー登録ボタン2つをその後ろへ。要素の追加削除ゼロで並びと見出しだけ直す。
- **実装規模**: S
- **憲法チェック**: 並び替えと見出し是正のみ。○

## 案10: ひとことにっきの「7件で切れている」事実を1行で見えるようにする

- **対象**: iOS `DiaryView.swift:22-24` / Android `DiaryScreen.kt`
- **いま何に引っかかるか**: にっきは新しい順に最大7件のみ表示(Web版と同仕様)。8件目以降のメモは消えていないのに、この画面では黙って出てこない。実は全メモはマイ記録のカレンダー日タップ(dayInfo)で読めるが、その接続を知る手がかりがゼロ。30日目の人ほど「昔のメモが消えた？」と不安になる(「記録が消えた」系FAQが最重要導線になっているアプリなので、この不安は重い)。
- **提案**: **既にある閲覧経路を見えるようにする**: リスト末尾に目立たない1行「まえのメモは マイ記録のカレンダーで日にちをタップすると見られます」(G15「この記録はこの端末に保存されるよ」と同格のトーン)。
- **実装規模**: S
- **憲法チェック**: 機能追加なし・既存経路の案内1行。○

## 案11: じまんカードの動画検索リスト、入れ子スクロールをやめて検索タブと同じ平置きに

- **対象**: iOS `BragView.swift:127-141` / Android `BragScreen.kt`対応箇所
- **いま何に引っかかるか**: A5でページ全体がScrollViewになった結果、その中に`maxHeight 240pt`の**内側ScrollView**(検索結果)が入れ子になっている。ページを撫でるつもりが内側だけ動く/その逆というスクロール衝突は高齢層の典型的なつまずき(実機の触感は未確認だが構造はコードから確定)。また結果行が文字だけで、アプリ内の他の動画一覧(VideoRow・サムネ付き)と見た目の文法が違う。
- **提案**: 内側ScrollViewの**引き算**: 検索タブと同じく先頭数件をページフローに直接並べ「さらに表示」で伸ばす(既存パターンの流用・`SearchView.swift:343-346`と同型)。
- **実装規模**: S〜M
- **憲法チェック**: 構造の簡素化と既存部品への統一のみ。○

## 案12: せんぱいの声もページ全体1枚スクロールに揃える(A5の残り)

- **対象**: iOS `VoicesView.swift:50-80` / Android `VoicesScreen.kt`要確認
- **いま何に引っかかるか**: A5(2026-07-29)は「じまんカードだけ他の全画面と違いページ全体のScrollViewが無い」を直したが、せんぱいの声も同構造が残っている: もどるボタン+説明カードが固定で、カード一覧だけが内側スクロール。Web版の#voicesはページ全体がスクロールする通常フロー。bigtext ON+小さい画面で固定ヘッダーが縦を圧迫する(実機未確認)。※再生リストタブの同型構造はHANDOFF.mdで「意図的に触らない」判断済みのため対象外とする。
- **提案**: A5と同じ修正の**横展開**(外殻ScrollView化)。
- **実装規模**: S
- **憲法チェック**: 既存判断(A5)との整合を取るだけ。再生リストの保留判断は尊重。○

## 案13: じまんカードの数字キーボードに「閉じる」手段がない

- **対象**: iOS `BragView.swift:104-105`
- **いま何に引っかかるか**: 日数入力は`.numberPad`で、iOSの数字パッドには改行(閉じる)キーが無い。リポジトリ全体に`scrollDismissesKeyboard`もキーボードツールバー(完了ボタン)も0件(grep確認)。入力後にキーボードが画面下半分を覆ったまま、閉じ方が分からず下の「カードをつくる」へ辿り着けない恐れがある。**実機での再現は未確認**(SwiftUIの既定動作に依存するため要実機)。
- **提案**: **摩擦の引き算**: `.scrollDismissesKeyboard(.interactively)`またはキーボード上の「完了」1個。見た目の追加は実質ゼロ。
- **実装規模**: S
- **憲法チェック**: OS標準の操作補完のみ。○

---

## トップ5(本人に見せる価値がある順・Fable判断)

1. **案2(markDoneの労い3点)** — 「記録した直後に労ってほしい・わくわくしたい」という本人の生の言葉に、既に書かれ承認済みの文言を配線するだけで直撃する。今日の完了演出再設計の正統な続き。
2. **案1(記録後も「押してね」バブル)** — 労った直後の画面に矛盾した指示が残るのは、せっかくの完了体験を毎日1回汚す。1条件の是正で消える最安の直し。
3. **案3(daylog未配線)** — 「この日の動画」という思い出導線が両OSで一度も動いたことがない。実装済み表示を生き返らせる「見えるようにする」の典型。
4. **案7(相談室の会話全損)** — 誤タップ1回で相談やり直しは、このアプリの主対象層に最も酷な摩擦。Web版は既に守れている。
5. **案6(ガイドが教える存在しない切替)** — 初めての人の信頼を守る直し。G3で本人GOが出た型と同一なので判断も早いはず。

---

## 次のステップ

本タスクは指示どおりアイデア出しのみで、実装は行っていません。alan5の仕分け→本人選択→別発注をお待ちします。
