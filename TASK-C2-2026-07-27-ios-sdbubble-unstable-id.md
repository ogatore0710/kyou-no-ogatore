# タスク（C2/appdev向け）— iOS: SdBubbleのidが毎回新UUIDでForEachの識別が壊れている

## 背景

alan5が段階表示タスクの検収中に発見したiOS固有の問題。iOSシミュレータはタップ自動化ができず
実挙動の計測ができないため、コードレビューで見つけた**高確度の実装バグ**として報告する
（視覚症状の確定は未。修正自体はSwiftUIの原則として無条件に正しい）。

`SoudanSheetView.swift:72`:
```swift
enum SdBubble: Identifiable {
    ...
    var id: String { UUID().uuidString }   // ← 計算プロパティ。アクセスのたびに新しいUUIDを返す
}
```

これが`SoudanSheetView.swift:334` の `ForEach(messages) { m in bubbleView(m) }` の識別子として
使われている。SwiftUIは`Identifiable.id`が安定していることを前提に差分更新するが、毎回違うidが
返るため**再描画のたびに全要素が「別物」と判定され、全吹き出しのViewが破棄・再生成される**。

### 段階表示(staged reveal)実装で問題が顕在化する理由

`revealBotMessages()`は1回の応答中に`messages`を何度も更新する（タイピングドット追加 →
`removeLast()` → 吹き出し追加、を吹き出しの数だけ繰り返す）。その更新ごとに全要素が再生成されるため:

1. **タイピングドットのアニメーションが動かない可能性が高い**
   `SdTypingDots`は`@State private var animate`＋`.onAppear { animate = true }`で明滅させているが、
   Viewが作り直されるたびに`animate`が`false`にリセットされ`onAppear`が再発火するため、
   0.6秒の繰り返しアニメーションが始まっては潰されるを繰り返す（＝静止して見える/ちらつく）
2. チャットログ全体のちらつき・スクロール位置の喪失・会話が伸びるほど無駄な再生成コストが増える

Android側は`for (m in messages)`の素朴なループで、Composeのスロット位置で識別しているためこの問題は
発生しない（＝iOS限定の症状）。

## やること

- `SdBubble`に**安定したid**を持たせる。案: 各caseに生成時に確定するUUID/連番を持たせる、または
  `messages`側を`[(id: UUID, bubble: SdBubble)]`のような安定id付きの構造にする
  （どの実装にするかは任せます。要件は「同じ吹き出しは再描画をまたいで同じidを返す」こと）
- `ForEach`が安定idで差分更新できることを確認する
- 修正後、**タイピングドットの3点明滅が実際にアニメーションするか**をシミュレータで目視確認すること
  （このタスクはビルド確認だけでは検収不十分。見た目の演出が対象のため）

## 検収基準

- [ ] 同一の吹き出しは再描画をまたいで同じidを返す（計算プロパティでの毎回UUID生成を解消）
- [ ] シミュレータで相談室のタイピングドットが3点明滅アニメーションしていることを目視確認
- [ ] 段階表示中にチャットログがちらつかない・スクロール位置が飛ばないことを目視確認
- [ ] Android側の挙動に変化がないこと（このタスクはiOSのみの修正）
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- 段階表示のタイミングロジック（待ち時間の計算式）は変更しない。今回は識別子とアニメーションの
  安定化のみ
- Android側は変更しない
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で報告してください。**タイピングドットが実際に明滅している証拠**
（シミュレータで時間差の2枚以上のスクショ、または動画）を添えてください。
