# タスク（C2/appdev向け）— 「ひとことにっき」機能が完全に欠落

## 背景

alan5の独自調査の続き。Web版の「お楽しみ機能」（index.html:872-892 `<section id="fun">`）には
じまんカード・せんぱいの声に加えて**「ひとことにっき」**（保存済みメモの一覧表示）がある。
ネイティブ版のマイ記録タブ「お楽しみ機能」カードはじまんカード・せんぱいの声への導線のみで、
**「ひとことにっき」機能自体がどこにも実装されていない**。

しかも使い方ツアーのスライド7（`TourSlideDef`「📅 マイ記録でふりかえる」）の説明文には
**「🎉お楽しみ機能（じまんカード・せんぱいの声・ひとことにっき）もこのタブの『見てみる』から
見られるよ」と明記されており、存在しない機能をユーザーに約束してしまっている状態**。優先度高。

## Web版の仕様（app-record.js:267-273 `renderDiary()`が正本）

```
「きょうやった！」時に保存できる「ひとことメモ」(kyono_memos)の一覧表示。
- 保存済みメモ(日付キー)を新しい順に最大7件表示
- 各行: 日付(MM/DD形式) + メモ本文、下線区切り
- メモが1件も無いときは「「きょうやった！」のあとにメモをのこせます」の案内文
```

メモの保存自体（`memoInput`/`saveMemo()`）はStep3のデータ層（`kyono_memos`）で既に移植済みのはず。
**今回追加するのは「保存済みメモを一覧表示する画面」のみ**（保存ロジックの変更は不要）。

## やること

- マイ記録タブの「お楽しみ機能」カードに「📔 ひとことにっき」ボタンを追加（じまんカード・
  せんぱいの声と並列）
- タップ先に一覧画面を新設: 保存済みメモを新しい順に最大7件、日付＋本文で表示。0件時は案内文
- 既存の`kyono_memos`データ（Step3で移植済みのはず）を読むだけで、書き込みロジックには触れない

## 検収基準

- [ ] マイ記録タブから「ひとことにっき」一覧画面へ遷移できる
- [ ] 保存済みメモが新しい順に最大7件表示される（実機で複数メモを保存して確認）
- [ ] メモ0件時に案内文が表示される
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- メモの保存・編集・削除ロジックは変更しない（一覧表示の追加のみ）
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で簡潔に報告してください（スクショ1枚で可）。
