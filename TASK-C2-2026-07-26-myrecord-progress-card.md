# タスク（C2/appdev向け）— マイ記録タブに「続けた記録」進捗カードが欠落

## 背景

ホーム画面の構造修正と同種の見落としをマイ記録タブでも発見。Web版マイ記録タブの先頭カード
「続けた記録」（次のお祝いまでの進捗バー＋通算/いま連続のミニ表示＋おやすみ券の説明文）が
ネイティブ版に存在しない。

## Web版の仕様（index.html:752-763・app-record.js:274-287 `renderHistory()`が正本）

```
続けた記録カード（sec-head「続けた記録」）
├ msNote: 「次のお祝い「◯◯」は通算◯日目🌱 マイペースでどうぞ」
│   （全部達成済みなら「全部の節目をたっせい！すごすぎます」）
├ msBar: 進捗バー（現在の通算日数/次の節目日数 の割合。0〜100%）
├ 通算◯日 / いま連続◯日（横並びミニ表示。それぞれ色分け: 通算=pink, いま連続=teal）
└ histFreeze: 「おやすみ券 のこり◯枚 休んだ日に自動でつかわれて連続がつながります」
```

- 次の節目は`MILESTONES`（`MS`配列のday値の一覧）から`st.total`を上回る最小値を探す
- 進捗率 = `Math.min(100, Math.round(st.total/next*100))`
- **`MS`（節目データ）はStep4で既にCardCoreへ移植済み**（iOS: `CardData.swift`の`MS: [MilestoneInfo]`）。
  新規に定義し直さず、これをそのまま参照すること

## やること

- マイ記録タブの`KyonoCard`列の先頭（カレンダーカードより前。Web版の並び順どおり）に
  「続けた記録」カードを追加
- 上記4要素（msNote相当・進捗バー・通算/いま連続ミニ表示・おやすみ券説明文）を実装
- `MS`はCardCoreから参照するだけで新規データ定義はしない

## 検収基準

- [ ] マイ記録タブ先頭に「続けた記録」カードが表示され、次の節目名・進捗バー・通算/いま連続・
      おやすみ券枚数が正しく表示される（実機で複数の通算日数パターンを確認）
- [ ] 全節目達成済みの場合の文言分岐も確認
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- ロジック・判定・データ構造は変更しない（表示の追加のみ）
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で簡潔に報告してください（スクショ1枚で可）。
