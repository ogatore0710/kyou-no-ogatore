# UX監査からの実装(G) — 本人が選んだ6件

Fableによる全体UX監査から、**本人が「①②③と劣化3つをやって」と選んだもの**です。

**着手はF(検査の底上げ)を終えてから。** Fが先にあることで、この移植作業の回帰が機械で見張れます。

**優先度は G1 → G2 → G4 → G5 → G6 → G3。** ただしG3は30分で終わるので、
気分転換にいつ挟んでも構いません。

**6件とも、Web版配信ファイルの変更は不要です。**

---

## 僕(alan5)が自分で確かめた範囲を先に書きます

今日、確かめずに書いた指示で4回まわしています。**同じことをしないため、裏を取った箇所と
取っていない箇所を分けて書きます。**

- **G1: 自分で確認済み。** `HomeView.swift:265-282` を読み、`https://www.youtube.com/` を
  開くだけの仮実装であることを確認しました
- **G2: 自分で確認済み。** Web版に `welcomeBack`(`index.html:606`)・`welcomeCheck()`(2028)・
  `renderRecheck()`(2043)が実在し、`render()`(2925)から呼ばれていること、
  **ネイティブには判定そのものが無い**ことを確認しました
- **G3・G5・G6: 自分で確認済み**(該当行を目視)
- **G4: 監査の指摘のまま。僕は確かめていません。** 着手前にあなたが確認してください

---

## G1 🔴🔴 「きょうの1本」が実体を持っていない（最重要）

### いま何が起きているか（確認済み）

`HomeView.swift:265-282`:

```swift
KyonoSectionTitle("▶️ きょうの1本")
KyonoPrimaryButton("きょうの1本を見る") {
    if let url = URL(string: "https://www.youtube.com/") { ... }
}
```

**押すとYouTubeのトップページが開きます。** サムネイルもタイトルも無く、「あなた用/あさ/よる」の
切替も、かたさタイプ別の3本も、2週間プランの今日の1本も、ネイティブには存在しません。
Androidも同型(`MainActivity.kt:992-1008`)。

コメントに「動画カタログ本体はStep7aの範囲」とありますが、**Step7a(検索・再生リスト)にも
含まれないまま宙に浮いています。**

### なぜ最優先か

**このアプリの背骨は「今日の1本をやって記録する」です。** いまTestFlightのテスターは、
ボタンを押すとYouTubeのおすすめ欄(ショート・広告・無関係な動画)に放り出され、**自力で
ストレッチ動画を探すことになります。**

さらに悪いことに、**アプリの中の説明は全部Web版の動作を約束しています。**

- 使い方タブは「あなた用/あさ/よる」の実物そっくりのモック付きで説明(`GuideView.swift:258-269`)
- FAQは「あなた用にタイプに合わせたおすすめが出ます」(`GuideData.swift:62`)
- 相談室はプラン開始時に「ホームの『きょうの1本』があなたの◯◯用になったよ」(`SoudanSheetView.swift:302`)

**ホームは1ミリも変わりません。** 約束と実物が食い違っています。

### やること

Web版 `renderToday()`(`index.html:1711-1753`)と `#todayCard`(`index.html:653-664`)の1:1移植。

**部品はそろっています** — `catalog.json`、`VideoRow`、QuizEngineの `rx`、プランの今日の1本。

**最低ライン**: 今日の具体的な1本を**サムネイル付きで出し、タップでその動画を開く**。
セグメント切替(あなた用/あさ/よる)は第2段でも構いませんが、**入れるなら日替わりローテ
(TODAY_ASA/TODAY_YORU)まで含めてWebと同じ挙動に**してください。

**「ホームの説明どおりに動く」が合格条件です。** 使い方タブのモック・FAQ・相談室の
プラン開始メッセージ、この3つが嘘にならない状態にしてください。

### 検収基準

- ホームに**今日の1本が、サムネイル・タイトル付きで出る**
- タップで**その動画**が開く(YouTubeのトップページではない)
- かたさチェック済みの人に、**タイプに合った本**が出る
- 2週間プラン中の人に、**プランの今日の1本**が出る
- **シミュレータ/エミュレータで実際に見ること。** 両OS

---

## G2 🟠 「おかえりなさい」と「2週間後の再チェック」が未移植

### いま何が起きているか（確認済み）

Web版は、**3日以上あいて戻ってきた人**に専用カードを出します(`index.html:606-616`, 2028-2053):

> おかえりなさい！また会えてうれしいです / 休んでも習慣はこわれません / 通算◯日は残っています

かたさチェックから14日後の「前屈どこまで届くようになった？」も同様。

**ネイティブは両方とも未移植です。** サボり明けの画面は普段とまったく同じです。

ネイティブに出てくる「おかえりなさい」は**動画から戻ったときの見出しの差し替え**
(`HomeView.swift:230`)で、**これとは別物です。**

### なぜやるか

**「休んでもいい」はこのアプリの世界観の中心です。** そして使い方タブで
**明文で約束しています**(`GuideView.swift:312`):

> ひさしぶりに開くと「おかえりなさい」から始まります 責められません

**約束したものが無い。** サボり明けは離脱のいちばんの崖で、そこで迎える一言があるかどうかは、
このアプリの存在理由に直結します。

### やること

`welcomeCheck()` / `renderRecheck()` の1:1移植。判定はどちらも数行(最終記録日との差分＋既読フラグ)。

### 検収基準

- 最終記録日を3日以上前に細工した状態で起動し、**カードが出ること**
- 一度閉じたら同じ日には出ないこと(`wb_seen` 相当)
- かたさチェックから14日後の状態で、再チェックのカードが出ること
- 両OS

---

## G4 🟠 検索画面の手がかりが丸ごと欠落

**この項目は僕が確かめていません。監査の指摘のままです。着手前に確認してください。**

監査によると、Web版にあってネイティブに無いものが4つ:

- **入力欄の消去ボタン**(`index.html:949` 大きな✕)
- **絞り込み中のピル**「肩こり ✕ タップで解除」(`app-search.js:62-64`)
- **「もっと見る」の残り本数**「さらに表示（あと◯本）」(`app-search.js:71`)
- **0件のときのキャラ付き「まだないみたい」カード**(`app-search.js:68`)

**優先はピル → 消去ボタン。** 監査の言い分は「タグで絞り込み中だと分からないまま、
なぜか11本しか出ない状態から抜けられなくなる」。

`index.html:430` に「50-60代・屋外」を明記したコメントがあるとのことなので、
**Web側がなぜそう作ったかを読んでから**移植してください。

---

## G5 🟢 相談室の入力欄から例文が消えている（確認済み）

- ネイティブ: `SoudanSheetView.swift:640` `TextField("気になることを入力", ...)`
- Web: `index.html:1230` `placeholder="例: 肩がこる／前屈できない"`

**何を打てばいいかの手本が消え、抽象的な指示文になっています。**
自由入力欄の前で固まるのがこの層の典型で、Web版の例文はまさにその対策です。

**Webの文言に戻すだけ。** Androidも確認してください。

---

## G6 🟢 PWA前提のFAQがネイティブアプリの中に生きている（確認済み）

`GuideData.swift:31`:
> Androidでも使えるの？ → 使えます！**ブラウザでひらいて「ホーム画面に追加」するのも同じです**

`GuideData.swift:36-37`: 「ホーム画面に追加（ついか）ってどうやるの？」の項目そのもの。

**ネイティブアプリの中で「ブラウザで開いてホーム画面に追加」と言われたら、この層は本当に
やろうとします。** A2HS系を隠す仕組み(hiddenフラグ)は既にあり3件は処理済みなので、
**この2件が漏れています。**

引き算で。隠すか、「使えます！Google Playからどうぞ」等へ書き替え。Androidも同様に。

---

## G3 🟢 記録カードのボタン名が、ツアーの案内と食い違う（確認済み）

- ネイティブのボタン: 「**記録カードを見る**」(`HomeView.swift:530`)
- ツアーSlide3(`OnboardingViews.swift:1004`)と使い方(`GuideView.swift:289`)の案内:
  「「**記録カードを画像でのこす**」を押す」
- Web版のボタン名: 「記録カードを画像でのこす」(`index.html:703`)

**ツアーを真面目に読む人ほど、存在しないボタンを探します。**

**Webに合わせてボタン名を戻すのが一番安い。** あわせて、未記録時に常時出ている
ヒント「カード画像を保存かシェアでのこしてね」は**非表示にしてください**(引き算)。

---

## 全体の注意

- **Web版配信ファイルは6件とも変更不要です。** 触らないこと
- Androidは `--rerun-tasks`
- **G1・G2は「シミュレータで実際に見る」まで**が仕事です。ビルド成功では受け取りません
- Fで作った検査に引っかかったら、**テストのほうを緩めずに実装を直してください**
- **指示書とコードが食い違ったら、実装せずに言ってください。** 今日4回そうなっています
