# TASK-C2-2026-08-06 ビルド30発注: ラウンド8（本人の朝の実機フィードバック8件）

- 発注元: alan5（頭）／実装: appdev
- 対象: **両ネイティブ（ios-native / android-native）**。web版（index.html等）は今回触らない（Web実配信は本人ゲートで駐車中のため。挙動の参照元としてのみ使う）
- ゴール: 8件実装 → `npm test`（qa.js 462+）全緑 → 実描画スクショ → 報告 → alan5検収 → TestFlightビルド30
- 本人GO済み（2026-08-06朝ラウンド・アイコン刷新R-30だけは素材待ちで**本発注に含まない**）
- 文言はすべて仮案ではなく本人指定 or 本人裁定済み。ただし本人が後から赤ペンする前提の箇所は各項に明記

---

## R-23 起動の一瞬ズレ（LaunchScreenと実スプラッシュの二重写り）修正

**現象（実測済み）**: 起動録画のフレーム分解で、起動画像→実スプラッシュの交差フェード中に
ロゴと「きょうのオガトレ」文字が**二重写り**（位置・スケール不一致のままクロスフェード）。
証拠: `art-raw/specs-build30/r23-launch-ghost-f0038.jpg` / `-f0040.jpg`（本人実機・30fpsで約10コマ続く）。

**要件**: LaunchScreen（R-11でImageRenderer生成したPNG）と実スプラッシュ画面の**描画ジオメトリを完全一致**させる。
- 原因候補: PNG生成時のsafe area/ステータスバー扱い・中央寄せ基準の差・倍率。R-11実装は `REPORT-C2-2026-08-05-build27.md` 系の報告と `KyouNoOgatoreApp.swift` / LaunchScreen assets参照
- 直し方は任せる（PNG再生成でも実スプラッシュ側の座標合わせでもよい）が、**図柄・色は現状維持**（変えるのはズレだけ）
- Android: 元々#ロゴのみ（API制約・R-11時の裁定）なので対象外。iOSのみ

**検収**: シミュレータで起動を画面収録→フレーム分解し、交差フェード全コマで二重写りが無いこと（報告にフレーム画像添付）。最終確認は本人実機。

## R-24 使い方ツアー中の「けっか」画面: 「きょうやった！」ボタン削除

**本人指示**: ツアー step2（けっか）画面の最下部の黄色「きょうやった！」ボタンは**ツアー中は表示しない**。
- ツアー中は「動画をひらく練習」ピルが主役のため（本人スクショ赤ペン）
- **通常時（ツアー外）のけっか画面は現状維持**。ガイド中(fdActive)の既存の隠し挙動も現状維持
- 両OS

**検収**: ツアー中けっか画面の実描画（ボタンなし）＋通常けっか画面の実描画（ボタンあり）を両方提出。

## R-25 再生リストタブ: 「454本の動画すべてから探す」セクション削除

**本人指示**: 再生リストタブは手動キュレーションのプレイリスト（現16本・3グループ）だけにする。
下に続く全動画一覧（「454本の動画すべてから探す」見出し以下）は**セクションごと削除**。
- 実装: `SearchView.swift` CatalogListContentView / `SearchScreen.kt`・`CatalogListScreen.kt` の該当部
- 「動画を探す」タブ（検索）は**不変**。catalog.json自体も不変
- 両OS

**検収**: 再生リストタブの実描画（プレイリストのみ・スクロール末尾まで）。

## R-26 おすすめ再設計: 「メイン＋しあげ＋悩みのおまけ」最大3本

**本人裁定（カードUIで確定）**: 今の「①まずほぐす②メインの1本③しあげ＋悩みの+1本（計4本になりうる）」を廃止し、

1. **メイン** = 今の固定1本（`typeRxPool`のrx・不変）
2. **しあげ** = 今のプールからローテで**1本だけ**選出（今は2本。`currentRx`の`need`が実質1になる形。ローテの決定的計算・重複回避は現行ロジック踏襲）
3. **おまけ** = 悩み選択（肩こり/腰痛/疲れ）がある人だけ、既存WORRY対応の1本を3本目として**リスト内に**表示。悩みなしの人は**2本で終わり**
   - おまけがメイン/しあげと重複する場合はおまけを出さない（2本のまま）＝現行の重複ガード踏襲
   - 今の「カード外の＋もう1本」枠は廃止（リストに統合）

**ラベル（仮案・本人が後から赤ペンする前提）**: 「①メイン」「②しあげ」「③おまけ: 肩こりさんへ」形式。
**見出し（本人指定）**: 「おすすめの3本: 〜」→**「あなたへのおすすめ再生リスト」**。タイプ別のサブ文（まずは「足首」から！2週間続けてみて）はサブ行として残す（仮案）。
- ガイド中（①だけ表示）の既存挙動は現状維持
- 「3本続けて再生する」リンクは表示中の本数で連結（2本なら2本）
- ツアー中の「1本目だけタップ可」挙動は現状維持
- 両OS。実装位置: `OnboardingViews.swift` currentRx/結果画面・Android同等

**検収**: 悩みあり（3本・おまけラベル）／悩みなし（2本）の実描画を両OS分。ローテが日替わりで変わることのロジック確認（rotationIndex±1のユニットテスト or 実測）。

## R-27 入れ替わり表記を「毎日」に修正

**本人裁定**: 実装は毎日ローテのまま（`CardLottery.rotationIndex`不変）。表記だけ直す。
- 「おすすめは3日ごとに自動で入れ替わります」→**「おすすめは毎日自動で入れ替わります」**
- iOS: `OnboardingViews.swift:1287`。Android同等箇所。R-26後の新リストの下に置く

**検収**: 実描画（R-26のスクショに写り込みでよい）。

## R-28 続けた記録カード刷新（文言＋すごろく道マイルストーンUI）

**本人指示＋裁定済み（モック=`art-raw/specs-build30/ms-suguroku-mock-3an.png`の案A）**:

1. 見出しを**「次のお祝いポイントは「◯◯」」**に（◯◯=次の節目名・ピンク強調は現状踏襲）。
   「は通算N日目」「マイペースでどうぞ」は**削除**
2. 「休んだ日に自動でつかわれて連続がつながります」の行を**削除**（「おやすみ券 のこり3枚」は残す）
3. 進捗バーを**すごろく道**に置き換え:
   - 横一直線のトラックに節目ノードを並べる: 左端=「いま」（現在通算・teal）→ 次の節目（★・ピンク縁の大きめノード）→ その先の節目2〜3個（薄いドット）→ 右端に「…」
   - 達成済みの節目があるときは「いま」の左に塗りつぶし（teal）で直近の達成済み1個まで表示（ウィンドウ表示・全17個は並べない）
   - トラックの塗り（進捗）は 現在通算/次の節目 の割合
   - 節目一覧の正本 = `app-card.js:23-41 MS`（3/4/7/14/21/30/50/66/100/150/200/300/365/500/730/1000/1900・ネイティブ移植済みの同配列）
   - ノード下のラベルは「3日」「4日」形式・次の節目だけピンク太字
   - reduceMotion配慮のアニメは不要（静的でよい）
4. 「通算N日 いま連続N日」「おやすみ券 のこりN枚」は現状の並びのまま下に残す
- 両OS。`MyRecordView.swift:316-358`・`MainActivity.kt:2355-2403`が現状の該当部

**検収**: 通算1日（初期）と通算5日相当（3日★達成済み+次7日）の2状態の実描画。文言grep（「マイペースでどうぞ」「休んだ日に自動」が0件になること）。

## R-29 せんぱいの声: 開く前のカード高さを全カード統一

**本人指示**: 「開く前サイズが違うのきになる おなじにして」。
**現状の正体**: `VoicesView.swift:108-114`（Android `VoicesScreen.kt`同）で、ガタつき対策として
表裏を常時composeし**コンテナ高さ＝大きい方（=裏の長文）**に固定 → 閉じたカードの枠が裏文章の長さでバラバラ＋カード間に不自然な空白。

**要件**:
- **閉じた状態は全カード同一のコンパクト高さ**（表面の内容基準・min-height現行150pt相当）
- めくったら高さが**なめらかに伸びる**（アニメーション付き）。以前の「めくった瞬間に一覧全体がガタつく」不具合を**再発させない**（高さ変化を明示アニメーションにする）
- Fable監査GO-3の裏面ヒットテスト対策（`allowsHitTesting`）は維持
- 両OS

**検収**: 閉じた2枚の高さが同一であることの実描画＋めくり動作の録画（フレーム分解でガタつきなし）。

## R-31 キャラ絵の整理: 廃止4枚の全数置換＋ヘッダーイラスト削除＋ひとこと言葉合わせ

**本人指示（赤ペン画像で確定）**: `chara` / `chara-2` / `chara-3` / `chara-hitokoto` の4枚は**アプリ内全面廃止**
（ウィジェット・記録カードの既存禁止指定と同じ4枚。それをアプリ全体に拡大）。使用可 = cheer / congrats / cracker / crown / good / kaikyaku の6枚。

置換表（本人指示＋alan5仮案。★=本人指示そのもの）:

| 使用箇所 | 現在 | 置換 |
|---|---|---|
| ★ヘッダー「#きょうのオガトレ」横 (`KyonoComponents.swift:35`+Android) | chara | **イラストごと削除**（タイトル+サブタイトルのみに。レイアウト詰め） |
| ★きょうのひとこと吹き出し | chara-hitokoto | **言葉に合わせて5枚を使い分け**（crown以外。下の割り当て表・日替わり45篇と同indexで対応） |
| おかえりなさい吹き出し（動画復帰時） | chara-hitokoto | good |
| 相談室チャットの顔アイコン（sd-ava相当・全箇所） | chara-hitokoto | good |
| 練習宣言吹き出し（ガイド中結果画面） | chara-hitokoto | good |
| ホームかたさチェックカード (`HomeView.swift:1122`+Android) | chara-3 | good |
| 検索の空振り画面 (`SearchView.swift:400`+Android) | chara-2 | kaikyaku |
| 使い方「おぼえるのはこれだけ！」(`GuideView.swift:174`+Android) | chara | cheer |
| その他grepで出た残り全部 | — | 文脈に合う6枚から選び報告に明記 |

- 置換後、**両ネイティブのアセットから廃止4枚のファイルを削除**し、参照が0件であることをgrepで示す（iOS: `chara`/`chara-2`/`chara-3`/`chara-hitokoto`、Android: `chara`/`chara_2`/`chara_3`/`chara_hitokoto`。**命名規則がOS間で違う点に注意**）
- qa.jsに「廃止4枚への参照が0件」の機械検査を1本追加（70条・ウィジェットの既存検査があれば拡張でよい）

**きょうのひとこと割り当て表（index 0-44 → 画像。QUOTES配列と同順）**:

```
0 good / 1 cheer / 2 congrats / 3 kaikyaku / 4 kaikyaku / 5 kaikyaku / 6 kaikyaku /
7 cheer / 8 good / 9 cracker / 10 cheer / 11 cheer / 12 good / 13 congrats /
14 kaikyaku / 15 kaikyaku / 16 kaikyaku / 17 kaikyaku / 18 good / 19 good /
20 good / 21 cheer / 22 good / 23 cheer / 24 cheer / 25 cheer / 26 good /
27 good / 28 kaikyaku / 29 kaikyaku / 30 congrats / 31 cheer / 32 cracker /
33 cheer / 34 good / 35 good / 36 kaikyaku / 37 good / 38 cheer / 39 cheer /
40 congrats / 41 kaikyaku / 42 congrats / 43 kaikyaku / 44 cheer
```

（方針: 実技アドバイス系=kaikyaku（ストレッチ中の絵）／称賛・えらい系=congrats／応援・前向き系=cheer／
寄り添い・だいじょうぶ系=good／達成の瞬間系=cracker。crownは節目専用のため不使用=本人指示）

**検収**: 置換8箇所+残り全部の実描画（ひとことは割り当ての違う3例以上）。参照0件grepの結果貼付。qa.js新検査が緑。

---

## 共通ルール（毎回と同じ・省略禁止）

- 1項目=1コミット以上・コミットメッセージ日本語・R番号を頭に
- `npm test`（qa.js 462+新規）全緑になってから報告
- 実描画はライトテーマ・文字サイズ「ふつう」基準。R-29はめくり録画も。**文字サイズ「おおきめ」はR-24/R-26/R-28の3箇所で追加確認**（R-17の教訓: おおきめ検証漏れ）
- 見出し⇔絵の一致検収（恒久項目）
- 報告は `REPORT-C2-2026-08-06-build30-round8.md`。判断に迷ったら実装で先に進まずドアで質問
- ビルド番号30・whatsNewはalan5が書く（appdevは提出準備まで）
