# タスク（C2/appdev向け）— かたさチェック・結果画面・とどくメーターのパリティ7件

## 背景

夜間監査（Fable 4体）の第2弾。かたさチェック／結果画面／とどくメーターの3画面から
【大】1件・【中】3件・【小】3件が出ました。alan5がコードで裏を取っています。

判定ロジック本体（`decideType` の全分岐・閾値・タイブレーク表・`rotationIndex`・
`currentRx` の重複回避・`setReach` の当日上書き/200件トリム/自己ベスト保護・
`REACH_FROM_MOMO` の自動転記3条件）は**すべて1:1で一致していました**。
問題は判定の周辺、つまり「入力の守り」と「見せ方」に集中しています。

---

## 1.【大】ガイド中の結果画面が「削ぎ落とされて」いない（両OS）

### Web版の仕様（正本 app-quiz.js:291-299）

```js
// 2026-07-21 5視点検証C(PO承認): ガイド中の結果画面は「タイプ+①」に削ぎ落とす。
// 長文解説(rHope/rPT)・ペース目安(rPace)・②③・悩みの+1本・相談室リンクは翌日以降(通常表示)に回す。
// 下部の「きょうの1本へ」「もう一回チェックする」も本人指摘(2026-07-21)でガイド中は隠す
// （一本道=①をタップ→もどる→記録、以外の分岐を見せない。タブバーからの脱出は常に可能）
for(const id of ["rHope","rPT","rPace","rGoHomeBtn","rRecheckBtn"]){
  el.classList.toggle("hidden", guide);
}
```
加えて `app-quiz.js:332` `if(!guide && sdKb())` で**相談室リンクもガイド中は出さない**。

### ネイティブの現状

`fdGuideActive` は両OSとも**3箇所でしか使われておらず**、rxカードの差し替え（720行付近）と
ボタン文言の出し分けだけです。**「隠す」処理が1つも移植されていません。**

- Android `OnboardingScreens.kt:696-708`（hope/PT）、`829-850`（rPace・相談室リンク）、`872`・`875`（2ボタン）
- iOS `OnboardingViews.swift:761-772`、`847-862`、`878-880`

結果、**ガイド中の初回ユーザーに長文解説と複数ボタンが全部出ます。**

### なぜ重いか

`fd-guide-ui-branch` タスクで「ガイド中だけ出す専用UI」は実装してもらいましたが、
**その裏返しの「ガイド中は隠す」側が抜けていました。** 一本道設計は
「出すものを足す」と「余計なものを消す」の両方で成立します。片方だけだと、
①の下に通常どおりの解説とボタンが並び、結局どこを押せばいいか分からない画面になります。

これは 2026-07-21 の5視点検証Cで**PO承認済みの仕様**です。

### やること

ガイド中（`fdActive()`が真）は以下を**隠す**:
- 長文解説（rHope / rPT 相当）
- ペース目安（rPace 相当）
- 「きょうの1本へ」ボタン（rGoHomeBtn 相当）
- 「もう一回チェックする」ボタン（rRecheckBtn 相当）
- 相談室リンク（`!guide && sdKb()` の条件）

※ `rTourBtn` は既に呼び出し元で正しくガードされています（`obTourAfterQuiz && !fdGuideActive`）。
そこは変えないこと。

---

## 2.【中】クイズ選択肢の二度押しガードが無い（両OS）

**Web版**: `app-quiz.js:180` — 回答タップ直後に全選択肢を `disabled=true` にする。

**ネイティブ**: 両OSともガードなし（Android `OnboardingScreens.kt:557-575` の `.clickable`、
iOS `OnboardingViews.swift:629` の `.onTapGesture`）。

**なぜ直すか**: 想定層（機器が苦手な層）は**ダブルタップの癖がある人が多い**です。
素早く2回押すと次の設問が意図しない回答で確定し、**判定の入力そのものが汚れます**。
タイプ判定にも、とどくメーターへの自動転記にも波及します。
今回の監査で「判定の正しさ」に影響しうる唯一の項目でした。

**やること**: 回答を受け付けたら、次の設問が描画されるまで選択肢を無効化する。

---

## 3.【中】とどくメーターの点灯条件から「きょう記録した場合のみ」が落ちている（両OS）

**Web版**: `app-record.js:249`
```js
const on = arr.length && arr[arr.length-1].d===today && arr[arr.length-1].lv===i;
```
→ **最新記録が「今日」のときだけ**点灯。

**ネイティブ**: 日付を見ていない
- Android `MainActivity.kt:1477` `val on = latest?.lv == lv`
- iOS `MyRecordView.swift:342` `let on = reachList.last?.lv == lv`

**なぜ直すか**: Web版では**消灯＝「きょうはまだ測っていない」の合図**です。
ネイティブでは先週の記録が点灯し続けるため、「もう今日は記録済み」と誤解して
**週1回の計測をスキップする方向に誤誘導**します。とどくメーターは
「週1回でOK」と案内している機能なので、この合図が消えると計測されなくなります。

---

## 4.【中】Q3・Q4のポーズ図解（判定基準入り）が未移植（両OS）

**Web版**: `app-quiz.js:92-137` に `QUIZ_ART[2]`（ひじ上げ・**「はな/あご」の高さ基準線入り**）と
`QUIZ_ART[3]`（しゃがみ・かかと浮きのズーム円）の書き下ろしSVGがあります。
コメントに「本人のYouTube『肩甲骨12分』動画準拠」「あたま/あごの高さ線＝両ひじが上がる目安」と、
**判定基準そのものの説明**が書かれています。

**ネイティブ**: 両OSとも Q1/Q2 の写真だけで、Q3/Q4 は図なし。
しかもコメントが「kenko/ashi/worryは元々手描きSVG/**装飾**で写真なし」と書いており、
**装飾と誤認して移植対象から外しています**（Android `OnboardingScreens.kt:306-308`・
iOS `OnboardingViews.swift:313-314`）。

**なぜ直すか**: Q3の図は「はな/あごの高さ」という**判定基準の可視化**であって装飾ではありません。
文章だけだと動作を誤解して誤回答し、タイプ誤判定につながります。想定層ほどそうなります。

**やること**: Q3・Q4に図解を用意する。SVGをそのまま持ってくるのが難しければ、
**判定基準（高さの目安線・かかとの浮き）が伝わる図**であれば実装方式は任せます。

---

## 5.【小】まとめて直せるもの（4件・両OS）

| 項目 | Web版の正本 | ネイティブの現状 |
|---|---|---|
| **Q5だけ段階色を付けない** | `app-quiz.js:168-169` 「段階色(2026-07-21本人要望): **数値スコアの設問(Q1-Q4)だけ**」→ `typeof o[2]==="number"` で判定 | 両OSとも `i % 4` で全設問に着色。Q5「疲れ・眠りの浅さ」が最暗色＝「悪い」に見え、色の意味（柔らかい→硬い）が崩れる |
| **戻ったとき前回の選択に枠色** | `app-quiz.js:170-171` `.opt.on{border-color:var(--teal)}` | 両OSとも無し。「まえの質問へ」で戻ると自分が何を選んだか分からない |
| **クイズの進捗ドット** | `index.html:719` `.dots` + `app-quiz.js:175-176` | 両OSとも「Q1 / 5」テキストのみ。ツアー画面にはドットがあるのにクイズには無い |
| **ガイド中①のバッジ** | `app-quiz.js:320` `videoCard(rx[0], "きょうはこれ1本でOK！")` | 両OSとも `badge = null` |

---

## 6.【小・Android限定】システム「もどる」の無防備

**Web版**: `app-quiz.js:156-158` で設問ごとに `history.pushState` を積み、
**ブラウザ/スワイプの戻るで1問ずつ遡れる**設計。中断時は確認ダイアログ（index.html:1649）。

**Android**: `BackHandler` がアプリ全体に1つも存在しません（grep確認済み）。
ハードウェアの「もどる」を押すと前の設問に戻らず、
**「回答を消してホームにもどる？」の確認も通らずに回答が消えます。**

Androidの想定層は「◀」ボタンを常用するので、誤タップで5問分が飛びます。

**やること**: クイズ中の `BackHandler` で「1問戻る」、qi==0 なら既存の確認ダイアログを通す。

---

## 7.【小・Android限定】「👇 タップしてえらんでね」がお手本写真の上にある

**Web版の順序**（index.html:713-718）: qArt → qtitle → qnote → **tap-hint → opts**（ヒントは選択肢の直上）
**Android**（`OnboardingScreens.kt:536-544`）: title → note → **hint → 写真** → opts
→ 👇が写真を指してしまい、写真をタップして反応がなく戸惑います。iOSは正しい順序です。

---

## 検収基準

- [ ] ガイド中の結果画面から、長文解説・ペース目安・2ボタン・相談室リンクが消える（両OS）
- [ ] ガイド中でないときは従来どおり全部出る（退行させない）
- [ ] 選択肢を素早く2回タップしても、次の設問が勝手に確定しない（両OS）
- [ ] とどくメーターのボタンが「きょう記録したときだけ」点灯する（両OS）
- [ ] Q3・Q4に判定基準が伝わる図が出る（両OS）
- [ ] Q5に段階色が付かない／戻ると前回の選択に枠色が付く／進捗ドットが出る／①にバッジが出る
- [ ] Androidのシステム「もどる」で1問戻れる・qi==0では確認ダイアログが出る
- [ ] Androidの👇ヒントが選択肢の直上にある
- [ ] **判定ロジックは一切変更しない**（QuizEngine・RecordLogicのテストが緑のまま）
- [ ] 安全系テスト（111+engine-fixtures）緑・`npm test` 443緑・Web版配信ファイル無変更

## やらないこと

- `decideType` / `currentRx` / `setReach` の判定ロジックは触らない（一致確認済み）
- 結果画面のテーマが "auto" 固定になっている件は**別途**（今回は触らなくてよい）
- Web版（PWA）側の配信ファイルは一切変更しない

## 優先度

`search-playlists-and-fullwidth-space` の次。
その中では **1（ガイド中の削ぎ落とし）→ 2（二度押し）→ 3（とどくメーター）** の順でお願いします。
1は初回ユーザーの脱落対策の核、2は判定の入力が汚れる唯一の項目です。

## 報告

完了時、ドア配達で報告してください。
**ガイド中の結果画面のスクショ（削ぎ落とし後）**と、**Q3の図解**を添えてください。
