# アイコン方針(I) — 試作1件目 + 判断2件の報告

指示どおり、**まず「ふきだし」1つだけ**を描いて実際の画面に置きました。残り9個には
まだ着手していません。

## 試作: ふきだし(💬 → アイコン)

- 対象: Home画面「オガトレ相談室」カードの`KyonoPrimaryButton("💬 相談する")`
- 実装: 新しいアイコンを描き起こさず、**既存の`KyonoIcon.soudanBubble`(タブバーと同じ
  Canvas意匠・セクション見出し「オガトレ相談室」で既に使われている形)をそのまま再利用**
  しました。`KyonoPrimaryButton`に`icon: KyonoIcon?`という差し込み口を追加しただけです
  (新規部品ではなく既存部品の拡張)。
- 配色: 黄色ボタンの上に乗るため、塗り(fill)は無し(ボタンの黄色がそのまま透ける)、
  線(stroke)はタブバーと同じ固定inkColor、内側の2本線(accent)は`colors.yellowInk`
  (ボタン文字と同じ色・実は`inkColor`と同一16進値なので完全に馴染みます)。

**シミュレータで実際に確認しました**(iPhone 17シミュレータ・スクリーンショット取得済み):
「相談する」ボタンの中に、タブバーの5つ・セクション見出しの吹き出しと同じ手で描かれた
小さな吹き出しアイコンが文字の左に出ています。絵文字は完全に消えています。

**このスクリーンショット単体をここに貼れないため、必要であれば`git pull`後に
シミュレータで直接ご確認ください**(Home画面「オガトレ相談室」カード、`相談する`ボタン)。
コードはcommit `37ee548`でpush済みです。

## 判断① 「うつす・はこぶ」を1つにまとめるか

**分けたほうが良いと判断しました。** 理由: `SettingsView.swift`で実際のレイアウトを確認した
ところ、「📦 記録をコピーする」「📋 コピーした記録を自動で読みこむ」「📥 よみこむ」の
3つのボタンは**同じ「記録のひっこし」セクション内に隣接して並んでいます**(コピー→
自動読みこみ→手動よみこみ、という一続きの操作フローの中の3つの別ボタン)。ここを
1つのアイコンにまとめると、隣り合う3つのボタンが同じ絵になり、**「どのボタンが何をするか」
という、まさにアイコン化でいちばん守りたいはずの見分けやすさを損ないます。**
3つとも描く方針で進めます(具体的な絵柄は残り9個のバッチで提案します)。

## 判断② ✕ と ✓ について

**個別に見ました。すべてUI記号としての用法で、装飾目的ではありませんでした。そのままで
良いと判断します。**

- `SoudanSheetView.swift`・`ObuView.swift`の`✕`(18pt): シートの閉じるボタンそのもの
- `SearchView.swift`の`✕`(14pt・G4で追加): 検索欄の消去ボタンそのもの
- `SearchView.swift`の`"\(activeTag) ✕"`(G4で追加): 絞り込みピルの「解除できる」印
- `MyRecordView.swift`の`✓`(9pt・白文字): カレンダー上の「やった日」バッジの中身
- `HomeView.swift`の`"のこしました ✓"`: メモ保存後の確認文言に付く一言。**これだけは
  やや境界線上**(文章に近い使われ方)ですが、絵文字ではなく記号(Unicode dingbat)であり、
  かつ既存の「文章中の絵文字は残す」方針とも矛盾しないため、そのままにします。

いずれもタブバーの意匠を模した独自アイコンにする必要は無く、**既存のまま**が正しいという
判断です。

## HANDOFF.mdへの記録

指示どおり、「Web版は絵文字のまま・ネイティブだけアイコン化した」ことをHANDOFF.mdの先頭に
記録しました(commit `f6d6022`)。次に1:1移植をする人が移植漏れと誤認しないための注記です。

## 回帰確認

- iOS: `xcodebuild`(Debug・Simulator)ビルド成功
- iOS: `SafetyCore`・`RecordCore`・`CardCore`(card-golden 55/55)・`WidgetCore` すべてpass
- `npm test` 459 checks all green
- Web版配信ファイルは無変更

## 次

**OKをいただいてから残り9個(おいわい・ものさし・め・うつす/はこぶ×3・おと・チケット・
はぐるま・かね・はてな)へ進みます。** 使い回し4つ・削除ぶん・👇・✕✓の適用もその後です。
