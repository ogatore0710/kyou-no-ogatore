# タスク（C2/appdev向け）— とどくメーター記録時のお祝いメッセージが固定文言のまま

## 背景

alan5の第2弾監査（未監査領域スイープ）で発見。とどくメーターの段位ボタンをタップした直後の
メッセージが、Web版は3パターン分岐しているのに対し、ネイティブは常に「記録しました！」の
固定文言になっている（Android `MainActivity.kt:1088`、iOS `MyRecordView.swift:340`）。

## Web版の仕様（app-record.js:238-243が正本。`REACH_LV`配列=index.html:1971）

```
段位タップ→setReach(lv)実行後、タップ前の自己ベスト(best)と比較して3パターンに分岐:
1. lv > best && best > 0 (自己ベスト更新):
   「🎉 自己ベスト更新！「{REACH_LV[lv]}」」+ 別行で「記録カードにも入ります」
2. lv >= 4 && best === 0 (初回でいきなり高レベル4以上):
   「最初から「{REACH_LV[lv]}」！すばらしい」
3. それ以外(通常の記録):
   「記録しました！じわじわ伸びていきますよ」

REACH_LV = ["", "ひざまで", "すねまで", "足首まで", "つま先タッチ", "ゆかにベタッ"]
```

## やること

- 段位タップ時、タップ前の自己ベスト（既存の`getReach()`等から算出できる過去最高lv）を取得し、
  タップ後のlvと比較して上記3パターンのメッセージ分岐を実装
- 判定ロジック自体（setReach・段位記録）は変更しない。表示メッセージの分岐のみ追加

## 検収基準

- [ ] 自己ベスト更新時に🎉メッセージが表示される
- [ ] 初回記録でいきなりlv4以上を選んだ場合に「最初から」メッセージが表示される
- [ ] それ以外は通常メッセージが表示される
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- 段位の記録・判定ロジック自体は変更しない
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で簡潔に報告してください（スクショ1枚で可）。
