# タスク（C2/appdev向け）— ネイティブ移植 Step 7b（じまん・声・オガトレ部・設定+パリティ突合）

## 背景

Step 0〜7aすべて検収OK。**これがStep 8（9月頭の差分同期）を除く最後のUI実装ステップ**。§2-1の1:1対応表を全行「実装済み/削除済み」に塗り切る。

## やること

`NATIVE-MIGRATION-MASTERPLAN-2026-07-25.md`の**§6 Step 7b**をそのまま実行する。

1. じまんカード（drawBragCard→BragCardRenderer。§2-1備考のとおりCardRendererとは独立の描画器）
2. お楽しみ・声・オガトレ部（obu-feed.json同梱）
3. 使い方タブ（A2HS関連FAQは非表示、削除ではない。§2-2の一覧表どおり）
4. 設定（テーマ/文字サイズ/エクスポート・インポートUI。Step 3で移植済みのbuildExportString契約を使うだけ）
5. **§2-1対応表の全行を「実装済み/削除済み」のどちらかに塗り切る**（未実装行がゼロになることを確認）

## 検収基準（マスタープラン§6 Step 7bと同一）

- [ ] §2-1対応表の全行にステータスが付き、未実装行がゼロ
- [ ] エクスポートがWeb版へ逆インポート可能（PWA側で読み込めることを実ファイルで確認——Web版の既存インポートUIを使うだけでWeb変更なし）
- [ ] 安全系テスト（111+engine-fixtures）緑のまま

## やらないこと

- Step 8（9月頭の差分同期）は今回のスコープ外。それまでの開発はここで一区切り
- Web版（PWA）側の配信ファイルは一切変更しない（エクスポート逆インポートの確認で使う既存インポートUIも含め、Web側は読むだけ）
- 引き続き`git ls-files ios-native/ android-native/ | wc -l`が実ファイル数（.gitignore対象除く）と一致しているか時々確認すること

## 報告

Step 7b完了時、ドア配達で以下を含めること:
- 検収基準3項目のPASS/FAIL
- §2-1対応表の最終状態（未実装行ゼロの確認結果）
- ここまでのStep 0〜7b全体を通した消費トークンの概算（分かる範囲で）
