# REPORT-C2-2026-08-02-build17-feedback-fixes.md

発注元: TASK-C2-2026-08-02-build17-feedback-fixes.md（本人のビルド16実機フィードバック9件＋
ツアー内かたさチェック結果の格上げ）

以下、P部・Q部それぞれの結果。「自分で確認済み」と「未確認」は各項目内で明記する。

## P部

### P-1: ツアーシートの背後透け — 修正済み・両OS確認済み
`OnboardingScrim`(iOS)/背景色付きスクリム(Android)を不透明な`colors.bg`に差し替え。
シミュレータ/エミュレータで、システムダークモード+アプリ内テーマ「明るい」の食い違い条件下でも
背後が透けないことをビフォー/アフター両方で確認済み。詳細: `ios-native/verify/build17-p1-tour-scrim/`
`android-native/verify/build17-p1-tour-scrim/`。

### P-2: ツアー8枚+締めの改行 — 修正済み・8/9枚確認済み
発注書の改行位置をそのまま適用(6枚目「尾形さん」→「尾形」も同時実施)。8枚は両OS実描画で
確認済み。9枚目(締め)はUI自動操作の経路構築が難航し、コード確認のみで実描画は未確認と明記
(既存の代替導線=8枚版では検証済み)。詳細: `ios-native/verify/build17-p2-tour-linebreaks/`。

### P-3: 初回起動テーマ初期値 — 修正済み・両OS確認済み
現行の初期値は`"auto"`だった(alan5未確認事項として指定どおり報告)。`"light"`に変更。
全呼び出し箇所(iOS 18箇所・Android 19箇所)を揃え、設定ピクカーの選択状態まで含めて確認済み。

### P-4: ライトモード入力欄が黒いまま — 修正済み・両OS確認済み
発注書指定の3箇所(iOS: HomeView/SoudanSheetView/SettingsView・Android: SoudanSheet/
SettingsScreen×2)を修正。全数棚卸し(iOS/Android各8箇所)を実施し、iOSでは追加で
エクスポート欄(TextEditor)の同症状を発見・修正。システムダーク+アプリ内ライトの食い違い条件で
実描画確認済み。詳細: `ios-native/verify/build17-p4-input-theme/`
`android-native/verify/build17-p4-input-theme/`。

### P-5: 相談室チップの横スクロールが伝わらない — 修正済み・両OS確認済み
iOS `FadingChipRow`のcontentWidth測定が`.preference()`の初回パス値0に固定される実装バグを
発見・修正(GeometryReaderのonAppear/onChangeで直接@Stateへ書く方式に統一)。両OSとも右端のみ
だった既存の矢印+フェードと対になる左端版を追加し、両方向のスクロール可能性を伝えるようにした。
詳細: `ios-native/verify/build17-p5-chip-scroll/` `android-native/verify/build17-p5-chip-scroll/`。

### P-6: かたさチェック設問切り替えの残像 — 修正済み・iOS確認済み(Android対象外)
iOS: `QuizOptionCardStyle`の押下ハイライト用`.animation(value: pressed)`が祖先の
`.transaction{}`では上書きできず、タップ離しと設問切替が重なると背景色がクロスフェードして
見える欠陥を特定。選択肢一式に`.id(q.key)`を付け設問ごとに別ビューとして作り直させることで
補間の可能性自体を無くした。連写スクリーンショットで前後の設問間で色の混ざりが無いことを確認済み。
Androidは元々背景色にアニメーション修飾子が付いておらず対象外(エミュレータでの連写撮影で確認)。

### P-7: ライトモードのタブ切り替えで黒フラッシュ — 修正済み・iOS確認済み・Android対象外確認済み
iOS: ルートZStackに一度も`.background()`が指定されておらず、画面切替のクロスフェード中の隙間
から`UIWindow`既定の黒が露出していた欠陥を特定・修正(`KyonoBackgroundColor()`を明示追加)。
連写スクリーンショット(60枚)で黒フレームが無いことを確認済み。Android: `windowBackground`が
既にアプリの背景色(#FFFAF3)を指しており同種の欠陥が起きない構造であることをコード確認+
連写撮影で確認済み(対象外)。

### P-8: 相談室タッチグローの減光 — 発生源特定は未確定・暫定対処のみ実施
**alan5の実機実測(録画3フレーム差分)を受けた調査結果**: シミュレータでは輝度変化を再現
できず(alan5実測+4.4〜4.8/255に対し、シミュレータでの計測は0.1/255未満)、
`SoudanSheetView`内の`.animation()`/`.background()`修飾子の全数棚卸しでも該当する候補は
見つからなかった。alan5の仮説①(`.presentationBackground()`未指定によるシステム既定背景の
露出)に対する直接対処として`.presentationBackground(colors.bg)`を明示追加したが、
**この修正で実機の現象が解消するかは未検証**(シミュレータで現象自体が再現しないため)。
Android側はネイティブのシート機構を使わない自前描画(Box+不透明background)のため、
同種の欠陥が構造的に起こり得ないことも確認済み。詳細・提案:
`ios-native/verify/build17-p8-sheet-presentation-background/report.md`。
**実機での再検証をお願いします。**

### P-9: タッチグローをかたさチェック選択肢にも追加 — 保留
P-8の発生源特定・解消が未確定のため着手を保留。P-8の実機再検証結果を受けて、
①発生源が特定でき解消できていれば「意図した新規実装(押下中の要素への淡いハロー・
不透明度10〜15%・半径1.5倍・フェード各0.15s)」を相談室チップ+かたさチェック選択肢の
両方に実装、②なお発生源不明ならさらなる調査、の順で進めます。

## Q部(ツアー内かたさチェック結果の格上げ)

### Q-1: 結果をフル版に — 修正済み・両OS確認済み
`fdGuideActive`(初回ジャーニー中)のときだけ「タイプ+①」に削ぎ落としていた分岐をすべて撤去し、
通常のかたさチェック結果と同一のフル内容(タイプ解説+動画3本+ペース目安+相談室リンク)を
常に表示するよう統一。差分洗い出し・修正内容は
`ios-native/verify/build17-q1q3q4-result-fullversion/report.md`参照。

### Q-2: 結果が本物として残ること — 保存は元々無条件・再閲覧も確認済み
`store.set("type", ...)`は修正前から`fdGuideActive`の分岐が無く無条件に保存されていたため、
保存についてはコード改修不要と判明(alan5未確認事項として指定どおり調査・報告)。オンボ→クイズ→
結果の一連を実機相当で通し実行し、`kyono-store.json`にツアー内の結果が正しく保存されること、
マイ記録の既存「かたさタイプ」カードから同じ結果を再閲覧できることを両OSで確認済み。

### Q-3: 練習モードポップアップの廃止 — 修正済み・両OS確認済み
旧「きょうは練習してみよう」専用カード(単独動画+誘導文言)を廃止し、指示どおりの一行
「この結果はほんもの！マイ記録からいつでも見られるよ」+ボタン「つぎへ（記録の練習）」に
差し替え。ボタンタップで記録練習(1日目クリア演出・実際のタイプ結果を反映したカード)が
正しく発火することを両OSで確認済み。

### Q-4: 動画サムネはツアー中タップ不可のまま — 修正済み・両OS確認済み
Q-1でフル版の動画3本リストを表示するようにした際、`fdGuideActive`中は各動画の`openUrl`を
no-opに差し替え、タップしても何も起きないようにした(コード上の保証)。Q-2の検証で確認した
とおり、ツアー後(マイ記録からの通常結果表示)では`fdGuideActive`がfalseになるため、動画は
通常どおりタップ可能。

## 検証・ビルド

- `npm test`(node scripts/qa.js): 461件全通過(exit 0)。
- Android: `./gradlew testDebugUnitTest --rerun-tasks` 全通過。
- iOS: `xcodebuild build`成功(シミュレータ)。
- 一時検証用XCUITest/pbxprojエントリはすべて後始末済み(`npm test`の
  「DO-NOT-COMMIT/TEMP-TESTマーカー」チェックでも確認)。

## ビルド出荷について

P-8/P-9が保留(実機再検証待ち)のため、**ビルド17の出荷判断はP-8の実機再検証結果を待って
ご判断ください**。P-8を除く全項目(P-1〜P-7・Q-1〜Q-4)はゲート可能な状態です。
whatsNewはゲート後にお渡しください。ASC裏取りは本人にて実施とのご指示のとおり、
appdev側では実施していません。
