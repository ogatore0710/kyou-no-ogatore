# タスク（C2/appdev向け）— 保留から3件を並列で（触るファイルが重ならない組み合わせ）

## これは何

5視点ワンループで保留（HOLD）にしていた9件のうち、**本人の判断が要らず・触るファイルが
互いに重ならない3件**だけを選んで流します。本人GO（2026-07-28）。

**着手はD3・D4（ウィジェットの差し戻し）を閉じてから。** そちらが先です。

## 進め方

- **3件を並列のサブエージェントで**回してください（本人指示）。
- 下の3件は**触るファイルが完全に別**なので、worktree隔離は不要です。
  ただし**1件が他の2件のファイルに手を出さないこと**。はみ出しそうになったら止めて報告。
- ロジックが増えるところ（特に②）は**ユニットテストを必ず足す**。
- 3件そろってから1回で報告してください。バラバラに上げなくて構いません。

---

## ① iOSのエッジスワイプで「もどる」（Android側は対応済み）

**現状**: iOSはNavigationStackを使わない独自のScreen状態機械なので、
画面端からのスワイプで戻れません。AndroidはD1・G6で `BackHandler` を入れて対応済みですが、
iOSだけ「どこからでも効く逃げ道」がない状態です。

**やること**: サブ画面（相談室・図鑑・じまん・にっき・通信・使い方詳細・設定）で、
左端からのスワイプで `onBack` を呼ぶ。

**注意**:
- **Androidの`BackHandler`と同じ意味論に揃えること**。使い方タブでセクションが開いていれば
  まずそれを閉じる（D1で入れた挙動）。iOSだけ違う戻り方をさせない
- ホーム相当のルートでは何もしない（iOSにアプリ終了の概念がないため、Androidの
  二度押し終了に相当するものは作らない）
- スクロールビュー内の横スワイプと競合しないこと（画面端のみ反応させる）

**触るファイル**: `ios-native/.../KyouNoOgatoreApp.swift`（screenContent切替部）ほかiOS側のみ。
**Androidのファイルには一切触らないこと。**

---

## ② 動画サムネイルのディスクキャッシュ

**現状**: `KyonoAsyncImage` は毎回 `HttpURLConnection` / URLSession で都度取得するだけで、
キャッシュが一切ありません。電波の弱い場所で検索や再生リストを開くとグリッドがほぼ真っ白になり、
「アプリが壊れている」と受け取られます。

**やること**: 一度表示したサムネイルを端末のキャッシュ領域に保存し、次回以降は圏外でも出す。

**設計の条件**:
- **新しいライブラリ依存を足さないこと**（Coil/Kingfisher等を入れない。このプロジェクトの方針）
- キーはYouTubeのvideoId基準（サムネURLはvideoIdベースで不変）
- **保存先はキャッシュ領域**（Android: `cacheDir` / iOS: `caches`）。
  OSに消される前提の場所に置くこと。記録データと同じ場所に置かない
- **上限を必ず設ける**（枚数か総サイズ。目安50MB程度）。超えたら古い順に捨てる
- ネットワークが生きているときの挙動は変えない（キャッシュがあっても、これまでどおり
  新しい画像が出ることを優先しなくてよい。サムネは不変なのでキャッシュ優先で構いません）

**触るファイル**: `KyonoAsyncImage.kt` / `KyonoAsyncImage.swift` と、必要ならキャッシュ実装の新規ファイル。
**それ以外の画面ファイルには触らないこと。**

**テスト**: キャッシュの入出力・上限超過時の破棄をユニットテストで押さえること。

---

## ③ 吹き出しが増えたことを読み上げに知らせる

**現状**: オンボの4問チャットと相談室は1.5秒間隔で吹き出しが1つずつ増える演出ですが、
`liveRegion`（Android Compose）や `UIAccessibility.post`（iOS）の使用がコード全体で0件です。
読み上げを使っている人は、新しい文章が増えたことに自分で気づけず、毎回指で画面を探り直しています。

**やること**: 新しい吹き出しが出た瞬間に、その**新しい1件だけ**が読み上げられるようにする。

**注意（ここが肝心）**:
- **画面全体を読み直させないこと**。`liveRegion` の指定を誤ると、吹き出しが増えるたびに
  会話の最初から全部読み直され、かえって使えなくなります。**追加された吹き出しだけ**が
  読まれることを確認してください
- 相談室は不安なときに使う機能なので、応答が来たことが伝わる価値が一番高い場所です
- 音を鳴らす・振動させる等の別手段は足さないこと（読み上げ経路だけ）

**触るファイル**: `OnboardingScreens.kt` / `SoudanSheet.kt` / `OnboardingViews.swift` /
`SoudanSheetView.swift` のみ。

---

## 全体の検収基準

- [ ] ①iOSの端スワイプでサブ画面から戻れる／使い方タブはセクションを先に閉じる（Androidと同じ意味論）
- [ ] ①スクロール中の横方向操作と誤爆しない
- [ ] ②機内モードにしてから検索・再生リストを開き、**一度見た動画のサムネが出る**
- [ ] ②キャッシュ上限を超えたら古いものが捨てられる（ユニットテスト）
- [ ] ②新しいライブラリ依存が増えていない
- [ ] ③吹き出しが増えたとき、**新しい1件だけ**が読み上げられる（全文読み直しにならない）
- [ ] 3件が互いのファイルに手を出していない
- [ ] Android全テスト緑（`--rerun-tasks`）・iOS swift test緑・`npm test` 443緑
- [ ] Web版配信ファイル無変更

## やらないこと

- 保留の残り（読み上げボタン・メモの定型スタンプ・あとで見る・プリフェッチ・
  圏外の「いまできること」シート・手動バックアップ・通知アクション・Siri Shortcuts）は
  **今回やりません**。手を出さないこと
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

3件そろってからドア配達で1回。
**②は機内モードでサムネが出ているスクショ**を添えてください。
①③は実機の挙動を文章で説明してくれれば十分です（読み上げは僕の側では確認しづらいので、
「何がどう読まれたか」を具体的に書いてください）。
