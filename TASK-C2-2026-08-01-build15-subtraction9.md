# 発注: ビルド15（5視点監査の引き算9件・本人GO済み）

発注元: alan5（5視点監査→ネイティブ突き合わせ→仕分け→本人「1-9全部やって」2026-08-01昼）
すべて**引き算**（消す/まとめる/隠す/畳む）。機能追加はゼロ。対象行番号は突き合わせ調査の値（多少ずれても近傍）。

1. **結果画面「もう一回チェックする」削除**: `OnboardingViews.swift:1240`／`OnboardingScreens.kt:1300`。ホームの再チェック導線が既にあるため完全重複。通常時（非ガイド）でも出さない
2. **動画を探すの年セレクタ削除**: `SearchView.swift:363-368`／`SearchScreen.kt:437-466`。**ネイティブのみ**（Web正本は触らない）。7/26に「Web1:1」目的で追加した経緯があるが、本人ガイドライン＞1:1移植の既定で削除に本人GO済み。関連する絞り込みロジックの残骸も掃除
3. **FAB2連の非表示拡張**: `KyouNoOgatoreApp.swift:271-283`。ホームで実施済みの「相談FABのみ表示」ロジックを検索・マイ記録・再生リストの3タブへも適用（通信📣はホーム以外で隠す。既存のfabs-hide系条件の拡張で）
4. **設定のカレンダー・通知一式を開閉式に**: `SettingsView.swift:225-320`／`SettingsScreen.kt:289-407`。隣接する既存の開閉項目と同じ様式（DisclosureGroup相当）で畳む。既定は閉。通知が有効な人には見出しに現在時刻を出す等、閉じていても状態が分かる一行を残す
5. **使い方タブの2入口を1本化**: `GuideView.swift:168-180`。「はじめてガイド」「使い方ツアー」の2ピル＋区別説明文をやめ、**「📖 使い方ツアー」1本**に統合。はじめてガイド（質問のやり直し）への導線は「困ったときは」カード内へ移す（文言例: 「さいしょの質問をやりなおす」）。Android同等も
6. **かたさチェック通常時のドット二重解消**: `OnboardingViews.swift:717-724`／`OnboardingScreens.kt:766`。Q数字テキストを残し9pxドットを削除（ガイド中はJourneyBarがあるので現状のまま）
7. **カード図鑑の入口統合**: `MyRecordView.swift:341-345`と`:528-542`の2バナーを1つに（「お楽しみ機能」カード内に図鑑導線を吸収する形を推奨。実装の座りで判断可）
8. **コントラスト・文字サイズの引き上げ**: ピンク文字`#E56A9A`（`Theme.swift:67,79`）を小さい文字で使う箇所はAA(4.5:1)を満たす濃さへ（ライト/ダーク別に。大見出しでの使用は現状可）。10px級（`DexView.swift:140-141`、`MyRecordView.swift:252`等）は12px以上へ。**全数grepで棚卸しして報告に一覧を**
9. **相談室カテゴリ行の横スクロール化**: `SoudanSheetView.swift:664-665`＋Android同等。折り返し最大4段→1行横スクロール（既存FadingChipRowの作法流用。端が切れて見える=続きがあるの合図、も既存作法どおり）

## 検収・ビルド

- 各件ビフォー/アフターのスクショ（8は棚卸し一覧つき・ダーク含む）→alan5ゲート→ビルド15（14→15・既存グループ・公開メタデータ不可触・sw.js版数上げない・ASC裏取り）
- whatsNewはalan5がゲート後に渡す
- ⚠️ビルド14がまだアップロード中なら、それを先に完了させてから着手

報告: `REPORT-C2-2026-08-01-build15-subtraction9.md`

---

## 追記（本人GO 2026-08-01 昼過ぎ）: 10. ホームの並び替え

スケッチ承認済み（`alan5のスケッチ=毎日の動線「見る→やる→きろく」を上に`）。**消すものはなし・順番のみ変更**:

新しい並び（通常時・非ガイドのホーム）:
1. きょうのひとこと
2. **きょうの1本**（あなた用/あさ/よる セグメントごと）
3. **続けた日数＋きょうやった！**（記録カードボタン含む既存の塊のまま）
4. （条件もの）おかえりカード・再チェックお知らせ等はこの位置に小さく
5. 2週間プランカード
6. かたさチェックカード
7. オガトレ相談室カード

- オフライン帯は現状の最上部側のままでよい（安全情報のため）
- 初回ジャーニー（fdGuide中）の画面構成は**対象外**（触らない）
- 復帰時の「doneBtnへスクロール＋パルス」等、既存の位置参照ロジックが並び替え後も正しく動くことを実機で確認（スクロールアンカーのずれに注意）
- 検収: 並び替え後のホーム全景スクショ（ライト/ダーク）＋復帰スクロールの動作確認
