# タスク（C2/appdev向け）— 見た目パリティ第2弾: ツアー精密再現・動画サムネイル・ホーム/マイ記録のUX強化

## 背景

本人が実機で最新版（フォント・キャラ画像修正後）を確認。「だいぶ良くなってきている」との評価だが、
まだ3つの具体的な指摘と1つの新規要望がある。**優先度順: ①ツアー ②動画サムネイル ③ホーム/マイ記録の
UX強化**。「使い方タブの基本のき」は本人が「ネイティブ版ならでは、ここは良い」と明言しているので
**現状維持**（触らない）。

**進め方の大方針（本人の言葉そのまま）**: 「一旦はWeb版をほぼ完全に再現することを目標に開発を進め、
その上でよりネイティブ版ならではの仕様にしてください」——**まず忠実な再現、その次に改善**の順序を守る。

## 1) 使い方ツアー・オンボーディングの精密な再現（最優先）

現状は構造（ドットインジケーター・チャットバブル）はあるが、**中身がWeb版と全然違う**との指摘。
Web版は各スライドが「実際のアプリ画面のミニチュア再現」になっており、情報量・演出が現状のネイティブ版
より遥かに密度が高い。以下を**1:1で**再現すること。

### 使い方ツアー（index.html:4117-4143 `OB_TOUR_SLIDES`）
9枚構成（自動起動時のみ末尾に`OB_TOUR_CLOSING`が付いて計9枚）。**各スライドは説明文だけでなく、
実際のUI要素のミニチュアモックアップを埋め込んでいる**:
1. 📺まいにち1本: 「きょうの1本」カードの実物大ミニチュア（動画サムネイル＋タイトル＋「▶タップでYouTubeがひらきます」）
2. ✅きょうやった！: 「続けた日数」カードのミニチュア（大きい数字「8日目」＋done-btn）
3. 記録カードをつくる: `assets/card-sample.png`を180x180角丸で中央表示
4. 📖ためると図鑑がうまる: card-sample.pngの隣に「？」の点線枠3つ（図鑑の未収集感を再現）
5. 💬悩みは相談室で質問: 実際のチャット吹き出し2つ（ユーザー発言→オガトレくんの返答、アバター付き）
6. 📣オガトレ通信をのぞく: 丸い写真アイコン＋説明
7. 📅マイ記録でふりかえる: カレンダーのミニチュア（5個の丸、3個が塗りつぶし＝やった日）
8. 📖忘れてもだいじょうぶ: シンプルな案内カード
9. （自動起動時のみ）締めスライド

**ドットインジケーター**は現在の実装（`.dots`/`.dot.on`）のままでよい。中身の密度を上げること。

### オンボーディング4問チャット（index.html:4145-4210付近）
- **1.5秒間隔で吹き出しが1つずつ出る演出**（`obSay`関数。reduced-motion設定時は即時表示に切り替え）。
  現状即座に全部表示されているなら、この段階的表示アニメーションを追加すること
- **チップ出現時に「👇 タップしてえらんでね」のヒントテキスト**が毎問チップの直上に出る
- チップの配色は**段階グラデーション**（obg0〜obg3。回答した順に色が変わることで会話ログのどの行が
  どの設問への回答か見分けやすくする）。CSS値は index.html:537-542 参照（ライト: obg0=#EAF8F1,
  obg1=#FFF3CB…／ダークは別値）

## 2) 動画サムネイルの実装（再生リスト・動画を探す）

**現状ネイティブ側に一切実装されていない**ことをgrep確認済み。Web版は
`https://i.ytimg.com/vi/{動画ID}/mqdefault.jpg`（一覧）・プレイリストは個別に`hqdefault.jpg`URLを
使用（videos.js内）。

- 再生リスト（SearchScreen/カタログ表示）・動画を探すタブの両方に、各動画のサムネイル画像を表示
- Android: Coil等の非同期画像ライブラリ（新規依存追加が必要な場合はgojiai-app等の既存プロジェクトで
  実績があるか確認してから採用。無ければ標準の`AsyncImage`相当）／iOS: `AsyncImage`（標準API）
- 読み込み失敗時はWeb版と同じくサムネイル非表示（Web版`onerror="this.style.visibility='hidden'"`と
  同じ挙動＝崩れたアイコンを見せない）
- **既存のタップ→YouTube遷移導線（Step7aで実装済み）は変更しない**。サムネイルもタップ対象に含めること

## 3) ホーム・マイ記録: UI=Web準拠、UXはWeb超えを狙う

この2画面は本人から「UIはWeb版を踏襲しつつ、UXはWeb版よりさらに使いやすく、特にデジタル弱者
（高齢者・ITに不慣れな人）でも迷わないように」との要望。**独自の作り直しではなく、Web版の情報構造を
土台にした上での改善**という位置づけ。以下は方向性の例（強制ではない。判断はappdevに委ねる）:
- タップ領域はこのプロジェクト全体で44px以上を徹底する既存ルール（HANDOFF.md）を、ホーム/マイ記録でも
  再確認
- ネイティブ標準のアクセシビリティAPI（Dynamic Type/VoiceOver・TalkBack）が今どの程度効いているか確認し、
  素朴に対応できる箇所があれば対応（大掛かりな独自実装はしない）
- 「きょうやった！」のような主要アクションに軽いハプティクスフィードバックを検討してよい（Web版には
  無い、ネイティブならではの上乗せとして）
- **やりすぎ注意**: まずWeb版と同じ情報構造・文言・並び順を正確に再現してから、その上に自然に乗る
  改善だけを足すこと。独自の再設計・情報の並び替えはしない

## 4) 現状維持（触らない）

- 使い方タブの「きほんのき」セクション（FAQ）: 本人評価「ネイティブ版ならでは、見やすくて良い」

## 検収基準

- [ ] 使い方ツアー9枚が、各スライドのモックアップ内容も含めてWeb版と一致（実タップで9枚全部確認）
- [ ] オンボーディング4問チャットが段階的吹き出し表示・タップヒント・段階グラデーション込みで動作
- [ ] 再生リスト・動画を探すの両方で、動画サムネイルが表示され、タップでYouTubeに正しく遷移する
- [ ] ホーム・マイ記録がWeb版のUI構造を保ったまま、タップ領域等の見直しが入っている
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- 「きほんのき」（使い方タブFAQ）は変更しない
- ロジック・判定・データ構造は変更しない
- Web版（PWA）側の配信ファイルは一切変更しない
- ホーム・マイ記録の情報構造・並び順の独自再設計はしない（Web版準拠が土台）

## 報告

規模が大きいため、①ツアー→②サムネイル→③ホーム/マイ記録、の順で区切ってコミット・報告してください。
各区切りでドア配達をお願いします。
