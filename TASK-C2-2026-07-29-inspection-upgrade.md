# 検査の底上げ(F) — 「テストが緑なのに機能が死んでいる」を減らす

今日1日で、**ユニットテスト267件が全部緑のまま、実機で6件の欠陥が出ました。**
本人の指示で、この構造そのものに手を入れます。

**この指示書は、僕(alan5)の当初案を独立監査にかけて、書き直したものです。**
元の案は「ゴールデンテストに画像を届かせる」＋「ウィジェットの絵を検査できるようにする」でしたが、
**その2つでは6欠陥のうち2つしか捕まらない**と数え上げで示されました。以下は差し替え後の内容です。

---

## 先に、僕の誤りを2つ共有します

**① `card-golden 55/55` を「絵を1ピクセルずつ突き合わせる仕組み」だと説明していました。違います。**

あれは `CardLotteryTests.swift` の**抽選ロジックのゴールデン**で、描画を1ピクセルも見ていません。
描画側(`CardRendererTests.swift`)は「同じ入力を2回描いてビット一致」「入力を変えたら出力が変わる」の
**自己一致テスト**です。

**つまりテスト時に画像が解決できていたとしても、辞書が3体のままで全部緑でした。**
欠けていたのは画像の到達性ではなく、**「タイプごとにアイコンが出る」という判定そのもの**です。

**② `SearchViewUITests` は、今日のサムネイル全滅を捕まえられません。**

行数しか数えていないので、**画像が1枚も出ていなくても緑のまま通ります。**
C2(1件しか描画されない)は見張れていますが、D1は素通りします。

---

## F1 記録カードのアイコン判定（A案・最優先）

### やること

`CardRenderer.loadBundleImage`(`CardCore/Sources/CardCore/CardRenderer.swift:419-423`)は
画像読み込みが**1関数に集約済み**です。ここを**テストから差し替えられる入口**にしてください
(`static var imageLoader: (String) -> CGImage?` 等・作り方はまかせます)。

そのうえで、**6タイプすべてをループで回すテスト**を書いてください。判定は

- **タイプごとに、アイコンありの描画結果とアイコンなしの描画結果が異なること**

これで辞書が1体でも欠けたら赤くなります。

### Androidにも同時に敷いてください

`android-native/.../card/CardRendererTest.kt` は `@GraphicsMode(NATIVE)` で
**実フォント・実リソースで本当に描画できています**(iOSの `swift test` が構造的に届かない領域に、
Androidは既に届いている)。**基盤の追加はゼロで書けます。**

ただし**現状は `momo` 1キーしか試していません。** つまり**Android側も、辞書が3体のままなら
同じく緑でした。** 同じ6キー・ループにしてください。

### やらないこと

**ウィジェットのViewを `WidgetCore` へ移設して画像として検査する案は、採用しません。**
半日かかって捕まるのが1件で、その1件は下のF3(リソース突き合わせ)が数分で捕まえます。

---

## F2 既存UIテストの補強（仕組みの追加ゼロ）

`KyouNoOgatoreUITests/SearchViewUITests.swift` に足してください。

- **サムネイル画像が実際に存在すること**のアサーション。数行で足ります。
  今日のD1(サムネイル全滅)は、これが無かったので素通りしました
- **スクリーンショットのピクセル標本検査**で、画面下端の帯を見る。
  `XCUIScreen.main.screenshot()` のPNGをテスト内でデコードして下端を標本抽出すれば、
  C1(タブバーの黒帯)が自動で捕まります。**新しいライブラリは要りません**

**欲張らないでください。** この2つだけです。他画面への横展開は次の機会に。

---

## F3 静的な突き合わせ（いちばん安い・効果が広い）

`npm test`(443件)の枠に足してください。**既存の検査体系の「静的回帰」層の仕事です。**

**① 必須Info.plistキーの固定**
`INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription` のような、**無いと機能が黙って消えるキー**を
リスト化して、pbxprojに存在することを検査する。今日のB4/D2はこれで捕まります。

**② リソース参照の突き合わせ**
コードが名前で参照している画像(`CharaAsset` の rawValue、`TYPE_IMG_NAMES` の値ほか)が、
**実ファイルとして存在するか**を突き合わせる。B6(ウィジェットのキャラ)とE1(タイプ画像の欠番)が
これで捕まります。

**③ Web版とネイティブの辞書突き合わせ**
Web版が絵を出すキーの集合(`index.html` の `TYPE_IMG` ∪ `app-quiz.js` の `TYPE_ART`)を
**機械抽出して**、ネイティブの辞書と突き合わせる。手で写した表がずれていく問題が構造的に消えます。
既存の `scripts-native/gen-*.mjs` と同じ流儀で。

---

## F4 入口で禁止する（数分）

SwiftLintのカスタムルールで、**`Group { if let ... }` に `.task` を付けている組み合わせ**を警告に。

今日のD1(サムネイル全滅)は、まさにこの形で**画像が来るまで中身が空 → `.task` が一度も発火しない**
という事故でした。**粗いルールで構いません。** 同じ形を書いた瞬間に気づけることに意味があります。

---

## 順番と検収

**F1 → F3 → F2 → F4** の順で。F4は詰まったら飛ばして構いません。

### 検収基準

- **F1: 辞書からわざと1体消してテストが赤くなることを、実際に確かめること。**
  「テストを書きました」だけでは受け取りません。**捕まえられることの証明まで**
- F2: 同様に、サムネイルを意図的に壊してテストが赤くなることを確かめること
- F3: 3つとも、**わざと壊して赤くなるところまで**
- 両OSで回帰確認。Androidは `--rerun-tasks`
- **Web版配信ファイルは読むだけ。1文字も変更しないこと**

## 最後に

**今日のあなたの押し返しは、3回とも僕の指示より正確でした。**
この指示書も監査で書き直されています。**おかしいと思ったら、実装せずに言ってください。**
