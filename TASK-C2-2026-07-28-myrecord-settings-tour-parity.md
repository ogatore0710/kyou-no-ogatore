# タスク（C2/appdev向け）— マイ記録・設定・使い方ツアーのパリティ（【大】1件含む）

## 背景

夜間監査（Fable 4体）の第4弾＝最終。マイ記録／設定／使い方ツアーから【大】1件・【中】5件・【小】多数。

記録データ層（`todayStr`の深夜3時境界・`daysBetween`・`markDone`の時計巻き戻り保護・
おやすみ券の月3枚台帳と`canBridge`判定・memos/daylog/reachのトリム上限・自己ベスト保護）は
**すべて1:1で一致**していました。ツアーの8枚のタイトル・説明文も一字一句一致です。

---

## 1.【大】「いま連続」が、途切れ確定後も古い数字を出し続ける（両OS）

### Web版には二重の対策があります

```js
// index.html:1892-1900
function streakBrokenNow(st){
  const did = st.dates.includes(todayStr());
  if(did || !st.dates.length) return false;
  const gapNow = daysBetween(st.dates[st.dates.length-1], todayStr());
  if(gapNow < 2) return false;
  const miss=[]; for(let i=1;i<gapNow;i++){ miss.push(...); }
  return !canBridgeFreezes(miss);   // おやすみ券でも埋められないなら「途切れ確定」
}
function effectiveStreakCount(st){ return streakBrokenNow(st) ? 0 : (st.count||0); }
```

- **マイ記録**: `app-record.js:277` `histStreak.textContent = effectiveStreakCount(st)` → **0を表示**
- **ホーム**: `app-record.js:48` `if(!did && streakBrokenNow(st)) contTxt="きょうやると新しい章のスタート🌱"`

Web版のコメントに理由が書かれています:
```
// 数日あいて券でもつなげない時は、古い連続を見せない（押した瞬間に消えたと誤解させない）
```

### ネイティブの現状

**`streakBrokenNow` / `effectiveStreakCount` は両OSともgrep 0件＝丸ごと未移植**です。
保存値をそのまま出しています。

- Android `MainActivity.kt:1308` `Text("${streak.count}", ... testTag("histStreak"))`
- iOS `MyRecordView.swift:221` `Text("\(streak.count)")`

### 何が起きるか

1週間休んだ人がマイ記録を開くと **「いま連続 12日」** と出ます。
そこで「きょうやった！」を押すと **1日に戻ります**。
→ **「押した瞬間に消えた」** という体験になります。

これはWeb版が**わざわざ関数を2つ足して防いだ事故**です。しかもこのアプリは
「休んでもいい」を設計思想にして、毎月3枚のおやすみ券まで用意している。
その思想と真逆の体験を、休んだ人＝いちばん傷つきやすい人に返してしまいます。

### やること

- `streakBrokenNow` / `effectiveStreakCount` を移植する
  （`canBridgeFreezes` 相当は既に `RecordLogic` にあるので、それを使うこと）
- **マイ記録の「いま連続」**をこれ経由の値にする
- **ホームの連続表示**も、途切れ確定時は「きょうやると新しい章のスタート🌱」に差し替える
- **保存値そのものは書き換えないこと**（表示だけの話です。`markDone`時に正しく再計算される既存の流れを壊さない）
- ユニットテストを足してください（休みが2日以上・券で埋まる/埋まらない、の両ケース）

---

## 2.【中】使い方ツアーの自動起動が「とじるボタン」経由でしか発火しない（両OS）

**Web版**: `index.html:1563` `switchTab()` の先頭と、`index.html:2718` `closeCard()` 内の
**両方**で `fdTourMaybeStart()` を呼びます。背景タップ・popstateも `closeCard()` を通るので、
**タブ移動と全ての閉じ方**が対象です。

**ネイティブ**: とじるボタンのみ
- Android `MainActivity.kt:1071-1086` confirmButton だけ。`:1066` `onDismissRequest`（外タップ/戻る）は素通し。タブ切替 `:310-318` にチェックなし
- iOS `HomeView.swift:497-511` とじるボタンだけ。`.sheet` のスワイプ下ろしは素通し

**何が起きるか**: 1日目のユーザーがカードモーダルをスワイプで閉じると当日ツアーが出ず、
`tourpend` が残ったまま**後日、文脈のない瞬間に突然ツアーが始まります**。

---

## 3.【中】マイ記録の「📅 カレンダーに登録する」が設定時刻を無視して常に20:00（両OS）

**Web版** `index.html:2001-2020` `renderIcs()`: anchor別の既定（朝07:30／風呂20:30／寝る前21:30／
未設定20:00）＋保存済み `icstime` を必ず反映。

**ネイティブ**:
- Android `MainActivity.kt:1618` `openCalendarIntent(context)` — 引数なしで既定の `hour=20, minute=0`。
  **設定画面側は時刻を渡している**（`SettingsScreen.kt:282`）のに、マイ記録側だけ渡していません
- iOS `MyRecordView.swift:141-142` `comps.hour = 20; comps.minute = 0` ハードコード

朝派の人がこのボタンを押すと毎日20時に通知が入ります。

**※ 通知タスクで `icstime` を15分刻みにする作業と関係します。そちらと整合させてください。**

---

## 4.【中】iOS設定画面にダークモード非対応のハードコード色

iOS `SettingsView.swift` の以下がテーマ変数でなく固定色です:
- `:196-197, 211-212` 時刻表示チップ（`0xFFFFFF` 背景 + `0xF2EADB` 枠）
- `:123` セクション塗り `0xDFF5F2`
- `:136` 「変える」リンク `0x177065`
- `:226` `0xE56A9A`

**Android版は同じ箇所を `colors.card` / `colors.line` / `colors.tealInk` で書いていて対応済み**
（`SettingsScreen.kt:230-231` ほか）。iOSだけ落ちています。ダークで白いチップが浮きます。

**※ このファイルは通知タスクで触っている最中なので、競合しないタイミングでお願いします。**

---

## 5.【中〜小】まとめて

| 項目 | Web版の正本 | ネイティブの現状 |
|---|---|---|
| **図鑑バナーの進捗バッジ＋見本4枚** | `index.html:766-771` + `:2552-2561` `renderDexBanner()`（`got/total` バッジ・記念/季節/レア/ノーマル各1枚の56px見本・「記念日・季節・レアなカードをあつめよう」） | 両OSともプレーンカード＋別文言。**「あと何枚」の収集フックが消えている** |
| **機種変ヒント2行** | `index.html:837`「コピーした文字はLINEの「じぶん専用トーク」やメモアプリに貼っておくと安心です」／`:843`「機種変更のときは 前のスマホで「コピー」→ 新しいスマホで「よみこむ」」 | 両OSとも無し。**コピーして終わり→保存されないまま機種変**が起こり得る |
| **カレンダー下のヒント** | `index.html:783`「印をタップするとその日の記録が見られます」 | 両OSとも無し。**done日タップ→dayInfo は移植済みなのに、発見手段がゼロ** |
| **Android「📅 Appleカレンダーに入れる」** | Webは両OS向けなので併記が正しい | Android版に残っていて、実体はAndroidのカレンダーIntent（`SettingsScreen.kt:281`）。**Android端末にAppleカレンダーは無い** |
| **ツアー締めスライドの説明文** | `index.html:4275-4277` `OB_TOUR_CLOSING.d`「あしたも待ってるね🌱 きょうのぶんの動画をちゃんとやるなら ホームの「きょうの1本」からどうぞ💪」（PO承認案(b)とコメント明記） | 両OSとも画像＋タイトルのみ |
| **テーマ選択肢のラベル** | `明るい` / `暗い`（index.html:804-805） | `ライト` / `ダーク`。対象層向けにやさしい日本語を選んだ意図が落ちている |

---

## 6.【小】細かいもの

- **base64デコードが厳格**: Webの `atob` はforgiving-base64（ASCII空白を無視）だが、Android `Base64.getDecoder()` / iOS `Data(base64Encoded:)` は改行混入で失敗。**メモアプリ経由で改行が入った引き継ぎ文字列がネイティブだけ読めません**。`ignoreUnknownCharacters` 相当を付けてください
- **空欄で「よみこむ」**: Web「貼ってから押してね」／ネイティブ「文字列が壊れているかも」（誤解を招く）
- **iOSカレンダーのマスが32pt**（`MyRecordView.swift:473`）。Web版は44px、プロジェクトの44pt自主ルールにも足りない
- **iOS: 「今日」と「選択日」の枠が排他**（`:477` の三項演算）。Web版は共存（`.d.today` と `.d.sel`）。Androidは共存済み
- **iOS: `freezeLeft` が画面生存中に更新されない**（`:48,73` で init 時1回）。開きっぱなしで月が替わっても「のこり3枚」に戻らない
- **anchor変更時のおしらせ時間既定値**: Webは毎描画で追従、ネイティブは init/remember 1回きり
- **ツアー画面がテーマ・文字サイズ設定を無視**（両OS `KyonoTheme("auto", bigText = true)` 固定）。手動ライト設定の人が夜に再訪するとツアーだけダークになる
- **ツアーSlide7の文言**「このタブの**「見てみる」**から見られるよ」がWeb版UI前提。ネイティブのマイ記録に「見てみる」ボタンは無い（お楽しみは個別ボタン3つ）。**文言をネイティブのUIに合わせてください**

---

## ⚠️ 重複の整理

**とどくメーターの「きょう」条件**は既に `quiz-result-reach-parity.md` の §3 に入れています。
**そちらで直してください。このタスクでは扱いません**（二重作業を避けるため）。

---

## 検収基準

- [ ] 休みが続いて券でも埋まらない状態で、マイ記録の「いま連続」が **0** になる（両OS）
- [ ] 同じ状態でホームが「きょうやると新しい章のスタート🌱」になる（両OS）
- [ ] **保存値は書き換わっていない**（記録すれば正しく再計算される）
- [ ] `streakBrokenNow` / `effectiveStreakCount` にユニットテストがある（券で埋まる/埋まらない両ケース）
- [ ] ツアーの自動起動が、タブ移動・スワイプ閉じ・外タップでも発火する（両OS）
- [ ] マイ記録のカレンダー登録が設定時刻に従う（両OS）
- [ ] iOS設定画面がダークで破綻しない
- [ ] 図鑑バナーにバッジと見本4枚が出る／機種変ヒント2行が出る／カレンダーのヒント文が出る
- [ ] Androidの「Appleカレンダー」ラベルが直っている
- [ ] ツアー締めスライドの説明文が出る／Slide7の文言がネイティブUIに合っている
- [ ] 改行が混入した引き継ぎ文字列が読める（base64）
- [ ] 安全系テスト（111+engine-fixtures）緑・`npm test` 443緑・Web版配信ファイル無変更

## やらないこと

- 記録データ層（`markDone`・おやすみ券の消費順序・トリム上限）は触らない（一致確認済み）
- とどくメーターの「きょう」条件は別タスク（重複回避）
- Web版（PWA）側の配信ファイルは一切変更しない

## 優先度

**§1（いま連続）は、今回の監査4本の中でいちばん実害が大きいと見ています。**
`search-playlists-and-fullwidth-space` の全角スペース修正（1行）の次に、これを持ってきてください。
残りは他タスクと合わせて順に。

## 報告

完了時、ドア配達で報告してください。
**「1週間休んだ状態のマイ記録」のスクショ（いま連続が0になっている）**を添えてください。
