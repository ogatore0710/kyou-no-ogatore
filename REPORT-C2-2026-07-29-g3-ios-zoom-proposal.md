# G3(iOS): bigtext「画面全体ズーム」の実現方式 提案(実装はまだしていません)

## 前提: なぜAndroidのようにワンライナーで済まないか

Android版の修正(`c46e7f4`)は、Composeが**すべてのdp→px変換を`LocalDensity`という1箇所に集約している**ため、
`KyonoTheme`の中で`LocalDensity`が持つ`density`値そのものを1.18倍するだけで、以降の`Modifier.padding(16.dp)`
`.size(72.dp)`「角丸`RoundedCornerShape(16.dp)`」等の**全dp指定が自動的に連動**した(=CSSの`zoom`と同じ効果)。

SwiftUIには、この「全ポイント指定を1箇所で一律変換する」ような仕組みが存在しません。`.padding(16)`
`.frame(width: 72, height: 72)``.cornerRadius(16)`はいずれもその場に書かれたリテラル値がそのまま
レイアウトエンジンに渡るだけで、Compose の `LocalDensity` に相当する「間に挟まる変換レイヤー」が無いためです。
(既存の`.kyonoFont()`がフォントサイズだけ個別に`bigText`倍しているのも、まさにこの「一括変換手段が無い」ことへの
既存の回避策です。)

## 案A: 却下済み(alan5指摘のとおり) — ルートに`.scaleEffect`

`.scaleEffect`はレイアウト確定・ラスタライズ**後**のGPU変換(ビットマップの拡大)であり、CSSの`zoom`や
Androidの`density`倍率のような「レイアウト計算そのものをやり直す」ものではありません。そのため:
- 拡大後にぼける(整数ピクセルに再スナップされない)。
- タップ領域(hit-testing)が拡大前の座標のままズレる。
- 画面外にはみ出た分の余白調整を別途自分で計算する必要がある。
ご指摘のとおり実用に耐えません。

## 案B(推奨): `.kyonoFont()`と同じ手法を、余白/サイズ/角丸にも横展開する

`.kyonoFont(weight, size:)`が内部で`@Environment(\.kyonoBigText)`を読んで`size * 1.18`を返しているのと
同じ発想で、以下のヘルパーを新設し、**画面のリテラル値をすべてこのヘルパー経由に置き換える**:

```swift
// Theme.swift に追加するイメージ(まだ書いていません)
private struct KyonoScaleModifier ... // 既存のKyonoFontModifierと同型
extension View {
    // .padding(kyonoDim(16)) のように、既存の引数の値を包むだけで使える関数版が現実的
}
func kyonoDim(_ value: CGFloat) -> CGFloat { ... } // @Environmentは関数単体では読めないため、
    // 実際は「Viewの外(構造体のプロパティ)でbigTextを読める形」が必要になり、
    // KyonoFontModifierより一段ややこしくなる点が実装時の検討ポイント。
```

**規模の見積り**: `.kyonoFont()`移植のときに「既存呼び出し200箇所超」という記録が残っています
(`Theme.swift:214`のコメント)。今回対象になる`.padding(...)`/`.frame(width:height:)`/`Spacer().frame(...)`/
`.cornerRadius(...)`の類は、フォントより出現数が多いと見ています(検索するとフォントより広範囲に散らばっている
体感)。つまり**フォント移植と同等かそれ以上の機械的置換作業**になります。

**リスク**:
- 置換漏れが起きると「一部だけズームされて一部だけそのまま」という、現状(文字だけ拡大)より
  かえって不揃いに見える状態になりえます(全数チェックが必須)。
- 固定サイズの画像(`KyonoCharaImage`の`.frame(width: 72, height: 72)`等)も対象に含めないと、
  文字だけ大きくなってキャラ画像だけ小さいまま、という違和感が残ります。
- Web版の`zoom`は`<html>`ルート1点にかけるだけですが、iOS版は「触れていない箇所が必ず1つは残る」
  前提で臨む必要があります(Androidの`density`一括変換のような「絶対に漏れない」保証が構造的に無いため)。

## 案C(軽量・部分対応): 主要な「型」だけ広げる

全数ではなく、`KyonoCard`/`KyonoPrimaryButton`等の**共通コンポーネント内部の余白・角丸・アイコンサイズ**
だけをbigText連動にする(個別画面の`Spacer().frame(height: 10)`のような細かい値は対象外のまま)。
Web版ほどの完全一致にはなりませんが、着手範囲はG4(カード枠線)等の共通コンポーネント改修と同程度に収まり、
「見た目の大枠(カードの大きさ・ボタンの大きさ)は連動するが、行間や細かい余白は変わらない」という
部分的な効果になります。

## 提案

**案Bで進めることを提案しますが、これは「フォント移植と同等規模の機械的全数置換」であり、大改修に該当すると
判断したため、着手前に一報します。** 本日のG3〜G14バッチとは切り離し、別枠での実施(スコープ・見積り確認の上で
着手)を推奨します。もし今日中に何らかの見た目連動が欲しい場合は、案C(共通コンポーネント止まり)を
軽量代替として先に入れる、という2段階の進め方も可能です。

ご判断をお願いします。iOSはこの提案までとし、G4以降を先に進めます。
