# 記帳: iOS PWAで「下タブがキーボード表示時に浮く」既知バグ（艦隊共有知見・2026-07-29）

alan監督から艦隊へ横展開された知見。**このアプリでの対応要否は未確定。実機確認待ち。**

## 知見の内容

iOS Safari / PWAで `position:fixed` の下部ナビが、ソフトキーボード表示時に
画面中央へ浮き上がる既知バグ。実証済みの修正ロジック:

1. `focusin`（入力系）で下ナビを **`display:none` で消す**（位置調整ではなく存在を消す）
2. `focusout` で即復帰させず、`visualViewport` でキーボード高さがゼロ近くに
   戻ってから復帰（1.5秒の保険つき）
3. 常時 `visualViewport` の offset で `translateY` ピン留め（URLバー出入り対策）

参考実装: `squeeze-hunter.html` 末尾のIIFE（alan AIアプリで実機検証済み）

## このアプリでの判定（alan5）

### ネイティブ（iOS/Android）— **該当しない**
SwiftUI / Compose であり `position:fixed` も `visualViewport` も存在しない。
キーボード回避はOS側の仕組みが担当する。**適用対象外。**

### Web版PWA — **該当する可能性あり。ただし未確認。**

- `index.html:389 .tabbar{position:fixed;bottom:0;...}` → **条件は揃っている**
- 既存の手当て: `index.html:3413-3443` に `visualViewport` 対応が**すでにある**。
  ただし**相談室シート（`#soudanSheet`）の高さ調整専用**で、シートの開閉と連動して
  bind/unbindされる（`sdVvBound`）
- **`focusin` / `focusout` の処理は無い**。**`.tabbar` 自体への手当ても無い**

つまり**知見の①②は未対応、③に近いものが相談室シートにだけ入っている**状態。

症状が出るとすれば、相談室シート**以外**の入力欄（動画を探すの検索窓・にっきのメモ・
じまんカードの一言など）で、タブバーが見えている場面。

## 保留の理由

- **実機のiOSでしか再現確認ができない。** alan5の環境ではiOS実機の目視ができない
- 監督の指示も「**症状が再現したら適用**」
- **Web版の配信ファイルは本番で稼働中**。再現していない症状のために予防的に触るのは、
  「既に動いているものを壊す」リスクのほうが大きい

## 次にやること

**本人のiPhoneでPWA版を開き、「動画を探す」の検索窓をタップして、
下のタブバーが画面中央へ浮くかどうかを見てもらう。**

- 浮く → 上記①②③を、既存の `sdVvHandler` の流儀に合わせて移植（外部IIFEをそのまま
  貼らない）。**その際は本人のGOを取ってからWeb版に触ること**
- 浮かない → **この記帳で完了。** 対応不要
