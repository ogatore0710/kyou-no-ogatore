# 発注: 相談室の10分記憶（案7b・本人GO済み）

発注元: alan5（本人指示 2026-07-31 未明「10分ほど記憶しておいてくれたらユーザー体験的にはいいのでは」）
前提: 第2波の案7（セッション内保持）は実装・検収済み。これはその上に載せる追加。

## 仕様

**相談室の会話を、最後のやり取りから10分間だけ、アプリの再起動をまたいでも覚えている。**

- 保存: `messages`／`chipsMode`／`lastIntentId`＋**最終活動時刻**を`RecordStore`へ（既存の二重JSONエンコード規約・`plan`と同じパターン）。保存時に**直近30件へトリミング**（肥大化防止）
- 復元: シート初期化時に「いま − 最終活動時刻 ≦ 10分」なら復元、超えていれば保存データを破棄して通常の挨拶から。**判定は復元時のみ**（タイマー・バックグラウンド処理は作らない）
- 「新しい相談を始める」ボタンは**作らない**（TTLが役目を果たす。足さないのが引き算）
- iOS: `SdBubble`/`SdMessage`にCodable、Android: `@Serializable`（見積もりどおり）
- 両OS。挙動はWeb版に無いネイティブ独自だが、本人GO済みなのでパリティ例外として実装してよい（HANDOFFにその旨1行残すこと）

## 検収基準

- [ ] 相談→閉じる→**アプリを完全に落として再起動**→10分以内に開くと会話が残っている（実機/シミュレータ動画）
- [ ] 最終活動から10分超で開くと、まっさらな挨拶から始まる（時計を進めるかTTL定数を短くした検証でよい。**検証用に短くした値を戻し忘れないこと**——auto-syncが拾う環境なので即戻す）
- [ ] 31件以上の会話で古い方から消えている
- [ ] `npm test`＋両OS回帰

## ビルド8との関係

第3波・月アイコン修正と並行でよい。**ビルド8に間に合えば載せる・間に合わなければ正直にそう報告してビルド9へ**（無理に突っ込んでビルドを遅らせない）。

報告: `REPORT-C2-2026-07-31-soudan-10min-memory.md`
