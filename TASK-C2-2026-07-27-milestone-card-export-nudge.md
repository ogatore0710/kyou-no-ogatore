# タスク（C2/appdev向け）— 節目カード表示時の「記録のひかえ促し」が丸ごと欠落

## 背景

alan5の独自調査の続き。記録カードモーダル（`cardModal`）まわりを調査したところ、Web版に
2026-07-18本人承認済みで実装されている**節目カード表示時の「記録のひかえ（エクスポート）を
取っておこう」促し**が、ネイティブに一切存在しないことを確認した（Android/iOSともgrep0件）。
設定画面のエクスポート機能自体は両OSに実装済みなので、そこへの導線（促し文言+ボタン）だけが
欠けている状態。

## Web版の仕様（index.html:1199, 2783が正本）

```
記録カードモーダルの下部に、節目カード（通算100日等の特別な日）表示時のみ:
「せっかくの節目！記録のひかえを取っておくと あんしんです📦」
ボタン「記録のひかえを取る」→ タップで設定画面のエクスポート欄へ遷移（goToExportFromCard）

じまんカード（#brag発の記録カード）表示時は対象外（節目カードのみに出す）。
```

## やること

- 記録カード表示画面（毎日の「きょうやった！」記録カード相当。じまんカードは対象外）に、
  節目日（通算100日等のマイルストーン）を検知したときだけ上記の促し文言+ボタンを追加
- ボタンタップで設定画面の既存エクスポート機能へ遷移させる

## 検収基準

- [ ] 節目日の記録カード表示時に促し文言+ボタンが表示される
- [ ] 節目日でない通常の記録カード・じまんカードでは表示されない
- [ ] ボタンタップで設定画面のエクスポート機能へ遷移する
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- エクスポート機能自体（既存実装）は変更しない
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で簡潔に報告してください（スクショ1枚で可）。
