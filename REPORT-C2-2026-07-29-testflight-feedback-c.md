# TestFlight実機フィードバック続編 C1・C2 完了報告

指示どおり **C2 → C1** の順で対応しました。

## C2 🔴🔴 動画が1本しか表示されない(最優先)

### いつ壊れたか(最重要)

**ビルド1には含まれていません。今日(2026-07-29)の作業で壊れました。**

`SearchView.swift`(iOS)に`.kyonoScreenPadding()`が追加されたのはコミット`a713380`
(2026-07-29 01:00:25)で、ビルド1のアップロード(2026-07-28 16:36)より**後**です。
つまりビルド1の時点ではこの画面はまだ全辺16pt固定のままで、今回の欠陥は含まれていません。
alan5の仮説(「今日のG6/GO-9で入った」)は時刻の裏取りどおり正しいものでした。

### 原因(確定)

alan5の見立てどおり検索ロジックは無罪でした。ただし原因は「180ptパディングの意味の違い」
そのものというより、**画面の構造自体がWeb版と逆向き**だったことです。

`index.html:945-963`を確認すると、Web版の`#search`はsearchbox/catrow/chips/vlist/moreBtn/
reqBoxが全部同じ`<section>`内の兄弟要素で、**ページ全体が1枚のスクロール領域**です。
一方ネイティブは「非スクロールのVStack(検索欄・チップ)」の中に「結果だけを囲むScrollView」
をネストし、**その外側VStack全体**に`kyonoScreenPadding()`(`padding(.bottom,180*zoom)`込み)
を当てていました。ScrollViewが高さの手がかりを持たないVStackの中に置かれ、かつ外側に大きな
下パディングが乗ることで、ScrollViewの実効ビューポート高さの計算が壊れ、`LazyVStack`が
最初の1件しか実体化しない状態になっていました。

**Android版でも同型の欠陥を発見しました**(alan5の「巻き込まれていないか確認」指示どおり
横展開して見つかったもの)。`SearchScreen.kt`は非スクロールの`Column(Modifier.fillMaxSize())`
の中に`LazyColumn(Modifier.weight(1f))`を直接ネストしており、「からだの場所」のように
タグ行が4行に折り返す状態だと、bigtext 1.18倍込みでヘッダー・検索欄・チップ行だけで画面の
残り高さを使い切ってしまい、`weight(1f)`のLazyColumnに割り当てられる高さが0になって結果が
丸ごと描画されませんでした(logcatで`hits.size=51`と検索ロジック自体は正常なことを確認した上で
特定)。

### 直したこと

両OSとも「画面全体を1枚のスクロール領域にする」方向で直しました(Web版のDOM構造、および
既に正しく動いているHomeView.swift/MainActivity.kt HomeScreenと同じ構造)。

- **iOS**: `ScrollView`を画面最外殻にし、ヘッダー・検索欄・チップ行・年セレクタ・結果一覧
  (`LazyVStack`)を全部その中へ。`kyonoScreenPadding()`はScrollViewの中身(内側VStack)へ移動。
- **Android**: 外側の`Column`を`LazyColumn`に変え、ヘッダー・検索欄・チップ行・年セレクタは
  `item { Column {...} }`として、動画行は引き続き`items(hits.take(searchLimit))`として同じ
  LazyColumnの中へ統合。`KyonoScreenPadding`は`contentPadding`として指定。動画行自体は
  従来どおり遅延読み込み(`searchLimit`は最大でも数十件なので重くなりません・454件を一度に
  組み立てる形にはしていません)。

`searchLimit`の値そのものは1箇所も変更していません(24のまま)。

### 確認(合格条件)

Androidエミュレータで「からだの場所」→「全身」タグを選び、
- 「51本」の件数表示が正しく出る
- 動画カードが複数枚(24枚)スクロール表示される
- 一番下に「もっと見る」が出て、押すと48件追加される(スクリーンショットで確認済み)

まで確認できました。iOS側はこの環境でタップ操作の自動化ができないため(シミュレータの
インタラクティブ操作は引き続き不可)、ビルド成功・コードレビューでの確認までです。
Android/iOSとも根本原因(非スクロール親の中にウェイト付きスクロール子をネストし、上に
余白の大きいコンテンツが来ると子の高さが0になる)は共通のため、同じ直し方で解消している
はずです。

再生リスト側(`CatalogListScreen`、Android)も同じ`Column(fillMaxSize)`+`LazyColumn(weight(1f))`
の型でしたが、こちらはヘッダーがタイトル1行+任意のオフライン帯だけで軽く、実機(エミュレータ)
で複数件が正しくスクロール表示されることを確認済みです。現時点で壊れていないため、あえて
構造を変える改修はしていません(壊れていないコードへの不要な変更は避ける方針どおり)。

## C1 🟠 画面いちばん下の黒い帯

### 原因(確定)

`KyonoTabBar.swift`の背景に`ignoresSafeArea`が1つも無く、G7(タブバー半透明化)で単色不透明
背景からMaterial+ティントへ差し替えた際、背景がホームインジケータの安全領域まで伸びず、
後ろが透けて見えていました。

### 直したこと

背景だけに`.ignoresSafeArea(edges: .bottom)`を当てました。ボタン・文字(HStack本体のpadding)
は変更していません。

### 確認

`xcrun simctl ui <device> appearance dark`でシミュレータをダーク表示に切り替え、
タブバー下端をピクセル単位で確認したところ、背景色が均一に画面の一番下まで届いていることを
確認しました。

**Androidは同じ穴が無いことを確認済みです。** `KyonoTheme`(Theme.kt)で
`window.navigationBarColor`を`colors.bg`へ明示的に合わせているため、システムナビゲーション
領域(ジェスチャー領域含む)は最初から背景色と一致する設計になっています(iOSにはこの
「システムバーの色を直接指定する」API相当が無く、安全領域を明示的に背景で覆う必要がある、
という両OSのAPI差がそもそもの原因)。

## 回帰確認

- Android: `compileDebugKotlin` / `testDebugUnitTest --rerun-tasks` 全区間green
- iOS: `xcodebuild`ビルド成功・CardCore `swift test` 16/16・card-golden 55/55
- `npm test` 443 all green・Web版配信ファイルは無変更

## 検収基準チェック

- [x] C2: 「動画を探す」で24本並び、いちばん下に「もっと見る」が出て押すと増える
      (Androidエミュレータで確認・スクロールした状態も撮影済み)
- [x] C2: 「いつ壊れたか」を確定(ビルド1には未含有・2026-07-29 01:00:25のコミットから)
- [x] C1: タブバー背景が安全領域の下端まで届く(シミュレータのダーク表示で確認)
- [x] Android側の横展開確認(C2の再生リスト・C1のナビゲーション領域とも実施済み)

以上でC1・C2は完了です。指示どおりB1・B4・B6・B2の続きに戻ります。
