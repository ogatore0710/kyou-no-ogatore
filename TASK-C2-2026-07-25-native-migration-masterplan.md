# タスク（C2/appdev向け）— ネイティブ移植マスタープランをWorkflowで作成

## 背景・目的

#きょうのオガトレのストア版（iOS/Android、10月リリース枠）方針が確定した（alan監督経由・本人承認2026-07-25）。

- 8月上旬のβ配布（25人・fanicon）は**現行PWAのまま**実施。フィードバック反映もWeb版に対して行う。
- **9月頭からフルネイティブ移植を開始**する予定だったが、βで構造は大きく変わらない完成度と判断され、**今すぐ開始に前倒し**が承認された。
- 方式=**スナップショット移植**: 現時点のWeb版を凍結基準として移植する。8月のβ修正はWeb版のみに当て、9月頭に差分を一括同期する。二重メンテはしない。
- 鉄則=**テスト先行移植**: `soudan-ai-poc/redflag-safety-test.mjs`の111項目を含む検査3層（qa/smoke/safety）をネイティブ版に**先に**移植してから実装を移す。安全系（赤旗検知・受診導線・crisis挙動）はこのアプリの心臓部として最優先で扱うこと。
- ラッパー方式（WebViewで包む）は**不採用**。先行するジャーナルアプリ（gojiai-app）が「1:1移植」方式を確立済みなので、それを土台に流用する。

このタスクの成果物は**実装そのものではなく、実装に入る前のマスタープラン**。プラン完成後はalan5（このプロジェクトの頭）が検収し、OKが出たら量産移植（Sonnet）を即開始する段取り。

## 参照必読（着手前に）

- `/Users/ryunosuke/Claude/gojiai-app/docs/NATIVE-BUILD-GUIDE-2026-07-25.md` — ジャーナルアプリでの1:1移植の実績まとめ。プロジェクト雛形の作り方・署名まわりの未着手範囲・gradlew等の落とし穴・シミュレータ/エミュレータでの検証手順・ストア提出前に残る作業一覧が入っている。**方式選定（ラッパー不採用の理由）を含めて全文必読**。
- `/Users/ryunosuke/Claude/gojiai-app/ios-native/DESIGN.md`・`/Users/ryunosuke/Claude/gojiai-app/android-native/DESIGN.md` — 実プロジェクトでの1:1対応表の具体例
- `/Users/ryunosuke/Claude/gojiai-app/MASTERPLAN-2026-07-23.md` — 「マスタープラン」という文書の型そのものの実例（正本の書き方・本人判断待ち論点の集約の仕方・既定案の付け方）。**今回作るマスタープランもこの型を踏襲すること**
- 本プロジェクトの`WORKING_NOTES.md`（特に2026-07-19〜25の全エントリ）・`HANDOFF.md`・`docs/HANDOVER-to-alan5-2026-07-24.md` — 現在の仕様・壊れやすい箇所（`drawCard()`の決定性・`fdFocusHome()`の当日限定・モーダルのfocus管理順序等）
- `soudan-ai-poc/redflag-safety-test.mjs`・`soudan-kb.js`の`redFlags`・`soudan-ai-poc/norm.mjs` — 安全系の実体。111項目のテストが何を検査しているかを把握すること

## やること

### 第一手: Workflowでマスタープランを作成

Workflowツールを使い、以下の構成でマスタープラン文書を作成する。

- **agentはmodel:fable明示**（判断・設計の質を優先。この工程は40〜60万トークン級の消費を本人承認済み）
- 設計監査員（judge/reviewerのような独立した査読エージェント）を編成に含めること。単一エージェントの一発生成にしない
- 出力は本プロジェクト直下に `NATIVE-MIGRATION-MASTERPLAN-2026-07-25.md` として保存（gojiai-appの`MASTERPLAN-*.md`と同じ「正本」の型で）

マスタープランに含めるべき内容（骨子）:

1. **iOS(SwiftUI)/Android構造設計**
   - `index.html`/`app-*.js`/`soudan-kb.js`/`videos.js`等の現行実装から、gojiai-appの1:1対応表（`PWA → iOS → Android`）に倣った対応表を作る
   - データ形式（localStorage実データ・drawCard等の決定的ロジック）をどうネイティブ側の型に落とすか

2. **safety111項目のテスト先行移植の段取り（最重要）**
   - `redflag-safety-test.mjs`の111ケースを、iOS/Android側でどう先に再現するか（テストランナー・アサーション方式含む）
   - 赤旗検知(`soudan-kb.js`の`redFlags`)・crisis応答・受診導線の判定ロジックをどのファイルに1:1移植するか
   - 「テストが先に緑になってから実装を進める」を各プラットフォームでどう運用するかの具体的な手順

3. **ジャーナルの実例とNATIVE-BUILD-GUIDEの適用**
   - gojiai-appの雛形作成手順（Xcode本体・プロジェクト新規作成は本人GUI操作必須、以降はエージェントで完結等）を本プロジェクトにどう当てはめるか
   - 本人に1回だけやってもらう必要がある操作（Bundle ID指定等）を明確に洗い出す

4. **8月βとの差分同期戦略**
   - スナップショット移植の基準日（本タスク着手時点のWeb版）を明記
   - 8月中にWeb版へ入るβフィードバック修正を、9月頭にどう差分抽出してネイティブ側へ反映するか（gojiai-appの1:1対応表があれば機械的にできる、という知見を活かす）

5. **Sonnet量産移植のステップ分解**
   - マスタープラン承認後、実際の移植作業を担うSonnetエージェント向けに、着手可能な粒度のステップに分解する
   - **各ステップに検収基準を付けること**（例: 「Q3判定ロジック移植→redflag-safety-testの該当ケースが緑になること」等、機械的に確認できる基準）

6. **対外系ゲート**
   - ストア提出・署名関連の作業は本タスクのスコープ外。10月に本人承認ゲートを通すことを明記するだけでよい（実装しない）

### 第二手（マスタープラン完成後）

- 完成したら**alan5（このプロジェクトの頭）にドア配達**すること。配達方式は既存の定型（フルパス＋一言のみ・screen経由）。
- alan5が検収してOKが出るまで、量産移植の実装には着手しないこと（マスタープラン作成だけがこのタスクのスコープ）

## やらないこと

- 実装（Swift/Kotlinのコードを書くこと）はこのタスクの範囲外。マスタープランの作成のみ
- ストア提出・署名・Apple Developer Program登録等の対外系操作は一切不要（10月の本人承認ゲート）
- Web版（PWA）側の`index.html`/`app-*.js`等への変更は一切禁止（8月のβ運用はWeb版のみで行うため、このタスクで触ると差分同期が壊れる）
- gojiai-app側のファイルは参照のみ。編集禁止

## 報告

マスタープラン完成時、以下をalan5への配達メッセージに含めること:
- 生成したファイルパス（`NATIVE-MIGRATION-MASTERPLAN-2026-07-25.md`）
- 消費トークンの概算
- 設計監査員から出た主な指摘・却下/採用の判断があれば一言
