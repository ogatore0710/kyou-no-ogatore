# タスク（C2/appdev向け）— ホーム画面ウィジェット（Duolingo式・両OS・新規ターゲット）

## これは何

本人指示「うぃじぇっとも作ろう。duolingo式で」（2026-07-28）。参考画像はDuolingoの
ホーム/ロック画面ウィジェット2枚。5視点ワンループで5視点すべてが挙げた唯一の項目（H1）が、
本人GOで着工になりました。

**Duolingo式の本体は「連続日数の数字」ではなく「時間帯と状態でキャラの絵が変わること」です。**
参考記事の見出しも『キャラクターの表情にも注目』でした。朝に見た顔と夜に見た顔が違う、それだけで
人はアプリを開きます。ここを外さないでください。

**ただしDuolingoの煽り側は移植しません。**（後述の「やらないこと」を必ず読むこと）

---

## 1. スコープ（Phase 1 = 表示のみ）

- ウィジェットは**表示専用**。タップするとアプリが開く（それだけ）
- **ウィジェットから直接記録する機能は作らない**（本人判断で今回は見送り。Phase 2として別途）
- したがって **RecordStore（記録の正本）には1バイトも書き込まない**

### サイズ
- iOS: `systemSmall` と `systemMedium`
- Android: 2×2 と 4×2
- ロック画面ウィジェットは**今回やらない**（ホーム画面のみ）

---

## 2. 表示する要素

### 2-1.【最重要】連続日数は `effectiveStreakCount` を使うこと

**生の `streak.count` を読まないでください。**

`RecordLogic` に `streakBrokenNow` / `effectiveStreakCount` があります
（今朝の `myrecord-settings-tour-parity` §1 で移植したもの）。休みが2日以上あいて
おやすみ券でも埋められない状態では **0** を返します。

理由はそのタスクに書いたとおりで、生の値を出すと
**「12日と表示 → 押した瞬間1日に戻る」** が起きます。ホーム画面は毎日目に入るので、
アプリ内より事故が目立ちます。ここは検収でも必ず見ます。

### 2-2. 連続が0のときは「0日」と出さない

数字の代わりに **「きょうから また1日め🌱」** と出してください。
（数字は出す方針だが、ゼロを突きつけない。alan5判断・本人には共有済み）

### 2-3. 直近7日のドット

- やった日 = 色つきの丸（テーマのメイン色）
- おやすみ券で埋めた日 = 空きとは区別できる淡い印（実装方式は任せます）
- 何もない日 = **ごく薄いグレーの丸**
- **「×」も赤も絶対に使わないこと**

### 2-4. キャラの絵（時間帯 × 状態）

`assets/chara-*.png` に10枚ありますが、**本人指示で4枚は使用禁止**です（2026-07-28）。

### ⛔ 使ってはいけない絵（本人指定）
`chara-2.png` / `chara-3.png` / `chara-hitokoto.png` / `chara.png`
→ **この4枚はウィジェットに一切登場させないこと。**

### ✅ 使える6枚と、その割り当て

**新規に絵は起こしません。この6枚だけで組んでください。**
両OSのバンドルにコピーして使うこと。

| 状態 | 時間帯 | 使う絵 |
|---|---|---|
| きょうまだ | 朝〜昼(5-17時) | `chara-cheer.png`（旗で応援） |
| きょうまだ | 夕夜(17-5時) | `chara-kaikyaku.png`（開脚・いっしょにやる感） |
| きょうやった | 記録から**4時間**以内 | `chara-congrats.png`（ガッツポーズ） |
| きょうやった | 4時間後〜**当日いっぱい**（深夜3時境界まで） | `chara-good.png`（両手グッド） |
| 節目（大きい節目） | — | `chara-crown.png` |
| 節目（小さい節目） | — | `chara-cracker.png` |
| 連続0からの再開初日 | — | `chara-cheer.png`（応援なのでここが最適） |

> **訂正（2026-07-28・alan5のミス）**: 当初この表は「きょうやった／翌日以降に見たとき＝good」と
> 書いていましたが、翌日は `doneToday=false` になるため「まだ」側の絵に行き、**goodに到達する条件が
> 存在しませんでした**。Androidはgoodがデッドコードに、iOSは独自に4時間窓を作る、と実装が割れたため、
> 上表のとおり「記録から4時間＝congrats／その後は当日いっぱい＝good」に定義し直しています。

**時間帯の枠が3→2に減っているのは意図的です。** 使用可の6枚のうち congrats/cracker/crown/good は
「やったあと」の顔なので、「まだ」側に使えるのが cheer と kaikyaku の2枚しかありません。
足りない分を禁止された絵で埋めないこと。3枠に戻すなら「まだ」側のポーズを新規に描く必要があり、
それは別件（絵の発注）です。**このタスクでは2枠で完成させてください。**

### 2-5. 一言（コピーはalan5が確定。変更しないこと）

視聴者向けコピーの決まりに従っています（**句読点を使わない・絵文字と「！」で区切る**）。

| 状態 | 文言 |
|---|---|
| まだ・朝〜昼 | `きょうもいこう！💪` |
| まだ・夕夜 | `ねる前に1本 どう？🌙` |
| やった・記録から4時間以内 | `きょうもおつかれさま！` |
| やった・4時間後〜当日いっぱい | `つづいてるね！` |
| 連続0 | `きょうから また1日め🌱` |

---

## 3. データの渡し方（ここが設計の要）

### Android
Glance の AppWidget。同一アプリなので `filesDir/kyono-store.json` を**読むだけ**で足ります。
共有コンテナの新設は不要です。

### iOS ← 注意
WidgetKit Extension は**別プロセス**なので、アプリのDocuments配下を読めません。
だからといって **RecordStore本体を App Group コンテナへ引っ越さないでください。**
記録の正本を動かすのは今回のスコープではないし、リスクが釣り合いません。

**やり方**: アプリ側が記録を保存したタイミングで、App Group の共有コンテナに
**ウィジェット表示用の小さなサマリJSONだけ**を書き出す（ミラー）。ウィジェットはそれを読む。

サマリに入れるのはこれだけ:
- `doneToday`: Bool
- `streak`: Int（**`effectiveStreakCount` を通した値**）
- `last7`: 直近7日の状態配列（done / freeze / none）
- `milestone`: Bool

**RecordStore の読み書き経路・保存場所・形式は一切変えないこと。** ミラーは片道の書き出しだけで、
ウィジェットからサマリを書き換えることはありません。

---

## 4. 更新のタイミング

- 記録した瞬間にウィジェットを更新（Android: `updateAppWidget` / iOS: `WidgetCenter.shared.reloadAllTimelines()`）
- あとは**時間帯の境界（5時・11時・17時）**でタイムラインを組む
- 高頻度ポーリングはしない（電池を食うだけです）

---

## 5. やらないこと（Duolingoから移植しない側）

このアプリは「数字で追い立てない・休んでもいい」を設計思想にしていて、
毎月3枚のおやすみ券まで配っています。以下は**参考画像に写っていても作らないでください**。

- **悲しい顔・怒った顔・泣いている絵を作らない**（手持ち10枚が全部いい顔なのはむしろ好都合です）
- **赤い警告色を使わない**（Duolingoの `Time's almost up!` の赤背景に相当するものは作らない）
- **煽り文言を書かない**: 「もうすぐ切れる」「まもなく終了」「〜しないと」「守ろう」の類は禁止
- **ウィジェット起点の通知を増やさない**（通知は既存のおしらせだけ）
- **達成率・パーセント・ランキングを出さない**
- Web版（PWA）側の配信ファイルは一切変更しない

---

## 6. 検収基準

- [ ] Android 2×2 / 4×2 がホーム画面に置ける
- [ ] iOS systemSmall / systemMedium がホーム画面に置ける
- [ ] 時間帯を変えるとキャラの絵が切り替わる（朝〜昼／夕夜の2枚を実機で確認）
- [ ] **使用禁止の4枚（chara-2 / chara-3 / chara-hitokoto / chara）がバンドルにもコードにも入っていない**
- [ ] 記録すると絵が「やった」側に変わり、ウィジェットが即更新される
- [ ] **1週間休んで券でも埋まらない状態で、ウィジェットの連続日数が 0 → 「きょうから また1日め🌱」表示になる**（`effectiveStreakCount` を通していることの証明）
- [ ] 直近7日ドットに「×」も赤も出ない
- [ ] タップでアプリが開く
- [ ] **RecordStore の保存場所・形式が変わっていない**（既存の記録が読める・引き継ぎ文字列も従来どおり）
- [ ] 安全系テスト緑・`npm test` 443緑・Android全テスト緑（`--rerun-tasks` で実行すること）
- [ ] Web版配信ファイル無変更

## 7. 進め方

**先に D1・D2 の差し戻しを片付けてから着手してください。** そちらが先です。

着手前に、**iOS側のApp Group／新規ターゲット追加の手順で詰まりそうな点があれば先に一報**ください。
Xcodeプロジェクトへのターゲット追加は今回が初めてのはずなので、そこだけ様子を見たいです。

## 8. 報告

完了時、ドア配達で。
**ホーム画面に置いた実機のスクショ（朝・昼・夕夜で絵が違うこと／やった後の絵）**と、
**連続0のときの表示**を必ず添えてください。
