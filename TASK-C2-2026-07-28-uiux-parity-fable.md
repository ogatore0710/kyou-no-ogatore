# タスク（C2/appdev向け）— UI/UXがWeb版に追いついていない件（Fable監査 → 実装）

## これは何

**本人がTestFlightで実機を触っての感想（2026-07-28）:**
> アプリとしては成立するって印象。UI/UXがweb版に追いついてないのが気になる
> （使いにくいし見にくいし、洗練されていない）。どこからどうやって直したらいいのやら

**今日1日の作業は、ほぼ全部「振る舞いのパリティ」でした。** 判定ロジック・条件分岐・値・テスト。
**見た目と手ざわりは一度も見ていません。** Web版の完成度はCSSに宿っていて、それを
Compose/SwiftUIで手作業で書き直している以上、値が合っていても質は別物になります。ここを埋めます。

**本人追記: 「カードの挙動もさが大きいから、その辺も見てね」「Fableしっかり目に使って」**

## alan5が先に撮った比較（必ず先に見ること）

`.uiux-compare/cmp-{home,history,search,guide}.png`（左=Web / 右=ネイティブ・同一解像度・同一データ）
再撮影: `node scripts/ui-compare-web.js` → ネイティブは `adb screencap` で同じタブを撮る。

### alan5が既に見つけた5件（重複調査は不要・裏取りだけしてよい）

1. **【最重要】ページの「頭」がタブによって有ったり無かったりする**
   ホームには `#きょうのオガトレ` の見出し＋キャラがあるのに、**マイ記録・動画を探すには無い**。
   Web版は全タブで同じ頭を持つ。**「洗練されていない」の最大要因はこれだと見ています。**
2. **全体が一回り大きく、情報が入らない/見切れる**
   動画を探すのカテゴリ切替チップ「目的」が**右端で切れている**。同画面に入る情報量がWeb版より明確に少ない。
3. **カードの内側の余白が広すぎる**
   かたさチェックのカードが、同じ文言でWeb版より2〜3割高い。1画面に1枚半しか入らない。
4. **コピーが冗長化している箇所**
   Web「454本」→ ネイティブ「454件見つかりました」。**短く言い切る声**が失われている。
5. **カレンダーの月移動ボタンが重い**（大きな塗りボタン2つ）。Web版は控えめ。

※ Web版ホームの「ホーム画面に追加すると…」バナーがネイティブに無いのは**正しい**（ネイティブには不要）。
これは差分ではありません。同種の「PWA固有だから無くて当然」を差分として上げないこと。

---

## Fable監査（6視点・しっかり目に）

**読むだけ。コードを変更しない。** 各視点、最大10件。**必ず両側の実値を並べて書くこと**
（例: `CSS .card{padding:14px 16px}` ↔ `Compose padding(20.dp)`）。値を書かない指摘は受け取りません。

**A. 器（ヘッダー・余白・角丸・区切り・幅）**
`index.html` のCSSと、Compose/SwiftUIの実装値を突き合わせる。左右のガター、カード間の間隔、
角丸、境界線、セクションの区切り方。**全画面に効く共通の器**を優先。

**B. 文字とスケール**
font-size/line-height/font-weight の階層が、Web版の序列どおりに移植されているか。
bigtext倍率の効き方。**見切れ・折り返しの不自然さ**（1文字だけ次行に落ちる等）。

**C.【本人名指し】カードの挙動・演出・時間**
**これが今日いちばん見てほしいところです。**
`index.html` の `transition` / `animation` / `@keyframes` と、Compose の `animateXxxAsState` /
`AnimatedVisibility` / `tween`、SwiftUI の `.animation` / `withAnimation` を**1件ずつ突き合わせ**、
**duration・easing・delay・順序**の差を列挙する。対象:
- 記録カード（きょうやった！の直後に出るもの）の出方
- 節目のゴールドカード
- カード図鑑の開き方
- じまんカード
- 相談室の吹き出しの段階表示（**1.5秒間隔**が正・過去に416msで一気に出ていた事故あり）
- オンボのチャット
- 画面遷移そのもの
**「アニメーションが無い」だけでなく「速すぎる/遅すぎる/イージングが違う/順番が違う」も差分です。**

**D. タップの手ざわり**
押した瞬間の反応（press状態・スケール変化・色変化・ripple）、無効化中の見え方、
連打時のふるまい。Web版の `:active` / `.tapx` 系と突き合わせる。

**E. コピーの変質**
Web版の文言がネイティブで**長く・説明的に**なっている箇所を洗い出す（上記4番の型）。
このアプリの声は**短文・句読点なし・絵文字と「！」で区切る**。冗長化は品位を落とします。

**F. 配置と順序**
セクションの並び順、カード内の要素順、タブ内のスクロール順。Web版と入れ替わっている箇所。

---

## そのあと

1. そちらで重複を除いて**深刻度順**にまとめ、`REPORT-C2-2026-07-28-uiux-audit.md` にpush＋ドア配達
2. alan5が15分以内にGO/保留/却下を返す
3. GOしたものだけ実装
4. **実装後、`.uiux-compare/` を撮り直して before/after を並べる**（効いたことを画像で示す）

**まずは「全画面に効く器」から。** 1画面ずつの手直しは後回しです。

## やらないこと

- ロジック・判定・データ層は触らない（今日さんざんやりました）
- Web版（PWA）側の配信ファイルは一切変更しない
- 設計思想に反する追加はしない（数字で追い立てない・煽らない・登録不要）
- **1巡で止める。** 監査→仕分け→実装→撮り直し、で終わりです
