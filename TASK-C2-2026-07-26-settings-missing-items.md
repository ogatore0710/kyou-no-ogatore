# タスク（C2/appdev向け）— 設定画面に「やるタイミング」「カレンダー通知時間」設定が欠落

## 背景

alan5の独自調査8件目。設定画面（`続ける設定`）は画面のみため・もじの大きさ・記録のひっこしのみで、
Web版（index.html:799-830）にある以下2項目が欠落している。

## Web版の仕様

### 1. やるタイミング（index.html:800）
```
「やるタイミング: [未設定 or 選んだタイミング] 変える」
```
ホーム画面で新規ユーザーに聞く「いつやる派？」（朝おきてそのまま/おふろ上がりに/寝るまえ/きめてない）
の現在値を表示し、「変える」リンクで選び直せる。判定・保存ロジックはStep5a等で既に移植済みのはず
（anchorCard相当）。表示と変更導線を設定画面にも追加。

### 2. カレンダーのおしらせ時間（index.html:809-812）
```
「カレンダーのおしらせ時間: [時刻ピッカー]」
→ 📅 Appleカレンダーに入れる（ボタン）
→ 📅 Googleカレンダーに入れる（ボタン）
```
時刻を選んでから、Apple/Google個別にカレンダー登録できる。**マイ記録タブに既にある
「📅 カレンダーに登録する」単一ボタン（ホーム構造修正タスクで追加済み）とは別物**——あちらは
時刻指定なしの簡易版、こちらは設定画面での時刻指定つき詳細版。両方あってよい（Web版もそう）。

## やらないこと（重要な判断）

- **「毎日自動でアプリがひらく設定（iPhone）」セクション（index.html:815-823・
  Shortcutsアプリの自動化手順）は移植しない**。これはPWAが通知を送れないことへのiOS限定の
  回避策であり、ネイティブアプリには元から関係ない問題（ネイティブは通常のローカル通知が使える）。
  ここは「まずWeb版に忠実」の例外として、ネイティブに存在しない問題への回避策は持ち込まない
- 記録のひっこし（export/import）は既に実装済みのため変更しない
- ロジック・判定・データ構造は変更しない（表示・設定項目の追加のみ）
- Web版（PWA）側の配信ファイルは一切変更しない

## 検収基準

- [ ] 設定画面に「やるタイミング」の現在値表示＋変更導線が追加される
- [ ] 設定画面に「カレンダーのおしらせ時間」の時刻ピッカー＋Apple/Google個別ボタンが追加される
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## 報告

完了時、ドア配達で簡潔に報告してください（スクショ1枚で可）。
