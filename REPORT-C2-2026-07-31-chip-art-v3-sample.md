# B部: 生成絵文字バッチ — B-0(絵柄ルール改定)+B-1(腰サンプル)報告

`TASK-C2-2026-07-31-feedback-round2.md` B部。**サンプル関門のため、B-2(本生産24枚)には
着手していません。** ここまでの結果を検分いただき、OKが出てから残りへ進みます。

## B-0: `scripts/gen-chip-art.py` STYLE改定

alan5分析(既存9種の実機ダークモード確認)の3つの敗因への対応:

| 敗因 | 対応 |
|---|---|
| ①白・薄色の塗りがダーク背景で浮く | シルエット全体を暖色系(テラコッタ/コーラルオレンジ/琥珀)の単色で塗りつぶす方式に変更。白・薄色塗りは禁止句を明記 |
| ②32ptで細部が潰れる | 「32x32pxまで縮めても意味が読める大きな単純形1つ」を必須条件として明記。細い副次的な線・小さい飾りを禁止 |
| ③抽象形が読めない(「とくにない」の同心円等) | STYLE自体はモチーフ非依存の指定のため、個別モチーフの読みやすさはWHAT辞書側の記述で担保(「とくにない」の再考自体はB-2スコープ) |

新STYLEは「暖色の単色塗り＋墨色(#3A3A35系)の太い輪郭」の2色構成に固定。前回(v2)は
「輪郭線＋一部だけアクセント色」という構成で、塗られていない部分がテーマ背景色に応じて
見え方が変わっていた(=白浮きの原因)。2色を毎回同じ組み合わせで乗せる方式にしたことで、
背景色に依存せず同じ見え方になるようにした。

## B-1: 「腰」(youtsuu)サンプル1枚

新ルールで`chip-youtsuu.png`を1枚だけ再生成し、iOS(`ChipArt/`)・Android
(`drawable-nodpi/`)両方に反映(MD5一致・`md5`コマンドで確認済み)。

### 検分方法

1. iOS: 一時XCUITestでオンボQ3(気になる部位)の画面まで自動タップ→
   `xcrun simctl ui <udid> appearance light/dark`をhost側で切り替えてから2回実行→
   スクリーンショット抽出→検証後にテストコード完全削除(git diff差分ゼロ確認済み)
2. Android: エミュレータでオンボQ3まで`adb shell input tap`で到達→
   `adb shell cmd uimode night yes/no`でテーマ切替→スクリーンショット

### 結果(`.art-staging/chip-v3-sample-verify/`)

- `ios-light-full.png` / `ios-dark-full.png`: iOSオンボQ3の実画面(5チップ全部が並ぶ中の「腰」)
- `ios-light-dark-closeup.png`: 「腰」チップ部分をライト/ダークで切り出して並べた拡大比較
- `android-light-full.png` / `android-dark-full.png`: Android版の同じ画面

**目視結果**: ライト・ダークどちらの実背景色に乗せても、オレンジ色のシルエットが同じ
濃さ・同じ輪郭ではっきり見え、白浮きは発生していません。「腰まわり」を示す横線
(ウエストのベルト状マーク)も32pt相当の実寸で判別できます。並んでいる旧v2の他4チップ
(肩こり・首/前屈できない/眠り/とくにない)と比べると、新v3の方が塗りが締まっていて
輪郭がくっきりしている違いが目視でも分かります。

## 確認済み/未確認

- [x] 白・薄色塗りが無い(暖色単色塗り+墨色輪郭の2色構成)
- [x] 32pt実寸でシルエットとして判読できる(ライト/ダーク実機スクショで確認)
- [x] 光彩・影・グラデ無し・透過背景(`PIL`でアルファ値確認: 角のピクセルalpha=0)
- [x] 両OS適用済み・MD5一致
- [ ] **alan5による検分・OK待ち。OKが出るまでB-2(残り23枚の本生産)には着手しません。**

## 次

alan5のOKが出たら、`gen-chip-art.py`のWHAT辞書(katakori/zenkutsu/nemuri/none/asa/furo/neru/free)
も新STYLEの2色構成に合わせて改定してから本生産に入ります(「とくにない」のモチーフ再考も
指示どおりB-2側で対応)。
