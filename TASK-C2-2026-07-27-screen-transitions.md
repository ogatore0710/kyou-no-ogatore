# タスク（C2/appdev向け）— 画面遷移アニメーションと、相談室/オンボのシート化

## 背景

挙動パリティ監査§Aで、appdev自身が「構造的欠落・要判断」として報告してくれた2件目。
**本人判断で「今回まとめて直す」GO**が出た（alan5は提出前に再検討を推奨したが、10月の
ストア提出で第一印象に効く部分という判断）。

現状、ネイティブは手組みの`Screen` sealed class/enumで画面を丸ごと差し替える方式のため、
Web版にある以下が無く、全画面が瞬時切り替えになっている:
- `sdup`（相談室シートが下から .25s でせり上がる）
- `obpop`（オンボシートが .28s で拡大＋フェード）
- そもそも相談室・オンボが「画面下からのシート（角丸＋スクリム背景＋部分高さ）」ではなく
  通常の全画面コンテンツとして描かれている

## ⚠️ 実装方針（重要・これを外さないこと）

**ナビゲーションの仕組みそのものを置き換えないこと。** 約15画面が乗っている土台なので、
NavHost移行のような作り直しは回帰リスクが見合わない。既存の`Screen` sealed class/enumは
**そのまま維持**し、その外側に演出を被せる方式で実装する:

- **Android**: 既存のwhen分岐による画面切り替えを`AnimatedContent`で包む
  （`transitionSpec`でフェード＋スライドを指定）。相談室・オンボは`ModalBottomSheet`
  もしくはスクリム＋下からのスライドインで**シート表示**に変更
- **iOS**: 既存のswitch分岐に`.transition()`＋`withAnimation`を適用。相談室・オンボは
  `.sheet()`（`presentationDetents`で高さ指定）で**シート表示**に変更

Web版の該当値: 相談室 `.25s`、オンボ `.28s`、いずれも`ease-out`。
シートの見た目もWeb版に合わせる（相談室=高さ92%・上角丸20px・背後にスクリム、
オンボ=中央寄りで最大高92vh・角丸22px）。

**段階的に進め、1画面ずつ動作確認しながらコミットすること。** まとめて変えて一気に壊すのが
いちばん怖いパターンなので、「相談室 → オンボ → 残りの一般画面」の順で区切って進めてください。

## 検収基準

- [ ] 相談室が下からせり上がるシートとして表示される（スクリム＋角丸＋部分高さ）
- [ ] オンボが拡大＋フェードで表示される
- [ ] 一般画面の切り替えに遷移アニメーションが付く
- [ ] **既存の画面遷移が全て従来どおり動く**（各タブ・結果画面・カード・図鑑・じまん・
      せんぱいの声・オガトレ通信・設定・とどくメーター等。戻る操作も含めて壊れていないこと）
- [ ] Androidの戻るボタン／iOSのスワイプバックの挙動が従来どおり（シート化した画面で
      閉じる操作が正しく効くこと）
- [ ] Androidは実機、iOSはシミュレータで目視確認
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test` 443緑・Web版配信ファイル無変更

## やらないこと

- **ナビゲーションアーキテクチャの置き換え（NavHost移行等）はしない**。既存のScreen方式を維持
- 各画面の中身（レイアウト・文言・ロジック）は変更しない。遷移と器だけ
- Web版（PWA）側の配信ファイルは一切変更しない

## 優先度

`fd-guide-ui-branch`（初回体験の足場・欠落機能）を先に片付けてから着手。
こちらは演出の底上げなので、機能の欠落より後でよい。

## 報告

区切りごと（相談室／オンボ／一般画面）に報告してください。回帰が怖い箇所なので、
「既存の遷移が壊れていないこと」をどう確認したかも書いてください。
