# TestFlight ビルド7 配信報告

## 結果

- `CURRENT_PROJECT_VERSION`: 6 → 7(project.pbxproj 4箇所すべて確認済み)
- `MARKETING_VERSION`: 1.0(変更なし)
- アーカイブ・エクスポート・アップロードとも成功
- App Store Connect側で `version=7` / `processingState=VALID` を確認
- 既存の内部テストグループ「きょうのオガトレ 内部テスト」
  (`3b3f7a0b-3063-451d-acdd-404432f08a76`)に紐付け済み(新規グループは作成していません)
- 「テストする内容」(ja)を設定済み

## テストする内容(ja) — 実際に設定した内容

```
①相談室で動画をすすめられたとき、サムネイル付きのカードになりました(これまでは「動画を見る」という文字だけのボタンでした)
②見出しやボタンの絵文字を、アプリ専用のアイコンに置き換えました。タブバーと同じ絵柄でそろえています
③ホームの「きょうの1本」が実際の動画になりました(ビルド6から入っています)
④動画を探すで、絞り込み中の印・消去ボタン・残り本数が出るようになりました(ビルド6から入っています)
```

**③④は指示どおり「ビルド6から入っています」と明記し**、本人がビルド6を飛ばして7を
見た場合でも、これらがどの時点からの変更かが分かるようにしています。

## このビルドに含まれるもの

H1(相談室の動画紹介の本物カード化)+ アイコン方針(I)全体(34種すべて: 使い回し4種+
新規描き起こし10種+ボタン/目印の絵文字削除多数)。ビルド6にすでに含まれていたD群・E1・
F・G群も引き続き含まれています。

## 検収チェック

- [x] `version=7` が `VALID`
- [x] 既存グループ(`3b3f7a0b-3063-451d-acdd-404432f08a76`)に紐付け済み・新規グループなし
- [x] whatsNew(ja)設定済み(③④に「ビルド6から入っています」の注記あり)
- [x] `MARKETING_VERSION`は1.0のまま変更していない

## 作業中に発生したこと(正直に記録)

**ベータグループへの紐付けが、アップロード直後は`404 NOT_FOUND`で失敗し続けました。**
ビルド自体は`processingState=VALID`で直接取得できるのに、`betaGroups`への追加操作
だけが「そのIDのbuildは存在しない」というエラーを返す状態が、アップロード後
約20分近く続きました。過去のビルド1〜6は同じグループに正しく紐付いている(確認済み)ため、
API権限やグループIDの問題ではなく、**Apple側の内部インデックスがこの新しいビルドに
追いつくまでの遅延**だったと考えられます。時間を置いて再試行したところ成功しました。
