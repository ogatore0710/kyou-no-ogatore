# UI/UXパリティ監査GO項目バッチ(G1〜G14) 完了報告

alan5指示の順序どおり **G1 → G2 → G3(Android) → G4 → G5 → G6 → G7 → G8 → G9 → G10-G14** を実装し、
1巡を完了しました。以降このレポートに記載する差し戻し2件を除き、新規の監査ループは開始していません。

## 実装内容

| 項目 | 内容 | 状態 |
|---|---|---|
| G1 | 記録直後の紙吹雪(70/105粒)+節目カード内容(タイトル/メッセージ/せんぱいの声/王冠/動画リンク)をホーム画面へ接続 | 両OS完了 |
| G2 | クイズ選択肢・KyonoGhostButton/LineButton/SegmentedControlの押下フィードバックをWeb`:active`へ | 両OS完了 |
| G3 | bigtext時にdensityも1.18倍(dp/spを一律連動) | Android完了(下記差し戻し参照)/iOS提案のみ・未着手 |
| G4 | KyonoCard/GradientCardへ枠線+影(ダーク時は影なし) | 両OS完了 |
| G5 | 共通ヘッダー(#きょうのオガトレ)をマイ記録/動画を探す/使い方の3タブへ展開 | 両OS完了 |
| G6 | 画面ガター(左右18dp/上20dp/下180dp)を共通定数へ統一 | 両OS完了 |
| G7 | タブバーの半透明+上部境界線(iOSはMaterialで実backdrop-blurも実現) | 両OS完了 |
| G8 | 完了後の「きょうやった!」ボタンをフラット灰色化 | 両OS完了 |
| G9 | マイ記録タブの並び修正(カード図鑑をカレンダーより前へ)+重複おやすみ券カード削除 | 両OS完了 |
| G10 | コピー簡潔化(454件見つかりました→454本/設定のコピー案内をカジュアルに) | 両OS完了 |
| G11 | 見出しh1を22→20sp/pt+nowrapガード(G6と同時に前倒し実施) | 両OS完了 |
| G12 | 相談室吹き出しの行間をline-height 1.75相当へ | 両OS完了 |
| G13 | Android tween()にease-out相当のCubicBezierEasingを明示(cpop・sd-pop) | Android完了(SUSPECTED項目) |
| G14 | 使い方FAQ「通知はこないの?」の陳腐化した回答を実態に合わせて修正 | 両OS完了 |

## 途中の差し戻し2件(いずれも解消済み)

1. **G1「節目ブロックが実機で出ない」**: 新規ビルドでの追試の結果、スクロール範囲外だっただけで
   実際は正しく描画されていたことを確認(d=3/d=4双方で検証)。alan5も追試の上で撤回済み
   (REPORT-C2-2026-07-29-g1-milestone-block-investigation.md)。
2. **G3「bigtextの二重掛け」**: 最初の実装がdensityとfontScaleの両方に1.18を掛けており、
   `sp.toPx()=value×density×fontScale`の式で文字だけ1.18×1.18=1.39倍になっていた。density
   **だけ**に1.18を掛ける修正で解消(検索タブ「からだの場所」1行目が3列に復元、ヘッダーも1行に収まることを確認)。

## iOS G3(画面全体ズーム)について

方式(案B: `.kyonoFont()`と同じ発想を余白/サイズ/角丸へ横展開)はalan5承認済みですが、
**Androidで正解の見え方が確定するまで着手を待つ**という指示のとおり未着手です
(REPORT-C2-2026-07-29-g3-ios-zoom-proposal.md)。Android側が今回の一巡で「Web版と並べて実測ではなく
目視で一致」まで来たため、次にiOSへ着手する際は共有部品(KyonoCard・各ボタン・タブバー・左右余白)
限定から始める、という進め方も含めて判断をお願いします。

## 検証結果

- `./gradlew compileDebugKotlin`・`testDebugUnitTest --rerun-tasks`: 全工程でgreen
- `xcodebuild`(iOS Simulator)・`CardCore`/`RecordCore`/`SafetyCore`/`WidgetCore` 全4パッケージ`swift test`: green
- `npm test`: 443 checks green(Web版配信ファイル=index.html/app-*.jsは本バッチで無変更)
- `.uiux-compare/`をWeb版(`node scripts/ui-compare-web.js`で同一解像度・density再撮影)と
  Androidエミュレータ(実機と同じdensity 2.625)の4タブ(home/history/search/guide)で並べて確認。
  スクリーンショットは別途お送りします。

## 今回のスコープ外で気づいた点(未対応・判断待ち)

作業中に目に入った、今回のGOリストには無い2点です。実装はしていません。

1. **動画を探すタブのカテゴリ選択タブが省略されている**: Web版は「からだの場所/時間・シーン/目的/その他」
   の4つ以上が横スクロールで並ぶのに対し、ネイティブは「からだの場所/時間・シーン」の2つ+「›」ヒントの
   横スクロールになっている(実際にスクロールで先に進めるかは未確認)。今回のG6/G10などとは別種の
   欠落の可能性があります。
2. **保留#10(はじめてガイド/使い方ツアーの横並び)**: 「G3を入れてから撮り直して、まだ縦積みなら直す」
   という条件でしたが、G3(Android)導入後の今回の撮影でもまだ縦積みのままでした。次の判断材料としてご確認ください。

## iOS実機確認の限界(継続の注意点)

この環境にはiOS Simulatorへタップ/スワイプを送る手段(idb・System Eventsのアクセシビリティ権限とも
未整備)が無く、iOS側は「ビルド成功+コードレビューでAndroid版と1:1同一の実装であることを確認」までが
限界です。ホーム画面の初期表示1枚のみスクリーンショットで確認できています。

以上で本バッチはクローズとします。新しい監査ループは開始していません。
