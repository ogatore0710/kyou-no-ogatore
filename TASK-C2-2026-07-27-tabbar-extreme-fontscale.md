# タスク（C2/appdev向け）— 極大フォント設定で下タブバーが崩れる（軽微・follow-up）

## 背景

`text-size-accessibility`の検収でalan5が実機確認した際に見つけた残件。**タスク自体は合格**、
これはその上での小さな follow-up。

端末の文字サイズを**極大（font_scale 2.0・Android 14以降のアクセシビリティ設定で到達可能）**に
すると、下タブバーのラベルが2行に折り返してFABと重なり、見た目が崩れる。

切り分け済み:
- **通常の設定画面の最大（font_scale 1.3）＋アプリ「大きめ」＝問題なし**（実機確認済み）
- 崩れるのは font_scale 2.0 のとき（アプリ1.18倍と合成され、キャップ`KYONO_MAX_FONT_SCALE=2.2`
  に丸められた状態）

キャップ自体は正しく効いている（2.0×1.18=2.36 → 2.2）。ただし2.2でもタブバーが耐えられない。

## なぜ直す価値があるか

このアプリの想定ユーザーは50-60代以上で、**文字サイズを最大にする層と最も重なる**。
「見えにくいから最大にした人ほど、下のタブが読めなくなる」のは本末転倒なので直しておきたい。
ただし通常設定の範囲では問題ないため、**優先度は低め**。

## やること

下タブバーが極大フォントでも破綻しないようにする。方式は任せますが、例えば:
- タブラベルだけスケール上限を別途設ける（ナビゲーションバーのラベルは慣習的に一定以上
  大きくしない）
- 極大時はラベルを隠してアイコンのみにする（Material Designのナビゲーションバーで
  一般的な挙動）
- バーの高さを伸ばして2行を許容する

**タブの当たり判定（タップできる領域）は狭めないこと。**

## 検収基準

- [ ] font_scale 2.0＋アプリ「大きめ」でも下タブバーが破綻しない（ラベルの折り返しによる
      FABとの重なりが起きない）
- [ ] font_scale 1.0 / 1.3 での見た目が従来から変わらない（退行させない）
- [ ] タブのタップ領域が従来どおり確保されている
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test` 443緑・Web版配信ファイル無変更

## やらないこと

- 文字サイズの仕組み自体（`KYONO_BIG_TEXT_SCALE`・`KYONO_MAX_FONT_SCALE`・Dynamic Type対応）は
  変更しない。タブバーの見せ方だけで解決すること
- Web版（PWA）側の配信ファイルは一切変更しない

## 優先度

低い。`screen-transitions`・挙動監査§B§D・`local-notifications`を片付けた後で構いません。

## 報告

完了時、ドア配達で簡潔に報告してください（font_scale 2.0でのタブバーのスクショ添付）。
