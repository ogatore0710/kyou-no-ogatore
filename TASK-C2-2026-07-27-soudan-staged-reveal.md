# タスク（C2/appdev向け）— 相談室の返信が瞬時に全部表示され、Web版の段階演出が無い

## 背景

alan5が本人指摘を受けて実機検証（Android実機タイマー計測: チップタップ→416ms後には
ユーザー発言・共感文・長文回答・動画ボタン・フォローアップチップまで**全て同時に**表示済み）。

Web版の相談室（`sdPush()`）は、返信が複数の吹き出しに分かれているとき、**タイピングドット
（「…」アニメーション）を挟みながら1つずつ段階的に表示する**演出になっている
（本人フィードバック起点で2026-07-12/07-15に調整された既存機能）。ネイティブは
`applyResponse()`が生成した吹き出し配列を`messages = messages + newMsgs`で一括追加している
だけなので、この演出が丸ごと欠落している。オンボーディングの1.5秒間隔演出（`OB_ROUTES`等）は
別ロジックで正しく移植済み・問題なし。今回の対象は**相談室（soudan）だけ**。

## Web版の仕様（index.html:3090-3134 `sdPush()`が正本）

```
sdPush(msgs)は複数の吹き出しを1つずつ表示する:
- i番目の吹き出しを表示する前に、タイピングドット（sd-typing、「…」アニメーション）を追加
- 待ち時間: base = min(1600, 500 + そのメッセージの文字数*22)
  - 1個目(i===0): wait = 400ms固定
  - 2個目(i===1): wait = base
  - 3個目以降: wait = base*2（実質最大3200ms）
- 待ち時間が過ぎたらタイピングドットを消し、次の吹き出しを表示 → 繰り返し
- 全部出終わったらチップ（フォローアップ・カテゴリ等）を表示

対象は「1回の応答内の複数吹き出し」（例: 共感文→本文→動画案内→フォローアップ促し、が
最大4連続で来ることがある）。ユーザー自身の発言（チップタップ/入力送信直後の自分の吹き出し）は
即時表示のまま（Web版もsdUserBubbleは同期的・待ちなし）。
```

## やること

- Android `SoudanSheet.kt`・iOS `SoudanSheetView.swift`の`applyResponse()`（bot発言をまとめて
  `messages`に追加している箇所）を、上記のタイピングドット＋可変ウェイトによる段階表示に変更
- タイピングドット（「…」の3点アニメーション）のUIを新規追加（`chara-hitokoto`アバター＋
  吹き出し内に3つの点、既存の吹き出しCSS/デザイントークンの範囲で表現）
- ユーザー自身の発言（`SdBubble.User`）は即時表示のまま変更しない
- 文字数に応じた待ち時間の計算式（`min(1600, 500+文字数*22)`、1個目400ms・3個目以降倍）は
  Web版のロジックをそのまま機械移植すること（手で丸めない）

## 検収基準

- [ ] 相談室で複数吹き出しの返信が来たとき、タイピングドット→吹き出し、を1つずつ繰り返して表示される
- [ ] 1個目の吹き出しは約400ms後に表示される
- [ ] 2個目以降は文字数に応じた待ち時間（最大1600ms、3個目以降は最大3200ms）で表示される
- [ ] ユーザー自身の発言は即時表示のまま（変更なし）
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- マッチング・判定ロジック（どの回答を選ぶか）は変更しない。表示タイミングのみ変更
- オンボーディングの吹き出し演出（既に正しく実装済み）には触らない
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で簡潔に報告してください（動画やスクショで演出が分かる形が望ましい）。
