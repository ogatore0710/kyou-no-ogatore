# タスク（C2/appdev向け）— オガトレ通信・せんぱいの声・にっき＋画面構造のパリティ

## 背景

夜間監査（Fable 4体）の第3弾。お楽しみ系3画面から【大】1件・【中】7件が出ました。
**うち2件はalan5の発注ミスに起因します**（後述）。

なお、Voicesのデータ116件・日替わり選定ロジック（31進ハッシュ→mulberry32→Fisher-Yates→8件）・
フリップ0.55秒・にっきの表示ロジック・Obuの`obuIsLaterOrEqual`/`obuLatest`/30日stale判定・
既読タイミングは**すべて1:1で一致**していました。

---

## 1.【大】オガトレ通信のラジオ投稿が両OSとも聴けない

**Web版**: `index.html:1334-1336` — `<audio controls src=...>` で再生UIが出る。

**ネイティブ**: 両OSともプレースホルダー文言だけ
- Android `ObuScreen.kt:119-124` `Text("🎧 音声つき投稿(ネイティブでは再生UI未実装)")`
- iOS `ObuView.swift:98-100` 同文言

**なぜ重いか**: オガトレ通信は「ひとこと・写真・**ラジオ**」の3本柱として説明文で約束しています。
その1つが丸ごと聴けません。放送型のコンテンツは想定層（50〜60代）と相性がよく、
本人の声が届く数少ない導線です。

**やること**: 音声を再生できるようにする。
- Android: `MediaPlayer` / `ExoPlayer` などで再生・一時停止・シーク
- iOS: `AVPlayer` / `AVAudioPlayer`
- 最低限「再生/一時停止」「再生位置と長さの表示」があれば十分です（Web版の`<audio controls>`相当）
- **バックグラウンド再生や通知センター連携までは不要**（スコープを広げないこと）

---

## 2.【中】FABの表示範囲が狭すぎる ＋「とどくメーター重なり」対策が構造ごと消えている

### ⚠️ これはalan5の発注ミスです

`onboarding-sheet-tap-stolen.md` で `updateFabs()` の移植を発注したとき、
alan5は**「どの画面で隠すか」の表だけ渡して「どの画面で出るか」を確認していませんでした**。
結果、実装は `currentTab != null`（5タブ画面のみ表示）になり、**Web版より表示範囲が狭い**状態です。

**Web版の実際の挙動**（index.html:1419-1434）:
`hide` になるのは quiz / reach / 相談室シート / 各モーダル / welcome **だけ**。
つまり **result・voices・fun・brag・obuアーカイブでもFABは出ます**。

**ネイティブの現状**: `MainActivity.kt:344` / `KyouNoOgatoreApp.swift:190` とも5タブ画面のみ。
せんぱいの声・じまんカード・にっき・オガトレ通信アーカイブでは**通信の入口が消えています**。

### さらに: reach重なり対策が効かない構造になっています

Web版が `reach` でFABを隠す理由は、コードに実測記録が残っています（index.html:1415-1417）:

```
// 実測(390x844)でFAB2段(右下固定)がreach-rowの5番目のボタン「ゆか」に約2/3重なり、
// タップ可能な帯が18px程度しか残らないバグを発見。
// 短いページゆえ、5段階ボタンの行がFABの固定y帯とちょうど重なってしまうため（2026-07-15実測修正）
```

ところが**ネイティブはとどくメーターをMyRecordタブ内にインライン移植している**ため
（`MainActivity.kt:1540-1585` 付近 / `MyRecordView.swift:27-72`）、
MyRecordはFAB表示対象 → **Web版が実測で潰した「ゆかボタンにFABが被る」問題が再発しうる構造**です。

### やること

- FABの表示範囲をWeb版に合わせる（**タブ画面以外でも出す**。隠すのは quiz / reach相当 / シート・モーダル / オンボ / ガイドタブ / チュートリアル当日、という条件のみ）
- **とどくメーターのボタン行とFABが重ならないことを実機で確認**すること。
  重なるなら、その行が画面内にあるときはFABを隠すか、行の下に余白を足す等で回避する
  （Web版と同じ「実測して決める」でよいです）

---

## 3.【中】せんぱいの声・にっき・じまんカードでタブバーが消える

**Web版**（index.html:1541 `TAB_OF`）: `brag:"history"`, `voices:"history"`, `fun:"history"` と
**「マイ記録」タブに属する扱い**になっていて、タブバーは表示され続け「マイ記録」がハイライトされます。
（obuだけは全消灯だが、タブバー自体は表示される）

**ネイティブ**: 5タブ画面以外はタブバー非表示（`MainActivity.kt:190-197,309-319`）。
さらにコメントに「せんぱいの声…はWeb版でもタブに属さない別画面」と書かれていますが、
**これはWeb版の`TAB_OF`と食い違う誤認**です。

**やること**: これらの画面でもタブバーを出し、「マイ記録」をハイライトする。
逃げ道が常にある状態にしてください。

---

## 4.【中】せんぱいの声・にっきの「もどる」がホームに飛ぶ（入口はマイ記録なのに）

- **入口**: マイ記録画面の「お楽しみ機能」カード（`MainActivity.kt:256-257`）
- **戻り先**: `MainActivity.kt:277,280` `onBack = { screen = Screen.Home }` ← ホームに飛ぶ
- **Web版**: 「← マイ記録にもどる」（index.html:925, 891）

**同じアプリ内に正解の実装があります**。オガトレ通信アーカイブは `returnTo` 方式で
正しく「開く前の画面」に戻ります（`MainActivity.kt:281,527`）。同じ方式に揃えてください。

「さっきの画面に戻れない」は機器が苦手な層の迷子の典型です。3とセットで直すと効きます。

---

## 5.【中】オガトレ通信の日付が「2026-07-09」の生表示

**Web版**: `index.html:1314-1318` `obuFmtDate()` で `7月9日` / `7月9日 12:30ごろ` に整形。
さらに `index.html:277-278` で**30日超の古い投稿は11px・薄色に落とす**演出があります。

**ネイティブ**: 両OSとも生ISO表示（`ObuScreen.kt:97-99` / `ObuView.swift:85-86`）。
コメントに「見た目のみスコープを超えるため移植しない」と書かれたまま回収されていません。

**なぜ直すか**: 「2026-07-09」は想定層に読みにくいのが第一。
加えて、**「30日でNEWを引っ込める」設計（更新が止まっても目立たないようにする対策）のうち、
バッジ側だけ移植されて日付の"古さ表示"側が落ちている**という片肺状態です。

---

## 6.【中】オガトレ通信ポップアップにスクロールが無い

**Web版**: `index.html:265` `.obu-box{max-height:80vh;overflow-y:auto}`
**ネイティブ**: 両OSともスクロールなし（`ObuScreen.kt:141-168` / `ObuView.swift:131-165`）

text+photo+radioの3件が揃い、かつ**文字サイズ「大きめ」（既定ON）**だと、
下部の「もっと見る」リンクと✕ボタンが画面外に出て操作不能になり得ます。

---

## 7.【中】写真投稿が固定180で切り抜かれて見切れる

**Web版**: アーカイブは `width:100%`（自然なアスペクト比・切り抜きなし）、
ポップアップは `width:75%` 中央寄せ（index.html:268-269）。

**ネイティブ**: 両OSとも `height 180` 固定 + Crop
（`ObuScreen.kt:107-114` `ContentScale.Crop` / `ObuView.swift:92` `.fill` + `.clipped()`）。
→ **縦長写真は上下が欠けて届きます。**

本人の近況写真が見切れて届くのは、通信の価値を直に削ります。

---

## 8.【小】まとめて直せるもの

| 項目 | Web版 | ネイティブ |
|---|---|---|
| 同日投稿の並び順 | 日付のみの安定ソート（記載順を保持） | `thenByDescending{time}` でtime付きが先頭 |
| せんぱいの声の表裏カード高さ | `.vin{min-height:150px}`＋表面絶対配置で高さ不変 | 表裏で高さが違い、めくると一覧がガタつく |
| 一覧末尾の戻るボタン | voices・obuアーカイブとも**下端にも**あり | 上部のみ |
| にっきの区切り線 | `1px dashed`（破線） | 実線（コメントで近似と明記済み） |
| iOSにっきの日付ラベル | `flex-shrink:0` で潰れない | `layoutPriority`/`fixedSize` なし |

---

## 判断を委ねるもの

**「NEW📣」吹き出しチップ**（index.html:257-263, 1169, 1307-1313）が未移植です。
両OSとも「ドットと重複するため今回は見送り」と**意図的に判断した記録**が残っています
（`KyonoTabBar.kt:204-205` / `KyonoTabBar.swift:181-182`）。

alan5の見立てでは、**老眼前提だと15pxドット単体と「NEW📣」の文字では気づきやすさが段違い**で、
Web版が両方付けているのは冗長化が目的だと思います。ただし既に判断済みの項目を覆すことになるので、
**そちらで妥当と思えば入れてください。入れない判断でも構いません**。どちらでも理由を書いてくれれば。

---

## 検収基準

- [ ] オガトレ通信のラジオ投稿が再生できる（再生/一時停止・位置表示）（両OS）
- [ ] FABがWeb版と同じ範囲で出る（せんぱいの声・じまんカード・にっき・通信アーカイブでも出る）
- [ ] **とどくメーターのボタン行とFABが重ならない**（実機で確認）
- [ ] せんぱいの声・にっき・じまんカードでタブバーが出て「マイ記録」がハイライトされる
- [ ] それらの「もどる」がマイ記録に戻る（`returnTo`方式に統一）
- [ ] 通信の日付が「7月9日」形式・30日超は薄く小さく
- [ ] 通信ポップアップがスクロールできる（文字サイズ大きめ＋3件で✕まで届く）
- [ ] 写真が切り抜かれずアスペクト比のまま出る／ポップアップは75%幅
- [ ] 【小】5件
- [ ] 安全系テスト（111+engine-fixtures）緑・`npm test` 443緑・Web版配信ファイル無変更

## やらないこと

- Voicesのデータ・日替わり選定ロジック・Obuの判定ロジックは触らない（一致確認済み）
- ラジオのバックグラウンド再生・通知センター連携は作らない
- Web版（PWA）側の配信ファイルは一切変更しない

## 優先度

`quiz-result-reach-parity` の次。その中では **2（FAB範囲＋reach重なり）→ 3・4（迷子対策）→ 1（ラジオ）**
の順を推奨します。2はalan5の発注ミスで作り込んだ差なので先に戻したいのと、
3・4は同じ「迷子」の問題なのでまとめて直すのが効率的です。

## 報告

完了時、ドア配達で報告してください。
**とどくメーターのボタン行とFABが重なっていないスクショ**と、**ラジオ再生UIのスクショ**を添えてください。
