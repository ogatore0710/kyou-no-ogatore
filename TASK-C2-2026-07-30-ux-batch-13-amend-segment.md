# 追補: 第2波の案6を差し替え — セグメント切替（あなた用/あさ/よる）の本体移植

発注元: alan5（本人GO 2026-07-30 夜「案6もよろしく」＝セグメント前倒し移植のGO）
親発注: `TASK-C2-2026-07-30-ux-batch-13.md` 第2波

## 変更点

**案6は「ガイド文の暫定是正」を中止し、「セグメント本体の移植」に差し替える。**
ガイドの「気分をかえたい日は『あさ』『よる』へどうぞ」と静止モックは、本体が入れば真実に戻るので**触らない**。

## 移植元（Web版・すべて実在を確認済み）

- セグメントUI: `index.html:656-661`（segMine/segAsa/segYoru の3ボタン＋mineヒント行）。SVGはハート/太陽/月、`stroke-width 2.2`でタブバーのトーン既定と一致 → ネイティブは既存の`KyonoIconGlyph`方式のCanvasで同じ絵を描く（新規画像生成は不要）
- 切替ロジック: `index.html:1709`（`setMode`: `mode_manual`={m, d:きょう} をstore保存・当日限り有効）
- モード解決: `index.html:1711-1753`（`renderToday`）:
  - `mineAvail = タイプ判定済み || プラン実行中`。mineAvailのときだけ「あなた用」タブを表示
  - 実効モード = 手動選択（当日分のみ）→ 無ければ mineAvail?mine:自動判定
  - `:1815` 型なしユーザーに残った手動mineの掃除、`:1723` プラン終了直後の手動mine救済
  - mineヒント行「『あなた用』＝かたさチェックの結果に合わせたおすすめ3本です」は typed かつ プラン無し のときだけ表示（`:1717-1721`のコメントに理由が書いてある。1:1で踏襲）
  - あさ/よるは `TODAY_ASA`/`TODAY_YORU` から `dayIndex()`ローテ（**この選出部はネイティブに自動判定として移植済み**・G1）。今回足すのは手動切替とその永続化・表示分岐のみ
- `:1727` `logoMark`がモードで変わる件: ネイティブに同等物があるか**未確認**。あれば揃え、無ければ「無い」と報告に書くだけでよい（このために新設はしない）

## 注意

- ネイティブの`TodayVideoSection`（iOS `HomeView.swift:896-`）は自動選出のロジックを既に持っている。**選出はそのまま使い、上にセグメントUIと手動上書きを足す**形。選出ロジックの書き直しはしない
- はじめの1本ガイド中（fdGuide）はWeb版どおり「あなた用」も①1本に絞る（`:1739-1744`）
- Web版配信ファイルは無変更（読むだけ）
- 実装規模: M〜L（両OS）。第2波の中で最後に回してよい

## 検収基準

- [ ] タイプ判定済みユーザーで3タブが出て、手動切替が効き、**翌日には自動選出に戻る**（mode_manualの当日限り）
- [ ] 未チェックユーザーには「あさ/よる」2タブのみ（あなた用が出ない）
- [ ] プラン実行中は「あなた用」=プラン動画・ヒント行非表示
- [ ] タブの絵がタブバーと同じ手に見える
- [ ] 実機/シミュレータの動画つき報告
