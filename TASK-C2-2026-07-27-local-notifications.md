# タスク（C2/appdev向け）— 毎日のおしらせ通知（ネイティブならではの機能・本人と仕様合意済み）

## 背景

本人と仕様を相談した上での発注。**ネイティブ化で得られる最大の価値**にあたる機能。

Web版はPWAのため通知を送れず、その代わりに「カレンダーに予定として登録してもらう(.ics)」
「iPhoneではショートカットアプリで自動化してもらう」という回避策を案内している（index.html内に
関連記述が38箇所）。これは機器が苦手な層には重い手順で、想定ユーザーにとって実質的な障壁になっている。
ネイティブなら通常のローカル通知で置き換えられる。

**朗報: 仕様の土台はすでにWeb版に存在する。** `ANCHORS`（index.html:1974-1979）に、やるタイミング
ごとの時刻と文言が定義済みで、これはカレンダー登録用にすでに本人承認されて動いているもの。
新規に文言や時刻を発明する必要はない。

## 設計原則（最重要・ここを外さないこと）

**追い立てる通知にしない。** このアプリは「休んでもいい」を設計思想にしており、毎月3枚の
🎫おやすみ券で連続を守る仕組みまで用意されている。それと矛盾する以下は**禁止**:
- 「3日サボっています」「連続が途切れます」等、不在や中断を責める文言
- 数字・ノルマ・達成率で追い立てる表現
- 1日に複数回送る、無視すると頻度を上げる、等のしつこい挙動

送るのは**決めた時間に、一言だけ、やわらかく**。それ以上はしない。

## 仕様

### 1. 通知の時刻と文言（`ANCHORS` index.html:1974-1979 が正本）

| anchor | 既定時刻 | 通知文言 |
|---|---|---|
| `asa`（朝おきて） | 07:30 | おはようございます☀️朝の1本いきましょう！ |
| `furo`（おふろ上がり） | 20:30 | おふろ上がりの1本いきましょう♨️ |
| `neru`（寝るまえ） | 21:30 | 寝るまえの1本できょうをしめくくりましょう🌙 |
| `free`（きめてない） | 20:00 | **夜はストレッチにおすすめの時間だよ🌿 きょうの1本どうぞ** |

- `asa`/`furo`/`neru`の文言は`ANCHORS[x].g`をそのまま使う（既存の承認済み文言）
- **`free`だけ文言を変更する**（本人指示）。既存の`ANCHORS.free.g`は「すきな時間にきょうの1本を
  どうぞ🌿」だが、通知では「夜がおすすめの時間である」ことを伝える上表の文言にする。
  ※`ANCHORS.free.g`自体（アプリ内の挨拶で使われている）は変更しないこと。通知用の文言として別に持つ
- 時刻の既定は`ANCHORS[x].tm`（"073000"形式）から取る

### 2. 時刻の設定は既存のものを使う（新しい設定を増やさない）
設定画面に既にある「カレンダーのおしらせ時間」の時刻ピッカー（`icstime`・`SettingsScreen.kt:86,184` /
iOS同等）を、**そのまま通知時刻としても使う**。
- `icstime`が設定されていればそれを使い、未設定ならanchorごとの既定時刻を使う
  （Web版`renderIcs()`の`saved || def`と同じ優先順位）
- 既存のApple/Googleカレンダー登録ボタンは**残す**（消さない）。通知が使えない/使いたくない人の
  逃げ道として引き続き機能させる

### 2-2. 時刻ピッカーを15分刻みにする（本人指示・2026-07-27追加）
現在の「カレンダーのおしらせ時間」は1分刻み（Android `TimePickerDialog`・iOS `DatePicker`とも
標準は1分単位）だが、**15分刻み（:00 / :15 / :30 / :45 の4択）に変更する**。

理由: 想定ユーザー層（50-60代以上・機器が苦手な層）にとって、分を1刻みで合わせる操作は負担が大きい。
4択に絞れば「だいたいこの時間」を迷わず選べる。

- 「時」は0〜23、「分」は`00/15/30/45`の4択。**標準のスピナーではなく、時と分を並べて選ぶ形**が
  分かりやすい（実装方式は任せます。Androidは`NumberPicker`相当やドロップダウン、
  iOSは`Picker`2つ並べる等）
- **既に保存済みの`icstime`が15分刻みでない場合**（例: 07:23）は、表示・保存とも最も近い15分に
  丸めること。既定値（07:30 / 20:30 / 21:30 / 20:00）はいずれも15分刻みなので影響なし
- この変更は通知だけでなく、**既存のカレンダー登録ボタンにもそのまま効く**（同じ`icstime`を使うため）

### 3. 通知のオン/オフ
- 設定画面に「毎日のおしらせ」トグルを追加（既定=オフ。許可を取ってからオンになる）
- オフにしたら予約済みの通知をすべて取り消すこと

### 4. 許可を求めるタイミング（重要）
**1日目をクリアした直後**——「🎉 1日目クリア！ナイスご自愛！」のお祝いが出た場面で、
「あしたも おしらせしようか？」といった形で提案し、そこで初めてOSの許可ダイアログを出す。

理由: iOS/Android13+とも許可ダイアログは実質1回しか出せず、拒否されると設定画面へ誘導するしかない。
起動直後に出すと文脈が無いまま拒否されやすい。**1本やり終えて「続けたい」と思った瞬間**が
いちばん受け入れられる。

- 起動直後・オンボ中には**絶対に出さない**
- 断られた場合、その後しつこく再提案しない（設定画面からいつでもオンにできる形で十分）

### 5. すでに記録した日には鳴らさない
その日の記録が済んでいるのに「やろう」と通知が来るのは無意味かつ鬱陶しい。
**当日分を記録済みなら、その日の通知は出さないこと。**
（実装方式は任せます。アプリ起動時・記録時に次回分を組み直す等）

## 検収基準

- [ ] 設定画面の「毎日のおしらせ」をオンにすると、設定時刻に通知が届く
- [ ] anchorごとに既定時刻・文言が正しく出し分けられる（4パターンとも）
- [ ] 「カレンダーのおしらせ時間」を変更すると通知時刻もそれに追従する
- [ ] 時刻ピッカーが15分刻み（:00/:15/:30/:45の4択）になっている
- [ ] 15分刻みでない既存の保存値が、最も近い15分に丸められて表示・保存される
- [ ] その日すでに記録していると通知が来ない
- [ ] オフにすると通知が止まる
- [ ] 許可ダイアログが1日目クリアの場面で出る（起動直後・オンボ中には出ない）
- [ ] 通知タップでアプリが開く
- [ ] 既存のApple/Googleカレンダー登録ボタンが従来どおり動く
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test` 443緑・Web版配信ファイル無変更

## やらないこと

- サーバーからのプッシュ通知は作らない（**端末内のローカル通知のみ**。このアプリは
  「登録不要・記録は端末内だけ」が売りなので、サーバーへ情報を送る設計にしない）
- 記録・判定ロジックは変更しない
- `ANCHORS`のデータ自体は変更しない（通知用の文言だけ別に持つ）
- Web版（PWA）側の配信ファイルは一切変更しない

## 優先度

`text-size-accessibility` → `screen-transitions`／挙動監査§B§D → **このタスク** の順。
新機能なので、既存の欠落を埋め終えてから着手してください。

## 報告

完了時、ドア配達で報告してください。通知が実際に届いた画面のスクショを添えてください。
