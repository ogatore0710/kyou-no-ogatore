# タスク（C2/appdev向け）— 「はじめの1本ガイド」専用UIが丸ごと未実装（初回体験の足場）

## 背景

挙動パリティ監査§Aで、appdev自身が「構造的欠落・要判断」として報告してくれた1件目。
alan5が裏を取り、**本人判断で実装GO**が出た。

`HomeLogic.fdActive()`（ガイド中かどうかの判定）は移植済みで実際に動いているのに、
**ガイド中だけ出る専用UIがAndroid/iOS双方に1つも存在しない**（該当文言をgrepして0件）。
判定は動くのにUIブランチが無い状態。

**なぜ優先度が高いか**: これは演出ではなく初回ユーザーの体験そのもの。Web版のコードに動機が
明記されている——**「タスクスイッチできない層がYouTubeから戻れないのが最初の脱落点」**。
だからOS別に戻り方を案内し、3本ではなく①だけを見せて「練習」させる作りになっている。
2026-07-21の5視点検証Dで本人承認済みの機能。このアプリが想定している高齢者・機器が苦手な層が
最初につまずく地点の足場が、いま丸ごと無い。

## Web版の仕様（3箇所。いずれもガイド中=`guide`が真のときだけの分岐）

### 1. 結果画面をガイド専用UIに差し替え（app-quiz.js:300-322 が正本）
```
見出し(rxHead): 「きょうはこの1本だけでOK！」（通常時の「おすすめの3本: …」を置き換え）

rxListの中身を以下に差し替え（通常の3本リストは出さない）:
 ① 練習宣言の吹き出し（chara-hitokotoアバター付きの相談室と同じ吹き出し体）
    「ここからは練習だよ🏫 ①を試しにタップ→YouTubeがひらいたら すぐ戻ってきてね
     （ぜんぶ見るのは あとでゆっくりでOK）」
    ＋小さめの文字で「🔙 {OS別のもどりかた}」
      - iOS: 「見おわったら 画面ひだり上に出る「◀」か YouTubeをとじると この画面にもどれるよ」
      - Android: 「見おわったら スマホの「もどる」ボタン（◀）で この画面にもどれるよ」
 ② 指差し「👇 ここを押してみて」（fd-point = 上下に揺れ続けるアニメ fdBob 1.4s infinite）
 ③ おすすめ1本目だけを大きく表示（fd-hero）。ラベルは「きょうはこれ1本でOK！」
 ④ 「あと2本とくわしい解説は あしたから見られるよ🌱」
```

### 2. 記録直後の「つぎはここ」誘導（app-record.js:196-208 が正本）
```
ガイド中に「きょうやった！」を押した後、「記録カードを見る」ボタンの直上に
「👇 つぎは ここを押してみて」のヒントを挿入し、ボタン自体に呼吸アニメ
（fd-breathe = 1.8s ease-in-out infinite でふわっと明滅）を付ける。
使い方ツアーが自動起動するとき（fdTourMaybeStart相当）に、ヒントと呼吸アニメを両方片付ける。
```

### 3. 1日目クリア時のお祝い（app-record.js:140-149 が正本）
```
ガイド中かつ節目でないとき、cheer欄を以下に差し替え:
  「🎉 1日目クリア！ナイスご自愛！」
  記録カードのサンプル画像（card-sample）がポップイン（fd-cardpop = fdPop .5s、
  cubic-bezier(.34,1.56,.64,1)のバウンド付き）
  「きょうの記録が1まい目のカードになったよ ためると図鑑がうまっていく📖」
  「よかったら下に✍️きょうのひとことをどうぞ からだの感じをひとことでOK（あとからでもいいよ）」
※節目表示(ms)がある場合はそちらを優先する構造（Web版と同じ順序にすること）
```

## やること

- 上記3箇所のガイド専用UIを両OSに実装。ガイド判定は**既存の`HomeLogic.fdActive()`を呼ぶだけ**にし、
  判定ロジックを再実装しないこと
- アニメーション3種（fdBob=指差しの上下、fdBreathe=呼吸、fdPop=バウンド付きポップイン）も
  Web版の秒数・イージングに合わせて実装
- OS別のもどりかた文言は、Web版がUAで出し分けているのに対し、ネイティブでは自プラットフォームの
  文言を出せばよい（iOS版はiOS文言・Android版はAndroid文言で固定）

## 検収基準

- [ ] ガイド中の結果画面が専用UI（練習宣言＋指差し＋1本だけ＋あしたから案内）になる
- [ ] 指差しが上下に揺れ続ける／記録カードボタンが呼吸する／カードサンプルがバウンドして出る
- [ ] ガイド中でないときは従来どおり「おすすめの3本」が出る（既存挙動を壊さない）
- [ ] 記録直後の「👇 つぎは ここを押してみて」が出て、ツアー起動時に片付く
- [ ] 1日目クリア時のお祝いが出る（節目と重なる場合は節目を優先）
- [ ] **Androidは実機で一連の流れを通しで確認**（オンボ→クイズ→ガイド結果画面→動画タップ→
      復帰→きょうやった！→つぎはここ→カード→ツアー）。iOSはシミュレータで目視
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test` 443緑・Web版配信ファイル無変更

## やらないこと

- ガイドの発生条件・判定ロジック（`fdActive`/`fd`/`fdday`フラグ）は変更しない
- 通常時（非ガイド）の結果画面は変更しない
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で報告してください。ガイド中の結果画面のスクショを添えてください。
