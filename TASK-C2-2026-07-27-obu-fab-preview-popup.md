# タスク（C2/appdev向け）— オガトレ通信FABタップ時、Web版は「最新のみプレビュー」ポップアップ→ネイティブはいきなり全アーカイブ

## 背景

alan5の独自調査の続き（12セクション監査は#obu＝オガトレ通信アーカイブタブ自体はカバー済みだが、
**FABタップ時の挙動**はモーダルUIのため対象外だった）。

Web版はFAB（📣）タップで小さなプレビューポップアップ（`obuModal`・index.html:1177-1189）を開き、
「テキスト・写真・ラジオそれぞれの最新1件ずつ（最大3件）」だけを見せる→下部の
「もっと見る（過去の投稿もぜんぶ）」リンクを押して初めて全アーカイブ画面に遷移する2段階構成。

ネイティブ（Android `MainActivity.kt:269`、iOS `KyouNoOgatoreApp.swift:129`）はFABタップで
直接`Screen.Obu`（全アーカイブ）へ遷移していて、この「最新ハイライトだけを見せるプレビュー」の
段階が丸ごとスキップされている。

## Web版の仕様（`renderObuPopup`/`openObu` index.html:1344-1368が正本）

```
FABタップ→openObu():
- タイトル「オガトレ通信」+閉じるボタン(✕)
- 本文: text/photo/radioそれぞれの最新1件を取得(obuLatestByType)、存在するものだけ日付つきで
  表示（最大3件。1件も無ければ「まだ投稿がありません また今度のぞいてみてね🌱」）
- 下部リンク「もっと見る（過去の投稿もぜんぶ）」→ タップで#obuタブ（全アーカイブ）へ遷移+
  ポップアップを閉じる
- ポップアップを開いた時点で最新投稿IDを既読(obu_seen)として記録→バッジカウント更新
```

## やること

- FABタップ時、まず上記プレビューポップアップを表示するよう変更（全アーカイブへの直接遷移をやめる）
- 「もっと見る」リンクタップで初めて全アーカイブ画面（既存実装済み）へ遷移するよう配線
- 既読記録・バッジ更新のタイミングもWeb版と合わせる（プレビューを開いた時点で既読）

## 検収基準

- [ ] FABタップでプレビューポップアップが開き、text/photo/radio最新各1件（最大3件）が表示される
- [ ] 投稿が1件も無いときの空メッセージが表示される
- [ ] 「もっと見る」タップで全アーカイブ画面に遷移する
- [ ] プレビューを開いた時点でバッジの未読カウントが正しく更新される
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- 全アーカイブ画面自体（既存実装）は変更しない
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で簡潔に報告してください（スクショ1枚で可）。
