# G1差し戻し調査: ホーム画面の節目ブロックが実機で出ない件

## 結論

**Androidは実機相当(エミュレータ)の新規ビルドで再現手順どおり操作し、節目ブロックが正しく表示されることを確認しました。** バグではなく、「`きょうの分は完了！おつかれさまでした😊` ボタンの直後」ではなく「そこから1〜2カード分さらに下」に出る位置の問題だった可能性が高いです(bigtext=trueだと前段のカードが縦に伸びるため、フォールドの外に出やすい)。

iOSは同一ロジックであることをコードレビューで確認し、ビルドも通っていますが、**この環境にはiOS Simulatorへタップ入力を送る手段(idb等)が無く、実行して見た目を確認することはできませんでした**(この制約は既知どおりです)。

## Android: 再現手順の追試

1. `./gradlew assembleDebug` でコミット `c684e35`(G1)・`1919892`(G2)を含む最新コードからAPKを作成し、エミュレータへ新規インストール(`-r`で上書き)。
2. `adb run-as` 経由で `kyono-store.json` に報告と同じ値を注入:
   `{"kyono_streak2":{"dates":["2026-07-26","2026-07-27"],"count":2,"total":2},"kyono_onboarded":true,"kyono_bigtext":true}`
3. アプリ起動 → ホームを下へスクロール → 「きょうやった！」をタップ(→通算3日=`MS`最小節目)。
4. カードダイアログで「3日目！」・王冠・「がんばりすぎず、ほどよく。ご自愛くださいね。」を確認(報告と一致)。
5. 「とじる」で閉じてさらに下へスクロール →
   **`🎉 『気がする』チェックポイント！（通算3日）` と専用メッセージ(`m`)が表示されることを確認**(スクリーンショット添付)。
   - この節目(d=3)は `q`(せんぱいの声)が空文字のため引用ボックスは出ない仕様どおりの挙動。

念のため、`q` が入っている節目(d=4)でも追試:

6. `dates:["2026-07-25","2026-07-26","2026-07-27"],count:3,total:3` で再注入 → 「きょうやった！」→「4日目！」の節目カードを確認。
7. とじて下へスクロール → **タイトル・専用メッセージに加えて「💬 せんぱいの声」引用ボックスまで表示され、末尾の「（先輩の声）」が正しく除去されていることを確認**(スクリーンショット添付)。

いずれもタイトル・専用メッセージ・引用・王冠画像が `MainActivity.kt:1206` の `AnimatedVisibility(visible = milestoneInfo != null)` ブロックどおりに描画されており、コード上の欠陥は見当たりませんでした。

## 推測される原因

- 実機での確認時、`きょうの分は完了！` ボタンのすぐ下だけを見て、節目ブロックが実際に描かれている「そこからさらに1〜2カード分下」までスクロールしていなかった可能性。
- または、実機にインストールされていたAPKが `c684e35`(G1のUI配線コミット)より前のビルドだった可能性(紙吹雪と記録カード画像の節目内容は、それぞれ別コミットで直っているため、「紙吹雪は出た」ことは必ずしも「G1のUI配線コミットを含むビルドだった」ことを保証しません — 記録カード画像側の`m`/`q`はカード生成ロジックの側にあり、そちらは1日早い `auto-sync 2026-07-28 23:26` の時点で既に直っています)。

## お願い

再現される場合は、実機にインストールされているAPKのビルド元コミット(またはインストール日時)をご確認いただけますでしょうか。可能であれば端末を一度アンインストール→最新pull後に再ビルド→再インストールしてから再テストしていただけると切り分けが早いです。

## iOS: コードレビュー結果

`HomeView.swift` の該当箇所(`milestoneInfo`状態・`markDone`ハンドラの3分岐・`AnimatedVisibility`ブロック・confettiの`.id()`張り替え)をAndroid版と突き合わせて再確認しましたが、**構造・分岐条件ともAndroid版と1:1で同一**であり、ロジック上の欠陥は見当たりませんでした。`xcodebuild`(Simulator向け)・`CardCore swift test`(16/16)ともに green です。

ただし、この環境にはiOS Simulatorへスクリプトからタップ/スワイプを送る手段(idb・System Eventsのアクセシビリティ権限とも未整備)が無く、**Android同様に「操作→実際に画面に出るか」を目視で確認することはできませんでした**。iOS側の実機確認は、これまでどおり本人/alan5にお願いする形になります。

## 検証コマンド

```
./gradlew assembleDebug  # 現行コード(c684e35, 1919892含む)からAPK作成
adb install -r app/build/outputs/apk/debug/app-debug.apk
# store注入・起動・タップ・スクリーンショットは本文の手順どおり
```
