# アイコン方針への追補: 部位・時間帯選択チップのイラスト化

発注元: alan5（本人フィードバック2026-07-30）
**`TASK-C2-2026-07-30-icon-system.md`の続き。トーンの基準・検収基準はそちらと同一（タブバーの5つと並べて違和感が無いことが唯一の合格条件）。この追補は対象を4箇所追加するだけ。**

## 背景（本人の生の発言）

「腰とか肩とか選ぶときに、その腰肩って分かりやすく生成絵文字みたいなのが揃えてあったら分かりやすいなと思うし、朝とか夜とかお風呂上がりも生成イラストが、全体的にトンマナが揃った状態であるといいなと思っています。このトンマナは下のタブバーに揃えるのがベスト。硬さチェックで六種類画像が出るんだけど、あれはスクイーズハンターの感じですごく可愛くていいから、それ以外はタブバーと揃った生成絵文字が表示されていると分かりやすい。」

**注意（例外）**: `KyonoTypeArt.swift`/`.kt`の硬さチェック結果6タイプ（momo/kenko/yawara/koka/ashi/robot）は**この対象に含まない**。本人が明示的に「あれはそのままでいい」と指定している。触らないこと。

## 対象4箇所（すべて確認済み・現状は文字ラベル+色分けのみ、絵文字/イラスト無し）

1. **部位選択（worryチップ）**: 「肩こり・首」「腰」「前屈できない」「眠り」「とくにない」
   - iOS: `OnboardingViews.swift:51-55`（定義）／`:265-272`（描画、`Text(chip.label)`+`palette[i%4]`色分けのみ）
   - Android: `OnboardingScreens.kt:90-113`（定義）／`:299-308`（描画）

2. **時間帯選択（オンボーディング内・anchor質問）**: 「朝おきて」「おふろ上がり」「寝るまえ」「きめてない」
   - iOS: `OnboardingViews.swift:56-59`（定義）、描画は1と同じ`:265-272`
   - Android: `OnboardingScreens.kt:114-116`

3. **時間帯選択（設定画面「やるタイミング: 変える」）**: 上記と同じ`asa/furo/neru`キーを共有
   - iOS: `SettingsView.swift:18-26`（`settingsAnchors`定義）／`:152-183`（UI、`KyonoGhostButton(info.label)`で文字のみ）
   - Android: `SettingsScreen.kt:200-230`付近

## 進め方

`TASK-C2-2026-07-30-icon-system.md`の本編が完了・GO済みであることが前提。同じ生成パイプライン（`gen-type-art.py`踏襲、アンカー画像でトーンを固定）を使い、まず**部位選択の1つ（例: 「腰」）を1枚描いて、実際の画面に置いて報告**。タブの5つ・新規10種のアイコンと並べて違和感が無いか、alan5が見てからOKを出し、残りへ進む。

部位選択と時間帯選択（1・2）は同じ`ObChip`構造を共有しているので、実装上は同じ差し込み口になるはず。3（設定画面）はキーが共通なので、1・2で作った絵をそのまま再利用できる（3のためだけに新しく描き起こす必要はない）。

## 検収基準

- タブバーの5つと並べたときに、同じ手で描かれたものに見えること（本編と同一基準）
- 硬さチェック結果6タイプ（`KyonoTypeArt`）に触れていないこと
- 両OSで同じ絵になっていること
- `npm test` / 両OSの回帰
