# TASK-C2-2026-08-05-build27-round5: 本人ラウンド5

発注: alan5（2026-08-05 16:20頃）
背景: ビルド26提出中に本人ラウンド継続（IMG_8804・マイ記録のとどくメーター）。
追加項目はR-11…として追記していく。ビルド提出はalan5の合図待ち。

## R-10: とどくメーターの説明文ダイエット（両OS・本人文言指定）

本人指示: 「ここも 毎週月曜日は前屈チェック！ 手はどこまでとどく？ くらいの文字量にして」

- 現状（iOS `MyRecordView.swift:478,481`／Android `MainActivity.kt:2620,2625`）:
  - 「ひざを伸ばして前屈 手はどこまで届く？\n届いたところのボタンを押すと記録されます（週1回でOK）」
  - 「いたみがある日は むりしないでね（つよい痛みが続くときは お医者さんへ）」
- 変更: 上記2つのTextを**次の2行に置き換える**（本人指定文言・一字一句このまま）:
  - `毎週月曜日は前屈チェック！`
  - `手はどこまでとどく？`
  - スタイルは現状の説明行のトーン踏襲（1つ目のTextの見た目を流用・2行または2Text、
    いたみ行のスタイルは消滅）。
- **注意（alan5からの申し送り）**: 「毎週月曜日」というリズムはアプリ側にロジックが無い
  （記録は何曜日でも可能・週1回でOKの目安表示だった）。文言だけの号令であり挙動は変えない。
  医療注意行（お医者さんへ）はこのカードからは消えるが、ペースの目安カード側の
  「※効果には個人差があります 痛みが強いときは中止して医療機関へ」は残る（別画面・不変）。

## 検収

- `npm test` 再実行・実描画ライト1枚（マイ記録のとどくメーターカード全体）。

---

# 追加項目 R-11（2026-08-05 16:27追記）: 起動直後の「文字なし画面」の一瞬を解消

本人指摘（ScreenRecording_08052026_162354_1.mov）: 「最初の挙動変だね。一瞬映る文字なしの部分いらない」

## alan5のフレーム実測（10fps分解・scratchpad/rec-sheet.png）

- 0-100ms: ホーム画面→アイコンタップ
- **200-500ms: OSの起動画面（LaunchScreen）= キャラ絵だけ・文字なし。端末がダーク設定のため
  暗地(#211E19)で表示され、アプリ強制ライトへ明→暗→明の切り替わりも起きている**
- 600-1000ms: アプリ内スプラッシュ（W-7実装・#ロゴ+「きょうのオガトレ」+サブタイトル）
- 1100ms〜: ツアー1枚目

つまり「文字なしの部分」= LaunchScreen（静的・キャラのみ）。アプリ内スプラッシュとの
2段構えが割れて見えている。

## 変更方針

**LaunchScreenをアプリ内スプラッシュの初期フレームと同一の見た目にする**（＝起動が1枚の
連続スプラッシュに見える定石）。

1. **iOS**: LaunchScreenの構成（現: キャラ画像`LaunchChara`+`LaunchBackground`）を、
   アプリ内スプラッシュと同じ「#アイコン+きょうのオガトレ+みんなで一緒にストレッチを習慣化」
   の静的画像に差し替える。文字はテキストでなく**画像として焼き込み**（LaunchScreenの制約）。
   ライト/ダーク両バリアント（ライト=#FFFAF3地・ダーク=現行#211E19地に合わせて文字色反転）。
   スプラッシュ初期フレームと画素位置を揃えること（ズレるとかえって目立つ）。
2. **Android**: `windowSplashScreenAnimatedIcon`（現: splash_chara）は仕様上アイコン中央のみ。
   キャラ→**#ロゴマーク**に差し替えて、続くアプリ内スプラッシュと図柄を接続する（文字は不可・
   仕様の制約としてそこまででよい）。
3. アプリ内スプラッシュ(850ms)側は触らない。

## 検収

- iOS: 実機相当の起動録画をシミュレータで撮り、フレーム分解して「文字なし段階が無い」ことを確認
  （ライト・ダーク各1回）。
- Android: コードとリソース差し替えの確認でよい（実描画はストア前宿題に積む）。
- `npm test`。

---

# 追加項目 R-12: ツアー中は「ペースの目安」カードを丸ごと出さない

本人赤ペン（FullSizeRender 16:31・ツアーきろくステップ）: 「使い方ツアーのこの画面では赤い部分丸ごと取って」
対象=「ペースの目安」カード全体（箇条書き4行+個人差注意+「この悩み、相談室で聞いてみる」リンク含む）。

- `fdGuideActive` のときのみ非表示。**通常ユーザーの結果画面では従来どおり表示**（医療注意の
  文言はツアー外では残る）。
- 両OS。実描画: ツアー中（出ない）/ツアー外（出る）各1枚。

# 追加項目 R-13: ツアーのカード練習画面は「0日目」表示+保存シェア案内

本人指示（IMG_8811・ツアーステップ4カード）:
「この画面は0日って表示させて。テストだから」
「あと、自分用に画像を保存したり SNSでシェアしたりしてね！ と書いて」

1. **日数表示**: ツアー中（練習で作られるカード）は「1日目！」でなく**「0日目！」**にする
   （様式は現行踏襲・数字だけ0に。本人の言葉は「0日って表示」）。ツアー外の本物の記録カードは不変。
   実データ（連続日数カウント）への影響が無いことを確認すること（表示だけの変更。
   もし練習記録が実カウントに入っている場合はその旨を報告に明記・挙動は変えない）。
2. **案内文言追加**（ツアー中のこの画面のみ・シェアボタン付近）:
   `自分用に画像を保存したり SNSでシェアしたりしてね！`
   （一字一句このまま・sub色13pt目安・中央寄せ）。
- 両OS。実描画: ツアー中カード画面（0日目+案内文言が写る）1枚。

（ラウンド5はさらに継続中: かたさ/悩み/時間帯の意味リンク配色は本人モック裁定待ち。
確定したらR-14として追記する）
