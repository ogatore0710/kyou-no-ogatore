# タスク（C2/appdev向け）— オンボーディング最後の「締めメッセージ+専用ボタン」が丸ごと欠落

## 背景

alan5の独自調査の続き。`ONBOARDING_SCRIPT`（index.html:4087-4115）を全項目Web版と突き合わせた結果、
`greet`（あいさつ3行）・`questions`（4問+チップ）・`anchorAck`（4種の相槌）は両OSとも完全一致で
実装済みだったが、**`routes`（quiz/todayの締めメッセージ+専用ボタン）だけが両OSとも実装されて
いない**ことを確認した。

現状（Android `OnboardingScreens.kt:149-163` `finish()`、iOS `OnboardingViews.swift:119-134`
`finish()` で確認）は、anchor質問の相槌（`anchorAck`）を表示した直後、`onComplete(route, ...)` を
即座に呼んで次の画面（クイズ or 今日の1本）へプログラム的に遷移するだけで、Web版にある**会話の
締めくくりメッセージと、それに続く専用CTAボタン**が一切表示されないまま画面が切り替わる。

## Web版の仕様（index.html:4108-4111 `ONBOARDING_SCRIPT.routes`が正本）

```
anchorの相槌を表示した後、obDecideRoute()の結果に応じて追加でもう1往復ある:
- route==="quiz"の場合:
  - 吹き出しで「そしたら30秒で硬さチェックをしよう！下のボタンタップしてね！」を表示
  - 専用ボタン「かたさチェックをはじめる」を表示（タップで初めてクイズ画面へ遷移）
- route==="today"の場合:
  - 吹き出しで「じゃあ今日の1本から！むずかしいことはなしだよ👍」を表示
  - 専用ボタン「きょうの1本を見る」を表示（タップで初めて今日の1本画面へ遷移）
```

画面遷移はボタンタップ時に初めて起きる（Web版はメッセージ表示だけして自動では進まない）。

## やること

- Android `OnboardingScreens.kt` の `finish()` 直前、iOS `OnboardingViews.swift` の `finish()` 直前に、
  `obDecideRoute()` の結果に応じた締めメッセージ（`say()`と同じ1.5秒表示演出でよい）を追加表示
- メッセージ表示後、既存の質問チップと同じ位置に専用CTAボタン（「かたさチェックをはじめる」/
  「きょうの1本を見る」）を表示し、**タップされて初めて** `finish()`（=画面遷移）を呼ぶよう変更
  （現状の「相槌の直後に自動で`finish()`」という流れ自体をボタン待ちに変更する必要がある）

## 検収基準

- [ ] anchor相槌の後、routeに応じた締めメッセージが吹き出しで表示される
- [ ] 締めメッセージの後、専用ボタン（かたさチェックをはじめる/きょうの1本を見る）が表示される
- [ ] ボタンタップで初めて次の画面（クイズ or 今日の1本）へ遷移する（自動遷移しない）
- [ ] quiz/today両ルートで動作確認（stiff=hard等でquizルート、stiff=normal+worry=noneでtodayルート）
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- `obDecideRoute()`のルーティング判定ロジック自体は変更しない（既存のまま正しい）
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で簡潔に報告してください（スクショ1枚で可）。
