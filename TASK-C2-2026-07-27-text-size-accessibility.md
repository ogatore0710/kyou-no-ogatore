# タスク（C2/appdev向け）— 文字サイズが効いていない（自前設定・OS設定とも）＋読み上げラベル整備

## 背景

alan5の調査。「ネイティブならではの改善は何ができるか」を洗う過程で、**想定より重い欠落**を2件見つけた。
どちらもこのアプリの想定ユーザー層（50-60代以上・機器が苦手な層）に直撃する。

### 欠落1: 「もじの大きさ」設定が保存されるだけで、表示に一切反映されていない（両OS・重い）

Web版は`body.bigtext{zoom:1.18}`でアプリ全体を1.18倍に拡大し、**既定がON（大きめ）**。
これは「対象ユーザー50-60代」という理由で2026-07-12の本人フィードバックにより決まった仕様
（index.html:87, 2115）。

ネイティブは`bigtext`の読み出しが`SettingsScreen.kt:74,283` / `SettingsView.swift:50,253`
＝**設定画面でトグルの状態を表示するためだけ**にしか存在せず、実際の文字サイズには反映されていない
（`1.18`相当のスケール適用箇所をgrepしても存在しない）。

結果として:
- 設定の「もじの大きさ」を切り替えても**何も起きない**
- オンボ1問目で「大きめ（いまのまま）」を選んでも**何も起きない**
- **既定が大きめのはずなのに、常に「ふつう」相当で表示されている**＝Web版より約18%小さい

### 欠落2: iOSが端末の文字サイズ設定（Dynamic Type）を完全に無視している

`Theme.swift:163`が`Font.custom(psName, size: size)`（**固定サイズ版**）を使っている。
この書き方はDynamic Type非対応で、iPhone設定の「テキストサイズ」「さらに大きな文字」を
変更してもアプリの文字は変わらない。`Font.custom(_:size:relativeTo:)`を使えば追従する。

Android側は`fontSize = N.sp`なのでOS設定に追従する（＝この欠落はiOS限定・両OSで挙動が食い違っている）。

## やること

### 1. 「もじの大きさ」設定を実際に効かせる（両OS・必須）
- `bigtext`がONのとき、アプリ全体の文字が大きくなるようにする（Web版の1.18倍相当）
- **既定はON（大きめ）**。Web版と同じく「明示的に『ふつう』を選んだ人だけ外れる」
- 設定画面での切り替え・オンボ1問目の回答が、どちらも即座に見た目へ反映されること

### 2. iOSをDynamic Type対応にする
- `Font.custom(_:size:relativeTo:)`に変更し、端末の文字サイズ設定に追従させる
- サイズ→`TextStyle`の対応付けは妥当な範囲で決めてよい（例: 20以上→.title系、15前後→.body、
  12-13→.caption系）

### 3. 1と2の合成が破綻しないこと
アプリ自前の1.18倍とOS側の拡大が重なると文字が極端に大きくなりうる。**両方が効いた状態でも
主要画面のレイアウトが破綻しない**ことを確認し、必要なら上限を設ける。
「アプリで大きめ＋端末も最大」の組み合わせで、ホーム・かたさチェック・結果画面・マイ記録・
相談室が読める状態を保つこと。

### 4. 読み上げ（TalkBack / VoiceOver）ラベルの整備
現状ラベルがほとんど入っていない（Android `contentDescription`・iOS `accessibilityLabel`とも
数箇所のみ）。最低限、以下に読み上げ可能なラベルを付ける:
- 下部タブバーの5タブ
- ホームの主要ボタン（きょうの1本を見る／きょうやった！／記録カードを見る）
- FAB2つ（相談室・オガトレ通信）
- かたさチェックの選択肢・結果画面の動画カード
- 画像のみで意味を伝えている要素（キャラ画像等は装飾として読み上げ対象外にする）

## 検収基準

- [ ] 「もじの大きさ」を切り替えると実際に文字サイズが変わる（両OS）
- [ ] 初期状態（未設定）で**大きめ**になっている（Web版と同じ既定）
- [ ] iOSで端末の文字サイズ設定を変えるとアプリの文字も変わる
- [ ] 「アプリで大きめ＋端末の文字サイズ最大」でも主要5画面のレイアウトが破綻しない
- [ ] TalkBack/VoiceOverで主要導線が読み上げられる
- [ ] Web版と並べたときの文字の大きさが大きく違わないこと（1.18倍相当が効いている確認）
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test` 443緑・Web版配信ファイル無変更

## やらないこと

- ロジック・判定・データ構造は変更しない（表示とアクセシビリティのみ）
- Web版（PWA）側の配信ファイルは一切変更しない

## 優先度

進行中の`screen-transitions`より**こちらを先**にしてください（想定ユーザー層に直撃する欠落のため）。
挙動監査§B/§Dはその後で構いません。

## 報告

完了時、ドア配達で報告してください。**「ふつう」と「大きめ」の比較スクショ**、および
「アプリ大きめ＋端末最大」でのホーム画面スクショを添えてください。
