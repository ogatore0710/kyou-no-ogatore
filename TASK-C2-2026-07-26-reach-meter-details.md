# タスク（C2/appdev向け）— とどくメーターの自己ベスト・前回比・トレンドグラフが欠落

## 背景

alan5の独自調査の続き（同種パターンの3件目）。とどくメーター（前屈チェック）カードは現状
「いまの記録: 段位N」のみの表示だが、Web版（app-record.js:245-264 `renderReach()`が正本）には
以下が追加である:

## Web版の仕様

```
- 自己ベスト表示: 全記録中の最高段位を「自己ベスト: ◯◯」（teal色）で併記
- 前回比コメント（2回目以降の記録がある場合のみ・数字プレッシャーをかけない設計＝意図的に「◯段」の
  絶対値でなく前回との相対比較のみ表示）:
    上がった場合: 「前回（◯◯）より◯段とどくようになった！🎉」（pink色強調）
    同じ場合:     「前回とおなじ「◯◯」 キープも立派です！」
    下がった場合: 「体は日によってちがうもの またコツコツいきましょう🌱」
- トレンドグラフ（reachTrend）: 直近14回分の記録を棒グラフで表示（`.rbar`。各バーの高さ=段位×20%）
```

## やること

- マイ記録タブのとどくメーターカード（`reachCard`）に以下を追加:
  1. 自己ベスト表示（`arr.map(r=>r.lv)`の最大値）
  2. 前回比コメント（上記3パターンの文言分岐。数字そのものでなく「段」表現・励まし口調を維持すること）
  3. 直近14回のトレンド棒グラフ（シンプルな棒グラフでよい。Web版と完全に同じ見た目である必要はないが、
     「直近の推移が一目でわかる」という機能自体は再現すること）
- 判定ロジック（段位の記録・比較）自体はStep3で移植済みのはずなので新規に持ち込まない。表示の追加のみ

## 検収基準

- [ ] 自己ベストが表示される（複数回記録して確認）
- [ ] 前回比コメントが3パターン正しく出し分けられる
- [ ] 直近14回のトレンドが視覚的に表示される
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- 段位の記録・判定ロジックは変更しない（表示の追加のみ）
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で簡潔に報告してください（スクショ1枚で可）。
