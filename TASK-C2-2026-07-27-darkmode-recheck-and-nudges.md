# タスク（C2/appdev向け）— ダークモード再確認＋rDoneNudge/rTourBtn実装

## 背景

一晩の完全性監査で追加した約20個の新要素（#homeのmemoRow/plateauNote・#quizの誘導文言/戻る
ボタン・#resultのrPT/rxList等・#historyの日タップ詳細・#reachの説明文/お手本写真・#guideの
6セクション等）は、ダークモードでの見た目確認をまだ行っていない。また、#resultのfollow-upとして
保留にした`rDoneNudge`/`rTourBtn`にも着手する。

## やること

### 1. ダークモード再確認
一晩で追加した要素を中心に、ダークモードで配色破綻・コントラスト不足・ライト色の残留が無いか
確認する。特に新規追加した画像（`assets/check/meter.jpg`・`assets/check/q1.jpg`等）が
ダーク背景で浮いて見えないか、テキストの可読性は十分か、を重点的に見ること。問題があれば直す。

### 2. rDoneNudge/rTourBtn（app-quiz.js:262-269, index.html:745-746が正本）
- `rDoneNudge`: はじめの1本ガイド中、結果画面を表示したまま動画を見に行って戻ってきたときに、
  ホームのcheerの代わりに結果画面内へ「やった？」の復帰案内を出す（index.html:3977-3990付近の
  `checkDoneNudge`相当のロジックが、結果画面表示中かどうかで出し分けを行っている）
- `rTourBtn`: 「📖 つづき：使い方ツアーへ」ボタン。オンボ→クイズ経由で結果画面に来た場合のみ
  （ガイド中は非表示・設計指定どおり）表示し、タップで使い方ツアーへ
- 既存のHomeLogic.fdActive等のチュートリアルv2フラグ機構と重複しないよう、呼び出すだけで
  判定ロジックを再実装しないこと

## 検収基準

- [ ] 一晩の新規追加要素がダークモードで問題なく表示される
- [ ] rDoneNudge/rTourBtnが正しい条件下で表示され、機能する
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## 報告

完了時、ドア配達で簡潔に報告してください。
