# タスク（C2/appdev向け）— 再生リストが別物＋全角スペース検索が必ず0件（どちらも実害【大】）

## 背景

本人指示「全画面のUI/UXをWeb版と遜色ないように整える」を受けた夜間監査（Fable 4体）の第1弾。
一覧系3画面（動画を探す／再生リスト／カード図鑑）の監査から、実害【大】が2件出ました。
alan5が両方ともコードで裏を取っています。

---

## 1.【大】「再生リスト」がWeb版と別物 — 連続再生プレイリスト16本が丸ごと無い

### 現状

- **Web版**: 手動キュレーションした**16本のYouTubeプレイリスト**へのリンク集（index.html:3498-3521 `PLAYLISTS`）。
  3グループ構成:
  - `連続再生（流しっぱなしでOK・テレビにも）` — 朝30分／夜30分／座ったまま20分／全身35分／股関節&開脚30分／腰痛解消30分 …
  - `姿勢の悩みから探す`
  - `シリーズで深掘り`
  各項目は `[プレイリストID, タイトル, 説明, サムネ画像パス]` の4要素。
- **ネイティブ**: `catalog.json` の454本を絞り込みなしで並べた**平坦な一覧**
  （`SearchScreen.kt:389-418` `CatalogListScreen` / `SearchView.swift:317-358` `CatalogListView`）。
  **連続再生プレイリストへのリンクが1本もありません。**

### これは「判断待ち」のまま宙吊りになっていた案件です

`SearchScreen.kt:64-68` に、こう書かれたまま止まっています:

```
// 実際にはcatalog.jsonでなくindex.html内の別配列PLAYLISTS(手動キュレーションのYouTubeプレイリストID)
// ...
// 実装した(検索画面の絞り込みUIを持たない単純版)。PLAYLISTS配列の移植(要:抽出スクリプト新設)が
// 別途必要な場合はalan5への報告で選択を仰ぐ
```

**alan5として判断します: 移植してください。** 理由は3つです。

1. `連続再生（流しっぱなしでOK・テレビにも）` という**グループ名そのものが想定層の主用途**です。
   このアプリのユーザーは50〜60代以上で、動画を1本ずつ選ぶより「つけっぱなしにして体を動かす」
   使い方が中心。テレビで見る人もいます。454本の一覧はその代わりになりません
2. タブバーに**「再生リスト」という名前の入口が既にある**のに、中身が別物です。
   名前が約束していることと実物が食い違っています
3. 本人指示が「Web版と遜色ないように」なので、ここは埋めるべきところです

### やること

- `index.html:3498-3521` の `PLAYLISTS` を抽出してネイティブへ移植する
  （抽出スクリプトが要るなら `scripts/` に新設してよい。**Web版のindex.htmlは変更しないこと**）
- 3グループの見出し・並び順・各項目のタイトル/説明を**Web版のまま**にする
- サムネ画像（`assets/pl-*.jpg`）もバンドルに含めて表示する
- タップで `https://www.youtube.com/playlist?list={ID}` を開く
- Web版の末尾ヒント文「タップするとYouTubeで開きます！…📺」（index.html:941）は
  **既に移植済み**なので、そのまま活かすこと

### 判断を委ねる部分

現行の「454本の平坦な一覧」を**残すか消すか**は任せます。
Web版に無いものなので消してもパリティ上は問題ありませんが、
「全部の動画を見たい」という需要に応えている面もあります。
残すなら、プレイリストを**上**に置いて主役にし、平坦な一覧は下に従える形にしてください。

---

## 2.【大】全角スペースで区切った複数語検索が、両OSで必ず0件になる

### 何が起きるか

「肩こり　朝」のように**全角スペース**で2語入れると、該当動画があっても
**「ごめんなさい まだなかったみたい💦」と嘘の0件**が返ります。

日本語キーボードは**iOS/Androidとも既定でスペースキーが全角スペース**を入れます。
つまり想定層が普通に2語検索すると、高確率でこれを踏みます。

### 原因（監査で実測確認済み）

| | 分割方法 | 全角スペース(U+3000)を区切りとして扱うか |
|---|---|---|
| Web版 `app-search.js:48` | `q.split(/\s+/)` | **扱う**（JSの`\s`はU+3000を含む。Node実測で `'肩こり　朝'.split(/\s+/)` → `['肩こり','朝']` を確認） |
| Android `SearchScreen.kt:107` | `split(Regex("\\s+"))` | **扱わない**（Kotlin Regexが包むjava.util.regex.Patternの`\s`は`[ \t\n\x0B\f\r]`のASCIIのみ。`UNICODE_CHARACTER_CLASS`未指定） |
| iOS `SearchView.swift:46` | `q.lowercased().split(separator: " ")` | **扱わない**（Swift実測: `"肩こり　朝"` → parts=1、`allSatisfy=false`） |

半角スペースなら両OSとも正しく動きます。**全角のときだけ壊れます。**

### やること

- Android: `split(Regex("\\s+"))` → `\s` にUnicode空白を含める
  （`Regex("[\\s\\p{Z}]+")` あるいは `Pattern.UNICODE_CHARACTER_CLASS` 相当の指定）
- iOS: `split(separator: " ")` → Unicode空白全般で分割する形へ
  （`split(whereSeparator: { $0.isWhitespace })` など。Swiftの`isWhitespace`はU+3000を含みます）
- **この検索ロジックは「動画を探す」だけでなく「じまんカードのすきな1本さがし」でも使われます**
  （`BragScreen.kt` が `searchCatalog` を呼んでいる）。両方で直ることを確認してください

### 検収基準の追加

- [ ] 「肩こり（全角スペース）朝」で検索して結果が出る（両OS・両画面）
- [ ] 半角スペース区切りが従来どおり動く（退行させない）
- [ ] タブ・改行など他の空白でも破綻しない

---

## 全体の検収基準

- [ ] 再生リストに3グループ16本のプレイリストが出て、タップでYouTubeが開く（両OS）
- [ ] プレイリストのサムネ画像が表示される
- [ ] 全角スペース区切りの検索が動く（動画を探す／じまんカード の両方・両OS）
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test` 443緑・**Web版配信ファイル無変更**（index.htmlのPLAYLISTSは読むだけ）

## やらないこと

- Web版（PWA）側の配信ファイルは一切変更しない
- 検索の**判定ロジック本体**（タグAND年ANDキーワードの構造）は変えない。空白の扱いだけ直す

## 優先度

**通知タスクの区切りがついたら、これを最優先で。** どちらも実害【大】です。
とくに2は「普通に使うと検索が壊れる」ので、1より先に直してもらって構いません（1行で直ります）。

## 報告

完了時、ドア配達で報告してください。
- 1は再生リスト画面のスクショ
- 2は「全角スペースで2語検索して結果が出ている」スクショ

を添えてください。なお2の実機確認は `adb shell input text` では全角スペースを送れないので、
**エミュレータの日本語キーボードで実際に打つ**か、それが難しければ
ユニットテストで `"肩こり　朝"` を通す形でも構いません（その場合はテストコードを見せてください）。
