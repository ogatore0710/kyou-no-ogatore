# 発注: ビルド22（黄色ボタン復帰・案B仕様＋淡色選択肢の強化・本人裁定済み）

発注元: alan5（本人がビルド21実機で「やっぱり黄色がいい」→カード裁定=**案B: 黄＋濃い縁と文字**。＋IMG_8768「初回チャットの選択肢ボタン見にくい」。背景クリームは不変=本人の当初評価どおり・ボタン側で差を作る方針）

## Z-1: 主ボタンを黄色へ復帰（案B仕様・両テーマ）

- 地 `#FFD93B`（復帰）＋文字 `#26261F`（実測11.05:1 AAA・旧#3A3A35より一段濃く）＋**枠 `#8A6D00` 2pt新設**（背景に4.74:1・黄面に3.57:1）＋影 `#E8BE1E`（復帰）
- ダーク側も黄地＋`#26261F`文字＋`#8A6D00`枠で統一（黄面は暗背景に対して十分浮く・実測値を報告に）。ビルド21の藍`#073A71`/`#2278C0`トークンは主ボタンから撤去（トークン自体は削除してよい・全数grepで残存ゼロ確認）

## Z-2: ビルド21で藍化した他部位も案B仕様へ

- **セグメント選択ノブ（あなた用/あさ/よる等）**: 白ノブ＋枠 `#6B6857` 2pt（5.40:1）・文字 `#26261F`。よびな時も同じ（Y-1の右余白は維持）
- **セカンダリボタン**: ミント地 `#DFF5F2` 復帰＋文字 `#0F5A50`（7.11:1 AAA）＋枠 `#177065` 2.5pt（5.71:1）
- **ラインボタン（もどる/とじる）**: 文字・枠 `#4A473D`（8.95:1）
- **タブバー非選択の強化（`#57544A`・白抜き線画）とタブ選択の黄はビルド21のまま維持**（これは全案共通の改善）
- ツアー「つぎへ」等の進行ボタンも主ボタン扱い=黄（Z-1仕様）

## Z-3: 初回チャットの淡色カテゴリ選択肢の強化（IMG_8768）

肩こり・首／腰／前屈できない／眠り／とくにない等の淡色ボタン（ミント/淡黄/淡橙/淡ローズ/淡青）が背景クリームと同輝度で沈む。
- 各カテゴリの**濃色で縁2.5pt＋文字も同カテゴリ濃色**に濃化。基準: 縁vs背景3:1以上・文字vs地4.5:1以上（全カテゴリの採用hexと実測値の表を報告に）
- 地色のパステルは維持（可愛さは面が担当・視認性は縁と文字が担当）
- 同型の淡色チップ/選択肢が他画面（相談室チップ・時間帯選択等）にもあれば同基準で棚卸しして揃える（対象一覧を報告に）
- **背景 `#FFFAF3` は変えない**

## 検収・ビルド

- 実測コントラスト表（変更全部位・両テーマ）／実描画ビフォー/アフター（IMG_8768の画面・ホーム・ツアー・設定）／藍トークン残存ゼロのgrep棚卸し
- npm test・iOS build・Android unit test（JAVA_HOME=~/android-toolchain/jdk/Contents/Home）
- 文字サイズ（1.08/1.30）・Y系（図鑑ちら見せ・カレンダー削除等）はそのまま
- 全通過後ビルド22（21→22・既存グループ・公開メタデータ不可触・sw.js版数上げない・確認カードは推奨1で即進行）。whatsNewはalan5がゲート後に渡す

報告: `REPORT-C2-2026-08-04-build22-*.md`

## Z-4: ホーム動画カードの縦間隔を広げる（本人指示 2026-08-04 19:30・IMG_8769）

「きょうの1本」の動画カード3枚の縦間隔が狭すぎる。現行値から**1.5倍前後（目安+6〜8pt/dp）**へ広げる。カード内の密度は変えない。おおきめ文字（1.30）でも窮屈に見えないことを両サイズ実描画で確認。連続再生ボタンとの間隔も同じ比率で揃える。

## Z-5: ライト背景を一段深いクリームへ（本人カード裁定 2026-08-04）

- ライトの背景トークン `#FFFAF3` → **`#F7EEDC`**（白カードを浮かせて立体感を出す。温かさ維持）
- 玉突きの確認: セグメントトラック`#F2EADB`等、新背景と近接する面色は差が消えないか実測し、必要なら半段調整（採用値を報告に）。スクリム・タブバー地・オンボ背景など背景トークン参照箇所の全数棚卸し
- 文字・縁の対背景コントラストは背景が深くなる分ほぼ全て改善方向だが、主要ペア（墨文字/金茶縁/カテゴリ縁）は再実測して表を更新
- ダークは不変。Web版不可触

## Z-6: ダークモードの磨き込み・A案（本人カード裁定 2026-08-04）

今の温かい焦げ茶の世界観を維持したまま、面の分離と枠の視認性を上げる。
- 背景をわずかに深く（目安 `#211E19` → `#1E1B16` 前後）・カード面を半段浮かせる（目安 `#2C2822` → `#312C24` 前後）。**背景⇔カードの輝度差が現行より明確になること**（実測値を報告に）
- **枠線トークンの底上げ（既知問題1.52〜1.72:1の根治）**: ラインボタン枠・入力欄枠・チップ枠など枠系すべて対背景**5:1以上**へ（build21報告8節の棚卸し対象を流用）
- 文字系（`#F2EDE1`等）は現行維持。黄ボタン（Z-1ダーク仕様）はそのまま
- 色相は焦げ茶ファミリーから出ない（藍化・黒化はしない）
- 最終値は上記目安から±の実測調整可・採用hexと全ペア実測値の表を報告に

## Z-7: ホーム「続けた日数」セクションの再設計・案1「数字が主役」（本人カード裁定 2026-08-04）

対象はホームのこのセクションのみ（マイ記録・カード画像内の表記は不変）。
- 見出し `続けた日数（通算）` → **`つづけた日数`**（「通算」の言葉は本文からも全廃）
- **大きな「N日」を中央に主役配置**（数字は記録カードと同じBanananum流儀・ピンク系）。`通算 1 日` の行は廃止
- 完了時はその下に小さく1行 `きょうの分は完了！おつかれさま`（中央・折り返しなし。旧pillの「〜でした」折り返し問題の解消）。未完了時は従来どおり「きょうやった！」ボタン（Z-1黄仕様）を数字の下に
- **メモ行の合体**: 入力欄＋黄ミニボタン`のこす`を1行に（「メモをのこす」の全幅ボタンは廃止）。プレースホルダ`ひとことメモをどうぞ`の色を濃く（対地3:1目安・「枠の中の字が見えない」の解消）。入力文字は墨
- 記録カードボタン: 文言 `記録カードをつくる` へ（セカンダリ仕様）。下の説明行`カード画像を保存かシェアでのこしてね`は削除（モーダル内の案内で足りる）
- 検収: 完了/未完了の両状態×両テーマ×文字サイズ両方の実描画

## Z-8: ホーム相談室カードのチップ削除（本人指示 2026-08-04 19:48・IMG_8771）

ホームの相談室カードから「タップでそのまま聞けるよ」の行とチップ4つ（肩こり・首こり/腰痛/前屈できない/時間がない・続かない）を削除。カードは**見出し＋一言＋「相談する」ボタンだけ**に（引き算）。
- **相談室モーダル内のチップ行は不変**（あちらは入力の入口として機能維持）
- チップ経由の直接質問起動コードは、他から参照が無ければ削除してよい（参照棚卸しを報告に）
- 両OS・実描画ビフォー/アフター
