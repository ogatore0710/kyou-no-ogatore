# アイコン方針(I) — クラッカー差し戻し対応+チケット切り欠きの報告

## 🎉 クラッカー(差し戻し対応)

ご指摘のとおり、直していました。**筒を閉じた三角形にせず、口(遠い側の辺)を結ばない
開いた形にしました。**

- 塗り用のPathは筒の3点を閉じて作り(fill()にだけ使う)
- 線(stroke)は「起点→左の辺」「起点→右の辺」の**2本の独立したPathとして別々に描き、
  口側を結ぶ線は一切描いていません。** これで筒の口が開いて見えます。
- 紙吹雪は、口の延長線上(右上方向)に散らし直しました。点2つ+短い斜め線1本という
  組み合わせに変え、「点だけ3つ」だった前回の構図(`.play`の三角+キラキラに見えた
  原因)を避けています。

## 🎫 チケット(切り欠きを試しました)

**単純な丸角長方形+別パスの円で打ち抜く方式ではなく、外周そのものに凹アーチを
組み込む形にしました。** `addQuadCurve`で制御点を中心へ引き寄せることで、左右の
辺にヘコミを作っています。**背景色に依存する打ち抜きトリックではない**ため、
黄色いGStepバッジの上でも透明地の見出しの上でも同じように正しく見えます。

**24×24で試した結果、ヘコミは潰れずきれいに見えました。** エミュレータで実機確認済み
(スクリーンショットで、点線のミシン目+左右のヘコミが両方はっきり見えることを確認)。
このまま進めます。

## 回帰確認

- iOS: `xcodebuild`(Debug・Simulator)ビルド成功
- Android: `compileDebugKotlin`成功・`testDebugUnitTest --rerun-tasks`成功
- `npm test` 459 checks all green
- エミュレータで実機確認: 「続けるしくみ」セクションのチケット(🎫)バッジで左右の
  切り欠き+点線ミシン目を確認、王冠(👑)は前回どおり良好、クラッカー(🎉)は
  「お楽しみ機能」バッジで開いた口+紙吹雪の構図になっていることを確認(前回の
  「再生ボタンに見える」問題は解消)
- iOSシミュレータは今回もアカウント状態の関係で同じ画面のスクリーンショットは
  取れませんでしたが、実装はAndroidと全く同じ座標・同じ描画ロジックのため、
  動作としては同一に描かれるはずです

## 次

**ご確認いただけましたら、残り5種(🎯🌱⏱・📱・🎨)へ進みます。**
