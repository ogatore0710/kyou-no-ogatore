# タスク（C2/appdev向け）— 相談室チップの「まだ右にあるよ」が消えている＋吹き出しのポップイン未実装

## 背景

alan5が§A（`@keyframes`10種＋`transition:`6箇所）を独立に洗い直して見つけた欠落です。
**実機スクショで実害が確認できています。**

10種のうち9種（cpop/doneNudgePulse/fdBob/fdBreathe/fdPop/obpop/sddot/sdup）と、
transition 6箇所のうち4箇所（スプラッシュ=Web固有・vcard 3Dフリップ .55s・プランバー width .4s）は
正しく移植されていました。残りから3件出ています。

---

## 1. 相談室のチップが右端でぶつ切りになっている（両OS・実害あり・最優先）

**実機スクショで確認**: 相談室を開くと、チップ列が画面の右端で文字ごと切れています。

```
[からだの部位で] [脚・足まわりで] [状      ← 切れている
[肩こり・首こり] [腰痛] [前屈できな       ← 切れている
```

横にスクロールできることが**何も示されていない**ため、想定ユーザー（機器が苦手な50-60代以上）には
「切れた先のチップは存在しない」のと同じです。相談室の入口の選択肢が実質的に減っています。

Web版は同じ状況で2つの仕掛けを出しています（index.html:435, 472-474, 3190-3198 が正本）:

```css
/* 右端がグラデーションで薄くなる=「続きがある」の視覚的サイン */
.catrow.fade-r{mask-image:linear-gradient(to right,#000 88%,transparent)}
.sd-foot .chips.fade-r{mask-image:linear-gradient(to right,#000 86%,transparent)}
/* 右端に丸い「›」が出る */
.sd-swipehint{...opacity:0;transition:opacity .2s}
.sd-swipehint.show{opacity:1}
```
```js
function sdChipsFadeUpdate(){
  const hasMore = box.scrollWidth - box.scrollLeft - box.clientWidth > 8;
  box.classList.toggle("fade-r", hasMore);      // 右端フェード
  hint.classList.toggle("show", hasMore);        // 「›」ヒント
}
// スクロールイベントに紐づいていて、右端まで送ると両方消える
```

**やること**: 横スクロールするチップ列に「まだ右にある」ことを示す。
- 右端フェード（右端8〜14%をグラデーションで透明に）
- 右端に丸い「›」を重ねる（透明度 .2s でフェードイン/アウト）
- 判定は`hasMore`（残りスクロール量 > 8px）と同じ。右端まで送ったら両方消えること
- 対象は**相談室のチップ列**と、**検索画面のカテゴリ行（`.catrow` index.html:951 `#catRow`）**の両方

---

## 2. 相談室の「カテゴリ行」は折り返して全部見せる仕様（両OS）

上のスクショで切れている**1段目**（からだの部位で／脚・足まわりで／状況・シーンで）は、
Web版では横スクロールではなく**折り返して全部見える**指定になっています（index.html:471）:

```css
.sd-foot .sd-catrow{flex-wrap:wrap;overflow-x:visible;margin-bottom:6px}
```

`class="chips sd-catrow"`（index.html:1226）で`.chips`の横スクロールを上書きしています。
ネイティブは1段目も横スクロールになっていて、3つ目の「状況・シーンで」が読めません。

**やること**: 相談室の**カテゴリ行だけ**を折り返しレイアウト（`FlowRow` / `LazyVGrid`等）にして、
全項目が常に見える状態にする。2段目のチップ列は横スクロールのまま（そちらは項目数が多いため）。

---

## 3. 吹き出しのポップイン（`sdpop` .18s）が両OSとも未実装

```css
.sd-pop{animation:sdpop .18s ease-out}
@keyframes sdpop{from{opacity:0;transform:translateY(4px)}to{opacity:1;transform:none}}
```

Web版では吹き出しが**下から4pxふわっと上がりながら0.18秒で現れます**。使用箇所は3つ:
- index.html:3079 相談室の吹き出し（bot・user）
- index.html:3085 タイピングドットの行
- index.html:4149 **オンボーディングのチャット吹き出し**

ネイティブは`SoudanSheet.kt`/`SoudanSheetView.swift`ともタイピングドットの明滅とプランバーの
アニメーションはありますが、**吹き出しの出現そのものは無演出**（パッと出る）です。
相談室は段階表示で1件ずつ現れる場所なので、ここの有無は目に見えます。

### ⚠️ §Dとの関係（重要）
§Dの対象表で `index.html:497` を「相談室シートのスライドイン」と書いていましたが、497行は
`.sd-pop,.sd-sheet{animation:none}` の**2つ**を指しています。`.sd-sheet`=シート、
`.sd-pop`=**この吹き出しポップイン**です。`.sd-pop`側は元のアニメーション自体が未実装だったため、
**§Dの「8箇所すべて対応済み」は`.sd-pop`分がまだ埋まっていません**。

**やること**: 吹き出し出現に opacity 0→1 ＋ 下から4px の 0.18秒 ease-out を付ける。
上記3箇所すべて（オンボのチャットも忘れずに）。**同時にreduced-motion時は無演出**にすること
（これで497行の対応が完結します）。

---

---

## 4.【追記 2026-07-28】ガイド画面の目次チップが7個中3個しか見えていない（両OS・実害あり）

**実機スクショで確認**（font_scale=1.0・bigtext=既定ON＝**もっとも標準的な状態**）。

Web版の目次は**折り返して7個すべてが見えます**（index.html:178）:
```css
.gtoc{display:flex;flex-wrap:wrap;gap:8px;justify-content:center;margin-bottom:14px}
```
```html
<!-- index.html:980-989 -->
🆘 困ったときは / 🌅 はじめての日 / ▶ まいにちの流れ / 🛡 記録を守る
/ 🎫 つづくしくみ / 📅 マイ記録 / ❓ よくあるしつもん
```

ネイティブは`GuideScreen.kt:179`が`Row(Modifier.fillMaxWidth()...testTag("gtoc"))`＝**横一列**のため、
画面には「🆘 困ったときは」「🌅 はじめての日」と3個目の切れ端しか出ていません。
**隠れているのは「🛡 記録を守る」「🎫 つづくしくみ」「📅 マイ記録」「❓ よくあるしつもん」の4つ**で、
とくに**「❓ よくあるしつもん」は困ったときの入口**です。ここが見えないのは重いです。

コメントに「index.html:980-989の1:1移植」と書かれていますが、**`flex-wrap:wrap`が落ちています**。

**やること**: `Row`を`FlowRow`（iOSは折り返しレイアウト）に変えて、7個すべてが常に見えるようにする。
`justify-content:center`相当の中央揃えも合わせること。

### 4-2. ガイド画面ヘッダのボタン2つもラベルが割れている
同じスクショで、上部の「📖 使い方ツアー」のラベルが**「使い方ツ／アー」と2行に割れています**。
Web版（index.html:970）は`display:flex;justify-content:center;flex-wrap:wrap;gap:10px`で、
幅が足りなければ**ボタンごと下の行に落ちる**（ラベルは割れない）作りです。
こちらも折り返しレイアウトにして、ラベルが途中で改行されないようにしてください。

### 4-3. 検収時の注意
alan5が§Dの残件Aを検収しようとしたところ、**隠れている4つのアンカーはタップすらできない**ため
検証不能でした。この4を直すとAの残り4アンカーも検証できるようになります。

---

## 検収基準

- [ ] ガイドの目次チップ7個が折り返して**すべて画面に見える**（両OS・font_scale 1.0＋bigtext ON）
- [ ] ガイドヘッダの「はじめてガイド」「使い方ツアー」のラベルが途中で改行されない
- [ ] 相談室のチップ列の右端にフェードと「›」が出て、右端まで送ると消える（両OS）
- [ ] 検索画面のカテゴリ行にも同じフェードが効く
- [ ] 相談室のカテゴリ行（1段目）が折り返して3項目とも見える
- [ ] 相談室の吹き出し・タイピング行・オンボのチャットが 0.18秒でふわっと出る
- [ ] reduced-motion時は吹き出しが無演出で即表示（`.sd-pop`分の§D対応完了）
- [ ] **実機スクショで、チップの右端が「切れている」ではなく「続きがある」と見えることを示す**
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test` 443緑・Web版配信ファイル無変更

## やらないこと

- チップの中身・並び順・判定ロジックは変更しない
- Web版（PWA）側の配信ファイルは一切変更しない

## 優先度

`brag-card-thumbnail`と並べて、どちらを先にしても構いません。
ただし**この1と2は「選択肢が見えない」という実害**なので、`local-notifications`より確実に前です。

## 報告

完了時、ドア配達で報告してください。相談室のチップ列のスクショ（対応前後）を添えてください。
