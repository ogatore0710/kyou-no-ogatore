# TestFlight実機フィードバック（本人・ビルド2）続編 — C1・C2

alan5です。**本人が実機で続きを触って2件。うちC2は最優先です。**
アプリの中心機能である「動画を探す」が、**454本あるのに1本しか出ていません。**

**B1・B4・B6・B2より先にC2をやってください。**

---

## C1 🟠 画面のいちばん下に黒い線（帯）が出る

**本人の言葉:**「1番下に黒い線出てる」

写真2枚とも、**タブバーの下（ホームインジケータの領域）が真っ黒な帯**になっています。
タブバー自体の背景色（ダークで `#211E19`）とは別の、もっと黒い色です。

### 原因（ほぼ特定）

`KyonoTabBar.swift:43-46`

```swift
.background {
    ZStack {
        Rectangle().fill(.regularMaterial)
        Rectangle().fill(dark ? Color(hex: 0x211E19) : Color.white).opacity(0.97)
    }
}
```

**このファイルに `ignoresSafeArea` が1つもありません。**
背景が下の安全領域まで伸びず、**ホームインジケータのぶんだけ後ろが透けている**状態です。

**G7（タブバーの半透明・ぼかし・上部境界線）で背景を差し替えたときに入った副作用**だと見ています。
それ以前は不透明な単色だったので、目立たなかったのだと思います。

### やること
タブバーの背景を**下の安全領域まで伸ばす**こと。ボタンや文字は安全領域の内側のまま、
**背景だけ**が下端まで届く形にしてください（`.ignoresSafeArea(edges: .bottom)` を
背景に限定して当てる）。

**Androidにも同じ穴が無いか確認すること。**（ジェスチャーナビゲーション領域）

---

## C2 🔴🔴 動画が1本しか表示されない — 最優先

**本人の言葉:**「動画リストひとつしか表示されない」

写真2枚目、「動画を探す」で**「454本」と出ているのに、カードは1枚だけ**。
その下は空白で、**「もっと見る」ボタンも出ていません。**

### 僕が確認した範囲（ここまでは事実です）

**検索ロジックは正常です。切り分け済み。**

- `SearchView.swift:268` の「454本」は **`hits.count` をそのまま表示**している
  → **`hits` には454件ちゃんと入っている。絞り込みは壊れていない**
- `searchLimit` の初期値は **24**（`SearchView.swift:145`）。Web版
  （`app-search.js:4 let searchLimit=24`）と**一致**している
  → **上限値の設定ミスではない**
- `SearchView.swift:272` に `if hits.count > searchLimit { KyonoGhostButton("もっと見る") }`
  があり、454 > 24 なので**本来は出るはず。それも出ていない**

**つまり壊れているのは検索でも上限でもなく、`LazyVStack` が最初の1つしか実体化していない
という描画の問題です。** 行も、その後ろのボタンも、まとめて出ていない。

### 僕が疑っている先（断定していません）

**今日のG6/GO-9で、この画面に `.kyonoScreenPadding()` を当てました。**
`SearchView.swift:283` のコメントに「この画面だけ全辺16ptだった欠落を、共通の
`kyonoScreenPadding()` へ統一する」と残っています。

`kyonoScreenPadding()` は `Theme.swift:95-97` で **`padding(.bottom, 180 * zoom)`** を含みます。
Web版の `body{padding:20px 18px 180px}` の移植ですが、**Webのbodyの下パディングと、
SwiftUIでScrollViewを含むVStackに当てる下パディングは、意味が違います。**
**いちばん怪しいのはここです。**

### 調べかた

1. **まずビルド1（`4da19e0c`のコミット時点）で再現するか確認してください。**
   出ていなければ**今日のどれかが原因**と確定します。**推測より先に、いつ壊れたかを確定させること**
2. `.kyonoScreenPadding()` をこの画面から外して再現するか見る
3. `LazyVStack` を素の `VStack` に替えて再現するか見る（原因の切り分け用。
   **これを最終的な直しにしないこと**。454件を一度に組み立てると重くなります）

### 守ってほしいこと

- **`searchLimit` の値をいじって「たくさん出た」で終わらせないこと。**
  1件しか出ていないのは24が悪いのではありません。**症状を隠すだけの直しは差し戻します**
- **Androidでも同じことが起きていないか必ず確認すること。**
  `SearchView.swift:449-458` の**再生リスト側にも同じ `ScrollView` + `LazyVStack` の形**が
  あります。**そこも巻き込まれていないか見てください**

### 合格条件

**「動画を探す」で24本並び、いちばん下に「もっと見る」が出て、押すと増えること。**
エミュレータでスクロールした状態も含めて撮って見せてください。

---

## 順番

**C2 → C1 → （B1・B4・B6・B2の続き）**

C2は中心機能が使えない状態なので、他を止めてでも先にやってください。

## 補足

**本人はビルド2を触っています。** C1・C2ともビルド2に入っている不具合なので、
**B系と一緒に直してビルド3で出します。** 焦って単独ビルドしなくて構いません。

## 完了報告

`REPORT-C2-2026-07-29-testflight-feedback-c.md` にpush＋ドア配達。
**C2は「いつ壊れたか」を必ず書いてください。** 直し方より、そこがいちばん大事です。
