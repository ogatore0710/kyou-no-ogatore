# 設計書: 完了の瞬間（労い→カード）の再設計

発注元: alan5（本人フィードバック2026-07-30、Fable監査済み、本人GO済み「すべてやりましょう」）
対象: iOS・Android両方

## この設計が7/22の決定と矛盾しないことの宣言

2026-07-22、収集ループを記録カード1本に一本化する方針が確定している（かたさモンスターガチャ企画を「記録カードと役割が被る」という理由で却下・`2026-07-22-アプリセッションからの返答-かたさモンスターガチャ案.md`）。**この設計は新しい収集要素・新しい演出語彙を追加しない。既存の労い・紙吹雪・カードの3要素の「順番」だけを直す。** 一本化方針への抵触なし。

## 背景（本人の生の発言・要約せず）

「今日の記録をしたときに、記録カードが出ると思うんだけど、そのカードを出る時が、画面が、こう、わくわくしない感じがします。それがもったいないなと思います。通常のカードだったらそんなに気にならないけど、レアだったり、季節、記念日だったりは、今の演出もちょっと若干微妙だと思っていて、そのひらひらとね、お祝いみたいな感じの上から紙吹雪みたいな感じで降りてくるのも若干微妙で、そこの画面自体もう少し楽しい、やって良かったみたいな感じになれるといい。動画を使うときは毎日使いやすいようなシンプルだけど分かりやすくて生活になじむ感覚が良くて、ただストレッチをやった後はそれを労ってくれて偉いねって言ってくれて、明日も頑張ろうって思えるようなわくわく感も欲しい。」

## 見つかった核心（Fable監査で判明・裏取り済み）

「きょうやった！」タップの**同一フレーム内**で、(a)労いメッセージ`cheerText`を`withAnimation`でセット→(b)紙吹雪(confetti)発火→(c)`cardResult`セットでカードモーダルが**無遷移で即座に全画面に被さる**、が全部同時に走る。

- 労いセット: `HomeView.swift:448`
- confetti発火: `HomeView.swift:451-454`
- カードモーダル: `HomeView.swift:463`（`cardResult = renderTodayCard(...)`）→ `KyonoComponents.swift:528-556`の`KyonoCardModalOverlay`
- 労いメッセージの表示位置: `HomeView.swift:533-536`（ホーム画面内）。**カードモーダルの下敷きになる位置**。

死んだコードの確認: `KyonoComponents.swift:553`に`.transition(.opacity)`があるが、`:463`の`cardResult`代入が`withAnimation`で包まれていないため発火しない（SwiftUIの`.transition`は状態変化が`withAnimation`/`.animation(value:)`で囲まれて初めて動く）。

**つまり「労ってくれて偉いね」は実装済み。祝い・労い・カードが同じ瞬間に潰れて、味わう「間」が存在しないために埋もれている。** 演出を足すのではなく、順番を与えることが主要な直し方。

## 禁止事項（Fable監査より・厳守）

- **入場演出を「開封・めくり・タメ」の文法にしないこと。** フリップ演出・ドラムロール・スロット的なサスペンスを1つでも入れると、「レアは運ではなく続ければ順番にまわってくる」というFAQの約束（`GuideData.swift:79`）と体験が矛盾し、「運っぽく」見えてしまう。
- tier（記念日/季節/レア/ノーマル）別の強弱は「大当たりの音圧差」ではなく「季節は季節の色、記念日は記念の色」程度の**性格の違い**に留める。
- **本人が語っている軸（記念日・季節・レア＝tier）を、別の軸（初出/再会など）に黙って差し替えない。** tier軸のまま設計する。
- 新しい演出語彙を増やさない。既存の「弾むポップイン」「ふわっと表示」「紙吹雪」「ボタンパルス」の範囲で組む。

## 設計の骨子

1. **一拍を作る**: タップ→労い+confettiが主役の間（目安0.6〜0.9秒。**数値は実機で計測して決める**。既定「演出は実機で時間を測る」に従う）→そのあとカードが入場する。同時発火をやめ、`cardResult`のセットを労い表示の完了後（またはconfettiのピーク後）へ遅延させる。
2. **死んだtransitionを直す**: `cardResult`のセットを`withAnimation`で包み、`KyonoComponents.swift:553`の`.transition(.opacity)`を正しく発火させる。カードは「フェードして受け取る」入場になる（開封演出ではなく、贈り物を静かに手渡される文法）。
3. **tier別の性格差**: 特別tier（記念日・季節・レア）の入場には、既存の節目ポップインカーブ（`HomeView.swift:505-510`の`.timingCurve(0.34, 1.56, 0.64, 1, duration: 0.5)`）を流用してよい。ノーマルは現行のフェードのみで十分（本人も「通常のカードはそんなに気にならない」と言っている）。
4. **紙吹雪の質**: 種類は増やさず1種類のまま、タブバートーンに合わせて粒のデザインを作り直す（**この作業は本設計とは別発注とし、C1の実機検収と同時に本人へ見せて判断を仰ぐ**。まず順番を直すだけで「微妙」の印象が消える可能性もあるため、質の作り直しは1段待つ＝引き算的に進める）。

## 実装への注文

- タイミング値（遅延・duration）は実機/シミュレータで実際に触りながら決めること。数値をそのまま決め打ちで実装しない。
- reduceMotion時は現行どおり無演出・即時表示を維持。
- 実装後、**この画面のスクリーン録画または動画を撮って報告に添付すること**（「絵で確かめる」の作法どおり。文字列だけの報告は検収不可）。
- ノーマルカードの入場（死んだtransitionの修正のみ）と、特別tierの入場（ポップインカーブ追加）を分けて、まず前者だけ実装→報告→OK後に後者、の順で進めてよい。

## 検収基準

- [ ] 労いメッセージが、カードに覆われる前に本人の目に触れる間がある（実機動画で確認）
- [ ] カードの入場に「開封・めくり・タメ」に相当する演出が入っていない
- [ ] tierごとの入場差が「性格の違い」の範囲に収まっている（大当たり感が出ていない）
- [ ] reduceMotion時は無演出即時表示
- [ ] 新しい演出コンポーネント・新しいアニメーション語彙を追加していない（既存4種の組み替えのみ）
- [ ] 通算日数・連続日数などの数字を煽る表現を新たに足していない
