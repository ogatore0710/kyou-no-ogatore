# 発注: UX13案の一括実装（3波構成）＋ボタン押下感の診断

発注元: alan5（本人GO 2026-07-30 夜「1〜13やって」＝全案承認済み）
正本: 各案の詳細は `REPORT-C2-2026-07-30-ux-ideation.md`。この発注書は順序・本人注釈・検収条件だけを足す。

## 進め方

3波に分けて実装し、**各波の完了ごとに報告ファイルをcommit＋ドア配達**。次の波は報告後すぐ進んでよい（alan5の検収を待たなくてよいが、alan5から差し戻しが来たらそちらを先に処理）。挙動が変わる修正は実機/シミュレータの動画・スクショを添えること。各波で `npm test`＋両OSビルド（Androidは`--rerun-tasks`）。確認できなかった項目は「未確認」と明記（全艦共通の書式）。

## 第1波: 記録直後の体験（案2 → 案1 → 案3）

- **案2（最優先）**: markDoneの戻り値`MarkDoneOutcome`を配線し、労いの一言3種（おやすみ券・第N章・あした節目予告）を表示。**文言はWeb版 `app-record.js:72-151` から1:1で持ってくる**こと（新規に書かない。あれは承認済みコピー）。表示位置はcheerTextの上に1行、cpopと同じ入場でよい。
- **案1**: `showDoneNudge`を「きょう未記録のときだけ」の導出型に。`OnboardingViews.swift:796`側にも同名の別Stateがあるので、練習ガイド画面の同種矛盾が無いかも見て、あれば同時に直す。
- **案3**: 「きょうの1本」タップ時の動画IDを控えてmarkDone時に`recordDaylog`を呼ぶ。過去日ぶんは遡れなくてよい（配線した日以降だけで十分）。

報告: `REPORT-C2-2026-07-30-ux-batch-wave1.md`

## 第2波: 迷子を減らす（案4・5・6・7・9）

- **案4**: 設定の戻り先を「来た場所へ」（`.obu(returnTo:)`と同型）。
- **案5**: `SettingsView.swift`の`.primary`/`.secondary`残存をテーマ変数へ。**直す前に「じどうダーク」（アプリ内ダーク・システムライト）の実機/シミュレータで現状のスクショを撮る**こと（本当に黒文字になるかは未確認のまま発注している。ならなかった場合は置換だけして「症状は再現しなかった」と正直に書く）。
- **案6**: ガイドの「あさ/よる」記述と静止モックを現状（自動でおすすめが替わる）に合わせて文言修正。⚠️ **本人注釈あり**: 「Web版は朝、夜、おすすめが出ていた」——切替セグメント自体はWeb版に実在した機能で、ネイティブでは第2段送りになっているもの。**セグメント本体の前倒し移植はこの発注に含まない**（本人が望めば別発注する。ガイド文は移植が来たら戻す前提の暫定是正と心得て、消しすぎない・控えめに）。
- **案7**: 相談室の会話状態をルートへ持ち上げ、**アプリを開いている間はずっと**会話が残るように（Web版パリティ）。⚠️ **本人注釈あり**: 「記憶していて欲しいな」——**アプリを再起動しても残る永続化（store保存）に本人の関心がある可能性**。実装は今回セッション内保持までとし、**再起動をまたぐ永続化に何が要るか（保存形式・容量・古い会話の扱い）を報告書に見積もりとして書く**こと。実装はGO後。
- **案9**: おしらせ設定の並び替えと見出し是正（報告書の案どおり）。

報告: `REPORT-C2-2026-07-30-ux-batch-wave2.md`

## 第3波: 磨き残しの一掃（案8・10・11・12・13）

- **案8**: ボタン用途の残存絵文字をCanvasアイコンへ（相談室FABは既存`soudanBubble`流用）。対応アイコンが無く新規に描く必要が出たものは、勝手に描かずリストアップして報告（アイコン新規はalan5検分ゲートがあるため）。
- **案10**: にっき末尾の案内1行（報告書の文言どおり）。
- **案11**: じまんカード検索結果の入れ子スクロール解消（`SearchView.swift:343-346`の「さらに表示」型に統一）。
- **案12**: せんぱいの声の外殻ScrollView化（A5の横展開）。再生リストタブは触らない（既存の保留判断を尊重）。
- **案13**: 数字キーボードを閉じられるように（`.scrollDismissesKeyboard(.interactively)`か「完了」ボタン。Android側の同種問題の有無も確認して書く）。

報告: `REPORT-C2-2026-07-30-ux-batch-wave3.md`

## 並行の診断タスク（実装禁止・調査のみ）: ボタンの押下反応が「鋭すぎる」

本人の実機所感（2026-07-30 夜・生の言葉）: **「ボタンの反応が鋭すぎる気がする」**。

alan5がコードで確認済みの事実: 全ボタン部品が`DragGesture(minimumDistance: 0)`＋`@State pressed`方式（`KyonoComponents.swift:261,300,373,425`）。`onChanged`で即座に`pressed=true`、`onEnded`で**指の位置を確認せずに**`action()`を発火する。

診断してほしいこと（実機/シミュレータで）:
1. **スクロール中にボタンを撫でただけで沈むか**（`minimumDistance:0`はタッチダウン即発火のはず。ScrollViewに奪われたときpressed=trueのまま固まらないかも見る）
2. **ボタンに触れてから指を外へずらして離すと、アクションが発火してしまうか**（標準Buttonのキャンセル挙動が無い疑い）
3. **押下の視覚変化が無遷移（0ms）で切り替わっていないか**（Web版の`:active`はCSSトランジションの丸みがある。パチンと切り替わるのが「鋭い」の正体の可能性）
4. Web版の同じボタンと触り比べて、感触の差を言語化する

報告には、再現動画と、**直し方の選択肢を2〜3案**（例: `ButtonStyle`+`isPressed`への移行はキャンセル挙動が標準で付く／移動閾値の追加／押下状態に0.1s程度のease）を利害つきで書く。**修正の実装はGOが出るまでしない。**

報告: `REPORT-C2-2026-07-30-button-feel-diagnosis.md`（第1波と並行で最初にやってよい。本人の生の指摘なので優先度は高い）

## 規模感

3波合計で数日規模。1波ごとに区切って報告する前提で着手してよい（本人GO済みのため追加確認は不要）。
