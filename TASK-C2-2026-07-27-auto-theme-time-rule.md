# タスク（C2/appdev向け）— 自動テーマの時刻判定（19時〜朝5時）が欠落＋開いたままの時間追従が無い

## 背景

alan5の実挙動調査で発見。Web版の「じどう」テーマは**OSのダーク設定 or 19時以降 or 朝5時前**で
暗くなるが、ネイティブは**OSのダーク設定しか見ていない**。時刻による判定が丸ごと欠落している。

`theme`の既定値は両OSとも`"auto"`なので**大半のユーザーが該当**する。さらに、今日の
`settings-clipboard-import-and-hints`タスクで設定画面に追加した説明文
**「「じどう」は夜（19時〜朝5時）やスマホがダーク設定のとき暗くなります」が、
ネイティブでは事実と異なる案内になってしまっている**（SettingsScreen.kt:152 / SettingsView.swift:167）。

## Web版の仕様（app-env.js `applyTheme()` が正本）

```js
const p = store.get("theme","auto");
const h = new Date().getHours();
const osDark = matchMedia("(prefers-color-scheme: dark)").matches;
const dark = p==="dark" || (p==="auto" && (osDark || h>=19 || h<5));
```

現状のネイティブ（`Theme.kt:137 resolveKyonoColors` / `Theme.swift:69 resolveKyonoColors`）:
```kotlin
else -> systemDark   // ← 時刻判定なし
```

## やること

### 1. 自動テーマに時刻判定を追加（必須）
`resolveKyonoColors`の`auto`分岐を `systemDark || 現在時刻>=19時 || 現在時刻<5時` にする。
判定に使う現在時刻は端末のローカル時刻（Web版の`new Date().getHours()`と同じ）。

### 2. 開いたままの時間追従（Web版 index.html:4017 `setInterval(refreshDay,60000)` 相当）
Web版は**フォアグラウンドのまま60秒ごと**に`refreshDay()`を回して、時間経過に追従している。
ネイティブは復帰時（ON_RESUME / scenePhase）にしか追従しないため、アプリを開いたまま
19時をまたいでもテーマが切り替わらない（#1を直しても、開いたままでは切り替わらない）。

60秒間隔の追従を入れて、以下がWeb版と同じく開いたままでも更新されるようにする:
- 自動テーマの明暗（19時/5時の境界）
- 日付跨ぎ（`todayStr()`の3時境界）での表示更新——通算日数・「きょうやった！」の状態・
  きょうのひとこと・せんぱいの声の日替わり8選など
  ※記録そのものはタップ時に`Instant.now()`/`Date()`を取っているので**データは正しい**（確認済み）。
  ここで直すのは**表示のズレ**のみ

実装方式は任せます（Android: `LaunchedEffect`＋`delay(60_000)`のループ、
iOS: `Timer.publish`＋`onReceive` など）。フォアグラウンドのときだけ回ること。

## 検収基準

- [ ] テーマ「じどう」＋端末がライト設定の状態で、端末時刻を20時にするとアプリが暗くなる
- [ ] 同条件で端末時刻を12時にすると明るくなる
- [ ] アプリを開いたまま18:59→19:01に時刻をまたぐと、操作しなくてもテーマが切り替わる
- [ ] アプリを開いたまま日付（3時境界）をまたぐと、ホーム/マイ記録の表示が新しい日に更新される
- [ ] 設定画面の説明文どおりの挙動になっている（説明文自体は変更しないこと）
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- 設定画面の説明文（今日追加したもの）は変更しない。**説明文が正になるよう実装側を合わせる**
- 記録・判定ロジック（`todayStr`の3時境界、`markDone`等）は変更しない
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で報告してください。端末時刻を変えた前後のスクショ（明→暗）があると確実です。
