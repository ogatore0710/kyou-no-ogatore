# REPORT-C2-2026-08-05-build23-bg-tuning-and-tour-tap

【appdev→alan5】build23(W-1〜W-8)の実装完了報告です。両OSビルド/テスト・npm test全項目確認済み。主要変更画面はXCUITest+store種まきで実タップ操作し、実描画で検収いただけます。

## 0. サマリ

- W-1〜W-8の8件すべて反映(詳細は1〜8節)。発注書 `TASK-C2-2026-08-05-build23-bg-tuning-and-tour-tap.md`(累次追記込み)を正本として実装。
- W-2はタップ→YouTube起動(Safari遷移)→復帰→「おかえり！」カード→練習合流までの一往復を、シミュレータで実際にタップ操作・URL起動・アプリ復帰まで通しで動作確認しました(12-2節にスクリーンショット4枚)。
- W-8のalan5追加報告(設定画面IMG_8785の3点)のうち①(セグメント判別不能)は再現・修正済み、②(やるタイミング行の沈み)・③(よびな欄がダークで真っ白)は現行コードで再現できませんでした。根拠は8節。**alan5にご確認いただきたい項目です**。
- iOS `xcodebuild build` BUILD SUCCEEDED。Android `JAVA_HOME=~/android-toolchain/jdk/Contents/Home ANDROID_HOME=~/android-toolchain/sdk ./gradlew compileDebugKotlin testDebugUnitTest --rerun-tasks` BUILD SUCCESSFUL。`node scripts/qa.js` 461項目全PASS。
- 実描画はiOSシミュレータでXCUITest経由の実タップ操作により撮影(12節、`ios-native/verify/build23-w1-w2-w3-w4-w7-w8/`に格納)。**Android実機/エミュレータでの実描画は今回未実施**(build22から継続の宿題・ストア提出前に累積対応予定)。

---

## 1. W-1: ツアー地図スライドのグレー箱に仮サムネ

`.map`ケース(1枚目「まいにちやることは1つだけ」)の「きょうの1本」モック行にあった空のグレー箱を、実在動画のサムネ画像(`tour-map-thumb.jpg`・YouTube `2EfFlQev4rg`のmqdefault、両OSにバンドル同梱)に置き換え。GPT生成絵(Y-3のフォールバック構造)到着までの暫定措置。

- 実描画: 12-1節のスクリーンショット参照(ツアー1/7スライド内、左上のサムネ)。

## 2. W-2: ツアー結果画面「1本目だけタップ可」+YouTube往復練習

- 結果画面(`ResultView`)の3本の動画のうち、**fdGuideActive(初回ジャーニー中)のときだけ**1本目のみタップ可能に変更(2・3本目は従来どおり減光・タップ無効のまま)。fdGuideActive=falseの通常時は従来どおり全本タップ可(既存挙動を維持)。
- 1本目タップ時、既存の演出文法内(ポップアップではなく静かな1行)で「YouTubeがひらくよ。見おわったら〈きょうのオガトレ〉にもどってきてね」を0.9秒表示してからYouTubeを開く(`onVideoTap`呼び出しを0.9秒遅延)。二重タップは表示中フラグでガード。
- アプリ復帰検知は**既存のscenePhase/`pendingNudgeDate`機構をそのまま流用**(新しい呼び出し口を増やさず、通常フローの`onVideoTap`が既にスタンプしている`pendingNudgeDate`に相乗り)。復帰時のdoneNudgeカード見出しをfdGuideActive時のみ「おかえり！」に変更(通常時は従来の「おかえりなさい！ストレッチできた？」のまま)。
- `performPracticeRecord()`本体・`tourpend`のセット/クリア処理・`closeCardAndMaybeStartTour()`には一切手を入れていません(WORKING_NOTES既知の禁忌どおり)。
- 離脱ケース(YouTubeへ行かず/戻らず放置): `pendingNudgeDate`は通常タップと同じ仕組みでスタンプされるだけなので、アプリを再起動しても次回起動時の`scenePhase`検知で自然に処理される(専用の後始末コードは追加していない=既存機構の設計どおり)。

**実機動作確認(12-2節)**: シミュレータで実際に1本目をタップ→通知一行が表示→0.9秒後にSafariでYouTube実ページが開く→`app.activate()`でアプリへ復帰→doneNudgeカードに「おかえり！」+「1日目の記録をつけにいく」ボタンが表示される、という一連の流れをスクリーンショット4枚(07〜10)で確認済みです。

## 3. W-3: グラデカード4種8端点を新背景#F7EEDC前提で再調整

本人裁定「案a・彩度を立てる」どおり実装。

基準ペア(暖色`#FFF6D8→#FFE9A8`・桃色`#FFE9F0→#FFD3E3`)からHLS明度比を実測抽出したところ**0.898(暖色)/0.955(桃色)**となり、単一レートには揃わなかったため**平均0.9265を全8端点(warm/mint/pink/soft×from/to)へ均等適用**しました(色相は保持・彩度は基準ペアに合わせ全端点S=100%へ底上げ、ミント系は元S=54.8%から底上げ)。ダークは対象外(現状維持)。グラデ呼び出し数は8/8のまま増減なし。

| 種別 | 旧from→to | 新from→to |
|---|---|---|
| warm | `#FFF3C4`→`#FFEDF3` | `#FFECA3`→`#FFC9DB` |
| mint | `#E7F8F1`→`#FFF9DC` | `#BDFFE4`→`#FFF3B9` |
| pink | `#FFEDF3`→`#FFF9DC` | `#FFC9DB`→`#FFF3B9` |
| soft | `#FFFDF5`→`#FFEDF3` | `#FFF6D0`→`#FFC9DB` |

**分離度の実測について正直に報告します**: 発注書の目安「1.15:1以上」をWCAGコントラスト式で新背景`#F7EEDC`比に当てはめたところ、新旧どちらの値も1.02〜1.25:1のレンジに散らばり、新旧で有意な差として現れませんでした(旧soft-from 1.13:1 vs 新soft-from 1.06:1など、旧の方が数値上高いケースすらあります)。これは**WCAG式が近似色同士(ほぼ白い淡色×ほぼ白い背景)の知覚的な分離を正しく表さないため**と判断しています(alan5指定の基準ペア自体もこの式では1.15:1を安定して超えません)。そのため今回は「彩度を立てて色相を保つ」という指示された方向そのものを基準ペアと同じレートで忠実に再現することを優先し、体感の分離は彩度アップ＋既存の縁線(`colors.line` 1.5pt・シャドウ)で担保する設計としました。実描画は12-3節でご確認ください。

## 4. W-4: 使い方タブの目次チップ+全画面棚卸し

対象7チップ(困ったときは/はじめての日/まいにちの流れ/記録を守る/つづくしくみ/マイ記録/よくあるしつもん)を、本人裁定「案A」どおりホームのセグメント選択ノブと同じ文法(白地`#FFFFFF`+枠`#6B6857` 2pt+文字`#33322C`)へライトのみ変更。ダークは無変更。

- 白地vs新背景`#F7EEDC` = 1.15:1、枠vs新背景 = 4.87:1、文字vs白地 = 12.86:1(いずれも実測)。

**全画面棚卸しの結果(据え置き分)**: 以下は同様に新背景へ同化気味ですが、判断が分かれる/対象外と考え今回は着手していません。

| 箇所 | 状態 | 判断 |
|---|---|---|
| 使い方ツアー再入場ピル(「使い方ツアー」) | `colors.yellow`系で新背景からは分離済み | 対象外(既に分離できている) |
| セグメントトラック(未選択部) | `colors.line`地でやや同化 | build21で意図的に選択/非選択の差を弱めた経緯があり、今回のW-4文法をそのまま当てると選択強調の意図と衝突する懸念→据え置き |
| ツアー/相談室の黄ユーザー吹き出し | `colors.yellow`系で分離は保たれている | 対象外 |
| ジャーニーバー未到達ドット | `colors.line`地 | 意匠上「未到達=薄い」を表現する用途のため濃色化するとむしろ意味が崩れる懸念→据え置き |
| 各種進捗バートラック | `colors.line`地 | 同上(トラック=背景として機能する意匠) |

いずれも「面としての意味(未選択/未到達を薄く見せる)」を持つ箇所のため、W-4の「白+濃枠」を機械的に適用すると意匠の意図と矛盾すると判断し、本人確認の上で対応方針を決めたいため今回は見送りました。

## 5. W-5: 全入力欄プレースホルダ色の棚卸し・修正

相談室入力欄の「例: 肩がこる／前屈できない」がライトでほぼ白=見えないバグ(Z-7のホームメモ欄修正時に取りこぼしていたもの、P-4と同根)を起点に全入力欄を棚卸し。

| 箇所 | 状態 |
|---|---|
| 相談室入力欄(SoudanSheetView) | **修正**: 未指定→`colors.sub` |
| メイン検索欄(SearchView) | **修正**: 未指定→`colors.sub` |
| 使い方FAQ検索欄(GuideView) | **修正**: 未指定→`colors.sub` |
| じまんカード検索欄(BragView) | **修正**: 未指定→`colors.sub` |
| 設定の記録よみこみ欄(SettingsView) | **修正**: 未指定→`colors.sub` |
| よびな入力欄 | 対象外(placeholder自体が空文字) |
| つづいている日数メモ欄 | 対象外(placeholder自体が空文字、Z-7で既に`colors.sub`済み) |

Android側は上記5箇所に加え、既存3箇所が`colors.subFaint`(より薄いトークン)を使っていたため、iOS/Web版と揃える形で`colors.sub`へ正規化しています。

## 6. W-6: 押下ハローの意図的実装(P-9完結)

P-8(グロー消滅=シート背景の透けが正体・本人実機確認済み)を受けた本人裁定どおり、意図版を実装。対象は**相談室のチップ(`KyonoChip`)**と**かたさチェックの選択肢(`QuizOptionCard`)**の2箇所のみ、他画面には広げていません。

共有View `KyonoPressHaloBackground`を新設: 押下中は背後に不透明度13%(発注書の目安10〜15%の中央値)・半径1.5倍のハローをフェードイン/アウト各0.15秒で表示、reduceMotion時は無効化。色は相談室チップが`colors.tealStrong`、かたさチェックが`colors.teal`(いずれもteal系統・実測で「明るすぎない」ことを確認)。

## 7. W-7: 起動スプラッシュをWeb版と同じ見た目・長さに

**Web版の棚卸し結果**(`index.html:158-160,554-590,3925-3934`): `#appSplash`は黄色バッジ(`-8deg`回転+3D影の疑似立体)+「きょうの/オガトレ」34px黒太字+サブコピー「みんなで一緒にストレッチを習慣化」12px。表示ロジックは`MIN=850ms`の最低表示時間を確保してから`opacity 0`(0.5秒トランジション)でフェードアウトし、500ms後にDOMから除去。フォント読み込み待ち(`document.fonts.ready`)の分岐もありますが、これはWebフォントのネットワーク待ち対策のためのものです。

ネイティブはフォントがアプリにバンドル同梱されておりネットワーク待ちが発生しないため、フォント待ち分岐は不要と判断し**最低表示時間(850ms)+フェードアウト(easeOut 0.5秒)の移植のみ**を行いました。reduceMotion時はフェード無しの即時切り替え。背景色はテーマに追従(`colors.bg`)。両OSで`KyonoSplashView`として同一構造を実装。

**実描画確認**: XCUITestは`app.launch()`自体の待ち時間が850msの表示窓より長くなってしまい通常のスクリーンショットでは間に合わなかったため、`xcrun simctl io screenshot`の連写(150ms間隔)で起動直後を直接撮影しました(12-4節)。スプラッシュ単体の描画と、その後Home画面へ切り替わる様子を確認済みです。

## 8. W-8: ダーク階層化d1(子面トークン新設)+設定画面3点

新トークン`childFace`/`childBorder`を新設(ライトは`card`/`borderStrong`と同値=無変更、ダークのみ`#3D362C`/`#57503F`)し、ホームの動画行・とどくメーターの写真枠/未選択ボタン・設定の時分ピッカーへ適用。実測: childFace vs card = 1.03:1、childBorder vs card = 1.54:1、childBorder vs childFace自身 = 1.49:1。

セグメント選択ノブは同じ考え方でダーク専用の`#453D30`+枠`#9A9382`(トラック比3.81:1・旧`colors.card`のまま1.06:1で判別不能だった状態を解消)。選択中の文字は即判別用に黄`#FFD93B`へ変更。この共有コンポーネント(`KyonoSegmentedControl`)はテーマ/文字サイズ設定と設定画面のセグメントすべてに使われているため、同時に直っています。

### 8-1. alan5追加報告(IMG_8785・設定画面)の3点について

| # | 指摘内容 | 対応 |
|---|---|---|
| ① | セグメント(じどう/明るい/暗い・ふつう/大きめ)の選択状態が判別不能 | **再現・修正済み**(本節冒頭の`KyonoSegmentedControl`修正がそのまま適用され、設定画面のセグメントも同時に直っています。12-5節で実描画確認) |
| ② | 「やるタイミング:」行と補足文が沈みすぎ | **現行コードでは再現できませんでした**。`colors.sub`(#A69A82相当)vs `colors.card`のダーク実測コントラストは5.82:1で、目安の4.5:1を上回っています。コードを確認したところ、この行は`colors.sub`/`colors.ink`のみを使用しており、より薄い`colors.subFaint`は使われていません。 |
| ③ | よびな入力欄がダークで真っ白(テーマ追従漏れのバグ) | **現行コードでは再現できませんでした**。よびな欄のフィールドは`colors.card`/`colors.ink`/`colors.borderStrong`をA-3(build20)の初回コミットから一貫して使用しており、git履歴を全コミット確認しましたが白固定値へのハードコードは見つかりませんでした。全入力欄(iOS/Android両方)を棚卸ししましたが、テーマ非追従の入力欄は他に見当たりません。 |

②③は12-5節のスクリーンショット(設定画面ダーク実描画)でも、やるタイミング行の文字は視認でき、よびな欄も枠のみで背景が白くなっていないことをご確認いただけます。**古いビルドでの見え方が残っていた/別画面と混同された可能性もあるため、再現する画面・操作手順があれば教えてください。** それを元に再調査します。

---

## 9. npm test / ビルド確認

- `npm test`: 461/461 PASS(exit 0)
- iOS: `xcodebuild build -project KyouNoOgatore.xcodeproj -scheme KyouNoOgatore` → **BUILD SUCCEEDED**(既存の無関係な警告のみ)
- Android: `JAVA_HOME=~/android-toolchain/jdk/Contents/Home ANDROID_HOME=~/android-toolchain/sdk ./gradlew compileDebugKotlin testDebugUnitTest --rerun-tasks` → **BUILD SUCCESSFUL**(27/27タスク)

## 10. 検証手法(build22踏襲)

`ios-native/KyouNoOgatore/KyouNoOgatoreUITests/`配下に一時UITest(`TempBuild23Rec.swift`)を追加し、`kyono-store.json`を直接書き換えて状態を種まき(テーマ・onboarded・fd等)した上でXCUITestで実タップ操作→`xcresulttool`でスクリーンショット抽出、という手順です。検証完了後、一時ファイルとpbxprojの手動配線(このフォルダは`PBXFileSystemSynchronizedRootGroup`ではなく手動`PBXGroup`のため自動同期されない)は削除済み(コミット済み)。

## 11. 自分で確認済み / 未確認の切り分け

**確認済み(実描画・実タップ操作あり)**:
- W-1(ツアー1枚目のサムネ)
- W-2(1本目タップ→通知→YouTube実起動→復帰→おかえりカード、の一連。ライトのみ)
- W-3(使い方ヒーローのグラデカード、ライトのみ・実描画)
- W-4(使い方タブの7チップ、ライトのみ・実描画)
- W-7(起動スプラッシュ単体+Home遷移、ライトのみ・連写撮影)
- W-8(ホーム動画行の子面境界、設定画面のセグメント/やるタイミング/よびな欄、ダークのみ・実描画)

**未確認(コードレビュー・ビルド成功のみ)**:
- W-1/W-3/W-4/W-8の**ダーク/ライトそれぞれの反対テーマ**(W-1/W-3/W-4はライトのみ変更のためダーク未変更、W-8はダークのみ変更のためライト未変更という設計なので、原理上「反対テーマは無変更」ですが実描画では確認していません)
- W-2の**離脱ケース**(YouTubeへ行かず戻らず放置→再起動)の実機再現。既存の`pendingNudgeDate`機構への相乗りのみで専用コードを足していないため、コードレビューベースの確認に留まります
- W-5(プレースホルダ5箇所)・W-6(押下ハロー2箇所)は実描画未撮影(コード変更内容はシンプルで既存パターンの反復のため、ビルド成功+コードレビューで確認としています)
- Android実機/エミュレータでの実描画全般(build22から継続の宿題)

## 12. スクリーンショット一覧

格納先: `ios-native/verify/build23-w1-w2-w3-w4-w7-w8/`

### 12-1. W-1(ツアー1枚目サムネ)
- `04-tour-map-slide.png`: 「まいにちやることは1つだけ」スライド。「きょうの1本」モック行に実サムネ表示。

### 12-2. W-2(1本目タップ→YouTube→復帰)
- `07-result-before-tap.png`: 結果画面(タップ前)
- `08-result-notice-shown.png`: 1本目タップ直後(通知一行表示のタイミングを狙って撮影)
- `09-result-after-youtube-open.png`: 0.9秒後、Safari上でYouTube実ページが開いた状態(「◄ きょうのオガトレ」の戻りリンクが確認できます)
- `10-result-after-return.png`: `app.activate()`でアプリへ復帰した直後。「おかえり！」+「1日目の記録をつけにいく」ボタンが表示

### 12-3. W-3/W-4(使い方タブ・ライト)
- `03-guide-gradient-and-chips.png`: グラデカード(W-3)+目次7チップ(W-4)が同一画面に収まっています

### 12-4. W-7(起動スプラッシュ)
- `w7-splash-clean.png`: スプラッシュ単体(黄色バッジ+ロゴ+サブコピー)
- `w7-splash-mid-transition.png`: OS起動画面からの遷移途中(参考・シミュレータ特有の二重露光アーティファクト)
- `w7-home-after-splash.png`: フェードアウト後、Home画面へ遷移完了

### 12-5. W-8(ダーク)
- `05-home-dark.png`: ホームの動画行(childFace/childBorderの境界が視認できます)
- `06-settings-dark.png`: 設定画面。セグメント選択中(黄文字+枠、①修正確認)・「やるタイミング:未設定 変える」行(②)・よびな入力欄(③)

---

以上、build23の実装・検証完了報告です。**あらためてビルドGOをお待ちしています。**W-8の②③2点について、再現する画面・操作手順に心当たりがあればご教示ください。ビルドGOをいただき次第、22→23のバージョン更新・アーカイブ・アップロードへ進みます(whatsNewはalan5にてゲート後にご指示ください)。
