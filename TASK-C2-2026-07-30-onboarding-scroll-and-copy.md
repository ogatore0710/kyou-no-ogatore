# オンボーディング: スクロール競合修正・ボタン固定化・文言整理

発注元: alan5（本人フィードバック2026-07-30、Fable監査済み）
対象: iOS・Android両方
根拠: 本ファイル内の各項目に確認コマンド/ファイル:行番号を併記。書けない行は「未確認」と明記する運用（2026-07-30本人決定・全艦共通）。

## 背景（本人の生の発言・要約せず）

「初めての使い方のところは、文字が出るスピードとかは整ってきたけど、自動で今のやり取りが上に上がってこなくて、上がり切らない部分があって、ボタンタップするボタンが下の方に隠れてて、そこから上げてタップしなきゃいけなくなってたりとか、タップするボタンの色が被ってる色があったりして、どのボタンが何なのって分かりづらい。」
「最初は今日は練習だよ、動画やったらタップしたら戻ってきてねっていうところすごく文字が多くなっているから、そこもしっかり直してほしい。」

TestFlightビルド7が出たばかりで、この画面群は**新規ユーザーが最初の3分で必ず踏む**。設計判断はほぼ無く、迷いを減らすだけの修正。

## A1. オンボチャットの自動スクロール競合

**症状**: 新しい設問/選択肢が増えても、スクロールが上がりきらないことがある。

**原因（確認済み）**: 固定60ms delay後に1回だけ`scrollTo`する実装になっていて、バブル/選択肢のポップインアニメーション(180ms)と競合し、レイアウト確定前に着地することがある。
- iOS: `ios-native/KyouNoOgatore/KyouNoOgatore/OnboardingViews.swift:296-299`（60ms delay）／バブルのポップインは`:283`前後（0.18s easeOut）
- Android: `android-native/KyouNoOgatore/app/src/main/java/jp/ogatore/kyouno/OnboardingScreens.kt:240-242`（`delay(60)`→`animateScrollTo`）／バブルのポップインは`AnimatedVisibility(fadeIn+slideInVertically(tween(180)))`

**直し方**: このアプリには同じ症状を既に直した実績が2箇所ある。**プラットフォームごとに移植元が違うので注意**（Fable監査で判明・alan5の当初の想定は誤りだった）。

- **Android**: `android-native/.../SoudanSheet.kt:485-497`のフレーム単位リトライ方式をそのまま移植する。
  ```kotlin
  // 参考(相談室の実装・そのまま流用可):
  var y = rowPositions[idx]
  var tries = 0
  while (y == null && tries < 10) {
      withFrameNanos {}
      y = rowPositions[idx]
      tries++
  }
  ```
  固定delayをやめ、実際にレイアウトが確定するまで最大10フレーム待ってからスクロールする。

- **iOS**: `ios-native/KyouNoOgatore/KyouNoOgatore/SoudanSheetView.swift:500-518`のanchor方式（bot発言は`.top`、ユーザー発言は`.bottom`）を移植する。Androidのようなフレームリトライではなく、`.onChange(of:)`でスクロール先を都度計算する方式。オンボは全部ユーザー操作起点の追加なので、素朴には「常に`.bottom`」でよいはずだが、相談室が`.top`/`.bottom`を使い分けている理由（長い内容の頭が画面外に出るのを防ぐ）がオンボにも当てはまるか判断して実装してよい。

いずれも**reduceMotion時は無演出即着地**にすること（既存作法どおり）。

## A2. CTAボタンがコンテンツと同じスクロール領域にあり、画面外に出る

**症状**: タップするボタンが下の方に隠れて、スクロールしないと押せないことがある。

**原因（確認済み）**: `OnboardingContentView`（初回チャット）・かたさチェック（`QuizContentView`）・練習ガイド（`ResultContentView`内の`fdGuideActive`分岐）のいずれも、CTAボタンが本文と**同じ**`ScrollView`/`.verticalScroll`の中にあり、固定位置になっていない。

**直し方**: `TourContentView`（使い方ツアー）が2026-07-29のD6で既に同じ問題を直している。**そのパターンをそのまま移植する**。
- 移植元: `ios-native/KyouNoOgatore/KyouNoOgatore/OnboardingViews.swift:1040-1053`のコメントと構造（外側`VStack`でボタン列を分離、内容だけを`ScrollView`にする）
- 対象3箇所（iOS/Android両方）:
  - `OnboardingContentView`（初回チャット、iOS L226-301）
  - `QuizContentView`（かたさチェック5問、iOS L606-686）
  - 練習ガイド（`ResultContentView`内`fdGuideActive`分岐、iOS L873-903）※この画面のボタン埋没は実機未確認。同じ構造なので同じ直し方でよいはずだが、直したあとに実機/シミュレータで確認すること。

## A3. かたさチェックの「まえの質問へ」「ホームにもどる」が見分けにくい

**症状**: どのボタンが何のボタンか分かりづらい。

**原因（確認済み）**: `OnboardingViews.swift:676,680`で、機能が全く違う2つのボタン（前の設問に戻る／診断を中断してホームに戻る＝破壊的操作）が、**同じ**`KyonoLineButton`（枠線のみ）スタイルで縦に並んでいる。Q2以降の全設問画面で毎回発生。

**直し方**: 「ホームにもどる」（中断・破壊的な操作）を視覚的に控えめにする。案: 「まえの質問へ」は`KyonoLineButton`のまま残し、「ホームにもどる」は文字だけのテキストリンク（例: `SettingsView.swift:157-160`の「変える」リンクと同じ見た目）に格下げする。またはボタン間の余白を増やし誤タップを防ぐ。**判断は工場側で妥当なものを選んでよい**（両方とも「押せそうな装飾」を維持しつつ機能差を出せればOK）。

## B1. 練習ガイドの文言を減らす

対象: `OnboardingViews.swift:881`（iOS）／`OnboardingScreens.kt:892-893`（Android）

- 現行: 「ここからは練習だよ🏫 ①を試しにタップ→YouTubeがひらいたら すぐ戻ってきてね（ぜんぶ見るのは あとでゆっくりでOK）」（62字）
- **変更後**: 「①をタップ！ YouTubeが開くよ🏫」
- 理由: 直前の見出し「きょうはこの1本だけでOK！」で"これは練習/1本だけ"の意図はもう伝わっている。「ぜんぶ見るのはあとでOK」は下の「あと2本とくわしい解説はあしたから見られるよ🌱」と重複しているので削る。
- 補足文（🔙見おわったら〜）と「あと2本〜」の文言は**変更しない**（戻り方の案内は「タスクスイッチできない層の最初の脱落点対策」として2026-07-21に本人承認済みの機能なので削らない）。

## B2. ツアー8枚目の説明文を厚くする

対象: `OnboardingViews.swift:1012`（iOS `obTourSlides`8枚目）／Android同等（`OnboardingScreens.kt:1046-1062`、iOSと1:1一致）

- 現行: 「このツアーも使い方タブの「📖 使い方ツアー」から いつでももう一度見られるよ」（37字）
- **変更後**: 「困ったらいつでも読み返せるよ！ 使い方タブの「📖 使い方ツアー」からね」

（5枚目「相談室」は現行のままで十分という判断になった。変更不要。）

## 検収基準

- [ ] iOS/Android両方、オンボ4問チャットで選択肢を連続タップし、常に最新の選択肢とCTAボタンが画面内に見える
- [ ] かたさチェックで各設問のCTAボタンが画面下に固定され、本文の長さに関わらず位置が動かない
- [ ] 「まえの質問へ」「ホームにもどる」が見た目で区別できる
- [ ] 練習ガイド・ツアー8枚目の文言が変更されている
- [ ] reduceMotion有効時、スクロールが無演出で即座に着地する
- [ ] 完了後、この発注書のどの項目を「確認済み」で閉じたか・どの項目が実機確認できていないかを報告に明記する（文字列を見ただけで済ませず、動くところまで見て「確認済み」と書くこと）
