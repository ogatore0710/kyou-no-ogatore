# タスク（C2/appdev向け）— 2週間プラン完走時のお祝いカードが欠落

## 背景

alan5の独自調査の続き。2週間プランの進捗表示（進行中カード）は既に実装済みだが、
**14日を達成したときの完走お祝いカード（`planDoneCard`・紙吹雪演出）がネイティブに存在しない**。

## Web版の仕様（app-record.js:1781-1808付近 `renderPlanCard()`/`planFinished()`が正本）

```
プラン開始から14日経過（planFinished(p)===true）した後、初回のホーム表示で:
- 通常の進捗カード(planCard)は隠す
- お祝いカード(planDoneCard)を表示:
  - chara-congrats.png（既に他画面で移植済みのアセット）
  - 「🎉 {プラン名}プラン完走！すごい！{日数}日間続けたの、ほんとにえらい👏
    体はちゃんと応えてくれてるよ」
  - 紙吹雪アニメーション（launchConfetti(105)。既に相談室で使用中の仕組みを流用可能）
  - ボタン3つ:
    ①「💪 もう2週間続ける」（planAgain）→ 同じ動画リストで即座に新しい14日プランを開始
    ②「📏 かたさチェックで変化をみる」（planDoneClose→startQuiz()）→ カードを閉じてチェックへ
    ③「とじる」（planDoneClose）→ カードを閉じるだけ
- プランデータ自体はこの時点で削除（store.set("plan",null)）。進捗の記録（続けた日数）自体は
  別物なので消えない
```

## やること

- ホーム画面（またはプラン管理箇所）に、14日達成を検知してお祝いカードを表示するロジックを追加
- 既存の`chara-congrats`画像・confetti演出（SoudanSheetで使用中の仕組み）を再利用
- 3つのボタンの動作を実装（特に「もう2週間続ける」は同じ動画リストでの再開始）

## 検収基準

- [ ] 14日達成時にお祝いカードが表示される（紙吹雪演出込み）
- [ ] 「もう2週間続ける」で同じ動画リストの新しい14日プランが始まる
- [ ] 「かたさチェックで変化をみる」でカードが閉じてかたさチェックへ遷移する
- [ ] 「とじる」でカードが閉じるだけになる
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- プラン進行中の既存カード・進捗ロジックは変更しない
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で簡潔に報告してください（スクショ1枚で可）。
