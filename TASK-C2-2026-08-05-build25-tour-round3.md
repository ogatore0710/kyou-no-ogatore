# TASK-C2-2026-08-05-build25-tour-round3: 本人ラウンド3（おすすめ3本の短タイトル化・動画タップ練習の明示）

発注: alan5（2026-08-05 12:40頃）
背景: ビルド24配信後、本人の実機ラウンド継続（IMG_8794・ツアー「けっか」のおすすめ3本）。
本人の生指摘2件:
1. 「動画のタイトル短くなっていない」
2. 「この1本目だけタップして戻ってくる、練習モードなのが伝わらない」

**このラウンドは継続中**（本人「気づいたとこいくつか言うね」）。追加項目はこのファイルに
R-5, R-6…として追記していく。ビルド提出は今回もalan5の合図待ち。

## R-3: おすすめ3本リストの短タイトル化（両OS）

- 現状: `VideoRow`（iOS `SearchView.swift:92`）は `v.t`（フルタイトル・lineLimit(3)）を表示。
  build20の短タイトル`st`（catalog.json・142本・本人と4ラウンド往復して確定した資産）は
  ホーム「きょうの1本」カードだけで使われている（`HomeView.swift:1306` で確認済み）。
- 変更: **かたさチェック結果の「おすすめの3本」リスト（＋悩み別のもう1本）で、
  stがある動画はstを表示**する（`v.st ?? v.t` 相当）。無い動画はフルタイトルのまま。
- メタ行（`v.s`＝年・分・回数）は**据え置き**（ホームと違い、ここは比較リストなので
  年・再生数の信頼情報を残す。stに分数が含まれ一部重複するのは許容）。
- **スコープ注意**: 「動画を探す」タブの検索結果リストは今回**変えない**（同じVideoRowを
  使っているため、無条件にst化すると検索まで変わる。結果画面からの呼び出しだけst表示に
  なるよう、パラメータ等で分岐すること）。検索側もst化するかは本人に別途確認する（alan5が持つ）。
- Android側の対応箇所は同構造のVideoRow相当（OnboardingScreens.kt / SearchScreen.kt）。
  実装時に特定を（alan5未確認）。

## R-4: 1本目タップが「れんしゅう」だと分かる枠組み（両OS・ツアー中のみ）

- 現状: W-2（build23）で1本目だけタップ可・2,3本目は減光no-op。タップ時に
  「YouTubeがひらくよ。…」の一言（0.9秒）→ YouTube遷移 → 復帰検知「おかえり！」。
  ただし**タップする前に「これは練習だ」と分かる表示が何も無い**。
- 変更: ツアー中（`fdGuideActive`）のみ、おすすめ3本リストの見出しと1本目カードの間に
  R-2と同じ視覚言語の練習ピル＋案内1行を挿入する:
  1. ピル（中央寄せ・R-2と同スタイル=tealSoft地+tealInk文字12pt black900・角丸フル）:
     `＼ 動画をひらく練習 ／`
  2. 案内行（sub・bold700・13pt・中央寄せ）:
     `練習だよ！`

  ⚠️ **文言は本人校正済み（2026-08-05・漢字表記含め一字一句このまま）**。alan5の当初案
  （ひらがな長文）から本人が差し替えた。「＼ ／」の飾りはR-2ピルの型として維持
  （本人指定はピル内文言のみ。飾りが不要と言われたら外す）。
- 既存のタップ時notice・復帰フロー（pendingNudgeDate/showDoneNudge/練習合流）は**一切触れない**。
- 通常ユーザー（ツアー外）の結果画面には何も出さない。

## 検収

- `npm test` 再実行。
- 実描画: ツアー中の結果画面（ライト。R-3の短タイトル+R-4のピルが1枚で写る構図）＋
  ツアー外の通常結果画面（R-4が出ない・R-3は効いている）＋検索タブ（フルタイトルのままの確認）。
- st表示の実データ確認: 表示中の3本がcatalog上stを持つか確認し、持たない動画が出る場合の
  フォールバック（フルタイトル）も1例スクショ（無理に作らなくてよい・rx構成上出ないなら省略可で
  その旨報告）。
