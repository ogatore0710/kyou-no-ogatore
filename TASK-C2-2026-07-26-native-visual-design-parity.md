# タスク（C2/appdev向け）— ネイティブUI 見た目のWeb版パリティ移植（アプリ全体・最優先）

## 背景・このタスクの位置づけ

**`TASK-C2-2026-07-26-native-migration-card-visual-assets.md`はこのタスクに吸収・上書き。着手前ならそちらは実施しないこと**（範囲がカードだけでは足りないと判明したため）。

Step 0〜7bで、ロジック・データ・安全系は1:1で忠実に移植できていた（テストで裏取り済み）。しかし**見た目（テーマ・配色・フォント・アイコン・カード型コンポーネント・下部タブバー）は誰も検収基準に入れておらず、丸ごと未実装のまま**だった。本人が実機で見た瞬間に「PWA版と全然違う」と気づいた。本人の当初の期待は明確に「https://kyou-no.ogatore.net/ と同じようなものができる」であり、これはマスタープラン発足時からの目標（「見た目・機能は同一」）そのもの。今回はその見た目の層を作り込む。

**このタスクはアプリの全画面が対象**。1画面の手直しではなく、デザインシステムを作って全画面へ適用する規模。

## Web版の正本（デザイントークン。index.htmlのCSSから抽出済み・そのまま使うこと）

### 配色（ライト）
```
--yellow:#FFD93B; --yellow-soft:#FFF3C4; --ink:#3A3A35(本文文字); --sub:#6E6B5F(サブ文字);
--teal:#2BB3A3; --teal-strong:#1E7B70; --teal-soft:#DFF5F2; --coral:#FF8A70; --coral-soft:#FFE8E2;
--bg:#FFFAF3(背景); --card:#FFFFFF(カード背景); --line:#F2EADB(枠線); --radius:22px(角丸の基本値);
--sub2:#6B6857; --tealink:#177065(リンク文字); --sub-faint:#827F72;
ピンク系(コード中に散在): --pink相当#E56A9A・pink-soft相当#FFEDF3
```

### 配色（ダーク。body.dark時の上書き値）
```
--bg:#211E19; --card:#2C2822; --ink:#F2EDE1; --sub:#B9B2A0; --line:#3D382F;
--yellow-soft:#3A3423; --teal-soft:#22403B; --pink-soft:#3A2730; --coral-soft:#3A2A24;
--sub2:#C6BFAE; --tealink:#7BD0C4; --sub-faint:#8C8676
タブバー: 非選択アイコン#3D382F(dark)/#C4BDA9(light)・選択時アイコン=var(--yellow)
```

### フォント
- 本文: "M PLUS 1p"（weight 700/800/900。ファイルは`assets/fonts/mplus1p-700/800/900.woff2`）。フォールバック: ヒラギノ角ゴ/游ゴシック
- カードの日数等の大数字: "BananaNum"（`assets/fonts/banana-card.woff2`）。両方とも自己ホストのwoff2で、TrueType/OpenType版が無い場合はwoff2からフォント埋め込み用に変換が必要（iOS=.ttf/.otf、Android=.ttf。variable fontでなければwoff2→ttfの変換ツールが要る）

### コンポーネント様式
- **カード（`.card`）**: 白（ダークは`--card`）・角丸22px・padding20px・margin-bottom16px。**このアプリの見た目の基調**——ほぼ全セクションがこのカード型ボックスの積み重ねで構成される（今のネイティブ版はプレーンな縦積みリストのみでこれが無い。最優先で直すべき箇所）
- **ボタン**: `.btn-primary`=黄色背景+太字20px+**下方向の立体シャドウ（box-shadow: 0 4px 0 #E8BE1E）**という「押せそう」な3D風スタイル。`.btn-ghost`=teal-soft背景・teal文字・15px
- **下部タブバー（`.tabbar`）**: 固定表示・5項目（使い方/マイ記録/ホーム/再生リスト/動画を探す）・アイコンは選択時に黄色く塗りつぶされる。**現状のネイティブ版はこのタブバー自体が存在せず、画面上部からの縦スクロールリストになっている**（構造から作り直しが必要）
- **セクション見出し（`.sec-head`）**: 各カードの先頭に、丸背景付きの手描き風インラインSVGアイコン+タイトル、という形式が全編で繰り返される（例: 時計アイコン+「いつやる派？」、虫眼鏡アイコン+「かたさチェック」等）。アイコンはindex.html内に大量にインラインSVGとして埋め込まれている（`<div class="sec-head"><svg...>`で検索すれば全部拾える）。**これらは手描き風カスタムアイコンであり、SF Symbols/Material Iconsへの置き換えは見た目が変わってしまうため不可**。SVGのpath/circle/rectデータをそのまま両OSのベクター形式（iOS=SwiftUIのPath直書き or PDF/SVGアセット、Android=VectorDrawable XML）に移植すること
- FAB（相談室・オガトレ通信）: 円形・カラーボーダー3px・影付き

## やること（進め方の目安）

1. **デザイントークンの定義**: `Theme.swift`（iOS）/`Theme.kt`（Android）に上記の配色・フォント・radius値を定義。ライト/ダーク両対応（システムのダークモード設定 or アプリ内のテーマ設定=Step7bで移植済みの`kyono_theme`キーに連動）
2. **共通コンポーネント化**: Card風コンテナ・btn-primary/btn-ghost相当のボタン・下部タブバー（5項目）・FABを再利用可能な部品として作る
3. **フォント同梱**: M PLUS 1p（700/800/900）・BananaNumをアセットとして両OSに組み込み、全テキストに適用（数字表示箇所はBananaNum、本文はM PLUS 1p）
4. **アイコン移植**: sec-headまわりの主要インラインSVGアイコン群を両OSのベクター形式へ移植
5. **全画面へ適用**: ホーム・かたさチェック・診断結果・相談室・カレンダー/マイ記録・カード図鑑・じまんカード・せんぱいの声・オガトレ通信・使い方・設定——全画面をカード型レイアウト+下部タブバー構成に作り替える

## 検収基準

- [ ] ホーム画面のライトテーマが、Web版と横に並べて見比べて「同じアプリ」だと分かる程度に一致（背景色・カードの角丸・黄色ボタンの立体シャドウ・下部タブバーの有無）
- [ ] 同じくダークテーマも一致
- [ ] 主要3画面以上（診断結果・相談室・設定）でも同様にWeb版とスクショ突合
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・これまでの全ステップのテストが緑のまま（回帰なし）
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- ロジック・判定・データ構造は一切変更しない（見た目だけの変更）
- Web版（PWA）側の配信ファイルは一切変更しない（読むだけ）
- Step 8（9月頭の差分同期）は対象外

## 進め方の注意

- 規模が大きいので、**画面ごとに区切ってコミットし、途中経過が分かる形で進めること**（1画面通し切ってから次、を繰り返す）
- 迷ったら「Web版のCSS/SVGの値をそのまま持ってくる」を優先し、独自解釈でのデザインアレンジはしない（今回のゴールは再現、創作ではない）

## 報告

完了時、ドア配達で以下を含めること:
- 検収基準5項目のPASS/FAIL
- Before/After（現状のプレーン版 → 新版）のスクショ、できればWeb版とも並べたもの
- 消費トークンの概算（規模が大きいため、途中経過でも構わないので分かる範囲で報告してほしい）
