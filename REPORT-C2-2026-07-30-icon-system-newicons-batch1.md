# アイコン方針(I) — 新規描き起こしバッチ1(うつす・はこぶ×3)の報告

指示どおり、検索欄の虫めがね実装・`.guideBook`削除・HANDOFF追記を終えたうえで、
**新しく描く絵のうち3つ(うつす・はこぶ×3)だけ描いて一度止めます。**

## 検索欄の虫めがね(🔍→`.search`)

`SearchView.swift`/`SearchScreen.kt`の検索欄、`GuideView.swift`/`GuideScreen.kt`のFAQ検索欄
両方に、タブバーと同じ虫めがね(`.search`)を**左側**に実装しました。G4の消去ボタン(✕)は
右側のままなので競合しません。シミュレータで実際に文字を入力し、虫めがね(左)と✕(右)が
窮屈にならず両方きれいに収まることを確認しました。

## `.guideBook`の削除

未使用のまま残さず、enumケース・Canvas描画コードとも両OSから削除しました。

## HANDOFF.mdへの追記

指示どおり「見出し文字列がジャンプ先のキーとして直接参照されている箇所がある」ことを
先頭に1行(実際には状況の説明を含む1ブロック)残しました。

## 新規描き起こし: 「記録のひっこし」3ボタン

対象は`SettingsView.swift`/`SettingsScreen.kt`の「記録のひっこし」セクション:

| ボタン/見出し | 絵文字 | 新規アイコン | 意匠 |
|---|---|---|---|
| 見出し「記録のひっこし」 | 📦 | `.exportBox`(同上を再利用) | 段ボール箱(開いた蓋の山形+ガムテープの十字) |
| 「記録をコピーする」 | 📦 | `.exportBox` | 同上 |
| 「コピーした記録を自動で読みこむ」 | 📋 | `.clipboardPaste` | クリップボード(上部の留め具+中に2本線) |
| 「よみこむ」 | 📥 | `.importTray` | 受け皿(台形のトレー)+上から下向きの矢印 |

見出しと「記録をコピーする」ボタンは元々同じ📦だったため、同じ`.exportBox`を再利用しています
(判断①の「見分けをつける」目的に沿い、この2箇所は同じ意味なので同じ絵で問題ありません)。

## 副次的に見つけた欠落: Android版のボタンアイコン差し込み口が無かった

`KyonoPrimaryButton`/`KyonoLineButton`に`icon`引数を追加する対応(iOS版は試作1件目
commit `37ee548`で実施済み)が、**Androidには一度も移植されていませんでした。**
確認したところ、Android版ホーム画面の「相談する」ボタンはいまだに`"💬 相談する"`という
絵文字混じりの文字列のままでした(試作1件目の報告はiOSのシミュレータ画面のみで検証しており、
Android側の未対応に気づいていませんでした)。

今回`KyonoPrimaryButton`/`KyonoLineButton`両方にicon引数を追加し、Android版「相談する」
ボタンも`KyonoIcon.SoudanBubble`で揃えました。これでこの2つのボタン部品はiOS/Android
両方が同じ差し込み口を持ちます。

## 回帰確認

- iOS: `xcodebuild`(Debug・Simulator)ビルド成功
- Android: `compileDebugKotlin`成功・`testDebugUnitTest --rerun-tasks`成功
- `npm test` 459 checks all green
- シミュレータ(iPhone 17)・エミュレータ両方で「記録のひっこし」画面を実機確認: 3種類の
  アイコンがそれぞれ見分けのつく形で描かれていること、見出しとコピーボタンが同じ絵で
  揃っていることを確認
- シミュレータで検索欄の虫めがね(左)+文字入力時の✕(右)が競合しないことを確認

## 次

**残り8種(🎯🎫👑🌱⏱🎉・📱・🎨)は、この3つのご確認をいただいてから進めます。**
