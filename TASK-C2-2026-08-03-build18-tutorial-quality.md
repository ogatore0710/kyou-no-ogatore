# 発注: ビルド18（テーマ混在バグ＋チュートリアル品質・Fable監査12件→本人裁定済み）

発注元: alan5（本人のビルド17実機フィードバック→Fable監査（2026-08-03・コード実測つき）→alan5仕分け→本人カード裁定4件すべて推奨案でGO）

## B-0: テーマ混在バグ（最優先・別便で発注済みの件）

ResultViewだけ`themeSetting:"auto"`ハードコード（`OnboardingViews.swift:922`）・他画面はstore実設定参照、が原因候補（Fable監査がgrepで確認済み）。store参照に統一し、**`themeSetting`の同型ハードコードを全ファイル棚卸し**して報告。検証は「システム=ダーク＋アプリ内=明るい」条件の実描画で、ツアー内チェック結果〜練習〜カードモーダルまで通し。

## B-1: 「つぎへ」後に結果画面を視覚的に閉じてからカードを出す

現状: `performPracticeRecord()`発火後0.7秒で同一画面の`.overlay`にモーダルが乗るだけ。スクリム半透明0.55で背後に同じボタンとカードが透ける（本人指摘「イマイチ上手く移行できていない」の正体・IMG_8729）。
- タップと同時に「静かな一行＋ボタン」ブロックを非表示にする
- スクリムを不透明系へ（既存`OnboardingScrim`パターン流用・演出追加なし）
- 「1日目クリア！ナイスご自愛！」の労いがモーダルに覆われる問題（7/30にホーム側で直した構造の結果画面版再発）も表示順を整理して解消
- ⚠️ `performPracticeRecord`(:1016-1047)の副作用連鎖（markDone→通知resync→Widget→daylog→fd="1"→tourpend=true）の**順番は変えない**。tourpendを触るとツアー起動が死ぬ

## B-2: ジャーニーバーを4段に（「どうが」を外す）

fdGuide中はサムネno-op（本人裁定・維持）のため「どうが」は体験されず、バーが実態を裏切っている。「チェック→けっか→きろく→カード」の4段へ。
- ⚠️ `KyonoJourneyBar`は3画面共用（Quiz:709/Result:1058/Tour:1346）。`kyonoJourneySteps`(:606)と`journeyIndex`(:993-998)を**必ず同時に**修正（片方だけだと添字ズレ）

## B-3/B-4: ライトモードの淡いボタン（実測ベース・背景クリームは変えない）

- B-3: ゴーストボタン背景`tealSoft #DFF5F2`が地`#FFFAF3`と1.04:1 → 濃色化（目安#C6EAE4程度）または`tealStrong`2px縁取り。対象例:「ツアーをとばす」「記録カードを画像でのこす」
- B-4: ラインボタン（もどる/とじる）の枠`#E0D5BE`が1.33:1 → sub2系の濃ベージュで3:1超へ。文字色は5.12:1合格なので**枠だけ**
- 両テーマ実背景の実描画ビフォー/アフター＋実測コントラスト値を報告に

## B-5: ツアー表示中は相談室FABを非表示（本人GO）

全スライドで白丸FABが「つぎへ/おわる」右端に重なり誤タップ動線。ビルド16「FABの躾」の延長でツアー中は消す。5枚目の相談室紹介は本文とモックで足りる。

## B-6: 練習ボタンを本番と同じ「きょうやった！」に（本人GO）

結果画面下部を以下の文言に差し替え（alan5指定・このまま使う）:
- 一行: `この結果はほんもの！つぎは本番とおなじボタンで記録の練習`
- ボタン: `きょうやった！`
機能は現行の`performPracticeRecord`のまま・ラベルと一行のみ変更。

## B-7: fdGuide中のサムネを見た目でも「押せない」状態に

no-op裁定は維持したまま、減光（opacity等）で押せないことを明示。「押せるように見える嘘」を消す。

## B-8: 「きょうやった！」（旧つぎへ）ボタンの連打ガード

QuizViewの`answering`(:651-653)相当のガードを追加。モーダル表示までの0.7秒間の再発火（confetti・celebration・recordDaylog）を防ぐ。

## B-9: 初回チャットの4点バーを削除（本人GO）

「使い方ツアー」見出し下でバーが3種連続する問題の引き算。質問4つはチャットの流れで伝わる。チェック5段（→B-2で4段）とツアー8点（→B-10で7点）は残す。

## B-10: ツアー8枚→7枚＋文言の引き算（本人GO・alan5指定文言をそのまま使う）

- **2枚目**: おやすみ券の行を削除 → `アプリにもどったらこのボタンを押すだけ\n連続と通算がのびるよ`
- **3枚目（記録カードをつくる）を削除**し、4枚目（ためると図鑑がうまる）に吸収 → 新desc: `記録カードは記念日・季節・レアなど何種類もあるよ\n「保存・シェアする」で写真にのこせて SNSやコメント欄にもどうぞ\n毎日の記録でカード図鑑がすこしずつうまっていく（マイ記録→お楽しみ機能）`
- **旧7枚目（マイ記録でふりかえる）**: 6機能列挙をやめる → 新desc: `やった日に印がつくカレンダーがあるよ（×はつかないよ）\n毎日の合図（カレンダー通知）は続ける設定からいつでも入れられるよ`
- 他の枚は現行文言のまま。進行バーの点数・「N/7」表記等の追従漏れがないか全数確認

## 検収・ビルド

- B-0は食い違い条件の通し実描画、B-1は録画（alan5がフレーム抽出で「背後が閉じてからカード」を検分）、B-3/B-4は実測値つきビフォー/アフター、B-10は7枚全部の実描画
- 「自分で確認済み」と「未確認」を報告で分ける。Androidも同等修正（対象外項目はその根拠を書く）
- 全通過後ビルド18（17→18・既存グループ・公開メタデータ不可触・sw.js版数上げない）。whatsNewはalan5がゲート後に渡す
- P-8留意: 本ビルドでライト表示が直るため、相談室グロー（presentationBackground暫定対処の効果）の実機確認はビルド18で本人に依頼する。P-9はその結果待ち（着手しない）

報告: `REPORT-C2-2026-08-03-build18-*.md`（束ね報告可）
