# タスク（C2/appdev向け）— 使い方タブの詳細ガイド6セクションが丸ごと欠落（優先度高・ボリューム大）

## 背景

alan5の独自調査（7件目・最大の発見）。本人が「きほんのき、ネイティブ版ならでは見やすい」と
評価したのは`gd-faq`（よくあるしつもん・Q&A形式）だけで、その**上にある詳細ガイド6セクション
（目次チップ+階段状の説明カード）が丸ごと未移植**だったと判明。`gd-faq`（FAQ Q&A）は
**絶対に変更しない**（本人評価済みのため）。今回移植するのはそれより前のセクション群のみ。

## Web版の仕様（index.html:970-1075が正本）

### 目次チップ（index.html:982-987。既存の再入場チップの下に並ぶ想定）
```
🆘 困ったときは / 🌅 はじめての日 / ▶ まいにちの流れ / 🛡 記録を守る / 🎫 つづくしくみ / 📅 マイ記録
```
各チップは`gJump('gd-xxx')`でその見出しへジャンプ（対象のdetailsを開いてスクロール）。

### 1. `gd-help`「困ったときは」（index.html:992-998。折りたたみでなく常時カード）
4つのボタン（該当FAQ項目へジャンプ）:
「📅 記録が消えた・0日にもどってる」「📱 機種変更したい」「🩹 ストレッチ中に痛かった」
「🔔 通知・リマインダーについて」

### 2. `gd-start`「はじめての日にやること」（index.html:1002-1011。折りたたみ・**既定で開いた状態**）
5ステップ（stepn番号付き）: ①かたさチェック（30秒・お手本画像`assets/check/q1.jpg`）
②かたさタイプが出る ③まず1本やってみる ④💬オガトレ相談室の案内 ⑤🎯2週間プランの案内。
「チェックをはじめる」ボタン（`startQuiz()`）+「↑目次へ戻る」リンク

### 3. `gd-daily`「2日目からの毎日」（index.html:1013-1027。折りたたみ）
フロー図解（アプリをひらく→きょうの1本を再生→きょうやった！の3ステップ矢印表示）+
あなた用/あさ/よるセグメントのミニチュア（タップ不可の静止モック）+ ひとことメモの案内 +
オガトレ通信の案内。「↑目次へ戻る」

### 4. `gd-mamori`「記録が消えない3つの守り」（index.html:1029-1042。折りたたみ）
**最重要セクション**: ①ホーム画面に追加の手順（iPhone/Android別・詳細ステップ+
「📲もういちど かんたん案内を見る」ボタンで`a2hsShowForce()`相当を再表示）
②記録カードを画像で保存する手順（`card-sample.png`見本つき）③機種変更時の手順。
「↑目次へ戻る」

### 5. `gd-tsuzuku`「続けるしくみ（ここがやさしい）」（index.html:1044-1057。折りたたみ）
おやすみ券の説明・節目ゴールドカードの説明（`card-sample-gold.png`見本）・カード図鑑への案内・
「サボってもだいじょうぶ」の安心メッセージ・時間がない日のショート動画案内。「↑目次へ戻る」

### 6. `gd-myrec`「『マイ記録』タブでできること」（index.html:1059-1070。折りたたみ）
カレンダー・とどくメーター・お楽しみ機能・続ける設定・再生リストタブの5項目まとめ+
「マイ記録タブをひらく」ボタン（`switchTab('history')`）。「↑目次へ戻る」

## やること

- 目次チップ6個を追加（既存の再入場チップの下）
- 上記6セクションを1:1移植。**折りたたみ可能**（`gd-start`のみ既定で開いた状態、他は閉じた状態）
- 「↑目次へ戻る」リンク（目次チップの位置までスクロール）を各セクション末尾に追加
- **`gd-faq`（よくあるしつもんQ&A）は絶対に変更しない**
- A2HS関連の案内文言（`gd-mamori`内の「もういちど かんたん案内を見る」ボタン）は、既存のA2HS関連UI
  除外方針（マスタープラン§2-2）と整合するか要確認——**ネイティブアプリには「ホーム画面に追加」の
  概念自体が無い**ため、このボタン・iPhone/Android別の追加手順は**そのまま移植せず**、ネイティブに
  合わせた文言（例:「アプリを削除しないでね」等、シンプルな注意書き）に置き換えるか、セクション自体を
  簡略化すること（判断は適切と思う方でよい。安全側=情報を削りすぎない方向で）
- 画像アセット（`assets/check/q1.jpg`・`card-sample.png`・`card-sample-gold.png`）は既存の
  ものを流用（card-sample.png等は既に他画面で同梱済みのはず）

## 検収基準

- [ ] 目次チップ6個が表示され、タップで対応セクションへジャンプする
- [ ] 6セクション全部の内容が表示される（gd-startのみ既定オープン、他は折りたたみ）
- [ ] gd-mamoriのA2HS関連部分がネイティブに適した文言へ調整されている（Web版そのままの
      「ホーム画面に追加」手順は載せない）
- [ ] gd-faq（既存のFAQ Q&A）が変更されていないことを確認
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- gd-faq（よくあるしつもん）は一切変更しない
- ロジック・判定・データ構造は変更しない
- Web版（PWA）側の配信ファイルは一切変更しない

## 進め方の注意

ボリュームが大きいため、セクションごとに区切ってコミット・進めてください。

## 報告

完了時、ドア配達で検収基準6項目のPASS/FAILとスクショを含めて報告してください。
