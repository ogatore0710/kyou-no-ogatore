# タスク（C2/appdev向け）— 設定画面: クリップボード自動読みこみボタン欠落＋テーマ「じどう」の説明文欠落

## 背景

alan5の第2弾監査（未監査領域スイープ）で発見。設定画面（続ける設定）の「記録のひっこし」欄と
テーマ設定に、Web版にある2つの要素がネイティブに無いことを確認した。

## 1. クリップボード自動読みこみボタン（index.html:839, 2067-2074が正本）

```
Web版の「記録のひっこし」欄には読みこみ導線が2段構え:
①「📋 コピーした記録を自動で読みこむ」ボタン（新規・目立つ位置）
  → クリップボードを直接読み取り、成功したら自動でimportを実行。失敗時は
    「読みこめませんでした\n下のわくに手で貼り付けてね」等のアラートで②へフォールバック
②手動貼り付け欄+「よみこむ」ボタン（ネイティブに既存実装済み・変更不要）

Web版のコード内コメント: 「高齢者・デジタル機器が苦手な方向け: 長押しコピー→貼り付けという
操作を、ボタン1つで完結させる（2026-07-19 Fableレビュー「コピペ方式は挫折点」対応）」
——このアプリの想定ユーザー層に配慮した意図的な設計。ネイティブには①が無く②のみ。
```

やること: ①のボタンを追加。タップでクリップボードを読み取り、内容があれば自動でimport実行。
空・読み取り失敗時はWeb版と同趣旨のアラート文言で②へ誘導。

## 2. テーマ「じどう」の説明文（index.html:807が正本）

```
「じどう」テーマ選択肢の下に説明文が無い:
「「じどう」は夜（19時〜朝5時）やスマホがダーク設定のとき暗くなります」
```

やること: テーマ設定の「じどう」選択肢付近に上記説明文を追加（表示のみ・判定ロジックは既存のまま）。

## 検収基準

- [ ] 「コピーした記録を自動で読みこむ」ボタンが追加され、クリップボード内容で自動読みこみが動作する
- [ ] クリップボードが空/読み取り失敗時にWeb版同趣旨のフォールバック案内が出る
- [ ] テーマ「じどう」の説明文が表示される
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- 手動貼り付け欄・エクスポート機能（既存実装）は変更しない
- テーマ判定ロジック自体は変更しない
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で簡潔に報告してください（スクショ1枚で可）。
