# REPORT-C2-2026-08-04-build21-color-system-navy

【appdev→alan5】build21(本編C部=配色刷新D2「藍地×白文字」＋追補Y-1〜Y-6)の実装完了報告です。両OSビルド/テスト・qa.js全項目確認済み。**alan5検収後のビルドGOをお待ちしています。**

## 0. サマリ

- D2: 主ボタンを黄地→藍地×白文字へ。あわせてセカンダリ(旧ゴースト)・ラインボタン・ホームの切り替えノブ・検索の選択中チップ/カテゴリタブ・タブバー非選択を刷新(**すべてライトのみ**。ダークは主ボタンだけ実測選定の派生色で追随、他は既存値を維持)。
- Y-1〜Y-6の6件すべて反映(詳細は5節)。
- 実描画で**2件の実バグを発見してその場で修正**しました(3節・7節)。
- 実測コントラストの棚卸し中に、**このタスクの変更対象ではない既存のダーク側ラインボタン枠線が基準未達(1.52〜1.72:1)であることを発見**しました。今回は手を広げず、発見事実だけ報告します(8節)。
- iOS `xcodebuild build` BUILD SUCCEEDED。Android `JAVA_HOME=~/android-toolchain/jdk/Contents/Home ANDROID_HOME=~/android-toolchain/sdk ./gradlew compileDebugKotlin testDebugUnitTest --rerun-tasks` BUILD SUCCESSFUL。`node scripts/qa.js` 全項目PASS。

---

## 1. D2: 配色刷新の反映内容

### 1-1. トークン(両OS Theme.swift/Theme.kt)

| トークン | ライト | ダーク |
|---|---|---|
| `btnPrimaryBg`(新設) | `#073A71` | `#2278C0`(実測選定・後述) |
| `btnPrimaryShadow`(値更新) | `#052A52` | `#19578B` |

ダーク側は「藍そのまま」だと暗背景に沈む(`#073A71` vs `#211E19` = 実測1.47:1)ため、**白文字4.5:1以上・面vs暗背景3:1以上の両立**を条件に候補を実測探索しました。ご提示の候補`#1D5CA8`は白文字6.67:1は満たすものの暗背景との比が2.49:1で条件未達だったため不採用とし、代わりに`#2278C0`(白文字4.65:1・vs暗背景#211E19=3.57:1・vsカード#2C2822=3.15:1)を採用しました。影`#19578B`は主ボタン地から同じ明度比で導出した値です。

### 1-2. コンポーネント別の変更(すべてライトのみ・ダークは既存値維持)

| 部位 | ライト新値 | ダーク |
|---|---|---|
| 主ボタン(KyonoPrimaryButton) | 地`btnPrimaryBg`＋白文字 | 地`btnPrimaryBg`(ダーク値)＋白文字 ※両テーマ変更 |
| セカンダリ(KyonoGhostButton) | 白地＋文字`#073A71`＋枠`#3E70F5` 2.5pt | 既存teal系(tealSoft地/tealStrong枠2pt/tealInk文字)を維持 |
| ラインボタン(KyonoLineButton) | 文字`#33322C`・枠`#4A473D` 2pt | 既存(sub2文字・枠`#4A443A`)を維持 |
| ホームの切り替えノブ(セグメント選択中) | 地`#073A71`＋白文字・未選択文字`#57544A` | 既存(card地+ink文字選択中/sub未選択)を維持 |
| タブバー非選択 | ラベル/輪郭`#57544A`・アイコン塗りなし(白抜き線画) | 既存(tabbarIconOff/tabbarStrokeOff)を維持。選択時の黄アイコン+ink文字は両テーマ現行維持 |
| 検索/相談室のカテゴリタブ(catbtn) | 地`#073A71`＋白文字 | 既存(黄地+ライト固定inkの文字)を維持 |
| 検索チップ「時間・シーン」選択中 | 地`#6B5500`＋白文字(**両テーマ共通の単一値**) | 同上 |

### 1-3. 拡張判断(task本文に明記は無いが実施したもの・要確認)

- **カテゴリタブ(catbtn)の選択中も藍/白へ変更しました**(検索画面のカテゴリ選択・相談室の`KyonoCatButton`)。task本文は「選択中チップ」としか書かれていませんが、C-1項6「黄は藍の上専用のアクセント・面としては使わない」の原則をこの部位にも適用すべきと判断し、拡大適用しました。この判断が違う場合は差し戻してください(catbtnは既存の黄地に戻せます)。

---

## 2. 実測コントラスト表(全件)

| 部位 | 組み合わせ | 実測 | 判定 |
|---|---|---|---|
| 主ボタン ライト | 白文字 vs `#073A71` | 11.33:1 | AAA |
| 主ボタン ダーク | 白文字 vs `#2278C0` | 4.65:1 | AA |
| 主ボタン ダーク面 vs 暗背景`#211E19`(非文字3:1) | — | 3.57:1 | 合格 |
| セカンダリ ライト | `#073A71` vs 白 | 11.33:1 | AAA |
| セカンダリ青枠 ライト vs 白(非文字3:1) | — | 4.34:1 | 合格 |
| セカンダリ ダーク(既存維持) | tealInk vs tealSoft | 6.25:1 | AA |
| ラインボタン文字 ライト | `#33322C` vs 背景 | 12.38:1 | AAA |
| ラインボタン枠 ライト vs 背景(非文字3:1) | — | 8.95:1 | 合格 |
| ラインボタン ダーク(既存維持) | sub2文字 vs 背景 | 9.07:1 | AAA |
| **ラインボタン枠 ダーク(既存維持) vs 背景(非文字3:1)** | — | **1.72:1** | **未達(8節で報告)** |
| セグメント選択中 ライト | 白 vs `#073A71` | 11.33:1 | AAA |
| セグメント未選択文字 ライト | `#57544A` vs 白 | 7.58:1 | AAA |
| セグメント選択中 ダーク(既存維持) | ink vs card | 12.54:1 | AAA |
| セグメント未選択 ダーク(既存維持) | sub vs line(トラック地) | 5.51:1 | AA |
| タブ非選択 ライト | `#57544A` vs タブバー地 | 7.58:1 | AAA |
| タブ非選択 ダーク(既存維持) | sub vs タブバー地 | 7.86:1 | AAA |
| タブ非選択輪郭 ダーク(既存維持) vs タブバー地 | — | 4.06:1 | 合格 |
| 黄カテゴリチップ選択中(両テーマ共通) | 白 vs `#6B5500` | 7.18:1 | AAA |
| catbtn選択中 ライト | 白 vs `#073A71` | 11.33:1 | AAA |
| catbtn選択中 ダーク(既存維持) | yellowInk vs yellow | 8.30:1 | AAA |

---

## 3. 全数grep棚卸し(黄=面の残存確認)

`colors.yellow`(面としての使用)を全数grepし、以下のとおり仕分けました。

**刷新した箇所**(表2の対象・上記コンポーネント一覧のとおり)

**意図的に触っていない箇所**(理由つき)

| 箇所 | 内容 | 理由 |
|---|---|---|
| タブバー選択時のアイコン塗り | 両OS `KyonoTabBar` | task本文で明示的に現行維持指定(「ブランドの黄の居場所」) |
| 使い方ガイドの丸マーカー | iOS `GuideView.swift:490`/Android `GuideScreen.kt:578` | 小さな装飾バッジで「藍の上のアクセント」相当の使い方に近く、面(ボタン)ではない |
| オフラインバナーの枠線 | Home/Search両OS計6箇所 | 情報バナーであってボタンではない(「ボタン系の面だけ」の対象外) |
| オガトレ通信FABの枠線 | 両OS計2箇所 | 写真の周りの装飾リング(細い枠)であり面の塗りではない。既存の専用design判断(obu-fab-photo)を尊重 |
| かたさチェック選択肢の押下時フィードバック | iOS `OnboardingViews.swift:788`/Android `OnboardingScreens.kt:831` | 選択中の永続状態ではなく、タップ中だけの一過性フィードバック(P-8/P-9のタッチグロー系と同種の別テーマ)。「選択中チップ」の対象外と判断 |
| ツアーモック内の装飾 | 両OS `KyonoTourMockups` | タップ不可の静止イラストで実UIではない |
| `KyonoGhostNavigationLink`(iOS) | 定義のみ・呼び出し箇所0件 | 死んでいるコード。今回のスコープ外(削除するかは別判断) |

---

## 4. 実描画で発見・その場で修正した実バグ

作業の各所で実描画確認をしたところ、当初案のままでは崩れる箇所が2件見つかったため、範囲内で修正しました。

1. **タブバーの文字折り返し(Y-6起因)**: Y-6の「おおきめ」倍率底上げ(1.18→1.30)により、下部タブバーの「再生リスト」「動画を探す」が2行に折り返るようになっていました。旧倍率(1.18)では発生しないことを実測比較で確認した**新規の回帰**です。セグメントコントロールと同じ1行固定+自動縮小(iOS: `minimumScaleFactor`/Android: `KyonoAutoShrinkText`)で吸収し、1行に収まることを実描画で確認しました。
2. **`Y-1`のセグメントピル横パディング欠落**: 「あなた用/よびな」ピルに元々横方向のパディングが一切無く(縦のみ)、6文字よびなでピル右端に文字が接触していました。左右10ptのパディングを追加し、実描画で余白が確保されたことを確認しました(下記スクリーンショット参照)。

---

## 5. 追補Y-1〜Y-6

- **Y-1(セグメントピル余白)**: 上記4節②で対応。実描画確認済み。
- **Y-2(「きょうの(よびな)用」小見出し削除)**: 未設定時の「きょうのあなた用」も同じ行のため自動的に対象。行ごと削除。
- **Y-3(ツアー1枚目の画像フォールバック)**: `tour-map-illust`(iOS: `.png`資源/Android: drawable `tour_map_illust`)がバンドルにあれば表示、無ければ現行モックのまま。今回は絵が未着のため引き続き現行モック表示(実描画で確認)。ついでにモック内「きょうやった！」ボタンの地色をD2の藍へ更新。
- **Y-4(図鑑看板ミニサムネ4枚)**: 「カード図鑑」ボタンの下に4枚横並びで追加。獲得済みを新しい順に優先(ノーマル/レアはrot=日付→抽選位置の対応を反転し実際の獲得日で並べ替え。記念日/季節カードは獲得日を直接持たないため簡易的に末尾。コレクションの大半はノーマル/レアのため実用上の影響は小さいと判断)。不足分は未獲得の裏面/シルエット(図鑑画面の表現を流用)。タップで図鑑へ。獲得0枚(全部シルエット)・実機データの両状態を実描画確認。
- **Y-5(カレンダー登録機能の削除)**: マイ記録・設定の登録ボタンとEventKit/CalendarContract関連コードを削除。iOSはInfo.plistの`NSCalendarsWriteOnlyAccessUsageDescription`も除去(pbxproj 2箇所)。Androidは元々Intent委譲で権限宣言が無い設計だったため「対象なし」。文言grepで「カレンダー通知/登録」を洗い出し、ツアー・FAQ・使い方カード見出しを「毎日の合図（通知）」表記に統一。qa.jsの必須Info.plistキー検査もこの変更に合わせて反転(残っていないことを検査)。Web版(index.html)は不可触。
- **Y-6(文字サイズ底上げ)**: ふつう1.0→**1.08**・おおきめ1.18→**1.30**をそのまま採用(調整なし)。GO-G12の最小文字フロアは維持。崩れ確認で4節①のタブバー回帰を発見・修正。ホームカード/ツアー/かたさチェック/設定/相談室チップは実描画で崩れ・文字切れ・ボタンはみ出しゼロを確認。

---

## 6. 実描画スクリーンショット(iOSシミュレータ)

- `01d-home-newscale130-fixed.png` — ホーム(主ボタン/セグメント選択中/タブバー・Y-6修正後)
- `01b-home-normal-textscale.png` — 同・ふつう文字サイズ(セカンダリボタンの白地藍文字青枠も確認)
- `02-search-catbtn-chips.png` — 検索(カテゴリタブ選択中の藍地白文字)
- `04-myrecord-dexbanner.png` — マイ記録お楽しみ機能(Y-4の4枚プレビュー+セカンダリボタン)
- `05-settings-buttons.png` — 設定(ラインボタン・セグメント・カレンダーボタン削除済みを確認)
- `06-tour-map-slide.png` — ツアー1枚目(Y-3フォールバック+藍ボタンの反映確認)
- `07-home-dark.png` / `09-myrecord-dark-dexbanner.png` — ダークテーマ(主ボタンのみ刷新・他は既存維持を確認)

---

## 7. ビルド確認

- iOS: `xcodebuild build` BUILD SUCCEEDED。
- Android: `JAVA_HOME=$HOME/android-toolchain/jdk/Contents/Home ANDROID_HOME=$HOME/android-toolchain/sdk ./gradlew compileDebugKotlin testDebugUnitTest --rerun-tasks` BUILD SUCCESSFUL(全テストgreen、既存の無関係な警告のみ)。
- `node scripts/qa.js` 全項目PASS(NSCalendarsキー削除検査・重複キー・一時テストマーカー残存なし含む)。
- 検証用の一時XCUITest(`Build21VerifyUITests.swift`)は削除済み・pbxprojへのゴミ配線なし。

## 8. このタスクの対象外だが発見した既存の問題(未修正・報告のみ)

2節の実測棚卸し中に、**ラインボタンのダーク側の枠線**(`#4A443A`)が背景(`#211E19`/カード`#2C2822`)に対し実測**1.52〜1.72:1**しかなく、WCAG非文字コントラスト基準(3:1)を大きく下回っていることを発見しました。この値はbuild21で変更していない既存値(build18 B-4で導入されたもの)で、**今回のスコープ(ライトのみ)を広げずそのまま報告のみ**とします。次にダークモードの入力欄・枠線まわりに手を入れる回があれば、あわせて直す候補として記録しておいてください。

## 9. 次のステップ

ご確認のうえ、特に1-3節の拡張判断(catbtnの藍化)にご指摘があれば教えてください。それ以外は反映・確認済みです。ビルドGOをいただき次第、build21のTestFlightアップロードに進みます。
