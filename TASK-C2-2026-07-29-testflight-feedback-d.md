# TestFlight実機フィードバック 第3弾(D) — 不具合ぶんの発注

ビルド3を本人が実機で一巡した結果です。**まずC2の直りを確認できました。**
「動画を探す」は454本・複数行スクロール・黒い帯なしまで出ています。**あの直しは効いています。**

以下は今回あがった**不具合だけ**を切り出したものです。絵柄・演出まわり(D4・D8・D9・D10)は
本人確認待ちなので、この指示書には**入れていません**。勝手に先回りしないでください。

優先度は **D1 → D2 → D5 → D6 → D3** です。

---

## D1 🔴🔴 サムネイルが全画面で出ない(最優先)

### 症状(実機・ビルド3)

**3画面すべてで、サムネイル枠が空(グレーの角丸)のままです。**

- 「動画を探す」の検索結果 — 全行
- 「相談室」の動画カード
- 「使い方ツアー」1/9の「きょうの1本」

タイトル・年・再生数・タグは全部正しく出ています。**画像だけが1枚も出ていません。**

### 分かっていること / いないこと

僕が読んだ範囲では、**コード自体は正しく見えます。**

- `KyonoAsyncImage.swift:58` `youtubeThumbUrl()` → `https://i.ytimg.com/vi/<id>/mqdefault.jpg`
- `SearchView.swift:104` `KyonoAsyncImage(url: youtubeThumbUrl(v.id))`
- 枠(`RoundedRectangle(fill: colors.line)`)は出ているので、**レイアウトは生きています**

**だからこそ、読んで直さないでください。** C2のときと同じ順番でお願いします。
**まず計測して、どこで落ちているかを確定させてから手を入れる。**

### やってほしいこと

**① 落ちている場所を1つに絞る。** `KyonoAsyncImage.loadImage()` の各段に印をつけて、
どこでnilになっているかを実際のログで確定させてください。

1. `URL(string:)` がnil か
2. `URLSession.shared.data(from:)` が投げているか(**エラーの中身まで出すこと**)
3. `UIImage(data:)` がnil か(=返ってきたのが画像でない。中身の先頭を見ること)
4. **そもそも `.task(id:)` が一度も走っていないか**

**4を必ず確かめてください。** `KyonoAsyncImage` の `body` は
`Group { if let uiImage { ... } }` で、**読み込み前は中身が空**です。
空のViewに載せた `.task` が走っているかどうかは、**推測でなく観測で**判断してください。

**② シミュレータで再現するかを先に見る。** 再現するならそこで詰められます。
再現しないなら「実機だけの問題」まで分かって、それ自体が大きな前進です。

**③ Android側も同じ症状が出ていないか確認。** 本人はiOSしか触っていません。
**同じ穴が空いているのに報告が来ていないだけ、は今日すでに2回あります。**

### 検収基準

- 3画面すべてでサムネイルが出ること
- **「どこで落ちていたか」を、ログか実測で示すこと。** 「直りました」だけの報告は受け取りません
- 読み込み失敗時に崩れたアイコンを出さない現行の挙動(Web版 `onerror` 相当)は維持

---

## D2 🔴 共有メニューに「写真に保存」が出ない — **原因は確定しています**

### 原因

**`INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription` が `project.pbxproj` に1件もありません。**

```
$ grep -c "INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription" project.pbxproj
0
```

アプリ本体は `GENERATE_INFOPLIST_FILE = YES` で、Info.plistの実体は
`KyonoWidgetExtension/Info.plist` しか存在しません。**追記先のファイルが無いので、
権限文言は `INFOPLIST_KEY_*` ビルド設定でしか入れられません。**
`INFOPLIST_KEY_NSCalendarsWriteOnlyAccessUsageDescription` は入っています。
**カレンダーのときは足したが、写真のときは足していない**、というだけの話です。

**iOSは、写真への追加権限の説明文が無いアプリでは「写真に保存」を黙って隠します。**
クラッシュもしないしログにも出ません。**今回の症状と完全に一致します。**

### 実機の証拠が裏づけています

本人の共有シート(2枚目・3枚目)には
**AirDrop / ドライブ / Instagram / Kindle / Copy / Assign to Contact / Print /
"ファイル"に保存 / Acrobat / CapCutで背景を削除**が並んでいます。

**これらは全部、画像を受け取るアクティビティです。** つまり画像は正しく渡っています。
**画像を受け取る先が全部出ていて、「写真に保存」だけが無い。**
渡し方の問題ではなく、**「写真に保存」だけが名指しで抑止されている**ということです。

### 前回の僕の見立ては外れていました

B4で僕は「テキスト項目が混ざっているせいで保存系が除外される」と見て、
`ShareImage.swift` に `UIActivityItemSource` を入れさせました。**あれは原因ではありません。**
証拠として、**Print と Assign to Contact は現に出ています**——僕が除外指定した当のものです。
**指示が間違っていました。すみません。**

### やってほしいこと

`INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription` を**アプリ本体のDebug/Release両方**に追加。
文言は本人の言葉づかいで、**何のために要るのかが分かる短文**にしてください。
例:「つくった記録カードを写真フォルダに保存するときに使います」

**`ShareImage.swift` の `UIActivityItemSource` は、消さずに残してください。**
原因ではありませんでしたが、保存系にテキストを渡さないこと自体は正しい実装です。

### 検収基準

- 追加後、共有シートに「写真に保存」が出ること(**シミュレータで確認できます**)
- **押して実際に写真フォルダに入るところまで**見ること。出るだけでは足りません
- Androidに同種の権限漏れが無いか確認

---

## D5 🔴 相談室でキーボードを出すと、会話が全部隠れる

### 症状

入力欄をタップすると、**会話エリアの高さが0になります。** 5枚目のスクリーンショットでは
ヘッダー・注意書き2行・「この悩み、オガトレに届ける」・カテゴリタブ2段・サブタグ1段・
入力欄・「とじる」が全部残っていて、**肝心のやりとりが1行も見えていません。**

**本人の言葉:「文字入力時に表示範囲が極端に狭くなる」。**

### 見立て

**C2と同じ家系の問題です。** 縮まない固定物が場所を取り切って、
可変であるべきスクロール領域に高さが残らない。**ただし今回は「壊れている」のではなく、
「キーボードが出たときに何を残すかを決めていない」のが理由です。**

### 方針 — **足すのではなく、畳んでください**

キーボードが出ている間は、**打っている人に要らないものを消す**。

- 注意書き2行 — 消す
- 「この悩み、オガトレに届ける」 — 消す
- 「とじる」 — 消す(**閉じるは右上の×が既にあります**)
- カテゴリタブ2段+サブタグ — **畳む**。打ち始めた時点でタグ選びは終わっています

**残すのは、会話・入力欄・送信の3つだけ。** これで会話に高さが戻ります。

キーボードが引っ込んだら元に戻すこと。**戻すときにガタッと跳ねないように**してください。

### 検収基準

- キーボードを出した状態で、**直前のやりとりが最低2往復ぶん見えていること**
- 打ち終わって送信したあと、返事の頭が見える位置まで動くこと(B5の挙動を壊さないこと)
- Android側も同じ状態か確認し、同じなら同じ方針で直すこと

---

## D6 🟠 使い方ツアーで「つぎへ」が毎回上下に動く

### 症状

**本人の言葉:「タップするたびにボタンの位置が上下に変わってしまう」。**

ステップごとに本文の量が違うので、その下に置かれたボタンが動きます。
**押した指を毎回置き直さないといけません。** 9ステップぶん続きます。

4枚目を見るかぎり、**ボタンの下は画面の半分近くが空いています。**
内容が上に詰まって、余白が全部下に溜まっている状態です。

### やってほしいこと

**「つぎへ」と「ツアーをとばす」を画面下端に固定してください。** 内容の量にかかわらず動かない。
内容領域は残りを使い、**あふれるステップだけ中でスクロール**させる。

- 内容の高さでボタンが動かないこと
- ステップを進めても**ボタンの位置が1ptも変わらないこと**

### ついでに直してほしいこと

ボタンの文言が **「つぎへ ➡️ （1/9）」** です。**絵文字は外してください**(D7と同じ理由)。
「（1/9）」は既に点のインジケータが上に出ているので、**どちらか一方で十分です。**
僕なら点を残して文字の(1/9)を落としますが、**判断はまかせます。理由を書いてください。**

---

## D3 🟠 記録カードのボタンが不揃い

### 症状

「とじる」と「保存・シェアする」が横並びですが、
**黄色の方だけ文字が2行に折り返して、高さも位置もそろっていません。**
1枚目で、黄色ボタンの上端が「とじる」より上に飛び出して、カードの下端に食い込んで見えます。

### やってほしいこと

**2つのボタンの高さと上端をそろえてください。** 折り返さない文言に詰めるか、
縦積みにするか。**「保存・シェアする」というボタン自体は残します**(本人が7/29に決定済み)。

**カードの下に空いている大きな余白も見てください。** 1枚目・2枚目とも、
ボタンの下に画面の1/3ほどの何も無い領域があります。

---

## 全体の注意

- **Web版配信ファイルは1つも触らないこと**(`index.html` `app-*.js` `videos.js` `sw.js`
  `manifest.json` `soudan-kb.js` `obu-feed.js`)。読むのは自由です
- Androidは `--rerun-tasks` を必ず付けること
- **D1は「直りました」だけの報告を受け取りません。** どこで落ちていたかを観測で示すこと
- 途中で「これはalan5の指示のほうがおかしい」と思ったら**そう言ってください。**
  今日D2で、僕の見立てが外れたまま1ビルド無駄にしています

報告は `REPORT-C2-2026-07-29-testflight-feedback-d.md` にpush＋ドア配達で。
