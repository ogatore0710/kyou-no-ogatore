# TASK-C2-2026-08-05-build26-round4: 本人ラウンド4

発注: alan5（2026-08-05 15:05頃）
背景: ビルド25配信後の本人実機ラウンド4。**本人「いくつかある」＝継続中**。
追加項目はこのファイルにR-7, R-8…として追記していく。ビルド提出はalan5の合図待ち。

## R-6: 動画復帰後に練習ブロックが残って記録の入り口が二重になる（本人赤ペン・FullSizeRender.jpeg）

- 現象: ツアーで1本目の動画（W-2練習）からアプリへ戻ると、復帰カード
  「おかえり！／1日目の記録をつけにいく」が出るが、その上に**R-2の練習ブロック
  （ピル「＼ きろくの れんしゅう ／」+2行+「きょうやった！」）が残ったまま**。
  記録の入り口が2つ並び、本人赤ペンで練習ブロック側全体に取り消し線。
- 原因（alan5がコードで確認済み）: practiceBlockの表示条件は
  `fdGuideActive && !videoTapped`（iOS `OnboardingViews.swift:1282`付近）、復帰カードは
  `showDoneNudge && cardResult == nil` で**独立**。videoTappedは「きょうやった！」練習ボタン
  経由でしか立たないため、動画タップ→復帰の経路では両方trueになる。
- 変更: **復帰カードが出ている間は練習ブロックを出さない**。
  iOS: 表示条件を `fdGuideActive && !videoTapped && !showDoneNudge` に。
  Android: 同ブロックの対応条件に同じガードを追加（該当箇所は実装時に特定・alan5未確認）。
- 挙動の連鎖に注意: 復帰カードから「1日目の記録をつけにいく」を押した後の流れ
  （練習合流・cardResult表示）は一切触れない。復帰カードを閉じる/戻る経路で
  showDoneNudgeがfalseに戻る場合に練習ブロックが復活するのは**許容**（未記録なら
  練習の誘いが戻るのはむしろ正しい）。

## 検収

- `npm test` 再実行。
- 実描画: 動画復帰後の同状態（ライト）で、復帰カードのみ表示・練習ブロックが無いこと1枚。
- 復帰前（動画未タップ）の状態で練習ブロックが従来どおり出ることの確認1枚。

---

# 追加項目 R-7（2026-08-05 15:30頃追記）: 動画をひらく練習のピンク強調＋ふわふわ＋改行（本人モック確認済み）

本人指示の経緯: 「動画をひらく練習ってこのはピンクで目立たせて／タップする部分もピンク枠か
ピンクで目立たせて／1度ここで見せて」→ alan5がモック提示 → 「もっと大きい方がいいね」→
「動き入れて／ひらくよ！で改行して」→ ふわふわGIF+改行版モック提示済み。
見た目の正解=alan5スクラッチパッドの`mock-pink-highlight-v3.png`と`pill-float-preview.gif`。

## 変更（両OS・ツアー中のR-4ブロックの改修）

1. **ピル「＼ 動画をひらく練習 ／」のピンク化+拡大**:
   tealSoft/tealInk → **pinkSoft地(#FFEDF3)+pinkInk文字(#C04570)**（既存トークン使用）。
   文字12pt→**16pt** black900・パディングも比例拡大（横14→18pt・縦4→6pt目安）。
2. **ふわふわ動き**: ピルをゆっくり上下（**±4pt・周期1.6s・easeInOut・無限リピート**）。
   `reduceMotion`時は静止。実装はwithAnimation(.easeInOut(duration:0.8).repeatForever(autoreverses:true))系の
   標準手法でよい。**演出はピルのみ**（カードや案内行は動かさない）。
3. **案内行の明示改行**: 「今は1本目だけタップできるよ！\n動画をひらいてもどってきてね！」
   の2行・中央寄せ（自動折返しに任せない）。
4. **1本目カードのピンク強調**: VideoRowの既存`hero`スタイル（pink枠2.5pt+pinkSoft地）を
   ツアー中の1本目に適用（`hero: isFirst`）。減光中の2本目以降は従来どおり。

## 検収

- 実描画: ライトの静止1枚+動きのフレーム連続抽出（0.4s間隔×3枚で上下が分かること）。
- reduceMotion時に静止することの確認（コード根拠でよい）。
- ダークでピル(pinkSoft/pinkInkのダーク値)が沈まないか実描画1枚。

# 追加項目 R-8（同時追記）: ホームのセグメントと動画カードの間隔

本人指示: 「あなた用 あさ よる のその下のボタン近いからはなして」（IMG_8803・ホーム）。

- ホーム「きょうの1本」のセグメント選択（あなた用/あさ/よる）と直下の動画カード群の間隔を
  広げる。現状の実測を確認の上、**+6〜8pt**目安で（過剰に空けない・引き算の範囲で）。
- 両OS同値。実描画ビフォー/アフター各1枚。
