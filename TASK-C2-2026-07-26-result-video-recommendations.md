# タスク（C2/appdev向け）— 診断結果画面「おすすめ動画3本」が丸ごと未実装（アプリの核心機能）

## 背景

全画面完全性監査の#resultフェーズでappdev自身が発見・報告した follow-up。「かたさタイプ診断→
あなた専用のおすすめ動画3本」は、このアプリの提案価値の中心と言ってよい機能だが、
現状ネイティブの結果画面には**動画の推薦リストが1本も表示されていない**（rPT等の周辺テキストは
既に対応済み）。

**朗報**: 必要なパーツの大半は既に他の場所で移植済みで、今回は主に配線作業になる。
- `rotationIndex()`（+6h・日付ベースの決定的ローテーション）→ Step4で既にCardCore/QuizEngineに
  移植済み（decideTypeのタイブレークで使用中。テスト`testRotationTiebreakIsDeterministicAndVariesByDate`
  も既存）
- 動画サムネイル表示（`videoCard`相当）→ 見た目パリティ第2弾でKyonoAsyncImageとして実装済み
  （検索・再生リスト画面で使用中）
- 動画カタログ（`V`/videos.js）→ 既にカタログ移植済みのはず（検索・再生リストが参照している）

## Web版の仕様（app-quiz.js:238-337が正本）

### 1. `currentRx(typeKey)`（app-quiz.js:238-255）— 決定的な動画選出ロジック
```
T = TYPES[typeKey]（各タイプのrx=固定動画1本・pool=候補プールの配列。TYPES定義はapp-quiz.js:45-90）
need = 3 - T.rx.length（通常2本を追加選出）
r = rotationIndex()
spacing = floor(pool.length / need)
picks: r起点からspacing間隔でpoolを巡回し、rx・既選出と重複しないものを拾う（need本になるまで）
戻り値 = T.rx + picks（計3本の動画キー配列）
```
**乱数不使用・日付のみで決まる決定的ロジック**（rotationIndex経由）。TYPES.rx/poolの中身は
app-quiz.js:45-90の各タイプ定義からそのまま抽出すること（§1-2手写し禁止のため機械抽出）。

### 2. 結果画面への表示（app-quiz.js:295-337）
- `rxHead`: 見出し文言（プラン中は「まずは「{area}」から！2週間続けてみて」、通常時は
  「柔らかさを守る毎日の1本をどうぞ」）
- `rxList`: 3本の動画カード（プラン中のみ「①まずほぐす」「②メインの1本」「③しあげ」のラベル付き）
  + 「▶ 3本続けて再生する」ボタン（YouTube `watch_videos`複数再生リンク）
- `rRotateNote`: 「おすすめは3日ごとに自動で入れ替わります」の注記（既存要素・hidden切替のみ）
- `worryExtra`: Q5「いちばんの悩み」の回答に対応する追加1本（`WORRY[saved.worry]`。3本と重複しない
  場合のみ「＋ {悩みラベル}」バッジ付きで追加表示）

### 3. `rDoneNudge`/`rTourBtn`（優先度低・任意）
はじめの1本ガイド専用の復帰ナッジ・ツアー継続ボタン。Step5a/Phase3のチュートリアルv2フラグ機械
（fd/fdday/tourpend等）と関連する可能性が高いため、**既存の類似機構と重複しないか確認した上で
対応するか判断してよい**（このタスクの必須範囲ではない。時間が無ければ見送り可）。

## やること

- `TYPES`データ（各タイプのrx/pool配列）を機械抽出し、両OSへ1:1移植（判定ロジックとは別に、
  動画選出専用のデータとして）
- `currentRx()`相当のロジックを実装（rotationIndexは既存のものを再利用）
- 結果画面にrxHead・rxList（3本のサムネイル付きカード+続けて再生ボタン）・rRotateNote・worryExtraを追加
- rDoneNudge/rTourBtnは任意（上記参照）

## 検収基準

- [ ] 診断結果画面に3本のおすすめ動画（サムネイル付き）が表示される
- [ ] 同一日付・同一タイプなら常に同じ3本が選ばれる（決定的であることを確認）
- [ ] 日付を変えると（またはWeb版の同日実出力と比較して）選出される動画が一致する
- [ ] 「▶ 3本続けて再生する」ボタンでYouTubeの複数再生リンクが開く
- [ ] worryExtra（悩み別の追加1本）が正しい条件で表示される
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- ロジック・判定・データ構造（選出アルゴリズム自体）は変更しない。Web版の忠実な移植のみ
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で検収基準7項目のPASS/FAILとスクショを含めて報告してください。
