# TestFlight実機フィードバック（本人・ビルド2）— B1〜B7

alan5です。**本人が実機で触って出た指摘です。写真つき。**
僕のほうで**原因まで特定できたものは特定してあります**。裏取り済みの事実と、僕の仮説を
分けて書くので、**仮説のほうは鵜呑みにせず確かめてから**直してください。

---

## B1 🔴 ダークモードで、黄色いボタンの文字が白くて読めない

**本人の言葉:**「黄色い枠の背景に白い文字だと視認性が悪い」

**原因は特定済みです。しかもWeb版から受け継いだ欠陥です。**

```
index.html:99   .btn{ ... color:var(--ink)}
index.html:101  .btn-primary{background:var(--yellow); ...}
index.html:73   :root{--yellow:#FFD93B; --ink:#3A3A35;}
index.html:112  body.dark{ ... --ink:#F2EDE1; ...}   ← --yellow は上書きしていない
```

つまり**ダークモードでは「黄色の背景」はそのままなのに「文字色」だけ near-white に反転する。**
`#F2EDE1` on `#FFD93B` は**コントラスト比およそ1.2:1**。ほぼ読めません
（黒 `#3A3A35` なら約8:1）。

**ネイティブはWeb版を忠実に写した結果、この欠陥まで一緒に写しています。**
パリティを目標にすると、こういう受け継ぎが起きます。

### やること
**黄色いボタンの文字は、ライト・ダークどちらでも濃い色（`--ink`のライト値 `#3A3A35`）で固定。**
ダークモードでも反転させないこと。

**光彩（グロー）は入れないでください。** 本人が選択肢として挙げていましたが、
装飾を足す方向であり、しかも薄い文字のままコントラストは上がりません。
**ライトモードで既にそうなっている「黄色地に黒文字」に揃えるのが正解**です
（写真3枚目の「送信」ボタン・「保存・シェアする」ボタンが、ライトでは黒文字なのが確認できます）。

**同じ型が他に無いか探すこと。** `--yellow` 系の背景に `--ink` を載せている箇所を
両OSで洗い出してください。**穴を1つ塞いで終わりにしないこと**（PRINCIPLES 140条）。

### Web版について
**index.html等の配信ファイルには触らないこと。** ただし**同じ欠陥がWeb版の本番に生きている**
ので、事実として報告に書いてください。直すかは本人が決めます。

---

## B2 🟠 記録カードの文字同士の距離が近い

**本人の言葉:**「文字同士の距離が近いです」（写真2枚目・3枚目のカード画像）

**ここは僕も原因を掴んでいません。推測で触らないでください。**

「2 日目！」と「連続記録2日」の縦の間隔が詰まって見えます。
考えられる先は2つあり、**どちらの話かを先に確定させてください**:
- **(a) 生成されるカード画像そのもの**（CardCoreの描画）
- **(b) ホームの「続けた日数（通算）」カード**（画面上のテキスト）

**(a)ならgoldenテスト55件が通っていることは根拠になりません。** goldenはネイティブの出力を
そのまま固定しているだけで、**Web版と一致している証拠にはならない**からです。

### 確かめかた（これを守ってください）
**Web版で同じ日付・同じ連続日数のカードを出して、2枚を並べること。**
`scripts/ui-compare-web.js` が使えます。**ピクセル数値ではなく、並べた絵で判断**してください。

### 合格条件
**同じ条件で出した2枚のカードが、行間について見分けがつかないこと。**
差が無ければ「Web版と同じでした」と報告してください。**それも立派な結果です。**

---

## B3 ⏸ 「保存・シェアする」ボタンの要否 — 保留

本人から「この項目は必要ないと思う」と出ましたが、**B4と噛み合わないので確認したところ、
「ボタンは残す。中身を直す」**との回答でした。**現状維持。触らないこと。**

判断の記録としてHANDOFFに残しておいてください。

---

## B4 🔴 共有シートに「写真に保存」「画像を保存」が出ない

**本人の言葉:**「共有ボタンを開いた際、「画像をダウンロード」および「写真に保存」の
ボタンが表示されません」

**原因はほぼ特定できています。**

```swift
ShareImage.swift:18
UIActivityViewController(activityItems: [uiImage, text], applicationActivities: nil)
```

**画像とテキストを同時に渡しています。** iOSの「写真に保存」(`saveToCameraRoll`)系の
アクティビティは**渡された項目が全部画像でないと候補から外れます。** テキストが1つ混ざって
いるせいで、保存系だけが丸ごと消えている。

**写真3枚目がその証拠です。** 共有シートの上段が「あ」アイコンの**テキスト項目**として
`#きょうのオガトレ 2日目！` を表示していて、画像のプレビューになっていません。

### やること
`UIActivityItemSource` を使って、**保存系のアクティビティには画像だけを渡し、
テキストは件名(subject)やSNS系のときだけ添える**形にしてください。
「画像は渡すがテキストは捨てる」だけの雑な直しにしないこと。**SNSに投稿したときに
ハッシュタグが消えては本末転倒**です。

**直す場所は1箇所で済みます。** 呼び出し元3つ（`BragView.swift:190` /
`HomeView.swift:605` / `MyRecordView.swift:130`）はすべて `ShareImage.share` を通っています。

### 合格条件
**共有シートに「写真に保存」（もしくは「画像を保存」）が出て、実際に写真フォルダに入ること。**
かつ**Instagram等に共有したときテキストが付いてくること。** 実機で確認してください。

---

## B5 🔴 相談室が自動で下にスクロールしない

**本人の言葉:**「自動で下にスクロールするようにしてください。ボタンを押しても画面が
切り替わらないので、ボタンが押さったかわからない」

**確認しました。実装がありません。**
`SoudanSheetView.swift:409` に `ScrollView` はありますが、**このファイルに
`ScrollViewReader` も `scrollTo` も1つもありません。**

写真4枚目で、新しい返事「まずはこれ」がチップ行の裏に隠れて途中で切れています。
**チップを押しても画面が動かないので、押せたのかどうかが分からない。**

これは見た目の問題ではありません。**押した手ごたえが無い＝壊れているように見える**ので、
今回の5件でいちばん体験に効きます。

### やること
- 新しい発言が増えたら**いちばん下へスクロール**（`ScrollViewReader` + `scrollTo(anchor: .bottom)`）
- **「動きを減らす」設定がオンのときはアニメーションなしで飛ぶ**こと（A8で入れた分岐と揃える）
- **最後の吹き出しがチップ行・入力欄の裏に隠れないこと**（下の余白が足りているか確認）
- **Android側にも同じ穴が無いか確認**すること。あれば同時に直す
- **Web版がどうしているかを先に読んでから**実装すること（`app-*.js`）。読むのは自由、変更は禁止

---

## B6 🔴 ウィジェットにキャラクターが出ていない

**本人の言葉:**「ウィジェットは、正方形と横長の両方が表示されているのを確認できました。
デザインがシンプルすぎます」

**まず良い知らせ。実機で両サイズ出ました。App Groupが通っている証拠です。**

**そのうえで、「シンプルすぎ」は設計の問題ではありません。出るはずのものが出ていません。**

```swift
KyonoWidgetExtensionBundle.swift:118  Image(state.chara.rawValue)   // 小
KyonoWidgetExtensionBundle.swift:136  Image(state.chara.rawValue)   // 横長
```

**コードは両方でキャラクターを描いています。写真2枚のどちらにもいません。**
小さいほうが文字1行だけに見えるのは、**消えたキャラの64pt分の空白がそのまま残っている**からです。

### 僕が確認した範囲（つぶし込み済み）
- `CharaAsset` の rawValue（`chara-cheer` 等）と**ファイル名は一致**している
- 6枚のPNGは**ウィジェット拡張のResourcesビルドフェーズに入っている**（`687E0762...`）
  ＝ターゲット所属は正しい
- データ経路は生きている（「2日つづいてる」は正しく出ている）

### 僕の仮説（断定していません・確かめてから直すこと）
**PNGが素のファイルとして置かれていて、アセットカタログに入っていないこと。**
ウィジェットは別プロセス（WidgetKitのホスト）が描画するため、名前で引く画像の解決が
アプリ内と同じに働かないことがあります。

→ **拡張ターゲットに `Assets.xcassets` を作ってそこへ移す**のが本筋の直しです。
それで駄目なら `Bundle.main` からパスで読んで `Image(uiImage:)` に渡す形に落としてください。

### 合格条件
**実機のホーム画面で、小・横長の両方にキャラクターが出ること。** シミュレータのプレビューで
出ても実機で出ないことがあるので、**実機で確認**してください。

### そのあと（キャラが出てから判断すること）
**キャラが出た状態を見てから、まだ寂しいかを本人に見てもらいます。**
いま先回りして装飾を足さないでください。**空白の原因が消えるだけで印象は変わります。**

---

## B7 🟠 アプリの表示名が英語「KyouNoOgatore」になっている

写真でウィジェットの下のラベルが **`KyouNoOgatore`** です。隣のPWAのアイコンは
「きょうのオガトレ」と日本語で出ています。**本人からの指摘ではありませんが、明らかな不揃いです。**

**原因はこれだと見ています（要確認）:**
- ウィジェット拡張は `GENERATE_INFOPLIST_FILE = NO` で、`Info.plist` に
  `CFBundleDisplayName = きょうのオガトレ` が**入っている**
- **アプリ本体は `GENERATE_INFOPLIST_FILE = YES`** で、
  **`INFOPLIST_KEY_CFBundleDisplayName` が設定されていない**
  → `PRODUCT_NAME`（＝`KyouNoOgatore`）にフォールバックしている

→ アプリ本体に `INFOPLIST_KEY_CFBundleDisplayName = きょうのオガトレ` を設定してください。
**ホーム画面のアプリアイコンの下も英語のままのはず**なので、そこも一緒に直ります。

---

## 順番

**B5 → B4 → B6 → B1 → B7 → B2**

B5が体験にいちばん効きます。B2は調査から入るので最後で構いません。

## 守ること

- **Web版の配信ファイルは変更しない**（`index.html` / `app-*.js` / `videos.js` / `sw.js` /
  `manifest.json` / `soudan-kb.js` / `obu-feed.js`）— 読むのは自由
- **Androidのテストは `--rerun-tasks` で回すこと**（UP-TO-DATEの偽の緑を避ける）
- **B2とB6は仮説の段階です。** 確かめて違ったら「違いました」と報告してください。
  **僕の見立てを守るために事実を曲げないこと。**

## 完了報告

`REPORT-C2-2026-07-29-testflight-feedback-b.md` にpush＋ドア配達。
**B6は実機写真**、**B4は実際に写真フォルダへ保存できたか**まで書いてください。
終わったらビルド3を上げます（`CURRENT_PROJECT_VERSION` は 2 → 3）。
