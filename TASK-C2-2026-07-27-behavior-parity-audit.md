# タスク（C2/appdev向け）— 挙動（タイミング・アニメーション・時間追従）の全面パリティ監査

## 背景（本人指示）

本人から「挙動のところ、Web版と遜色なくできているか確認して」との指示。

これまでの監査（12セクションの完全性監査・その後のalan5の抜き打ち調査）は
**「要素・文言が存在するか」**が中心で、**「挙動＝タイミング・アニメーション・時間追従」は
一度も体系的に見ていない**。実際、今日この盲点から3件の欠落が出た:

1. **相談室の段階表示**（実機タイマー計測で発見）— Web版はタイピングドットを挟んで1.5秒間隔で
   1つずつ出るのに、ネイティブはタップ後416msで応答全部が一気に表示されていた
2. **自動テーマの時刻判定**（コード調査で発見）— 19時〜朝5時のルールが丸ごと欠落
3. **60秒ごとの時間追従**（同）— 開いたまま時間が経っても何も更新されない

**重要な教訓**: 1件目は`npm test`もgradle testも全部緑のまま素通りした。
**ユニットテストとコードレビューだけでは挙動の差は絶対に見つからない**。

## 方法論（必ず守ること）

- **実際にアプリを動かして確認すること**。コードを読んで「同じロジックが入っているからOK」で
  済ませない（それで今日の1件目を取り逃している）
- **Androidは時間を測れる**。alan5が今日使った手法をそのまま使ってよい:
  ```
  操作(adb shell input tap)と同時に高頻度でスクリーンショットを撮り、
  経過ミリ秒をファイル名に埋めて、どの時点で何が出ているかを突き合わせる
  ```
  （Python等で`tap`実行→ループで`adb exec-out screencap -p`＋経過ms記録）
- **iOSはタップ自動化ができない**ため、シミュレータで手動操作しながら目視確認する
- 判定は「ミリ秒まで完全一致」ではなく**「体験として遜色ないか」**。
  段階的に出るべきものが一気に出ていないか／動くべきものが止まっていないか、が本質

## 監査対象（Web版から機械抽出した実物リスト。この順で確認）

### A. アニメーション（index.htmlの`@keyframes`全10種）
| keyframe | 使用箇所 | 何のアニメか |
|---|---|---|
| `cpop` | `#cheer>div` .3s | ホームの応援メッセージがポップして出る |
| `doneNudgePulse` | `.done-btn.nudge-pulse` 0.7s×2回 | 動画から戻った時「きょうやった！」ボタンが2回パルス |
| `fdBob` | `.fd-point` 1.4s infinite | はじめの1本ガイドの「👇つぎはここ」が上下に動き続ける |
| `fdBreathe` | `.fd-breathe` 1.8s infinite | ガイド中の対象ボタンが呼吸するように明滅し続ける |
| `fdPop` | `.fd-cardpop` .5s | ガイドのカードがポップして出る |
| `obpop` | `.ob-sheet` .28s | オンボーディングシートが拡大しながら出る |
| `sddot` | `.sd-dots span` 1s infinite | 相談室のタイピングドット（※今日実装済み・**iOSは要目視確認**） |
| `sdpop` | `.sd-pop` .18s | 相談室の吹き出しが出るときのポップ |
| `sdup` | `.sd-sheet` .25s | 相談室シートが下から上がってくる |
| `toastIn` | トースト .25s | トーストが出るときの動き |

`transition:`指定も6箇所ある。あわせて確認すること。

### B. 時間差のある挙動（`setTimeout`/`setInterval`から抽出）
- 紙吹雪`launchConfetti`の演出時間と後片付け（index.html:1943 `DUR+600`）
- 記録カード生成中の「カードをつくってます…」表示（app-card.js:60,65 フォント待ち最大2.2秒）
- 「コピーしました✅」が2秒で元の文言に戻る（index.html:2949）※実装済みのはず・要確認
- 検索入力の180msデバウンス（app-search.js:55）
- ツアー起動の350ms待ち（index.html:4293）／オンボ起動の600〜1500ms待ち（4454）
- 起動スプラッシュの最低表示時間とフェードアウト（index.html:3930-3932）
- 「きょうの1本」への自動スクロール（index.html:4393）

### C. 時間追従（別タスク`auto-theme-time-rule`で対応中。重複しないこと）
`setInterval(refreshDay,60000)`まわりは別タスクの担当なので**このタスクでは扱わない**。

### D. reduced-motion（アクセシビリティ）
Web版は`sdReduced()`/`obReducedMotion()`で`prefers-reduced-motion`を見て、
アニメーションを即時表示に切り替えている。ネイティブは`OnboardingScreens.kt`のコメントに
「今回は見送る」と明記されており未対応。
**OS設定（Android「アニメーションを削除」/ iOS「視差効果を減らす」）を読んで、
オンにしているユーザーには演出を飛ばして即時表示にする**対応を入れること。

## やること

- A〜B、Dについて、Web版とネイティブ（Android/iOS両方）の挙動を突き合わせる
- **差があればその場で直すところまで含む**（調べて報告するだけのタスクではない）
- セクションごとに区切ってコミットし、進捗が分かるようにすること

## 検収基準

- [ ] A（アニメーション10種＋transition）を1つずつ確認し、結果を一覧で報告
- [ ] B（時間差のある挙動7項目）を1つずつ確認し、結果を一覧で報告
- [ ] D（reduced-motion対応）を実装
- [ ] 見つかった差は全て修正済み
- [ ] Androidは実測（時間を測った証拠つき）、iOSはシミュレータ目視で確認したことを明記
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- ロジック・判定・データ構造は変更しない（演出・タイミングの修正のみ）
- Cの時間追従（`auto-theme-time-rule`タスクの担当）には手を出さない
- Web版（PWA）側の配信ファイルは一切変更しない

## 優先度

先に出している2件（`ios-sdbubble-unstable-id`・`auto-theme-time-rule`）を片付けてから着手。
このタスクは量があるので、セクションごとに区切って進めて構いません。

## 報告

セクションごとに区切って進め、A/B/Dそれぞれ完了時に一覧で報告してください
（項目ごとに「一致 / 差があったので修正 / 該当なし」が分かる形で）。
