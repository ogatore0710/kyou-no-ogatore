# タスク（C2/appdev向け）— ホーム画面・マイ記録タブの構造修正（本人指摘: ホームがWeb版とかなり違う）

## 背景

本人が実機/シミュレータで最新版を見て「ホーム画面の見た目がWeb版とかなり違う」と指摘。調査の結果、
**単なる見た目の差ではなく、情報構造そのものが違っていた**ことが判明:

- Web版のホーム（index.html:602-708 `<section id="home">`）は「きょうのひとこと」「かたさチェック」
  「オガトレ相談室」「きょうの1本」「続けた日数」という**それぞれ独立したイラスト付きの大きなカード**の
  積み重ねで構成されている
- ネイティブ版は「きょうの1本」「続けた日数」の2カードまではあるが、**残りを「メニュー」という
  プレーンなボタンリストに圧縮**しており、しかもその中身（図鑑・じまんカード・せんぱいの声・設定）は
  **Web版ではそもそもホームに存在しない**（`TAB_OF`マッピング=index.html:1536で全部`history`
  ＝マイ記録タブに属している）。かたさチェック・オガトレ相談室という**ホームの主役級カードが
  ネイティブでは完全に欠落**していた

**方針**: 情報の置き場所をWeb版どおりに正す（ホームに無いものをホームから追い出し、ホームにあるべき
ものを追加する）。既存の各画面（図鑑・じまんカード・せんぱいの声・設定）の中身自体は作り直さなくてよい
——**どこから開けるか（導線の入口）を直すだけ**でよい。

## やること

### 1. ホーム画面の再構成（index.html:602-708が正本）

現在の「メニュー」カード（図鑑・せんぱいの声・じまんカード・設定へのリンク）を**丸ごと削除**し、
以下の順でカードを並べる:

1. **きょうのひとこと（qbubble）**: 画面最上部、カードの外。`chara-hitokoto.png`アバター＋日替わりの
   ひとこと（QUOTES配列から日付ハッシュで選択。index.html:2141 `renderQuote()`参照）
2. **かたさチェックカード（ckCard・index.html:627-640）**: sec-head「かたさチェック」＋
   `chara-3.png`イラスト＋「タップするだけ30秒でチェック✅」の説明＋黄色い大きい
   「チェックをはじめる」ボタン＋安全注意文。**チェック済みの人は「もう一回チェックする」に文言が変わり
   ボタンがghost色になり、カードがミニ化して続けた日数カードの直後に移動する**（index.html:2929-2937
   `renderHome()`のckCard分岐ロジック参照）
3. **オガトレ相談室カード（soudanCard・index.html:643-651）**: sec-head「オガトレ相談室」＋
   `chara-hitokoto.png`イラスト＋「からだの悩み オガトレに聞いてみて💬」＋「💬 相談する」ボタン＋
   おすすめチップ（`renderSoudanEntry()`参照）
4. **きょうの1本カード（todayCard）**: 既存実装のまま（あなた用/あさ/よるのセグメント切替含む）
5. **続けた日数カード（streakCard）**: 既存実装のまま

**本人が新規ユーザーの場合のみ表示される`anchorCard`（いつやる派？の選択）・`welcomeBack`
（再開ユーザー向け）・`recheckCard`（2週間再チェック）は今回のスコープ外**（優先度低・既存の
オンボーディング/はじめの1本ガイド系フローとの整合確認が別途必要なため）。まずは「いつものホーム」
（かたさチェック・相談室・きょうの1本・続けた日数の4枚）を正しく再現することを優先。

### 2. マイ記録タブの再確認（index.html:752-800が正本）

Web版のマイ記録タブは「続けた記録」「カード図鑑（バナー＋『図鑑をひらく』ボタン）」「カレンダー」
「とどくメーター（バナー＋『見てみる』ボタン）」「お楽しみ機能＝じまんカード/せんぱいの声/
ひとことにっきへの入口（バナー＋『見てみる』ボタン）」「続ける設定」の順のカード積み重ね。

Phase 3で図鑑・じまんカード・せんぱいの声・設定の**画面自体は実装済み**なので、**マイ記録タブから
これらへ正しく遷移できるようになっているか確認**し、なっていなければ導線を追加/修正すること
（画面の中身は作り直さない）。ホーム側から直接これらの画面へ行けていた古い導線（メニューリスト経由）は
削除でよい。

## 検収基準

- [ ] ホーム画面が「きょうのひとこと→かたさチェック→オガトレ相談室→きょうの1本→続けた日数」の
      順で表示される（実機/シミュレータでスクショ確認）
- [ ] かたさチェックカードにイラスト（chara-3.png）が表示され、チェック済み/未チェックで
      ボタン文言・色・カード位置が変わる
- [ ] オガトレ相談室カードにイラスト（chara-hitokoto.png）が表示され、タップで相談室が開く
- [ ] マイ記録タブから図鑑・じまんカード・せんぱいの声・設定へ正しく遷移できる
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- 図鑑・じまんカード・せんぱいの声・設定の画面の中身自体は作り直さない（導線の入口だけ直す）
- anchorCard/welcomeBack/recheckCardの新規実装は今回のスコープ外
- 「きほんのき」（使い方タブFAQ）は変更しない
- ロジック・判定・データ構造は変更しない（表示構造・導線の修正のみ）
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で以下を含めること:
- 検収基準6項目のPASS/FAIL
- 新ホーム画面のスクショ（かたさチェック未チェック時・チェック済み時の両方）
- 消費トークンの概算
