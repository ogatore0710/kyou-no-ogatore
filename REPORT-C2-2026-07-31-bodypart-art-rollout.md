# 体の部位イラスト本生産(11部位・記録カード風) — 完了報告

`TASK-C2-2026-07-31-bodypart-art-rollout.md`。本人トーン選定(記録カード風)を受けて
「からだの場所」タグ11部位を本生産・適用まで完了。

## やったこと

新規スクリプト`scripts/gen-bodypart-art.py`(トライアル用スクリプトとは別)で11部位を生成。

- **トライアルで露見した欠陥を修正**: `background=transparent`を必ず指定した上で、生成後に
  alpha bboxトリム→正方形キャンバスへ再配置(既存チップと同じ寸法整形)。全11枚とも
  透過確認済み(`PIL`でコーナーピクセルのalpha=0を実測)。
- **ポーズと表情で伝える方向**(部位の図解はしない): 全身=ニュートラルな直立/肩・肩甲骨=
  肩をすくめる/首・肩こり=首をかしげて自分の首をさする/姿勢・背中=両手を上げて伸びをする/
  股関節=あぐら(蝶々のポーズ)/開脚=べたっと開脚して座る/もも裏=前屈でつま先タッチ/
  太もも・お尻=片足立ちで足を後ろに引く(立ち前もも伸ばし)/腰=腰に手を当てて反る/
  ひざ・O脚=両手をひざに当てる/足首・足うら=座って自分の足をもみもみ。文字なし。
- スタイルアンカーはトライアルで承認済みの`koshi-card.png`/`kata-card.png`そのものを使用
  (assets/cards/の生画像より安定してブレが少なかったため)。

## ⚠️「腰」のキー衝突を解消(重要な設計判断)

B-2時点では「からだの場所」タグの「腰」はオンボの`chip-youtsuu.png`(かたさチェック
ワーリーチップ)を再利用する設計だった。今回タグ側だけ記録カード風に差し替えると、
同じファイルを共有しているオンボのチップまで変わってしまい、「オンボのチップ類は触らない」
という指示に反する。そのため**「腰」タグ専用の新規キー`koshi`を新設**し、
`SearchView.swift`/`SearchScreen.kt`の`bodyTagChipKey`マッピングを`"腰": "youtsuu"` →
`"腰": "koshi"`に変更した。`chip-youtsuu.png`(オンボ側)は無変更(`git diff`で確認済み)。

## 適用

- iOS `ChipArt/chip-<key>.png`・Android `drawable-nodpi/chip_<key>.png`の両方に11ファイル
  配置。**MD5完全一致確認済み**(11ファイル全て)。
- `SearchView.swift`/`SearchScreen.kt`の`bodyTagChipKey`マッピングを更新(上記「腰」の1件)。
- 対象外(オンボのかたさ・部位・時間帯チップ=旧ステッカー調12種)は無変更。`chip-youtsuu.png`
  への変更が無いことを個別に確認済み。

## 検分用グリッド

`.art-staging/bodypart-rollout/grid-light-dark-32-22pt.png`: ライト/ダーク実チップ背景色
(obgLight/obgDark[1]相当)に合成した状態で、32pt相当・22pt相当それぞれの縮小結果を並べた
4段グリッド(11枚×4パターン)。

目視所見: 32pt・22ptのどちらでもポーズのシルエットで判別できる。特に開脚(べたっと座って
脚を大きく開く)・もも裏(前屈)・足首(座って足をもみもみ)は縮小してもポーズが明確。
太もも・お尻(片足立ち)は他よりやや複雑だが、22ptでも「何か特徴的なポーズ」であることは
判別できる。

## 確認済み

- [x] 11枚とも透過(alpha=0)確認済み
- [x] iOS/Android間でMD5完全一致(11ファイル)
- [x] `xcodebuild build` 成功・`gradle assembleDebug` 成功
- [x] `npm test` 459 checks green
- [x] オンボの`chip-youtsuu.png`(かたさチェック「腰」)は無変更(`git diff`で確認)
- [x] オンボの他11種(かたさ4種+時間帯4種+worry3種)は無変更
- [ ] alan5のグリッド検分(発注書どおり、この検分ゲート後は適用済みでOKという前提で
      先に適用まで完了させています。もし差し戻しがあれば該当分のみ再生成します)

## 次

alan5の検分待ち。差し戻しがあれば個別に再生成、OKであればこのまま次のビルドへ含めます。
