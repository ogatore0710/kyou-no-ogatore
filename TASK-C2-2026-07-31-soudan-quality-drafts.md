# 相談室(オガトレ相談室)返答パターン拡充 — 一部実装済み・残りは下書きから次回

発注元: 本人(2026-07-31 15:20頃、口頭で直接)「相談室の質を高めて。色んな相談への返答パターンを
大量に用意して。ファブル使って」。その後「15艦隊へ増強」の追加指示あり。16時締切の時間制約下で
Fable(メイン実装1体)+下書き専門サブエージェント計14〜15体を並列で走らせた。

## 進捗(2026-07-31 16:00時点)

- **✅ メイン実装は完了・コミット済み**(`4bc2111`): 既存25インテントにkw約90語追加、
  新インテント2件(`seiza`/`sports`・**本文は本人検収前**)、smalltalk 10グループ追加
  (54→64)。redFlags/crisis/エンジンは無変更(verify-kb-sync緑で確認済み)。`npm test` 459
  checks green。iOS(`SafetyCore`)/Android両方のsoudan-kb.json資産に反映済み。
- **📝 下書きのみ・未実装**: 14ファイル(先行5本=`3b7b9e7`、増強分9本=`696a6b6`)、
  計750件超の言い回し→返答案が`soudan-pattern-drafts/`にある。**これらはメイン実装には
  含まれていない、別の調査結果**。次にこのタスクを拾う人はこのファイル群から必要な分だけ
  選んで`soudan-kb.js`へ反映すること(全部を無理に入れる必要はない・重複チェック要)。

## 前提の発見(全エージェント共通)

相談室の本体は`index.html`の`sd`プレフィックス関数群(`sdKb`/`sdNorm`/`sdRedFlagHit`/
`sdScoreIntents`/`sdAnswerIntent`等)で、実際の会話データは**`soudan-kb.js`**
(グローバル`SOUDAN_KB`・intents 126件・redFlags・crisis・commonFollowups・smalltalk)に
分離されている。マッチ優先順位: crisis → redFlags → intents(キーワードスコアリング) →
smalltalk → フォールバック。

**「大量にパターンを用意」の実態**: 調査の結果、既存126インテントのカバレッジは元々かなり
広く、依頼にあった具体例(猫背・反り腰・デスクワーク・産後の体形等)はほぼ全て既存インテントで
拾えることが判明した。したがって新規に必要なのは「白紙から新規インテントを126個追加」では
なく、**①既存インテントのkw(キーワード)バリエーションを増やす ②言い回しの違いによる
すり抜けを防ぐ ③本当に未カバーな少数の穴(下記)を埋める**、の3種類。

## 下書き5ファイル(`soudan-pattern-drafts/`・計約289件の言い回し→返答案)

| ファイル | 担当範囲 | 件数 |
|---|---|---|
| `soudan-draft-bodyparts.md` | 肩こり・首/腰/前屈・股関節周り | 46 |
| `soudan-draft-sleep-etc.md` | 睡眠/むくみ/自律神経・だるさ | 45(+境界例6) |
| `soudan-draft-motivation.md` | 続かない/効果を感じない/罪悪感/時間がない/モチベ | 60 |
| `soudan-draft-purpose.md` | 姿勢/反り腰/デスクワーク/産後の体形等(目的別) | 114 |
| `soudan-draft-safety.md` | **危険信号(要受診/要注意)の穴** | 24 |

各ファイルとも「本体コードは一切変更せず、下書きのみ」という指示で作業させたため、
**index.html・soudan-kb.js・videos.jsは今回一切変更していない**(`git status`で確認可能)。

## ⚠️ 最優先で見てほしい発見: 安全域(redFlags)の穴

`soudan-draft-safety.md`が最重要。既存の`redFlags.kw`/`stateKw`を`grep`で実照合した結果、
以下のキーワードが**一語も存在しない**ことを確認した(=これらに言及されても危険信号として
拾われず、通常の運動アドバイスがそのまま返る):
- 高血圧・心臓病・糖尿病・喘息・緑内座・ペースメーカー等の**持病・既往症**
- 痛み止め・抗凝固薬等の**服薬中**の申告
- 脳卒中サイン(顔の歪み・ろれつが回らない等のFAST基準)
- 失神・立ちくらみ
- DVT(深部静脈血栓症)疑い
- 「妊娠」という単語を含まない形での切迫流産/早産のほのめかし(既存は「妊娠」語で判定して
  いるため表現がずれるとすり抜ける)

これは**新規パターンの追加というより安全上の欠落**なので、他の4ファイル(会話の豊かさを増やす
もの)より優先度を上げて先に対応することを推奨する。

## 次にやること(実装フェーズ)

1. `soudan-draft-safety.md`のredFlags穴から着手し、`soudan-kb.js`の`redFlags.kw`/`stateKw`に
   追加。安全文言のトーン(診断しない・断定しない)は`SAFETY-REDFLAG-FIXES.md`のルールに厳密に
   従うこと。
2. 残り4ファイルは「新規intent追加」ではなく大半が「既存intentのkw拡充」で対応できる設計に
   なっている(各ファイル内に対応先intent idが明記済み)。intentごとにまとめて`soudan-kb.js`へ
   反映。
3. 反映後は`npm test`(`node scripts/qa.js`)を必ず通すこと。
4. Web版のみの拡充であれば、iOS/Android版とのパリティがずれる(意図的差分として記録すること。
   HANDOFF.mdの「Web版とネイティブの挙動が意図的にずれる」節と同じ扱い)。ネイティブ版
   (`SoudanSheetView.swift`/`SoudanSheet.kt`)への移植は別タスクとして切り出すことを推奨
   (本人へは着手前に確認)。
5. kwを追加する際は、既存intentとの衝突(あるフレーズが意図と違うintentに誤マッチする)に
   注意。各下書きファイル末尾に衝突リスクの注記がある。

## 確認済み/未確認

- [x] 5ファイルとも本体コード無変更(`git status`で確認)
- [x] 既存126インテントとのマッピング調査済み(重複追加を防ぐため)
- [x] 安全性ルール(`SAFETY-REDFLAG-FIXES.md`)を踏まえた文言トーンで下書き作成
- [ ] **実際のコード統合は未実施**(時間切れ)
- [ ] 統合後の`npm test`実行(統合作業自体が未実施のため)
- [ ] ネイティブ版(iOS/Android)への移植要否の判断(本人確認が必要)
