# 5視点監査: ④小柄・片手レンズ

お題: 「ごちゃごちゃを引き算でほどく」（機能追加は禁止。提案は 消す/まとめる/隠す/順番を変える の4種のみ）

想定ユーザー: 手が小さい・片手操作（もう片方がふさがっている）。画面上部に親指が届かない、密集したタップ対象で誤タップ、選択肢が多い画面で迷う。

調査対象:
- `/Users/ryunosuke/Claude/kyou-no-ogatore/index.html`（正本。L602-708 ホーム / L711-723 かたさチェック / L726-749 結果 / L752-847 マイ記録 / L850-965 じまん・お楽しみ・とどく・検索 / L968-1143 使い方 / L1158-1175 タブバー+FAB / L1217-1247 相談室シート・はじめてガイド / L2924-2942 renderHome / L3141-3200 sdRenderChips / L4089-4145 オンボ台本・ツアー）
- `/Users/ryunosuke/Claude/kyou-no-ogatore/app-search.js`（L2-39 タグ・カテゴリ・チップ描画）
- `/Users/ryunosuke/Claude/kyou-no-ogatore/app-quiz.js`（L154-180 設問描画 / L300-328 結果動画リスト）
- iOS: `ios-native/KyouNoOgatore/KyouNoOgatore/HomeView.swift`（L492-496, L862-865 カード並べ替え確認）ほか SoudanSheetView.swift / SearchView.swift / GuideView.swift / MyRecordView.swift / SettingsView.swift / KyonoTabBar.swift / OnboardingViews.swift
- Android: `android-native/KyouNoOgatore/app/src/main/java/jp/ogatore/kyouno/MainActivity.kt`（L452-495 FAB縦積み確認）ほか SoudanSheet.kt / SearchScreen.kt / GuideScreen.kt / SettingsScreen.kt / KyonoTabBar.kt / OnboardingScreens.kt

**本ファイルは下書きのみ。コードは一切編集していません。**

先に良い点（既に片手向きにできている所・壊さないでほしい）:
- タブバーが下端固定・チェック済みユーザーはきょうの1本が上に来る並べ替え済み（`index.html` L2937-2938、iOS `HomeView.swift` L862-865 も同じ）
- 相談室FABはホーム/使い方/結果では消して重複導線を避けている（`index.html` L1419-1434 updateFabs）
- オンボの回答チップは全幅1列・画面下側（L528-531）、かたさチェックの選択肢も全幅（L293-297）
- 検索の✕クリアは38px（L431）、カレンダー送りは44px（L401）

---

## 画面ごとの指摘

### ホーム（`index.html` L602-708、iOS `HomeView.swift`、Android `MainActivity.kt`）

- 指摘1: **条件カードが最大3枚（welcomeBack L606 / recheckCard L612 / anchorCard L617）、todayCard/streakCard より上に積める構造**。片手スクロールの起点（画面上部）に「再開する/測る/あとで/いつやる派4択」と選択肢が並び、毎日の目的地（きょうの1本→きょうやった！）までの距離と迷いが増える。recheckCard と anchorCard の同時表示を防ぐ排他は見当たらない。
- 引き算提案: **隠す** — 条件カードは「1画面につき1枚まで」の優先順位を決め、2枚目以降はその日は出さない（次回起動へ回す）。新規UIは不要、表示条件の絞りだけ。
- 確信度: カード構造・並びは確認済み／「同時に3枚出る日が実在するか」は表示条件を全走査しておらず推測

- 指摘2: **streakCard（L686-707）が1カードに詰め込みすぎ**。「きょうやった！」後は cheer + a2hsAsk + memoRow（入力+ボタン）+ plateauNote + makeCardBtn + cardHint + calAsk が同カード内に出得る。片手だと押した直後に画面が伸びて親指の下にボタンが増え、誤タップしやすい。
- 引き算提案: **消す** — cardHint「カード画像を保存かシェアでのこしてね📤」（L705）はボタンラベル（L703-704）とカードモーダル内の説明（L1196）と三重。1行削れる。**まとめる** — a2hsAsk と calAsk（L696, L706）が同日に両方出る状況は1つに絞る（もう1つは翌日）。
- 確信度: DOM構成は確認済み／a2hsAsk・calAsk の同時出現条件は推測

### かたさチェック（`index.html` L711-723、`app-quiz.js` L154-180、iOS `OnboardingViews.swift`、Android `OnboardingScreens.kt`）

- 指摘: 選択肢は全幅・下部ボタンで片手向きにできている。ただし**「← まえの質問へ」（L720）と「ホームにもどる」（L721）が同じ btn-line スタイルで縦隣接**。片手・小さい手で「まえの質問へ」を狙って下の「ホームにもどる」に当たると、途中の回答が全部飛ぶ（最も重い誤タップ）。
- 引き算提案: **隠す** — 「ホームにもどる」はQ1のみ表示し、Q2以降は隠す（Q2以降は「まえの質問へ」で戻れる。ブラウザ/端末の戻る操作も生きている）。もしくは**順番を変える**で2ボタンの上下を離す。
- 確信度: 確認済み（誤タップ時に回答が破棄されるかは quizGoHome の実装まで追っておらず、その点のみ推測）

### 結果画面（`index.html` L726-749）

- 指摘: 最下部に3ボタン縦積み（rTourBtn L746 / rGoHomeBtn L747 / rRecheckBtn L748）。**「もう一回チェックする」はホームに戻れば ckCard.mini の同名ボタン（L198-202 + L2931）として常設されており、完全な重複導線**。primary（きょうの1本へ）の真下にあるため、片手で誤タップすると5問やり直しの画面に飛ぶ。
- 引き算提案: **消す** — rRecheckBtn を削除（ホーム側に同機能が残る）。ボタンが2つに減り、下部の親指ゾーンがすっきりする。
- 確信度: 確認済み

### 使い方タブ（`index.html` L968-1143、iOS `GuideView.swift`、Android `GuideScreen.kt`）

- 指摘1: **最上部（親指が最も届かない位置）に「🌱はじめてガイド」「📖使い方ツアー」2ピル+使い分け説明文**（L970-971）。この2つはFAQ「使い方をもう一度おしえてほしい」（L1130）とも重複しており、画面上端に手を伸ばさせる導線になっている。
- 引き算提案: **順番を変える+まとめる** — 2ピルを「🆘 困ったときは」カード（L992-1000、ボタン4本のハブ）へ移動して5・6本目のボタンにする。最上部の2ピル+説明文の3行ぶんが消える。
- 確信度: 確認済み

- 指摘2: 目次チップ7個（L981-989）の先頭「🆘 困ったときは」は、**ジャンプ先の gd-help カードが目次の直下に見えている**ためチップとしてはほぼ無意味で、チップ数を1つ増やしているだけ。
- 引き算提案: **消す** — 先頭チップを削除して6個にする（チップが減るぶん、片手で狙う面積が広がる）。
- 確信度: 確認済み

### マイ記録タブ（`index.html` L752-847、iOS `MyRecordView.swift`+`SettingsView.swift`、Android `SettingsScreen.kt`）

- 指摘1: カード6枚（続けた記録/図鑑バナー/カレンダー/とどくメーター/お楽しみ機能/続ける設定）の縦長画面。**図鑑バナー（L766-771）は使い方タブの説明「カード図鑑は『🎉お楽しみ機能』の中」（L1055）・FAQ（L1125-1126）と場所の説明が食い違っている**うえ、お楽しみ機能カード（L792-796）と入口が分裂している。
- 引き算提案: **まとめる** — 図鑑バナーをお楽しみ機能カードに統合（サンプルサムネ行 dex-banner-samples を、お楽しみ機能カード内に移すだけ）。カードが1枚減り、片手スクロール距離が縮み、説明文との食い違いも解消。
- 確信度: 食い違い・分裂は確認済み

- 指摘2: **続ける設定カード（L798-846）が1枚で設定7種**（タイミング/テーマ3択/文字2択/通知時刻/カレンダー2ボタン/自動オープン手順/ひっこし）。うち「自動でひらく設定」「記録のひっこし」は既に details で畳まれているのに、**カレンダー通知ブロック（L813-817、time入力+ボタン2+ヒント2）だけ常時展開**で、カード中央の密度が高い。
- 引き算提案: **隠す** — カレンダー通知ブロックも同カード内で既に使われている details 様式（L818, L829 と同型）で畳む。新規UIなしで縦を約5行分圧縮。
- 確信度: 確認済み

### 動画を探すタブ（`index.html` L945-965、`app-search.js` L2-39、iOS `SearchView.swift`、Android `SearchScreen.kt`）

- 指摘1: 上から 検索箱／カテゴリ4ボタン（横スクロール）／チップ最大11個／**年セレクタ+件数行（L954-957）**／結果。片手ユーザーの実働線は中段のチップ（届く位置）なのに、その直下に利用頻度の低い「すべての年」セレクタが割り込み、チップと結果リストの間の視線・親指移動を分断している。
- 引き算提案: **順番を変える**（または**隠す**） — ySel の行を結果リストの下（moreBtn L959 の隣）へ移す。絞り込みたい人だけが下まで行けば良く、通常時は「チップ→結果」が直結する。
- 確信度: 確認済み

- 指摘2: このタブでは**FABが2個縦積みで表示される**（相談室FAB=ホーム/使い方/結果では非表示だが検索・マイ記録・再生リストでは表示。L1419-1434）。56px円が2つ（L247, L250）右下に並び、(a) リクエスト箱・moreBtn の右端を物理的に覆う、(b) 同形の円ボタン2つが12px間隔で並ぶため、小さい手でも押し分けにくい。
- 引き算提案: **隠す** — 通信FABは未読ドット（obuDot）が点いているときだけ表示し、普段は1個にする。または**まとめる**で検索タブは相談室FABのみ（通信はホームで見る役割分担）。
- 確信度: 表示条件・座標は確認済み／誤タップ頻度は推測

### 再生リストタブ+タブバー（`index.html` L939-942, L1158-1164、iOS/Android `KyonoTabBar.swift`/`.kt`）

- 指摘: タブ5個は幅340pxクラスの小型端末で1タブ約66px・ラベル12px。**「再生リスト」タブの中身はリンク16本のみ（L3500-3527）で、役割は「動画を探す」の一形態**。動画系の入口がタブ2つに割れているぶん、タブ1個あたりの幅も狭くなっている。
- 引き算提案: **まとめる** — 再生リストを「動画を探す」内の1グループ（例: カテゴリ「その他」相当の並びに「まとめ再生」グループとして plList を移設）にし、タブを4個へ。全タブ幅が約25%広がり、小さい手の押し分けが楽になる。※タブ構成変更は影響大のため優先度は下げて提案のみ。
- 確信度: 中身の単純さは確認済み／統合時のUI詳細は要検討（推測）

### 相談室シート（`index.html` L1217-1235, L455-500, L3141-3200、iOS `SoudanSheetView.swift`、Android `SoudanSheet.kt`）

- 指摘1: **フッターが最大4段**（カテゴリ5チップが flex-wrap で2〜3行に折り返し（L471 `.sd-foot .sd-catrow{flex-wrap:wrap}`）+「👇タップでえらべるよ」ヒント行+チップ横スクロール行+入力行）。フッターが高いほどログが狭くなり、片手の親指ゾーンにボタンが密集して押し分けが難しくなる。カテゴリラベル（「からだの部位で」等5個、L2983-2989）は全部で最大3行を占有。
- 引き算提案: **まとめる** — sd-catrow を sdChips と同じ横スクロール1行（fade-r 付き、既存様式 L470）に変更する。CSS1行の変更でフッターが1〜2行分低くなる。あわせて「👇タップでえらべるよ」（L1227）はチップに影が付いた現在では役目が薄く**消す**候補（※このヒントは2026-07-21の本人指摘で追加された経緯（L3187コメント）があるため、削除は本人確認が前提）。
- 確信度: 折返し挙動はCSS指定から確認済み（実機の行数は幅依存で推測）

- 指摘2（参考・4手では解けない）: 閉じる✕（L1221、40px）がシート上端右=片手で最も届かない位置。背面タップで閉じられる領域はシート高92%（L459）の外側=上部8%しかない。戻る操作でも閉じる実装はある（L1537コメント）が、見えない機能。引き算の4手で直せないため**指摘のみ**（もし触るなら「シート高を下げて背面タップ領域を広げる」が最小変更）。
- 確信度: 確認済み

### オンボ初回チャット（`index.html` L1238-1247, L4089-4117、iOS `OnboardingViews.swift`、Android `OnboardingScreens.kt`）

- 指摘: 質問4つ・回答チップ全幅縦積み（L528-531）で本編は片手向きに良くできている。唯一「**スキップ**」（ob-skip L1242）がモーダル上端右=初回の最初の操作候補が親指の届かない位置にある。
- 引き算提案: **順番を変える** — スキップボタンを obChips の並びの最後（最下段）へ移す。部品はそのまま、位置だけ変更。
- 確信度: 確認済み

### 使い方ツアー（`index.html` L4119-4145 OB_TOUR_SLIDES）

- 指摘: スライド8枚（自動起動時は締め1枚追加で9枚）。操作ボタンは obChips=下部に出るので片手操作自体は問題ないが、片手・すきま時間で見る前提だと8タップは長く、後半（通信・マイ記録・忘れてもだいじょうぶ）で離脱しやすい。
- 引き算提案: **まとめる** — 「💬相談室」と「📣通信」（どちらも右下ボタンの案内、L4133-4138）を1枚に、「📖図鑑」（L4130）は「✅きょうやった！」の補足に畳み、6枚程度へ。文言は既存の再配置のみで可。
- 確信度: 枚数・構成は確認済み／離脱傾向は推測

---

## まとめ（優先度順）

1. **相談室フッターのカテゴリ行を横スクロール1行化**（まとめる・CSS1行）— 片手の主戦場（画面下部）の密度を直接下げる。`index.html` L471
2. **結果画面の「もう一回チェックする」を消す**（消す）— 完全重複導線+primary直下の危険な隣接。`index.html` L748
3. **マイ記録: 図鑑バナーをお楽しみ機能カードへ統合**（まとめる）— カード1枚減+使い方タブの説明との食い違い解消。`index.html` L766-771 / L1055
4. **使い方タブ最上部の2ピルを「困ったときは」カードへ移動+🆘目次チップを消す**（順番を変える+消す）— 画面上端の操作をなくす。`index.html` L970-971, L982
5. **続ける設定のカレンダー通知ブロックを details で畳む**（隠す・既存様式）。`index.html` L813-817
6. **検索タブの年セレクタを結果リスト下へ**（順番を変える）+ **通信FABを未読時のみ表示**（隠す）。`index.html` L954-957, L1419-1434
7. **かたさチェック中の「ホームにもどる」をQ1のみに**（隠す）— 全回答破棄の誤タップ防止。`index.html` L721
8. **streakCardの cardHint を消す**（消す・三重説明の1つ）。`index.html` L705
9. **オンボのスキップをチップ列最下段へ**（順番を変える）。`index.html` L1242
10. （大玉・提案のみ）**再生リストタブを「動画を探す」へ統合しタブ4個に**（まとめる）— タブ幅+25%。`index.html` L939-942, L1158-1164
11. （参考・4手対象外）相談室シートの✕上端問題／カレンダー日セルが340px以下端末で36px（L157）に縮む点は、引き算では解けないため記録のみ。

3プラットフォーム適用時の対応先: 上記各項の iOS/Android ファイルは「調査対象」欄と各画面見出しに記載（Webの該当行コメントに `index.html:行番号` 形式で1:1参照が入っているため、同じ行番号で検索すると移植箇所が見つかる。例: Android `MainActivity.kt` L452「index.html:1166-1175 obuFab/soudanFab…の1:1移植」）。
