# 5視点監査: ②老眼・低視力レンズ

お題: **「ごちゃごちゃを引き算でほどく」**（提案は 消す・まとめる・隠す・順番を変える の4種類のみ。機能追加なし）

調査対象:
- `/Users/ryunosuke/Claude/kyou-no-ogatore/index.html`（正本。CSS変数・全5タブ・相談室シート・オンボ・ツアー・FAB）
- `/Users/ryunosuke/Claude/kyou-no-ogatore/app-quiz.js`（かたさチェック・結果画面）
- `/Users/ryunosuke/Claude/kyou-no-ogatore/app-search.js`（動画を探す）
- iOS/Android対応ファイルの所在確認: `ios-native/KyouNoOgatore/KyouNoOgatore/HomeView.swift ほか` / `android-native/KyouNoOgatore/app/src/main/java/jp/ogatore/kyouno/MainActivity.kt ほか`（1:1移植のため、本稿の指摘は原則3OS共通。個別行の突合は未実施）
- コントラスト実測: WCAG相対輝度式で主要ペアを計算（本文中に数値併記。計算済み=確認済み扱い）

**本ファイルは下書きのみ。コードは一切編集していません。**

前提として良い点（引き算済みの資産）: 本文18px+zoom既定1.18の「大きめ」既定（index.html L86-87, L568）、チェック済み後の `#ckCard.mini` 縮小（L198）、記録後の `#streakCard.mini` 縮小（L204）、ガイド中のホーム絞り込み `fdFocusHome`（L1662）、chip-c文字色のAA修正実績（L443コメント）。この「あとから縮む」設計は正しい方向で、本監査はその徹底漏れを拾うもの。

---

## 画面ごとの指摘

### ホーム（index.html L601-708）

- 指摘1: **カードの積み過ぎで「きょうの主行動」が最下段**。ホームには qbubble / envBanner / welcomeBack / recheckCard / anchorCard / ckCard / soudanCard / todayCard / planCard / planDoneCard / streakCard の最大11枠が縦積みで、毎日の主行動「きょうやった！」（L694）は streakCard 内=ページ最下部。低視力ユーザーは「読む対象を探すスクロール」自体が負担で、途中の黄色い大ボタン（チェックをはじめる L635・💬相談する L649）に視線を吸われて主行動に辿り着けない。
- 引き算提案1: **順番を変える** — streakCard（続けた日数+きょうやった！）を todayCard（きょうの1本）の直後に移動し、「1本見る→すぐ下で記録」を1スクロール内に収める。ckCard(mini)・soudanCard はその下へ送る。
- 確信度: 確認済み（DOM順は上記の通り。「見失う」体感は推測）

- 指摘2: **btn-primary（黄色大ボタン）が1画面に最大3つ**（チェックをはじめる/💬相談する/welcomeBack系）。btn-primary は「いちばん大事な1つ」の記号なのに複数並ぶと、コントラストでしか画面を読めない人には全部が主役に見える。しかも soudanCard はカード全体に `onclick="openSoudan()"`（L643）が付いており、中のボタン（L649）は機能的に重複。
- 引き算提案2: **消す** — soudanCard 内の「💬 相談する」btn-primary を消してカード全体タップに一本化（チップ列 `soudanCardChips` は残す）。黄色ボタンをホーム1画面に実質1つへ。
- 確信度: 確認済み（重複onclickはコードで確認）

- 指摘3: **streakCard 1枚の中に10要素**（miniDone / fdDoneStaticNudge / doneBtn / cheer / a2hsAsk / calAsk / memoRow / plateauNote / makeCardBtn / cardHint、L686-707）。記録直後は「メモ・カード作成・ホーム追加案内・カレンダー案内」が同じカード内に同時出現しうる構造で、「どこまでが1つの用事か」を低視力では追えない。
- 引き算提案3: **順番を変える＋隠す** — a2hsAsk と calAsk は「1回の記録につきどちらか1件だけ」を出す運用に絞る（同時表示を隠す）。makeCardBtn+cardHint は記録済みになるまで隠す（doneBtn押下前のカード作成は意味が薄い）。
- 確信度: 確認済み（DOM構造）／同時出現の頻度は推測（askQueue側ロジックの全経路は未追跡）

- 指摘4: 常時ヒント文が小さい。`.hint` 13px（L385）がホームだけで3箇所（L639, L663, L705）。「動画がおわったら…押してね✅」（L663）は初週以降のユーザーには既知情報。
- 引き算提案4: **隠す** — L663のヒントは通算7日以上で非表示にする（fdDoneStaticNudge と同系の条件分岐は既にある）。
- 確信度: 確認済み（サイズ・文言）／閾値は提案

### かたさチェック（index.html L710-723 / app-quiz.js L154-177）

- 指摘1: 進捗ドット `.dot` は9px（L314）で、低視力にはほぼ視認不能。すぐ上に `Q1 / 5` のテキスト表示（qnum, L161）が既にあり情報が二重。
- 引き算提案1: **消す** — クイズ画面の dots（L719, renderQ L175-176）を消して qnum に一本化。縦の情報量も1行減る。
- 確信度: 確認済み

- 指摘2: 選択肢の段階色 g0〜g3（L301-304）は「明→暗=柔→硬」の色符号だが、色の明度差だけで意味を持たせている。テキストが主なので致命ではないが、低コントラスト視には g1(#FFF3CB) と g2(#FBE3C6) の区別がつかない。
- 引き算提案2: 現状維持で可（テキストが主情報のため）。指摘としてのみ記録。
- 確信度: 確認済み（色値）／見分け困難は推測

### 結果画面（index.html L726-749 / app-quiz.js showResult L257-342）

- 指摘1: **通常表示は1画面に9ブロック**（タイプ名/copy/hope/PT解説/おすすめ3本/連続再生リンク/worryExtra/ローテ注記13px/ペース目安14px×4行/免責hint/相談室リンク/ボタン3つ）。ガイド中は削ぎ落とし済み（showResult の guide 分岐、app-quiz.js L291-302)だが、通常時は全部盛り。読み終える前に力尽きる典型画面。
- 引き算提案1: **隠す** — 長文の rPT（理学療法士のひとくち解説）を、使い方タブで多用済みの `<details>` 折りたたみ様式（gd-fold と同型）に収める。新規部品ではなく既存様式の適用。
- 引き算提案2: **まとめる** — rPace の「痛い日は休むのが正解/痛みはイタ気持ちいいまで」（L741）と直下 hint の「痛みが強いときは中止して医療機関へ」（L742）は痛み注意が二重。安全文言を1箇所（hint側）に寄せ、rPace は頻度の2行だけに減らす。
- 確信度: 確認済み

- 指摘2: rRotateNote「おすすめは3日ごとに自動で入れ替わります」13px（L740→CSS L370）と rReachNote 13px（L734）は、最重要ではない補足が小さい文字で挟まり視線を分断する。
- 引き算提案3: **順番を変える** — rRotateNote を動画リスト直下から画面最下部（免責hintの隣）へ移動。
- 確信度: 確認済み

### マイ記録（index.html L752-847）

- 指摘1: **「続ける設定」カード1枚に約15要素**（L798-846: やるタイミング+変えるリンク/テーマseg3択+説明/もじ大きさseg/おしらせ時間input/カレンダー2リンク+hint2つ/iPhone自動起動details/ひっこしdetails一式）。設定・ヘルプ・バックアップが混在し、目的の項目を探せない。
- 引き算提案1: **隠す＋順番を変える** — (a)「もじの大きさ」を設定カードの最上段へ（老眼ユーザーが最初に探す設定。今はテーマの下=L808）。(b) カレンダー通知一式（時刻input+2リンク+hint2つ, L813-817）を既存 `<details>` 様式で「📅 リマインダー（カレンダー通知）」として畳む。既に details 2つが同カード内にあるので様式追加なし。
- 確信度: 確認済み

- 指摘2: バナー型カードが3枚連続（図鑑L766 / とどくメーターL786 / お楽しみL792）で、主役のカレンダー（L772）が図鑑バナーの下にある。記録タブに来る動機の第一は「やった日を見る」はず。
- 引き算提案2: **順番を変える** — カレンダーを「続けた記録」直後（2番目）へ、図鑑バナーはとどくメーター・お楽しみと並べて後方へ。
- 確信度: 確認済み（DOM順）／来訪動機は推測

- 指摘3: カレンダーのきょう表示は `box-shadow:inset 0 0 0 2.5px var(--pink)`（L408）の細いピンク輪郭のみ=**色+細線だけの区別**。やった日(teal塗り)は判別しやすいが「きょう」は低視力に見えない。
- 引き算提案3: 引き算では解けない（記録として残す。もし触るなら既存 `.sel`（3px ink outline, L409）と同じ太さ・ink色に寄せる=新部品なしの流用）。
- 確信度: 確認済み（CSS値）／視認不能は推測

### 動画を探す（index.html L945-965 / app-search.js）

- 指摘1: **結果に着くまで操作列4段**（検索box→カテゴリ4ボタン→タグ最大11チップ→filterNow→年セレクト+件数）。特に年セレクト `ySel`（L955）は「すべての年/2020年…」の選別で、ストレッチを探す高齢ユーザーに意味が薄く、1段まるごとノイズ。
- 引き算提案1: **消す**（または隠す）— ySel を消す。年で絞りたい需要はキーワード検索「2023」で代替可能（currentHits は `v.y+"年"` を検索対象に含む: app-search.js L47）。件数表示 hitCount は残す。
- 確信度: 確認済み（代替手段の存在もコードで確認）

- 指摘2: 結果カードのバッジ=タグ1個目（app-search.js L67 `vHTML(v,(v.tags||[])[0])`）は、直前に自分で選んだタグや動画タイトルと重複しがちで、12px（L334）の小さい色付き文字が全カードに付くノイズになっている。
- 引き算提案2: **消す** — 検索結果一覧ではバッジなし（`vHTML(v,null)`）にする。タグ絞り込み中は filterNow に選択タグが出ており情報は失われない。
- 確信度: 確認済み

- 指摘3: チップの4色分け（chip-a黄/b青緑/c桃/d紫、L441-445）はカテゴリ由来の色符号だが、**色の意味はどこにも説明されておらず**、選択状態も彩度の濃淡が主。色で画面を読めない人には全部「薄い丸」。文字コントラスト実測: chip-a 4.55:1・chip-b 5.39:1・chip-c 5.18:1・chip-d 4.98:1（14px太字なのでAA充足）だが、カテゴリボタン非選択 `#6E6B5F on #F2EADB` は **4.47:1で14pxのAA(4.5)を僅かに割る**。
- 引き算提案3: 選択中タグは filterNow の「タグ ✕」表示（app-search.js L62-63）が既に補完しているため、**消す** — チップ側の `.on` 色反転への依存を認めた上で、色符号の意味付け（4色ローテ）自体を減らし、全チップを chip-b 1色に統一する（=色の種類を消す）。カテゴリはボタン行で既に分かれており、色まで変える必要がない。
- 確信度: コントラスト値は確認済み／1色統一の是非は要PO判断

### 再生リスト（index.html L938-942, L3500-3527）

- 指摘: 比較的良好。行タイトル15px/サブ14px（L421-422）が本文18pxより小さいのは惜しいが、1行1リンクの構造は低視力向き。group-label 14px灰色（L423）が唯一の道しるべなのに小さい。
- 引き算提案: なし（現状維持）。強いて言えば3グループ中「シリーズで深掘り」（2件, L3517-3519）は「姿勢の悩みから探す」と**まとめる**余地あり=見出し1段減。
- 確信度: 確認済み

### 使い方タブ（index.html L967-1143）

- 指摘1: 冒頭の補足「『はじめてガイド』＝さいしょの質問から…」が **12px**（L971）、目次下の「下の見出しカードは タップするとひらきます」13px（L990）、FAQ冒頭の「しつもんをタップすると…」13px（L1074）。**「タップで開く」説明が3回・全部極小文字**。読めない説明は無いのと同じで、行数だけ消費している。
- 引き算提案1: **消す＋まとめる** — L971とL1074を消し、L990の1本に統合（目次直下の1箇所だけ残す）。
- 確信度: 確認済み

- 指摘2: gd-mamori（記録を守る）の手順1が**1つの gstep に iPhone手順+引っ越し注意+Android手順を全部内包**（L1032-1041）した巨大段落。14pxで10行超。しかも同じ内容へ飛べる「📲 もういちど かんたん案内を見る」ボタン（L1040）が直下にある。
- 引き算提案2: **隠す** — OS別詳細手順（L1033-1039の枠内）を `<details>`（既存様式）で畳み、開いた状態の本文は「ホーム画面に置く。手順は下をひらくか📲ボタンで」相当の骨子だけ残す。
- 確信度: 確認済み

- 指摘3: FAQが29問・summary/回答とも14px（L191, L196）。**一番困っている人が読む場所がアプリ内最小級の文字+最大級の物量**。内容の重複もある: 「通算が0日にもどってる」（L1102）「機種変更のときは」（L1103）「アイコンを消したら記録は？」（L1104）は3問とも回答が「記録をコピー→よみこむ」に収束する。
- 引き算提案3: **まとめる** — 上記3問を「記録が消えた・機種変更・アイコン削除」の1問に統合し、回答内の既存ボタン（L1103の📦ボタン）を共用。FAQ総数を減らす方向で他グループも同様に棚卸し。
- 確信度: 確認済み

### オンボ初回チャット（index.html L1238-1247, L4184-4267）

- 指摘: チップは16px・全幅・縦積み（L530）で低視力向けに良好。毎問「👇 タップしてえらんでね」（obAskQ L4208）が繰り返されるのは、1問目で学習済みのユーザーには行数ノイズ。
- 引き算提案: **隠す** — tap-hint は最初の1問だけ表示し、2問目以降は出さない（obQi>0 で抑止）。
- 確信度: 確認済み

### 使い方ツアー（obTourStep L4299-4326）

- 指摘: 進捗が「つぎへ ➡️（3/9）」のボタン内数字と 9pxドット列（L4304）で二重。ドットは見えない上にスライド数が多いと横に長い。
- 引き算提案: **消す** — ツアーの dots を消し、ボタン内の（n/N）に一本化（かたさチェックの dots と同判断）。
- 確信度: 確認済み

### 相談室シート（index.html L1216-1235, L3141-3200）

- 指摘1: 入力欄の上に **カテゴリ行(5チップ)+タップヒント+チップ横スクロール行+入力行の最大4段**が常時積まれ、会話ログの表示領域が狭い。L3095-3099のコメント自身が「狭いメッセージ欄で長文が見切れる」と自認しており、followups時にカテゴリ行を畳む対応は済んでいるが、初期表示(intents)では4段のまま。
- 引き算提案1: **隠す** — sdTapHint「👇 タップでえらべるよ」は初回表示の1回だけにする（現状はチップがある限り毎回表示: L3188）。1段回収できる。
- 確信度: 確認済み

- 指摘2: ヘッダ直下の免責 `sd-disc` が **13px×2行で常時固定**（L466, L1223）。安全文言なので消せないが、13pxでは当のリスク層（高齢・低視力）が読めておらず、「読めない字が場所を取る」最悪の組み合わせ。
- 引き算提案2: **まとめる（要PO判断）** — 1行目「※回答はオガトレ監修のパターン集から…」は初回挨拶の吹き出し側（既存 greet 文言）に寄せ、固定ヘッダには安全側の2行目「強い痛み・しびれがあるときは医療機関へ」だけを残して1行化。固定領域が半分になり、残した1行を現在の2行分の高さで表示できる余地が生まれる。
- 確信度: 確認済み（サイズ・構造）／文言移設の可否は安全設計に関わるため要PO判断

### 全体・横断（サイズとコントラストの実測）

- 指摘1: **10〜12px級の文字が常用されている**。最小は カード図鑑の獲得ヒント `.dex-hint` **10px**（L242）、次点で `.dex-name` 11px（L241）・logosub 11px（L94）・qbubble ラベル `.ql` 11px（L454）・古い通信日付 11px（L277）・badge 12px（L334）・voices「タップでめくれる」12px（L359）。zoom1.18でも10px→実質11.8px。老眼レンズではこの層は「存在しない情報」。
- 引き算提案1: **消す/隠す** — (a) dex-hint（未獲得カードの入手ヒント）は常時表示をやめ、サムネタップ時の表示に隠す（図鑑は一覧性が主目的）。(b) logosub「みんなで一緒にストレッチを習慣化」はスプラッシュ（L558）と重複するのでヘッダ側を消す=ヘッダが1行になり最上部がすっきりする。
- 確信度: 確認済み（サイズ）／dexタップ時表示の実装有無は未確認のため挙動変更の要否は推測

- 指摘2: コントラスト実測でAA未達の組み合わせ（小さい文字ほど致命的）:
  - `--sub-faint #827F72` on card = **4.02:1** を11pxで使用（obu-date-old, L277）
  - ピンク文字 `#E56A9A` on card = **3.06:1**（fdDoneStaticNudge 14px L693・fd-point 15px L212・streak数字46pxは大サイズなので許容）
  - 検索プレースホルダ `#948E7C` = **3.27:1**（L429。プレースホルダなので許容範囲だが「例:」が読めない）
  - 参考: 本文系は健全（--sub 5.14〜5.34:1、--ink on yellow 8.30:1）
- 引き算提案2: 色変更は引き算の範囲外なので指摘のみ。引き算で対処するなら: obu-date-old はそもそも「古い投稿の日付をさらに薄く小さく」する演出（L277コメント）であり、アーカイブ画面では通常の obu-date に**まとめる**（古い強調自体を消す）。fdDoneStaticNudge「動画を見おわったら、ここを押してね👇」はガイド当日限定表示なので許容。
- 確信度: 数値は確認済み（WCAG式で計算）

- 指摘3: 右下FAB2段（オガトレ通信56px+相談室56px、L247-254）は誤タップ・被り対策が updateFabs（L1419-1434）で相当数パッチされており、「隠す条件のリスト」が伸び続けている=構造がごちゃつきの源であることをコード自身が示している。
- 引き算提案3: **消す（片方）** — 相談室FABは既にホーム/使い方/結果で非表示（L1428）であり、残る表示先はマイ記録・再生リスト・動画を探すのみ。動画を探すには相談室への実質導線（リクエスト箱）が別にあり、FAB2段をやめて相談室FABを全廃→入口をホームカード+結果画面リンクに一本化する案を検討する価値がある。
- 確信度: 表示条件は確認済み／全廃の影響（相談室利用率）は推測・要PO判断

---

## まとめ（優先度順）

1. **ホームの順番替え**: streakCard（きょうやった！）を きょうの1本 直後へ。黄色大ボタンを1画面1つに（soudanCardのボタンを消す）。— 毎日触る画面で効果最大
2. **「続ける設定」カードの分解**: もじの大きさを最上段へ、カレンダー通知一式を details に隠す。— 老眼ユーザーが自力で文字を大きくできる導線の確保
3. **使い方タブの引き算**: 「タップで開く」説明3つを1つに統合、gd-mamori のOS別手順を details に、FAQの重複3問を1問に統合。— 「困った人が読む場所」の物量を削る
4. **動画を探す**: 年セレクトを消す・結果カードのバッジを消す。操作列4段→3段、結果一覧のノイズ減
5. **結果画面**: PT解説を折りたたみ、痛み注意の二重記載をまとめる
6. **進捗ドット全廃**（かたさチェック・ツアー）: 9pxドットはテキスト進捗と二重。消すだけで済む最小工数案件
7. **極小文字の棚卸し**: dex-hint 10px・logosub 11px・obu-date-old 11px(4.02:1) など「読めないのに場所を取る」層を消す/まとめる
8. **相談室**: sdTapHint を初回のみに、sd-disc の1行化（安全文言の扱いは要PO判断）
9. （要PO判断）チップ4色符号の1色化、相談室FABの全廃

※ 本監査はWeb正本ベース。採用が決まった項目は iOS（`HomeView.swift`/`GuideView.swift`/`SearchView.swift`/`SoudanSheetView.swift` 等）・Android（`GuideScreen.kt`/`SearchScreen.kt`/`SoudanSheet.kt` 等）へ同時展開が必要。
