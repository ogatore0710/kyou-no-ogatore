# タスク（C2/appdev向け）— オフライン時の案内バナーが無い

## 背景

alan5の独自調査の続き。Web版の`envBanner`（index.html:605,4064-4080）には複数の用途があるが、
その大半（YouTubeアプリ内ブラウザ脱出案内・LINE等アプリ内ブラウザ警告・ホーム画面追加リマインド）
は**A2HS/PWA固有の問題でネイティブには存在しないため移植不要**（マスタープラン§2-2の除外方針・
これまでの判断と同じ）。

ただし1つだけ、A2HSと無関係の**純粋なオフライン通知**（index.html:4069）があり、これは
ネイティブにも当てはまる汎用的な機能のため、対象を確認したところ両OSとも未実装だった
（`ConnectivityManager`/`NWPathMonitor`等の接続監視コードが見つからない）。

## Web版の仕様（index.html:4064-4080が正本）

```
オフラインになったとき: 画面上部に「いま電波がないみたい📡 動画を見るには電波が必要だよ
（「きょうやった！」の記録はつけられるよ）」の案内バナーを表示
オンラインに戻ったとき: バナーを消す（元々表示されていた別の案内があればそれに戻す。
ただしA2HS系案内は移植対象外なので、ネイティブでは単純に「表示⇔非表示」でよい）
```

## やること

- ホーム画面（またはアプリ全体の共通ヘッダー）に、オフライン検知時のみ表示される案内バナーを追加
  （Android: `ConnectivityManager`のネットワークコールバック、iOS: `NWPathMonitor`）
- 文言はWeb版どおり。「動画を見るには電波が必要」「記録はつけられる」の2点が伝われば表現は
  ネイティブUIに合わせて微調整してよい

## 検収基準

- [ ] 機内モード等でオフラインにすると案内バナーが表示される
- [ ] オンラインに戻るとバナーが消える
- [ ] オフライン中でも「きょうやった！」の記録操作は引き続き可能なことを確認
- [ ] 安全系テスト（111+engine-fixtures）緑のまま・回帰なし
- [ ] `npm test`442緑・Web版配信ファイル無変更

## やらないこと

- YouTubeアプリ内ブラウザ脱出案内・LINE等アプリ内ブラウザ警告・ホーム画面追加リマインドは
  引き続き移植対象外（A2HS/PWA固有のため）
- Web版（PWA）側の配信ファイルは一切変更しない

## 報告

完了時、ドア配達で簡潔に報告してください（スクショ1枚で可）。
